# 2026-07-04

## 上午
- 更新 docs/三场景完整结构树.md：基于 main.scene 文件扫描结果，修正 MainUI 子节点为真实扁平结构（无 TopBar/BottomBar 容器），补充 MainHubUI 字段绑定对照表（10 项全部 ✅）
- 同步更新 .workbuddy/memory 记录
- 推送远程仓库

## 上午 (续)
- 修复 sprite sheet 预览问题（CreatePanel 角色卡显示整张 sheet 而非单帧）
  - SpriteAnimationService.ts: 新增 `applyFrameByAssetDef(node, def, frameIndex)` — 切片加载 sprite sheet 单帧，帧缓存，非 sheet 回退
  - CharacterVisualService.ts: 新增 `applyPreviewFrame(node, visualKey)` — 调用 applyFrameByAssetDef(frameIndex=0) 显示第 0 帧
  - CreatePanel.ts: CARD_WIDTH 100→112, PREVIEW_SIZE=86, _buildCards/_selectCharacter 中 applyStatic→applyPreviewFrame
  - 验证: npm.cmd run validate:all 8 项全部通过 ✅
