# 2026-07-01 工作日志

## 编码质量专项治理

### 新增文件
| 文件 | 职责 |
|------|------|
| `tools/encoding_audit.py` | 编码质量审计：UTF-8 可解码性、乱码特征词、注释吞代码风险、字符串乱码 |
| `.editorconfig` | 强制 UTF-8、LF 换行、末尾换行、行尾空格修剪 |

### 修改文件
| 文件 | 改动 |
|------|------|
| `tools/config_pipeline/check_all.py` | 新增「编码审计」检查项 |
| `.workbuddy/memory/topics/ENGINEERING_STANDARDS.md` | 新增完整「编码规则(Encoding Rules)」章节 |

### 治理结果
- ✅ `python tools/encoding_audit.py --ci` → `issues=0, p0=0`
- 修复 2 个 docs 文件中的真实乱码 (`地牢地图`→`地牢地图`, `装[corrupt-text]备UI`→`装备UI`)
- 优化 3 处假阳性过滤：Python `//` 整除、注释文档 `{varName}`、跨行匹配
- 审计脚本自排除：不检测自身输出报告和自身源码中的模式定义

### 规则落地
- `.editorconfig` → 编辑器级别强制
- `check_all.py` → CI 门禁级别强制
- `ENGINEERING_STANDARDS.md` → AI 协作规则强制

---

## 运行时实体工厂 + 渲染管线修复

### 新增文件
| 文件 | 职责 |
|------|------|
| `runtime/RuntimeEntityFactory.ts` | 通用运行时实体工厂（Body/HPBar/Shadow/slots），避免 Graphics+Sprite 冲突 |
| `battle/MonsterRuntimeFactory.ts` | 怪物工厂，适配 RuntimeEntityFactory 的怪物层（改为工厂适配层） |
| `battle/MonsterRuntimeView.ts` | 怪物运行时视图（BodySprite/HPBar 同步/闪白） |

### 修复问题
| 问题 | 文件 | 修复方式 |
|------|------|---------|
| 背景 JPG Texture2D 加载失败 | `assets/AssetBundleService.ts` | 自动尝试 `path` 和 `path/texture` 两种子资源 |
| TextManager 没吃到 text.json | `core/ConfigService.ts` + `core/ConfigManager.ts` | ConfigService.loadAll() 完成后同步 text.json 到 TextManager |
| GameBootstrap 往 Scene 上挂组件 | `core/GameBootstrap.ts` | 跳过 Scene 自身，只挂到普通子节点 |
| 渲染组件冲突 | `runtime/RuntimeEntityFactory.ts` | Graphics 和 Sprite 在不同子节点 |

---

## 正式层级结构重构

修复怪物挂在 BattleManager 节点下的问题。新增 `_ensureLayer()` 创建 5 个可视层：
BackgroundLayer/TileLayer/ActorLayer/EffectLayer/DoorLayer。

### 修改文件
| 文件 | 变更 |
|------|------|
| `scene/DungeonSceneInstaller.ts` | 重写：_ensureLayer 创建5层，Player/ActorLayer，Grid/TileLayer，追加 layer refs |
| `battle/BattleManager.ts` | init() 增加 actorLayer 参数，_spawnMonster → actorLayer.addChild |
| `dungeon/GridManager.ts` | 新增 setTileContainer()，瓦片挂 tileLayer |
| `DungeonSceneController.ts` | 传 actorLayer，新增 _logSceneTree() |

### 验证
- ✅ `npm run validate:all` 3/3 通过
- 停预览重跑后看 `[SceneTree]` 日志：ActorLayer 应有 Player + monster_*
- ✅ `git diff --check -- assets/scripts` 无格式错误
- 需在 Cocos Creator 停预览重新运行，让新 chunk 编译生效

---

## 编码写入链路根治

### 根因确认
Edit 工具在 Windows 上默认使用 GBK 编码写入 UTF-8 源文件，导致中文注释变成 mojibake（[corrupt-text]），
进而造成注释边界异常、代码被吞、运行时错误。

### 工程红线
所有文件读写必须显式 `encoding="utf-8"`，禁止依赖系统默认编码。

### 编码审计加强
- 新增 `replacement_character` 检测（U+FFFD，P0）
- 新增 `comment_swallowed_code` 检测（// 后紧跟 const/let 等关键字，P0）
- 修改源码后必须跑 `npm run validate:all`，P0=0 方可继续
- 记入 `inbox/2026-07-01_1135_encoding_utf8_hard_rule.md`

---

## EventBus 健壮性 + BattleHUD 伤害数字

### 修复问题
| 问题 | 文件 | 修复方式 |
|------|------|---------|
| `_showDamageNumber` 方法缺失导致 `undefined.apply` 崩溃 | `ui/BattleHUD.ts` | 补齐方法 + 运行时 Label 伤害数字 + 清理乱码注释 |
| EventBus 单个监听报错拖垮循环 | `core/EventBus.ts` | `emit()` 捕获每个 listener 的异常，单报错不中断 |
| 注册非函数回调无告警 | `core/EventBus.ts` | `.on()` 检查类型，非函数直接忽略并 warning |
| 缺少 `off(event, callback, target)` 重载 | `core/EventBus.ts` | 新增兼容三参数调用 |

### 验证
- ✅ `npm run validate:all` 通过
- ✅ `git diff --check` 通过
- ✅ 静态扫描确认所有 `eventBus.on(..., this._方法, this)` 对应方法都存在

---

## 诊断记录：只显示背景、无主角/怪物时 7 条排查链

来源：用户分析。

### 排障顺序
1. **层级覆盖** — 背景 siblingIndex 比角色高，盖住一切。
   → 统一层级：BackgroundLayer(0) / TileLayer(1) / ActorLayer(2) / EffectLayer(3) / UILayer(4)，用 `ensureLayer()` 保证。
2. **坐标体系不一致** — 背景用 Canvas 坐标，角色用 Grid 坐标，跑到屏幕外。
   → 所有战斗单位走 `gridToWorld()` 统一转换。
3. **BattleManager 未真正 startBattle** — 背景是 DungeonSceneInstaller 加载，但战斗流程没触发。
   → 加启动链日志：`refs ready` / `dungeon generated` / `startBattle called` / `spawn monsters`。
4. **节点生成了但 SpriteFrame 为空** — 资源 ID 不匹配导致透明/空白。
   → `applyMonsterSprite()` 返回 null 时 warn 打印 `zoneId/monsterId/path`。
5. **背景太大挤没世界内容** — 背景铺满设计分辨率，角色层独立居中。
   → `fitBackground(bgNode)` 设尺寸/位置/siblingIndex，角色不要挂背景子节点。
6. **节点 active=false 或 scale=0** — 从 prefab/模板创建常见。
   → 生成后统一 `normalizeRenderableNode()` 确保 active/scale/transform。
7. **Camera cullingMask 没包含角色层** — 背景在 UI_2D，角色在 DEFAULT。
   → `setLayerRecursive(node, Layers.Enum.UI_2D)` 统一。

---

## 记忆写入规则纠正

- 上一轮直接修改 `topics/ENGINEERING_STANDARDS.md`，违反了记忆写入规则（应走 inbox → 确认合并流程）
- 已纠正规范流程：
  - 编码质量审计补丁 → `inbox/2026-07-01_1050_encoding_audit_completed.md` ✅
  - ENCODING_AUDIT 已加入 MEMORY.md 必读规则索引，暂指向 inbox 补丁，待"合并主记忆"时创建专题文件
- `MEMORY.md` 索引表中的 ENCODING_AUDIT 已修正为指向 `inbox/` 而非不存在的 `topics/` 文件

---

## 编码审计增强 + 记忆文件清理

### MEMORY.md 重写
- 主记忆入口已重写为干净、稳定、ASCII 为主的 UTF-8 索引
- 移除所有开头乱码/特殊字符

### encoding_audit.py 增强
| 变更 | 说明 |
|------|------|
| 扫描范围扩大 | 现在覆盖 `assets/scripts`, `assets/resources/config`, `tools`, `docs`, `art_source`, `.workbuddy/memory` |
| 自身免检 | 使用 Unicode codepoint 构造审计 token，不再自触发审计 |
| 误报规则修正 | `// Reserved for victory feedback.` 和 `{varName}` 等正常注释/正则不再误判 |
| U+FFFD 清理 | 不可恢复的 `[corrupt-text]` 全部替换为 `[corrupt-text]` 标记 |

### 验证结果
- ✅ `python tools/encoding_audit.py --ci` → `issues=0, p0=0, p1=0, p2=0`
- ✅ `npm.cmd run validate:all` 全部通过

### 落地机制
- MEMORY.md 主记忆第一屏强制提示：所有读写必须显式 UTF-8
- `validate:all` 已包含编码审计门禁
- `.workbuddy/memory` 已纳入审计范围
- 禁止默认编码写入（`open(path, "w")`、无 `-Encoding utf8` 的 `Set-Content`）
- CI 门禁直接阻断乱码/替换字符/注释吞代码

---

## ASCII_SOURCE_POLICY 落地

### MEMORY.md 新增高优先级规则
- `ASCII_SOURCE_POLICY - 2026-07-01`：源码注释/工具脚本/工程文档/记忆文件默认用英文/ASCII
- 玩家可见中文统一放到 `assets/resources/config/text.json`
- 设计/产品文档必要时可以用中文，但必须过编码审计
- 所有写入必须显式 UTF-8，禁止默认编码写入
- 修改后必须跑 `npm.cmd run validate:all`

### ENGINEERING_STANDARDS.md 重写
- 已重写成干净 ASCII/UTF-8 版
- 新增详细规则章节覆盖：源码注释、工具脚本、工程文档、记忆文件
- 明确英文/ASCII 与玩家可见中文的分界线

### CHANGELOG.md
- 追加了规则变更记录

### 清理修复
- `daily/2026-07-01.md` 中残留 U+FFFD 已清理

### 验证
- ✅ `python tools/encoding_audit.py --ci` → `issues=0 p0=0 p1=0 p2=0`
- ✅ `npm.cmd run validate:all` 全部通过
- 今后新对话启动时主记忆第一屏即可看到规则，不再依赖口头提醒

---

## 游戏流程设计

- 新增 `docs/完整游戏流程与界面设计.md` — 完整 8 步流程文档，含 UI 布局图、功能点、优先级、改动清单
- 新增 **第 12 章**：Step 1 Splash 详细实现规格
  - 分流逻辑：未登录→login / 首次→create_character / 非首次→main
  - 进度条：GameBootstrap 异步进度推送 + SplashUI 实时渲染
  - 新增接口：`PlatformService.wxLogin()`, `isLoggedIn()`
  - 新增接口：`PlayerDataManager.isFirstTime()`
  - 节点设计：LoadingContainer + BarBg/BarFill + 状态 Label
  - 验收清单 8 项 + text.json 8 个 key

- Step 2 登录授权详细规格：LoginUI 完整实现、PlatformService 补充、登录状态机、节点设计、验收 7 项
- Step 3 创建角色详细规格：名称验证、5 动物角色卡、PlayerDataManager.createCharacter()
- Step 4 角色选择详细规格：卡片列表、解锁/创建/选择逻辑、PlayerDataManager 扩展方法
- Step 5 主城界面详细规格：TopBar/ActionBar/BottomBar 三层布局、MarqueeManager 跨局跑马灯、UiRouter 集成
- Step 6 区域选择详细规格：6 区域 3 路线、ZoneInfo/RouteInfo 模型、解锁条件
- Step 7 地牢战斗修补规格：5 个 P0/P1 修复项 + VictoryUI 完整实现 + 玩家阴影/Y轴排序
- Step 8 战斗结算详细规格：死亡/通关双结算流程、GameManager 统一接口、完整游戏循环验证
