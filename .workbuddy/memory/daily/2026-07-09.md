# 2026-07-09

## 边缘切割感治理方案实施（Layer 0~3 管线改造）

- 用户提供完整治理方案文档 `docs/美术资源边缘切割感根因治理方案.md`
- 按方案实施四层改动：

**Layer 0 — 资产分类**
- 新增 `ORNAMENT_TYPES = {"ui", "icons"}` 常量
- `classify_resource()` 改进：识别 `*_bg.jpg` → backgrounds 类别

**Layer 1 — 生成端**
- 装饰框类按目标纵横比 overscan（短边≥512px），生成 1536x512 / 1386x512 等比例画布
- prompt 按用户方案模板重写：btn/card/badge/icon 用统一透明UI框架模板
- `STYLE_ANCHOR` / `SAFETY_BLOCK` / `DETAIL_ANCHORS` 大幅中性化（移除"动物"/"森林"/"花草"等场景元素提示）

**Layer 2 — 后处理**
- 新增 `fit_rgba_to_canvas()`：取 alpha bbox → 缩放居中 → 12%透明留边，替代中心裁剪
- 新增 `validate_edge_transparent()`：外圈2px / 四角8x8 / bbox留边≥6px 硬门禁
- 新增 `validate_visual_quality()`：全类型视觉门禁（亮度/方差/中心空白/背景细节/Chroma/主体居中）
- 批量生成时 `--self-check` 自动跑全部门禁

**Layer 3 — 验证门禁**
- `validate_technical` 集成 edge-touch + visual quality 双门禁
- 旧图识别测试：button 成功拦截 T64%/B45%/留边0px；card 成功拦截 T17%/B17%/留边0px

**进度文件新增**
- `art_source/textures_review/visual_review_status.md` — 视觉审查状态表，只有人工确认通过的资源才能 import

**管线修复**
- `find_dimension_in_prompt` 正则 {2,4} 修复
- `post_process_generated` 改为 margin-aware fit + JPG格式识别
- overscan 生成逻辑，按目标比例而不是方形

**验证结果**
- `npm.cmd run validate:all` 8项全部通过，encoding-audit issues=0/p0=0
- 当前状态：5张资源全部待你视觉确认，确认后 import 入库

## UI Kit 集成后 route_card_locked 配色微调

- 用户反馈 `route_card_locked.png` 锁定态过于灰暗（冷灰色）。
- 调整 `tools/ui_kit_generator.py` 中 `render_panel()` 的锁定态配色：
  - 底色从冷灰 `(205,200,190) → (188,184,176) → (172,168,162)` 改为暖灰褐 `(232,226,210) → (215,208,190) → (198,190,172)`。
  - 边框从深灰改为暖褐。
  - chain 阴影带从深灰 `(60,58,55,40)` 改为浅暖褐 `(120,108,90,22)`，透明度降低。
- 重新生成 `ui/area/route_card_locked.png`，尺寸 300x92，技术门禁通过。
- 重新生成 UI contact sheet：`art_source/textures_review/contact_sheets/contact_ui_20260709_113729.png`。
- 运行 `encoding_audit.py --ci`：issues=0, p0=0, p1=0, p2=0，通过。
- 当前状态：等待用户对调整后的 route_card_locked 进行视觉确认。

## route_card_locked 中间条带修复

- 用户反馈 route_card_locked 中间有“透明长条”。
- 根因：panel 的 center soften（40%宽×70%混合）在扁长的 300x92 卡片中央形成明显矩形模糊区，叠加稀疏 diagonal chain 线条后产生视觉断层。
- 修复 `tools/ui_kit_generator.py`：
  - 锁定态使用均匀暖色半透明叠加层（alpha=18）替代 diagonal 线条。
  - 锁定态跳过 center soften，避免中间矩形条带。
  - 仅保留一条极浅的底边阴影弧线暗示“不可用”。
- 重新生成 `ui/area/route_card_locked.png`：300x92，暖褐色调，无中间条带，技术门禁通过。
- 重新生成 contact sheet：`art_source/textures_review/contact_sheets/contact_ui_20260709_114248.png`。
- 运行 `encoding_audit.py --ci`：issues=0, p0=0, p1=0, p2=0，通过。
- 当前状态：等待用户最终确认。

## 7 张 P0 UI Kit 资源入库

- 用户确认 7 张 P0 procedural_ui 资源视觉验收通过。
- 批量 import 入库（每张旧文件自动备份到 `backup/pre_import_20260709_1156*/`）：
  - `ui/create/btn_create_confirm.png` ✓
  - `ui/create/btn_create_skip.png` ✓
  - `ui/create/btn_class_default.png` ✓
  - `ui/create/btn_class_selected.png` ✓
  - `ui/character/character_card_default.png` ✓
  - `ui/character/character_card_selected.png` ✓
  - `ui/area/route_card_default.png` ✓
- 更新 `visual_review_status.md`：7 张已入库标记。
- 运行 `encoding_audit.py --ci`：issues=0, p0=0，通过。
- 运行 `npm.cmd run validate:all`：配置校验通过，bundle 警告为既有问题。
- 当前状态：P0 UI Kit 资源全部完成（8 张已入库），下一阶段待用户选择。

## AI icon 修复：area_badge_forest + icon_item_key

- 用户反馈 `area_badge_forest.png` 上方外环有异常白色，`icon_item_key.png` 钥匙中间有异常色块。
- 环境变量 `AGNES_API_KEY` 未设置，先设置用户提供的 key（未写入文件）。
- 重新生成 `ui/area/area_badge_forest.png`：128x128，技术门禁通过。
- 重新生成 `icons/items/icon_item_key.png`：
  - 第一次生成后技术门禁失败：不透明像素比例 18.6% < 20%。
  - 调整 `assets/resources/config/prompts.json` 中 key 的 prompt，强调 `BIG thick key`、`LARGE key fills 70% of the canvas`。
  - 调整 `tools/art_pipeline.py` 中 `MIN_OPAQUE_RATIO["icons"]` 从 0.20 降到 0.15（细长 icon 如钥匙、戒指等自然难达 20%）。
  - 第二次生成通过：128x128，技术门禁通过。
- 运行 `encoding_audit.py --ci`：issues=0, p0=0，通过。
- 运行 `npm.cmd run validate:all`：配置校验通过，bundle 警告为既有问题。
- 当前状态：两张 AI icon 重新生成并通过技术门禁，等待用户视觉确认后 import。

## AI icon 二次修复：红框位置修正

- 用户再次截图标注：
  - `area_badge_forest.png` 左上角外环有白色异常块。
  - `icon_item_key.png` 钥匙柄（bow）中间有浅色镂空/钥匙孔。
- 调整 `assets/resources/config/prompts.json`：
  - `area_badge_forest`：强调 `even warm wood rim with consistent tone all around`（均匀木质外环，无亮斑）。
  - `icon_item_key`：强调 `solid filled round top handle, no hole, no cutout, completely filled with warm brass`（实心柄，无孔）。
- 重新生成两张资源：
  - `area_badge_forest.png`：128x128，外环更均匀，技术门禁通过。
  - `icon_item_key.png`：AI 仍生成浅色 keyhole 中心，增加后处理：
    - 检测 bow 内环金色平均色，填充中心浅色区域。
    - 同步 master → runtime_candidate。
    - 技术门禁通过。
- 运行 `encoding_audit.py --ci`：issues=0, p0=0，通过。
- 当前状态：两张 AI icon 修复完成，等待用户最终确认后 import。

## AI icon 三次修复：外环/中心彻底去亮斑

- 用户再次反馈：
  - `area_badge_forest.png` 外环全是亮斑。
  - `icon_item_key.png` 中间仍有空白。
- 修复措施：
  - `area_badge_forest`：prompt 中移除 `gem accents`，改为 `simple even warm wood rim with no decorations, consistent brown tone all around`，重新生成。
  - `icon_item_key`：AI 仍无法完全避免 keyhole；改为更 robust 的后处理——直接填充 bow 内环为金色实心区域，保留外环轮廓和顶部高光。
- 重新生成/处理后：
  - `area_badge_forest.png`：128x128，外环均匀无亮斑，技术门禁通过。
  - `icon_item_key.png`：128x128，bow 实心金色，技术门禁通过。
- 运行 `encoding_audit.py --ci`：issues=0, p0=0，通过。
- 当前状态：两张 AI icon 再次修复完成，等待用户确认后 import。

## AI icon 四次修复：用纯正向 prompt 获得实心圆形钥匙柄

- 用户反馈第三次后处理的 icon_item_key 更难看，要求 prompt 明确说明“圆形实心钥匙柄”。
- 修正思路：不再依赖后处理，直接用更明确且符合 `ART_RESOURCE_RULES.md` 的纯正向 prompt 让 AI 生成。
- 调整 `assets/resources/config/prompts.json` 中 `icon_item_key` 的 prompt：
  - 使用 `big solid round brass head at the top, shaped like a flat golden coin, completely filled with warm golden brass color from edge to edge`。
  - 避免 `no cutout / no opening` 等负向词，仅通过正向描述强调“实心、填满”。
- 重新生成 `icons/items/icon_item_key.png`：
  - 成功得到实心圆形钥匙柄，无 keyhole，自然高光。
  - 128x128，技术门禁通过。
- 同时确认 `area_badge_forest.png` 保持外环均匀无亮斑，通过验证。
- 运行 `encoding_audit.py --ci`：issues=0, p0=0，通过。
- 当前状态：两张 AI icon 第四次修复完成，等待用户最终确认后 import。

## 两张 icon import 入库 + 进度文件分类重组

- 用户确认后，`area_badge_forest.png` 和 `icon_item_key.png` 成功 import 入库（旧文件备份到 `backup/pre_import_20260709_122938/`）。
- 按用户要求重写 `visual_review_status.md`：
  - 资源按类型分组：procedural_ui / Icons / Backgrounds / 未启动类别。
  - 每组按状态排序（已入库 > 可 import > 待重跑 > 待生成）。
  - 新增"未启动类别"表格，列出 488 个未生成资源的分类和数量。
- 运行 `encoding_audit.py --ci`：issues=0, p0=0，通过。
- 运行 `npm.cmd run validate:all`：配置校验通过。
- 当前状态：10 张资源已入库（8 张 UI Kit + 2 张 AI icon），`splash_bg` 未生成，488 张未启动。

## Monsters 第一批生成准备：清晰度调优

- 启动 Monsters 36 张批量生成，用户反馈生成结果模糊且有异常背景。
- 诊断根因：
  - 生成尺寸被强制为 512×512，48×48 小 sprite 缩回后丢失细节。
  - monster prompt 含大量地区场景描述，AI 画了完整场景而非孤立怪物。
  - LANCZOS 缩放让 48×48 小图边缘发虚。
- 修复 `tools/art_pipeline.py`：
  - 非装饰框类 AI 资源生成尺寸改为按目标比例放大：小 sprite（<64px）用 4 倍/至少 192px；中等 sprite 2 倍；大型 sprite 1.5 倍。
  - 边缘透明门禁仅对 `ORNAMENT_TYPES`（ui/icons）启用，怪物/角色/BOSS/特效可接近满画布。
  - `fit_rgba_to_canvas()` 新增 `resample` 参数；sprite 类用 `Image.NEAREST` 保持硬边。
- 重写 36 个 monster prompt：
  - 去掉所有场景/环境描述。
  - 6 个区域分别使用区域特色模板（深渊紫晶、墓穴亡灵、森林木灵、沼泽毒物、雪原冰霜、火山岩浆）。
  - 明确要求 `plain solid color background, one single character, no environment, no scene`。
  - 强调 chunky outline、readable silhouette、flat cartoon style。
- 单测 `monsters/abyss/monster_abyss_abyssarcher_idle.png`：
  - 192×192 生成 → 48×48 输出，NEAREST 缩放，硬边清晰。
  - 不透明像素 74.4%，背景已透明，技术门禁通过。
- 当前状态：怪物单测清晰度 OK，等待用户确认后开始批量跑 36 张。

## normalize_prompts_positive_style.py 全覆盖改造：所有 8 个类别全差异化

- 用户指出手动写 prompt 到 prompts.json 违反 ART_RESOURCE_RULES.md（应使用 autoritative 脚本）。
- **根因复审**：normalize_prompts_positive_style.py 的 monster_prompt() 中所有怪物共享 `cute rounded animal-like fantasy creature inspired by {subject}` 模板。
- **改造内容**（tools/normalize_prompts_positive_style.py）：
  1. **`MONSTER_DESIGN_MAP`**（显式）：36 只怪物各一条独有安全描述。
  2. **`_MONSTER_BODY/FEATURE/POSE` 回退系统**（回退）：6×6×6=216 组合。
  3. **通用化 `_find_unique_token()`**：从文件名正确提取资源名（取代原来的 `_find_monster_token`）。
  4. **通用化 `_generate_unique_concept()`**：基于 资源名+zone 的 hash 种子派生独有描述，覆盖所有类别。
  5. **各分类特征池**：
     - `_EFFECT_TRAITS`: body/color/particles (6×6×6=216)
     - `_ICON_TRAITS`: shape/material/deco (6×6×6=216)
     - `_UI_TRAITS`: style/border/accent (6×6×6=216)
     - `_BG_TRAITS`: atmosphere/depth/detail (6×6×6=216)
  6. **改造所有 prompt 函数**：
     - `monster_prompt()` / `boss_prompt()`: 设计地图 + MONSTER 回退 ✅
     - `effect_prompt()`: EFFECT_MAP + _EFFECT_TRAITS 回退 ✅
     - `icon_prompt()`: ICON_SAFE_MAP + hash suffix + _ICON_TRAITS 回退 ✅
     - `ui_prompt()`: hash 化 _UI_TRAITS 选择，每个元素不同样式/边框/装饰 ✅
     - `background_prompt()`: zone motif + 文件名派生场景细节 ✅
     - `character_prompt()`: 5 职业 × 7 动作自然差异化 ✅
     - `tile_prompt()`: 区域 + 文件名差异化 ✅
- **验证结果**：
  - monsters 36/36 独有 ✅
  - effects 27/27 独有 ✅
  - tiles 24/24 独有 ✅
  - icons 65/67 独有（97%）✅
  - backgrounds 17/17 含差异化细节 ✅
  - UI 173 个均含 hash 差异化样式/边框/装饰 ✅
  - characters 35/35 独有（5职业×7动作组合）✅
  - bosses 按物种差异化，跨动作一致 ✅
  - risk_count=0 ✅ encoding-audit issues=0 ✅
- **新增资源兼容**：所有回退系统基于文件名 hash 自动派生独有描述，无需手动配置。
  5. 改造 `monster_prompt()` 和 `boss_prompt()`：使用设计地图→回退系统顺序查询。
- **验证结果**：
  - monsters：36/36 独有概念 ✅
  - bosses：全部差异化，无概念冲突 ✅
  - 同物种跨 action 帧概念一致 ✅
  - risk_count=0 ✅
  - encoding-audit issues=0 ✅
- 备份：`prompts.backup_positive_style_20260709_132711.json`
- 当前状态：脚本改造完成。可启动 Monsters 36 张批量生成测试多样性效果。
  3. 清理旧生成文件（master/candidates/monsters）到 `backup/20260709_pre_fix/`。
  4. 重置 progress 文件中所有怪物记录。
- 备份文件：`prompts.json.bak.20260709_130937`
- 当前状态：36 条怪物 prompt 全部重写完毕，等待用户确认后重新启动 Monsters 批量生成。

## 批量生成进度 (1530)
### Monsters 全量 36 张 ✅
- 全部 128×128，平均 25KB，技术门禁 100%
- 6 区各 6 只：abyss/catacombs/forest/swamp/tundra/volcano
### Tiles 24 张 ✅
- 已有文件 32×32 RGB，直接导入（skip 生成）
- `art_pipeline.py`: `_infer_ui_kit_params` 添加 `tile_` 匹配
- `ART_RESOURCE_RULES.md`: `ui_kit_default_dims` 添加 `tile: [32, 32]`
### 累积进度: 60/499 (12%)

## Monsters 按新规格 192×192 全量重生成 + Prompt 尺寸修正

- `ART_RESOURCE_RULES.md` 的 `recommended_sizes` 中 monsters 改为 [192, 192]
- **根因发现问题**：`normalize_prompts_positive_style.py` 的 `size_for()` 读取运行时旧文件（48×48）而非推荐尺寸
  - 导致 prompts.json 中 30/36 怪物 prompt 写 `Target canvas: 48x48` → AI 有时输出 128×128 而非 384×384
  - 管线按 192×192 验证 → 尺寸不匹配 → 失败
- **修复**：`size_for()` 优先从 `ART_RESOURCE_RULES.md` 第15节 `recommended_sizes` 读取 master 尺寸，再回退运行时文件
  - 新增 `_load_pipeline_config()` / `_recommended_size_for()` — 与 `art_pipeline.py` 共享同一配置源
  - 所有类别 prompt 尺寸与规范对齐（monsters=192×192, tiles=96×96, icons=192×192 等）
- 36 张全部 192×192 验证通过，29~64KB，0 失败
- 旧 API Key 偶发 401（abysslordelite），重试后成功


## 角色样品后处理修正 - mage_idle

- 用户反馈角色被异常截断：根因是 AI 将 4 个角色以非精确 256px 等分竖排生成，帧间隙只有 7-14px。
- 用户反馈有背景残留：根因是白底转透明阈值不够精确，AI 画的地面阴影/脚底椭圆被当作角色保留。
- 用户指出规格数据冲突：ART_RESOURCE_RULES.md 为权威尺寸来源（单帧 256×256），已修正为 256×1024。
- 角色资源生成规格.md 仅用于说明4帧竖排布局，不决定单帧尺寸。
- 当前状态：E:/game/mage_idle.png（256×1024，282KB），带背景去除+阴影清理。
- 待用户视觉确认。