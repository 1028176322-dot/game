# 2026-06-30 工作日志

## UI 文本配置化与美术无文字整改

### 阶段A/B：text.json + TextManager 修复 ✅
- TextManager.ts 重写：添加 `hasTextKey()` 函数，完善类型签名，修复注释乱码
- text.json 补充 15 个新增键：marqueeTitle/marqueeHint/shopTitle/shopTabCharacters/shopTabTalents/shopTabExtras/shopSoulStone/shopActionSelect/shopActionOwned/shopActionUnlock/shopCost/close/emptySlot

### 阶段C：UI 代码迁移 ✅
- 7 处硬编码文本全部迁移到 T()：
  - `MarqueeUI.ts`: 跑马灯标题/提示/进度/关闭按钮/钥匙获得
  - `ShopUI.ts`: 商店标题/魂石/拥有状态/价格
  - `ShopView.ts`: 商店标题/魂石
  - `DungeonMapUI.ts`: 探索者天赋提示
  - `MainUI.ts`: 版本号
  - `EquipmentView.ts`: 装备标题（fallback）
  - `InventoryView.ts`: 道具标题（fallback）

### 阶段E：扫描脚本 ✅
- 新增 `tools/scan_hardcoded_ui_text.py` — 扫描 .string = '...' 中硬编码玩家可见文本
- `python tools/scan_hardcoded_ui_text.py` — 输出空才算通过
- 当前扫描结果：**[PASS] 未发现硬编码玩家可见文本**

### 阶段D：prompts.json 检查 ⚠️ 延期
- prompts.json 尚未写入 NO-BAKED-TEXT CONTRACT（需要下次美术生成时添加）

## 记忆系统重构

### MEMORY.md 拆分为 9 个专题文件 ✅
- 旧 MEMORY.md 30KB → 拆为 10 个文件共 19KB
- MEMORY.md 降为纯索引（1.7KB），确保跨会话完整注入
- 标准化专题文件格式：状态/强制规则/当前实现/禁止事项/验收命令/最近变更
- 新增 ART_PIPELINE.md — AI 美术生成管线规则
- 新增 RESOURCE_STATUS.md — 418 个资源接入状态（文件存在 vs. assets.json映射 vs. 画面已接入）
- 创建 archive/ 目录用于存放已过期内容

### 建立记忆补丁工作流 ✅
- 采用 inbox → topics → MEMORY.md → CHANGELOG 四层结构
- 不同对话只写 `inbox/` 补丁，不直接改 topics/ 或 MEMORY.md
- 每日日志移入 `daily/` 目录，`MEMORY.md` 顶部增加硬规则禁止直接改主记忆
- 新增 `README.md` 说明工作流
- 新增 `templates/MEMORY_PATCH_TEMPLATE.md` 补丁模板
- 专题文件统一以 `.workbuddy/memory/topics/` 为权威路径
- 新增 `CHANGELOG.md` 记录每次合并历史
- 新增补丁示例：`inbox/2026-06-30_1700_memory_system_confirmed.md`

---

## 远程仓库重新初始化与推送

- 删除旧 GitHub 远程仓库 `1028176322-dot/game`
- 在 `E:/game/` 根目录新建 git 仓库，废弃旧 `回到地面/.git`
- 新增根级 `.gitignore`（排除 `art_source/`、`__pycache__/`、`* .bak_*` 等）
- 删除原 `回到地面/.gitignore`
- 初始提交：1446 文件，125757 行（含 `assets/`、`tools/`、`回到地面/`）
- 发现 `.workbuddy/` 被全局 gitignore `C:/Users/Administrator/.config/git/ignore` 排除，已移除该规则
- 第二次提交：32 个 `.workbuddy` 文件
- 远程推送完成：`origin main` 已追踪

- 更新 `回到地面/项目交接文档.md`：同步了 git 根目录结构、卡通动物风美术、分支策略、联系人、.workbuddy 说明等 10 余项变更
- commit + push 到远程

---

## 美术资源状态审计

用户称"418 条美术资源已由另一 AI 完成"。

| 指标 | 数据 |
|------|------|
| prompts.json 定义 | 418 |
| runtime_replace 生成 | 401 PNGs |
| 正式目录 textures | 401 PNGs（匹配） |
| 缺失 | 17 张（主要为 backgrounds） |
| master_review.csv 需重做 | 35 项 |
| rejected/ | 60 张 |
| approved/ | 0 张 |

生成 96% 但审批 0%。记入 `inbox/2026-06-30_1755_art_pipeline_resource_audit.md`。
