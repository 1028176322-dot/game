# 2026-07-14 工作日志

## ui3d_preview 文档一致性修复（遗留项 A）

回写 T4/T5 任务卡状态，消除"卡头 ON HOLD vs INDEX/REPORT 已完成"的自相矛盾（纯文档、零风险）：

- `T4_mainmenu_backdrop_3d.md`：卡头标题 + 状态行 + 文末执行 Prompt 的 `[ON HOLD]` → 改为「已完成（代码落地，B-lite 默认关闭待资产）」/ `[DONE]`。
- `T5_splash_3d_bg.md`：同上，改为已完成 + `[DONE]`，注明沿用 T4 的 ui3d.json、方案 B 复用 splashImage。
- `INDEX.md` §5 L147：删掉"T4/T5 仍 ON HOLD"，改为"代码已完成，待 BG_* 资产仅改配置即可开启"。
- `REPORT_T3.md` L41 的 ON HOLD 属历史快照，**保留原样**（那是 T3 完成当时真实状态，历史报告不回改）。

验证：encoding_audit --ci issues=0 / p0=0；ui3d_preview 目录再无遗留 ON HOLD（仅 REPORT_T3 的历史快照）。

## 遗留项 D 任务卡（门禁基线 + 角色 3D）

新建 `docs/ai-tasks/D_gate_baseline_and_character3d.md`，按协议 §4 拆三轨：

- **D-1 本机注册/配置/文档修复**（立即可做，清 3 门禁 FAIL）：修 assets.json（CHR_* 缺 .glb 扩展、5 个 character.preview.* 旧 2D 路径、清 154 未用 ui_assets key）、prompts.json character_parts 锚点、archer .prefab 归位 + 运行时绑定。**关键认知：3 门禁 FAIL 是注册/配置/文档失配，与"4 模型没生成"无关，本机即可清零。**
- **D-2 外机 4 角色 3D 生产**（交接契约）：warrior/assassin/mage/berserker 的 GLB+Weapon+prefab+manifest。**用户同步：所有 3D 资源制作在另一台电脑**，本机不生成 GLB，只产交接包 + 规格约束（命名正则/rules3d.characters 预算/安全规则/prototype-only/gap reconciliation）。
- **D-3 回机导入绑定**（依赖 D-2）：走协议 §7.3 审批入库（AI 不得 GLB 直入 runtime、approved 前不入库），4 角色绑定复用 archer 模板，characters 缺口 1/5→5/5。

**5 个待用户拍板决策点**：①character.preview.* 改指3D/删 ②character_parts 补/豁免 ③30 miniboss idle PNG 清/接 ④Warrior 狮/熊 ⑤Mage 兔/羊。D-1 待①-③；D-2 待④-⑤。

缺口清单 §6 已加任务卡指针。验证：D 卡 UTF-8_OK + encoding_audit issues=0/p0=0。

## ai-tasks 目录遗留盘点结论（供后续参考）

- **A 文档不一致**：已修（本次）。
- **B 引擎内验证**（沙箱跑不了）：flipUVY 朝向、RT 整链出图/层隔离/多面板并发、demo1/2 双轨渲染、demo6 真机 100 怪 ≥30fps — 待 Cocos Creator/真机确认。
- **C 功能待资产/接线**（刻意边界）：T4/T5 背景待 BG_* 资产；demo7 生产接线（onMonsterKilled 回传、2D 地图 UI 触发、每日挑战调度）；若干 [PLACEHOLDER] 常量待 playtest。
- **D 项目门禁基线 3 FAIL**（唯一实质技术欠账，非 ui3d 引入）：①资源注册（5 个 character.preview.* 指向遗留 2D PNG + 154 未用 ui_assets key）②非UI资源注册（CHR_* 3D 模型 unreferenced）③文档一致性（prompts.json 缺 character_parts 锚点）；根因是 4/5 角色 3D 模型未生成（warrior/assassin/mage/berserker，仅 archer 到位）。彻底清零需补齐 4 角色模型 + 清旧 2D 注册，属独立任务需走 Execution Plan。

## D-1 遗留清理(选A) + D-2 交接包产出（2026-07-14）

**D-1 遗留 miniboss 清理（Task #151/#158 completed）**：用户选 A(清理/归档)。30 个 unreferenced miniboss idle 资产，每个 3 份拷贝(master/runtime_replace/assets 运行时+meta)共 120 文件，归档到 `art_source/_archive/miniboss_idle_unreferenced_20260714/`(可 Git 回滚，非物理删除)。**关键联动**：先从 `assets.json` data 移除 30 个 SpriteFrame 键(466->436)再归档磁盘，否则会从 warning 恶化为 registered_but_missing_file error。脚本保留 CRLF+UTF-8。复验 `validate:all` 仍 **9/9**，unreferenced assetIds **32->2**(剩 CHR_Archer_A/_Weapon prefab，非本次范围)。

**D-2 外机 3D 角色交接包（Task #159 completed）**：产出 `docs/ai-tasks/handoff_D2_character3d/`(2.3MB 自包含,20 文件)。结构:README_HANDOFF.md(主文档,12 节:铁律/4角色定义/交付物/技术规格/11动画clip/8挂点/manifest/自检/回传/决策) + reference/(archer样板 glb+prefab+weapon + 5 avatar 2D) + specs/(art_quality_budget+5 character_*.json) + templates/(4 sidecar assetmeta 模板)。
- **archer 样板实测基准**：CHR_Archer_A.glb 1.23MB / 2800 tri / 3907 顶点 / 20 骨 / 1 mesh 1 material 2 textures / 1 skin / **11 动画**(player_idle/run/walk/walk_back/left/right/attack/skill/dodge/hit/die,各60通道);武器为独立 glb(802KB,无骨骼)挂 Weapon socket。
- **rules3d.characters 预算**：tri 2000-3000(建议2500)/bones≤30(建议26)/512²/ASTC 6×6/LOD 3级(100·60@12m·30@24m)/animClips≥5/8挂点(RightHand/LeftHand/Head/Chest/Back/Foot/Weapon/SkillOrigin)/capsule/medium。
- **角色定稿**：warrior=熊(剑盾)/mage=兔(法杖)/assassin=猫(双匕)/berserker=熊(巨斧)。⚠ warrior&berserker 同为熊,交接包要求体型+装备+配色区分(warrior 披甲持盾冷色/berserker 裸上身巨斧暖色)。可改 warrior=狮/mage=羊(开工前告知)。
- **D 任务卡回写**：D-2 决策点定稿、DoD 第一项打勾+指向交接包路径。外机流程:整个目录拷到外机→读 README→产出→`asset_validate.py` 自检→回传走 D-3。
- **待办**：D-2 外机生产(依赖用户带到 GPU 机);D-3 回传审批入库+运行时绑定(复用 archer 模板)+character_visuals.json 4 职业 sheet->parts。可选清理:154 unused ui_assets key、archer .prefab 归位 prefabs/。
