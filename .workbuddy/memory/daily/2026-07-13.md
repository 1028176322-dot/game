# 2026-07-13 工作日志

## ui3d_preview 方案对齐「三场景完整结构树」

用户要求把 `回到地面/docs/三场景完整结构树.md` 纳入 `docs/ai-tasks/ui3d_preview/` 方案，
并据结构树真实基线（附录 A）修正任务卡与优先级。全程只读扫描 + 只改文档，未碰 `assets/scripts/**`（遵守 ai-tasks 方法论批准前约束）。

### 关键结论落地
- **T1 API 定型**：`anchorWorldPos` → `showCharacterInSlot(slotNode, characterId, action?, opts?)`。
  理由：CreatePanel/SelectView/PreviewZone/ModelDisplay 均运行时创建，位置受 CreatePanelLayout /
  ResponsivePanelRoot / Canvas 缩放影响，只有传 slotNode 底座才能读 UITransform 自适应。
- **渲染路线**：RenderTexture 离屏渲染贴回 UI 槽位 Sprite（`ModelDisplay [UITransform, Sprite]`），
  UI 节点不直挂 ModelComponent。预览层用用户层 bit 0-19（`1<<20~1<<25` 全为 Cocos 保留层）。
- **CreatePanel 真实结构**：SelectView/NamingView/ActionZone 是 PanelRoot 直接子、运行时创建；
  PanelFrame/ContentRoot 仅旧兼容——方案不得假设所有 UI 都在 ContentRoot 下。
- **CharacterPanel**：复用固定 `ContentRoot/PreviewSlot [UITransform,Sprite]`，由 `CharacterPanelLayout`
  管理，禁止面板脚本运行时新建锚点/自算坐标。
- **MainMenuBackdrop3D**：附录 A 真实 main.scene 无该节点；T4 须显式创建 `Canvas/MainMenuBackdrop3D`
  并 `setSiblingIndex(0)` 置 MainUI 之前、不拦截触摸、层 UI_2D。
- **优先级定档**：P0 T1A/T1B、P1 T2/T3、P2 T4、P3 T5（Splash 3D 背景与「splash/main 保持 2D」相斥，属可选扩展）。

### 文档改动
- `docs/三场景完整结构树.md`（Task #121）：§2 引言/代码块/§2.1 说明订正为运行时真实结构 + RT 槽位路线；
  §1 splash 注明 T5 为 P3；页脚日期 2026-07-13。
- `docs/ai-tasks/ui3d_preview/T2/T3/T4/T5.md`（Task #122）：对齐结构树真实节点路径 + 优先级 + 销毁时机 + DoD。
- `docs/ai-tasks/ui3d_preview/INDEX.md`（Task #123）：任务表加优先级列；新增 §2.5「结构树对齐」章节
  （CreatePanel 真实结构 / slotNode API / CharacterPanel PreviewSlot / MainMenuBackdrop3D / 结构树自身订正）。
- `回到地面/docs/progress/三场景完整结构树.md`（Task #123）：Pending 项改为引用 ui3d_preview + PreviewSlot 落地，
  带 P0~P3 分档；Last updated 2026-07-13。

### 待办（等用户授权）
- T1A/T1B/T2/T3/T4/T5 均未写源码，需逐任务走 Execution Plan → 用户回 `Plan Approved` 才动 `assets/scripts/**`。
- 原待拍板点 T4/T5 配置方案：用户已拍板 **B-lite（配置驱动、默认关闭）**，不再阻塞；T1A 是否开始写源码仍待 `Plan Approved`。

## 第二轮修正（用户审查后的执行期"小坑"修复，2026-07-13 下午）

用户确认大方向已对，提出执行期会踩的小坑并逐条修正。全部为文档改动，未碰 `assets/scripts/**`。

### 必须修正
1. **T3 CharacterPanelLayout.ts 路径**：真实 `assets/scripts/ui/layout/CharacterPanelLayout.ts`（卡片误写 `ui/main/`）。T3 输出/允许范围/执行Prompt/Checkpoint 全改对；确认 `CharacterPanel.ts` 仍在 `ui/main/`（正确）。
2. **previewSlot 获取方式**：从"编辑器绑定优先"改为 `CharacterPanelLayout.ensurePreviewSlot(contentRoot)` 路径查找优先，编辑器 `@property` 仅作可选优化（契合自动化目标）。
3. **结构树 §4.4 例外**：新增 `CreatePanelLayout` 例外——不受"挂载于 ContentRoot"约束，可管理 PanelRoot 运行时子节点 SelectView/NamingView/ActionZone（ContentRoot 是旧兼容空壳）。
4. **T1A 层池池空策略**：从"复用 last+console.warn"改为 **fail-fast（acquire 返回 null）**，严禁静默复用 last（并发串渲染难查）；可选回收同 owner 最旧 surface。acquire 返回类型改 `SurfaceHandle | null`。
5. **T1A RT resize 提为最低要求**：slot 尺寸变化须更新 surface UITransform 并重建/重置 RT；acquire 按 slot×DPR 建 RT、面板重开重 acquire、clearOwner 后重建不复用旧尺寸；未做全局 resize 监听须写明 ResponsivePanelLayout 重排后需重调 showCharacterInSlot。
6. **T1A RT 尺寸上限写死公式**：`rtW/H = clamp(ceil(slot 尺寸 × dpr), 128, 1024)`；`dpr = min(2, max(1, view.getDevicePixelRatio?.() ?? 1))`。

### 建议补充（已实现）
7. **T1B backdrop API 闭合**：新增 `showBackdropInSlot(slotNode, assetId, opts?)` 规范 API；保留 `showBackdrop(assetId)` 便捷封装，内部按 ownerId 解析全屏 slot（MainScene→Canvas/MainMenuBackdrop3D / Splash→Canvas/SplashBackdrop3D），找不到返回 null（不自动建节点）。职责边界：T1B=通用 slot API + slot 解析；T4/T5=节点创建 + 配置加载。

### 结论：B-lite 配置驱动（用户拍板）
- 选 B 思路但最小字段、默认关闭，不绑死资源制作。配置文件 `assets/resources/config/ui3d.json`：
  `{ version, mainBackdrop{enabled,modelAssetId,fallback2dKey,quality,transparent}, splashBackdrop{...} }`，默认 enabled=false。
- 定义 `UI3DBackdropConfig` 接口；加载失败/缺失统一 `if (!cfg?.enabled || !cfg.modelAssetId) return;`（保持 2D，不阻塞）。
- INDEX §4 决策 7 + §5 Q-配置 已闭合为 B-lite；T4/T5 卡改写（移除 A/B 选择，B-lite 固定规范 + ui3d.json 由 T4 执行期创建，默认关闭）。
- T4/T5 仍 ON HOLD 至 backdrop 3D 资产就绪；配置结构已固定，T1A/T1B/T2/T3 落地不受阻。

### 待用户下一步
- 若计划 OK，回 `Plan Approved` 即按 Execution Plan 落地 T1A（PreviewSurface.ts）。

## T1A 执行（Plan Approved，2026-07-13 下午）

用户批准 T1A 执行。落地 `assets/scripts/render/PreviewSurface.ts` + `preview_layer_pool.ts`（纯 TS 层池）+ `tests/render/preview_layer_pool.test.ts`。全程未碰 T1B/T2/T3/T4/T5 源码。

### 实现要点
- 离屏 RT 路线：`acquire(slotNode,opts?): SurfaceHandle|null`；分配用户层(1<<19..1<<16)→建 RT→建预览相机(targetTexture=RT)→在 slot 内建 Sprite 显示 RT。
- 层池枯竭 **fail-fast 返回 null**（不静默复用 last）；RT 尺寸 = `clamp(ceil(slot×dpr),128,1024)`，`dpr=min(2,max(1,view.getDevicePixelRatio?.()??1))`；补 `depthStencilFormat=DEPTH_24` 保证 3D 深度；`sf.flipUVY=true` + `sf.packable=false`。
- resize 最低要求：acquire 按当前 slot 尺寸×DPR 建 RT；面板重开/clearOwner 后 re-acquire 重建 RT；注释写明 ResponsivePanelLayout 重排后需重调 `showCharacterInSlot`。
- 不耦合 CharacterVisualService；T1-A 懒加载单例；监听 `director EVENT_BEFORE_SCENE_LAUNCH` 销毁全部 surface。

### 验证结果
- vitest 层池单测 **6/6 通过**。
- `TS静态检查` 门禁 [OK]（`{}` 配平 + `cc` import 正确）。
- `validate:all`：T1A **未引入新失败**；3 个既有失败门禁（character.preview.* 缺美术资源 / WeChat 内容待审 / character_parts prompt 缺失）为改动前基线，无关。
- 整包 vitest：238 通过 / 13 失败（4 文件，均 IHitResolver 未注册，既有基线，与 T1A 无关）。

### 待编辑器验证
- 新建 .ts 的 .meta 由 Cocos Creator 打开项目时自动生成；flipUVY 朝向需目视确认；实际 RT 渲染需在引擎内验证。

### 同步文档小修（执行前用户要求，已落的 3 处）
- T1B：acquire 后补 `if(!surface)` null 安全判断（showCharacterInSlot + showModelInSlot）。
- T5：方案 B，复用现有 `splashImage` 作全屏 slot，调 `showBackdropInSlot(this.splashImage,...)`，不新建 Canvas/SplashBackdrop3D。
- T4 标题「待配置授权」→「待 backdrop 3D 资产就绪」。

### 下一步
- T1B/T2/T3 可在 T1A 后执行（T1B null check 已补）；T4/T5 仍 ON HOLD 至 backdrop 3D 资产就绪。

## T1B 执行（Plan Approved: T1B，2026-07-13 下午）

用户批准 T1B。落地 `assets/scripts/render/SceneModelPreview.ts`（面板级预览门面，懒加载单例，零挂载逻辑，100% 委托）。未碰 T1A/T2/T3/T4/T5 源码及被委托服务签名。

### 实现要点（对齐真实签名）
- `showCharacterInSlot(slot, id, action='idle', opts?)`：`acquire`→`!surface` return null→`CharacterVisualService.play(modelRoot, character.${id}.${action}, fps=8, forceUnlit=true, previewLayer)`→`!ok` 则 `surface.destroy()`。
- `showModelInSlot(slot, modelAssetId, opts?)`：同结构，改委托 `CharacterModelAssembler.mount(modelRoot, id, undefined,'Weapon', action, forceUnlit, previewLayer)`。
- `showBackdropInSlot` = 语义别名委托 `showModelInSlot`；`showBackdrop` 按 ownerId 用 `find()` 解析 `Canvas/MainMenuBackdrop3D`(MainScene)/`Canvas/SplashBackdrop3D`(Splash)，找不到 return null（**不建节点**，节点创建是 T4/T5 职责）。
- `clearOwner` 委托 `PreviewSurface.instance.clearOwner`（handle 回收单一真相源在 T1A，T1B 不重复登记避免 desync）。
- `PreviewHandle.setAction`：character→再调 `play`（内部 isMounted→clip 切换）；model→`assembler.play(modelRoot, action)`。
- `targetLayer` 全传 `surface.previewLayer`；仅从 cc import `find`/`Node`；所有 acquire 返回值先判 `!surface`（遵铁律 8）。

### 验证结果
- `TS静态检查` [OK]（`{}` 配平 + `cc` import 正确）。
- 整包 vitest：238 通过 / 13 失败（既有 IHitResolver 基线，与 T1B 无关，无新增失败）；T1B 全 cc 委托无纯逻辑，未加新单测。
- `validate:all`：编码/架构/TS静态/UI皮肤 [OK]；3 项 FAIL（资源注册/非UI资源注册/文档一致性）为既有基线，与 T1B 无关。
- 报告 `docs/ai-tasks/ui3d_preview/REPORT_T1B.md`；INDEX T1B 状态标 已完成 ✅。

### 待编辑器验证
- 新建 .ts 的 .meta 由 Cocos Creator 自动生成；showCharacterInSlot 实际出图 / 多面板并发独立层 / clearOwner 回收需引擎内目视。

### 下一步
- 可执行 T2（CreatePanel 接 slot）或 T3（CharacterPanel 接 slot + PreviewSlot）；T4/T5 仍 ON HOLD 至 backdrop 3D 资产就绪。

## T2 执行（Plan Approved: T2，2026-07-13）

用户批准 T2。改造 `assets/scripts/ui/main/CreatePanel.ts`：删补丁 + 改调 T1B。未碰 render/ 与被委托服务签名。

### 删除的补丁
- 字段 `_previewNode` / `_previewCamera`；方法 `_resolveUiLayer` / `_resolvePreviewLayer` / `_setSubtreeLayer`。
- `close()` 内 previewNode/previewCamera 清理段；`_updateModelDisplay()` 内"new 透视相机 + 挂场景根 + 找层 + `CharacterVisualService.play(previewNode,...)` + 子树设层"全部。
- import 收敛：移除 `CharacterVisualService`/`director`/`Vec3`/`Layers`/`Camera`。

### 新增 / 改写
- 字段 `_previewHandle: PreviewHandle|null` + `_previewGen=0`（in-flight 竞态令牌）。
- `_updateModelDisplay(id)`：`++_previewGen` 使旧 handle 失效 → `await SceneModelPreview.instance.showCharacterInSlot(this._modelDisplay, id, 'attack', {ownerId:'CreatePanel', forceUnlit:true})` → 若 `gen!==_previewGen` 则 `handle?.destroy()`（防 late-resolve 泄漏）否则赋值。
- 销毁时机全覆盖：`close()`（`_previewGen++` + destroy + `clearOwner('CreatePanel')`）；`_setPhase('naming')`（SelectView 隐藏时销毁）；回 SelectView 由 `_onSkip` 重调 `_updateModelDisplay`。
- `_modelDisplay` 语义 = 运行时 `PanelRoot/SelectView/PreviewZone/ModelDisplay` 节点（UITransform+Sprite 表面），非世界坐标锚点；位置/尺寸随布局自适应。

### 验证结果
- `TS静态检查` [OK]；`编码审计` [OK]（中途新注释以 `while ` 开头误触发 1 个 P0 `comment_may_swallow_code`，已重述注释规避——教训：TS 注释行首不得是 `const/let/var/return/if/for/while/switch/class/function/this.`）。
- 整包 vitest：238 通过 / 13 失败（既有 IHitResolver 基线，与 T2 无关，无新增）。
- `validate:all`：架构/TS静态/编码审计/配置校验/包体预算/UI皮肤 [OK]；3 项 FAIL（资源注册/非UI资源注册/文档一致性）为既有基线，与 T2 无关。
- 报告 `docs/ai-tasks/ui3d_preview/REPORT_T2.md`；INDEX T2 状态标 已完成 ✅。

### 待编辑器验证
- 进入创建冒险者界面：warrior 3D 模型显示于 PreviewZone 槽位内，标题/职业卡片/确认·跳过按钮完整可见、无扭曲；切职业/切 NamingView/close 无 RT/rig 泄漏。

### 下一步
- 可执行 T3（CharacterPanel 接 ContentRoot/PreviewSlot，由 CharacterPanelLayout 管理）；T4/T5 仍 ON HOLD 至 backdrop 3D 资产就绪。

## T3 执行（Plan Approved: T3，2026-07-13）

用户批准 T3。改造 `assets/scripts/ui/main/CharacterPanel.ts` + `assets/scripts/ui/layout/CharacterPanelLayout.ts`（2 文件），接 T1B 预览。未碰 render/ 与被委托服务签名。

### CharacterPanelLayout.ts
- 新增 `static ensurePreviewSlot(contentRoot: Node): Node`（幂等：确保 `ContentRoot/PreviewSlot [UITransform]` 存在并返回）。
- `applyLayout()` 内调 `ensurePreviewSlot` 并为 PreviewSlot 分区定位：`slotSize=round(min(h*0.28,180))`，`y=h/2-80-slotSize/2-16`（灵魂石下方独立 Y 分区）；`currentName/Info/Stats` 下推至 `below=h/2-80-slotSize-40` 三档（间距 36）；`slotContainer` 保持 y=20；`closeBtn` 置底。经测算 h=480/640 下均不重叠。

### CharacterPanel.ts
- import 加 `SceneModelPreview` + `PreviewHandle`(type) + `CharacterPanelLayout`。
- 字段 `_previewSlot / _previewHandle / _previewGen`。
- `_ensurePreviewSlot()`：编辑器绑定可选 → `panelRoot.getChildByPath('PanelFrame/ContentRoot')` / `getChildByName('ContentRoot')` / 兜底 `panelRoot` → `CharacterPanelLayout.ensurePreviewSlot`。
- `_refresh()` 改 async：`++_previewGen` + destroy 旧 handle → `_ensurePreviewSlot()`（无槽位安全 no-op）→ `await showCharacterInSlot(slot, selectedId,'idle',{ownerId:'CharacterPanel',forceUnlit:true})` → await 后校验 `gen!==_previewGen` 防 in-flight 泄漏。
- `open()/refresh()/_buildSlots 回调` 调 `void this._refresh()`；`close()` 加 `_previewGen++` + destroy + `clearOwner('CharacterPanel')`。

### 验证结果
- `TS静态检查` [OK]（`{}` 配平 + cc 仅 import Node）；`编码审计` [OK]（issues=0/p0=0，注释避开行首关键字，T2 教训已固化）。
- 整包 vitest：238 通过 / 13 失败（既有 IHitResolver 基线，与 T3 无关，无新增）。
- `validate:all`：架构/TS静态/编码审计/配置校验/包体预算/UI皮肤 [OK]；3 项 FAIL（资源注册/非UI资源注册/文档一致性）为既有基线，与 T3 无关。
- 报告 `REPORT_T3.md`；INDEX T3 状态标 已完成 ✅（顺手把 T4 行旧「待配置授权」对齐为「待 backdrop 3D 资产就绪」）。

### 待编辑器验证
- 角色管理面板：当前角色在 PreviewSlot 槽位内显示 3D 模型（idle），与文字信息区/卡列表并存不重叠、UI 不扭曲；切/解锁角色时旧预览回收无泄漏；close 后再开无残留。新建 .ts 的 .meta 由 Cocos Creator 自动生成。

### 主线进度
- T1A→T1B→T2→T3 已全部完成并贯通：离屏 RT 底座 + 面板门面 + CreatePanel/CharacterPanel 两个实际界面接入 3D 预览。
- 仅剩 T4/T5（MainMenuBackdrop / Splash 3D 背景）ON HOLD 至 backdrop 3D 资产就绪（B-lite 配置结构 `ui3d.json` 已固定）。

### 下一步
- 主线 3D 预览链路已闭环；T4/T5 待 3D 背景资产就绪后可执行（B-lite：仅改 ui3d.json enabled=true + modelAssetId 即可开，无需拆代码）。

## T4 + T5 执行（用户「依次进行T4/T5」，2026-07-13 晚）

用户批准 T4/T5 一并执行（两任务同 B-lite 方案、共用 ui3d.json）。

### T4 — MainMenuBackdrop 3D 背景
- 新增 `assets/scripts/config/ui3d.ts`：`UI3DBackdropConfig` 接口 + `loadUI3DBackdropConfig(group)` 加载器（永不抛错，缺失/解析失败/分组缺失返回安全默认 `enabled=false`，调用方据此静默降级）。这是计划允许的"接口置于 config/ui3d.ts"。
- 新增 `assets/resources/config/ui3d.json`：默认 `enabled=false` / `modelAssetId=""`；含 `metadata` 块（见下方门禁教训）。
- 改写 `assets/scripts/ui/main/MainMenuBackdrop.ts`：移除"UI 节点直挂 ModelComponent"（正交相机下不可见、无效）→ 读 ui3d.json `mainBackdrop`，降级规则 `if(!cfg?.enabled||!cfg.modelAssetId) return;`，有效时委托 `SceneModelPreview`。
- 改 `assets/scripts/MainSceneController.ts`：在 `_ensurePersistentMainBackground` 内创建/复用 `Canvas/MainMenuBackdrop3D`（`UITransform` 全屏拉伸 + 挂 `MainMenuBackdrop` 组件），`MainBackground`=siblingIndex(0) 最底、`MainMenuBackdrop3D`=siblingIndex(1)（在 2D 背景之上、MainUI 之下，使 3D 可盖住 2D 又不挡按钮）。

### T5 — Splash 3D 背景（方案 B，复用 splashImage）
- 改 `assets/scripts/ui/SplashUI.ts`：读 ui3d.json `splashBackdrop`，降级规则缺失保持 2D；有效时 `showBackdropInSlot(this.splashImage, cfg.modelAssetId, {ownerId:'Splash',...})`（直接复用现有全屏 `splashImage` 作 slot，不新建节点）；`onDestroy` 加 `clearOwner('Splash')`。复用 T4 的 `config/ui3d.ts` 加载器，未重复造轮。

### 关键偏差（T4，稳健性）
- 计划写 `showBackdrop(cfg.modelAssetId, {ownerId:'MainScene'})`（内部 `find('Canvas/MainMenuBackdrop3D')`）。改用 `showBackdropInSlot(this.node, cfg.modelAssetId, {ownerId:'MainScene',...})`：`MainMenuBackdrop` 组件即挂在 slot 节点上，`this.node` 即权威 slot，彻底规避运行时动态建节点后 `find()` 时序竞态；`ownerId` 仍为 `'MainScene'`，`clearOwner('MainScene')` 行为完全一致（同语义更稳健，非行为变更）。

### 验证结果
- `TS静态检查` / `编码审计` / `架构门禁` / `UI皮肤绑定` / `包体预算` 全 [OK]；注释无行首关键字（T2 教训已固化）。
- `validate:all`：**仅剩 3 项既有基线 FAIL（资源注册 / 非UI资源注册 / 文档一致性=character_parts prompt 缺失），与 T4/T5 无关，执行前后一致**；T4/T5 **未引入任何新失败**。
- 整包 vitest：238 通过 / 13 失败（既有 IHitResolver 基线，无关）。

### 门禁教训（新增，可复用）
- `tools/config_pipeline/validate_config.py` 对 `assets/resources/config/` 下**每个** JSON 强制要求顶层 `metadata` 块，且 `metadata.version` 须为非空字符串（缺失=error 致 `配置校验` FAIL；`metadata.lastUpdated` 缺失仅 warning）。新增配置 JSON 务必带 `metadata:{version:"x.y.z", lastUpdated:"YYYY-MM-DD", description:"..."}`，否则 `配置校验` 门禁失败。本次 ui3d.json 初版缺 metadata 曾触发 1 error，补 metadata 后 `配置校验` [PASS]。

### 报告 / 索引
- `docs/ai-tasks/ui3d_preview/REPORT_T4.md` + `REPORT_T5.md`；`INDEX.md` T4/T5 状态标 已完成 ✅，顶部状态行改「执行中（T1A–T5 已全部落地，B-lite 默认关闭）」。

### 待编辑器验证 / 资产就绪后
- 资产就绪只需 `ui3d.json` 置 `mainBackdrop.enabled=true`+`modelAssetId` / `splashBackdrop.enabled=true`+`modelAssetId` 即可开，无需拆代码。
- 引擎内目视：main/splash 全屏 3D 背景出图、层隔离、UI 不扭曲、按钮可点、切场景无泄漏；确认 MainMenuBackdrop3D 与 MainBackground 的 z 序（3D 盖住 2D 背景）符合预期。
- 创建 `docs/ai-tasks/AI_EXECUTION_PROTOCOL.md` —— 综合 _agent_contract.md、2D转3D实施计划.md、ui3d_preview INDEX、各 REPORT 的完整 AI 执行标准协议，涵盖 Pipeline/铁律/红线/任务卡规范/Execution Plan/门禁/失败处理/命名/交付物/记忆规则/附录。供所有 AI 对话统一遵循。

## 审查 ART_ASSET_GAP_CHECKLIST.md（2026-07-13 晚，用户问"还有没有要补充"）

用户指 `E:/game/docs/ART_ASSET_GAP_CHECKLIST.md` 让我基于 ui3d_preview 实现补缺口。结论：**初版漏了 T4/T5 引入的 3D 背景模型资产**，已直接在文件内补 8 处：

1. §0 结论补"新增 3D 类目：2 个 3D 背景模型（ui3d.json 默认关闭）"。
2. §1 表格 backgrounds 行：3D 状态 `0/2`、生成缺口改"2 个 3D 背景模型"。
3. §2.4 命名加 `BG_{Scene}.glb` 前缀；§2.4 正则扩展为含 `BG`（并标注须同步 `art_quality_budget.json → rules3d.naming.pattern`，见 §6.E）。
4. §2.5 预算表补"背景(3D)"行（≤6000 Tri / 1024²~2048² / ASTC 8×8 / LOD 100·50%）。
5. §4 末新增 §4.5「UI 3D 背景」小节：来源/渲染方式/`BG_MainMenu.glb`+`BG_Splash.glb` 清单/规格（静态可无骨、unlit 友好、forceUnlit=true、2D 兜底保留）+ **与 §16.1 规则冲突待拍板**（§16.1 写"backgrounds 保留 2D"，但 ui3d_preview 叠加可选 3D 背景，须在 ART_RESOURCE_RULES.md §16.1 补一句，本文件不擅自改权威源）。
6. §6 加 F 项：ui3d.json 启用前置（须先生成 BG_* + 填 modelAssetId，非门禁 FAIL）。
7. §8 交接包补 `ui3d.json`（生成 AI 据此命名 BG_* 并回填 modelAssetId）。

**可复用教训**：生成类清单须随代码实现增量更新——本次实现 T4/T5 时已引入新资产类目（`BG_`），但缺口清单初版未同步，导致"功能待资产"状态在清单里不可见。未来每完成一个会消费新资产的任务，应同步回写缺口清单。

## AI_EXECUTION_PROTOCOL.md 修订（2026-07-13，用户审查后修正）

用户审查 v1 后指出 7 个问题，已全部修正为 v1.1：
- **[P0]** 审批规则重构为三层模式：Read-only Review / Normal Execution / Direct Execution（Direct 优先级最高，用户说"直接执行"时覆盖等待）
- **[P0]** _architecture_report.md 规则统一为"默认只读；仅任务卡明确允许时才可写入"，Step0 改为检测+报告变更而非自动写入
- **[P1]** 权威来源和会话流程补入 3 个 3D 自动化文档：ART_ASSET_GAP_CHECKLIST.md / 3D资源全自动化资产工厂实施方案.md / 3D资源生成流程_完整说明_方案A.md
- **[P1]** 基线 FAIL 不再硬编码，改为引用 ART_ASSET_GAP_CHECKLIST.md 的"当前门禁基线"章节
- **[P1]** 代码块/目录树/表格符号/状态值全部 ASCII 化（[OK]代替emoji、纯数字标题代替罗马数字）；协议中文保留，纯英文注释规则不动
- **[P2]** 新增 §7「3D / Art Task Extra Gates」：前置必读、6 项附加验证命令、5 条 3D 资产硬规则（AI GLB 不入 runtime / Hunyuan3D 仅 prototype / 批量需 gap reconciliation / approved 前不入库 / 资产管线一致性）
- **[P2]** 记忆规则从"每次必须更新"改为"默认写 REPORT 不写记忆；仅长期规则/用户要求/重复踩坑才写"；Skill 创建需用户明确同意

## AI_EXECUTION_PROTOCOL.md v1.2 — 自包含改造（用户问"另一台电脑没有这些文件"）

v1.1 仍有大量外部路径引用，另一台机器的 AI 无法使用。v1.2 改造为**完全自包含**：
- 头部声明 `[PROJECT]` 标记机制：AI 看到 `[PROJECT]` 的段落为可选，文件不存在时跳过
- **嵌入 `_agent_contract.md` 全文**到 §2.1（最高约束不丢），另一台机无需外部文件
- 会话启动流程拆为 "CORE（本文件即可）" + "PROJECT-SPECIFIC（可选）"
- §1.1 范围改为项目无关描述，`[PROJECT]` 节单独标注
- 附录 C 拆为 "Standalone Methodology" + "Project-Specific Addenda"

## AI_EXECUTION_PROTOCOL.md v1.3 — 强制声明 + MEMORY.md 引用（"所有AI必须执行，先建任务卡"）

用户要求协议成为所有 AI 对话的强制标准，且执行前必须先建任务卡：
- **MEMORY.md** "Must Read First" 第 0 项加入协议路径，注明"优先级高于本文件其他所有规则"
- **协议头部** 新增 `MANDATE (P0, Highest Priority)` 声明 + `>>> MANDATORY: TASK CARD FIRST <<<` 醒目段落
- 核心：任何 code change / art gen / doc edit 前，必须先创建或确认已存在符合 §4 规范的任务卡

## D-1 落地完成 — validate:all 6/9 → 9/9（策略A 重指不删键 + 校验器修盲区 + 死键清理）

D 任务卡 D-1 子任务全部落地并复验通过（Task #153~#157 completed）。四类门禁 FAIL + 1 个独立 error 全部修复：
- **D-1a game_assets.json**：35 个 `character.{class}.{action}` 键 → assetId 重指 `textures/ui/character/avatar_{class}`，type sprite_sheet→sprite，删 frameWidth/frameHeight/frames/layout（`_check_type_vs_file` 要求单图配 sprite 且无帧元数据）。
- **D-1b ui_assets.json**：5 个 `character.preview.{class}` → assetId 重指 avatar_{class}，type/usage 不变。
- **D-1c check_assets_registry.py v2.0.0→v2.1.0**：修 textures-only 扫描盲区。新增 RESOURCES_DIR；Check A 拆纹理键/非纹理键，非纹理键按 config `path` 解析 `{path}.*` 校验存在性 → archer 模型误报清除（registered_but_missing_file: []）。
- **D-1d ART_RESOURCE_RULES.md _version 1→2**：§15 移除 `character_parts` 死键 6 处（detail_anchors/transparent_categories/matte_categories/min_opaque_ratio/palette_retry_steps/recommended_sizes）+ 加 _changelog 记录理由（2D 走 §16.2 母版切割、prompts.json 无此前缀致 doc_consistency 恒 FAIL）。保留 §16.2 散文引用。
- **追加修复（非D-1四类）art_quality_budget.json**：补 metadata 块（version 1.0.0 / lastUpdated 2026-07-13），修 config 校验 error=1（Task #142 遗漏），对齐其余 24 配置文件强制 schema。
- **关键纠偏**：原 D-1 三处假设被读码推翻——不是 .glb 扩展名问题（是校验器盲区）、不是补 prompts.json 锚点（是死键且前缀永不产生）、不是仅重指不改 type（需删帧元数据）。
- **决策记录**：策略 A（重指不删键）；决策点2 选"豁免 doc-consistency（改检查配置 = 清死键并记录理由）"；未新增接口/未改服务签名/未生成GLB/未 --skip 门禁。
- **交付物**：REPORT_D-1.md 新建；D_gate_baseline_and_character3d.md 回写（状态=D-1已完成 9/9，DoD 6项全勾）。
- **遗留（Task #151, warning 不阻塞）**：30 个 miniboss idle PNG unreferenced，待用户拍板清理/接线。D-2（外机 4 角色 3D 生产，warrior=熊/mage=兔已定）、D-3（回传入库+运行时绑定）待用户指示启动。
