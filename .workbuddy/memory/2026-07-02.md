## 2026-07-02

- 补全 `docs/场景编辑器搭建手册.md` 4A 节 LoginPanel 结构，增加 `AgreementLabel` 节点及 `agreementLabel` 字段绑定说明；对应 key 为 `ui.loginAgreement`。
- 修正 `tools/migration_guide.md`：将 `CharNameLabel`、`CharClassLabel`、`LevelLabel`、`SoulStoneLabel` 从"需挂 LocalizedLabel"改为"动态文本（代码刷新，跳过）"，并新增 `LoginPanel/AgreementLabel` 的迁移条目。
- 迁移 `LoginPanel._setStatus()` 内硬编码英文状态文案为 `T('ui.loading')` / `T('ui.loginFailed')` / `T('ui.loginRetry', { n })`。
- 迁移 `BattleHUD` 伤害数字里的硬编码 `暴击 ` 为 `T('ui.crit')`。
- 迁移 `EventUI` 自动选择倒计时文案为 `T('ui.autoSelect', { secs })`。
- 验证：`python tools/check_hardcoded_text.py` 0  issues；`npm.cmd run validate:all` 5 项门禁全部通过。
- 修正文档中冗余的"添加 UITransform"指引（Cocos 3.x 空节点自动自带 UITransform，无需额外添加）：CardRoot、RouteContainer、LockedContainer、PlayerInfo 共 4 处。
- **重构 CreatePanel.ts**：CHAR_OPTIONS 改用 textKey 索引（animalKey/classKey/descKey），所有硬编码英文（Bear/Warrior/Solid shield等）改为 T() 调用；错误提示文案迁移至 text.json。
- **text.json 新增** classAnimal（熊/鹿/狐/兔/猪）和 classDesc（5 个职业描述）两个分组，以及 createNameRequired/createOnlyWarrior 等错误 key；修复缺失逗号语法错误。
- **修复 scan_scene_labels.py**：增加 LoginPanel/CreatePanel/SettlementPanel 逐面板路径规则；扩展 DYNAMIC_NODES 集合（TopBar 4 个动态文本 + LoginPanel StatusLabel）；扩充 cn_map。
- **docs 2C 节新增步骤 4**：PanelRoot 全屏背景遮罩（Sprite + 半透明 Color），解决面板打开时主城按钮透过问题。
- 重新生成 migration_guide.md：全部 27 个待挂 LocalizedLabel（key✅）+ 5 个动态文本。
- 全部推送到远程：`634061d fix: CreatePanel hardcoded strings + LoginPanel labels + scanner rules`。
- **CreatePanel.ts 修复** classNode 未 addChild 到 card 的 bug + 移除错误的 Vec3 位置设置 + 默认名改用 T('ui.defaultName')。
- **docs 全面更新**：所有 7 个面板 + AreaSelectPanel 增加 DimMask 和 PanelFrame 结构，标注各 PanelFrame 尺寸（Login 640x420、Create 820x560、Character 900x600、Settlement 720x560、Settings 640x460、AdventureLog 720x520、AreaSelect 960x600）。
- **迁移 CharacterPanel**: CHAR_SLOTS 改用 classKey，所有文本走 char* 系列 text.json key（charInfo/charStats/charUnlocked/charLocked/charSelect/charUnlock）。
- **迁移 SettlementPanel**: 6 个结算统计值全部走 settlement* 系列 key。
- **迁移 SettingsPanel**: versionLabel 走 settingsVersion，accountLabel 走 settingsAccount（新增 settingsGuest/settingsWeChat 子 key）。
- **迁移 AdventureLogPanel**: 4 个统计值全部走 log* 系列 key。
- **迁移 AreaSelectPanel**: 箭头分隔符用 routeArrow，锁定路线用 routeUnlock。
- **text.json 新增**：ui.unknown、routeArrow、routeUnlock、settingsGuest、settingsWeChat。
- 全部推送到远程：`1674008 feat: migrate all remaining panel hardcoded strings to T()`。
- validate:all 5 项全通过。
- **新增 ResponsivePanelRoot.ts**：自适应面板布局组件，监听 canvas-resize，PanelRoot/DimMask 自动铺满父节点，PanelFrame 按比例缩放 + min/max 限制。
- **重构 AreaSelectPanel ROUTES**：用 UnlockCondition 联合类型（none/clear_zone/reach_floor/player_level）替代硬编码英文解锁条件。新增 _isRouteUnlocked/_getUnlockText。
- **PlayerDataManager 扩充**：zoneClearCounts 数据字段 + getZoneClearCount/recordZoneClear 方法。getBestFloor 可选 zoneId 参数。
- **text.json 新增**：unlockNone/unlockClearZone/unlockReachFloor/unlockPlayerLevel。
- **docs 全面更新**：2C 步骤 4 改为 ResponsivePanelRoot 组件方式，移除所有固定尺寸，新增各面板比例参数表。
- 全部推送到远程：`a8108ad feat: ResponsivePanelRoot + UnlockCondition route system`。
- 全部推送到远程：`c19b3a8 feat: implement 100% of adaptive UI plan - 4 new components`。

**新组件一览：**
- `ResponsivePanelContent.ts` — ContentRoot 根据 PanelFrame 尺寸 + padding 自动计算
- `ResponsiveUIRoot.ts` — 地牢 UIRoot 全屏自适应（替代固定 1280x720）
- `layout/SplashLayout.ts` — 启动页：图片居中、加载条底部、跳过按钮右上
- `layout/DungeonHudLayout.ts` — 地牢 HUD 四角定位（左上/左下/右下/右上）
- `ResponsivePanelRoot.ts` 更新：加 onEnable()、view.getVisibleSize() 兜底、触发 Content 布局
