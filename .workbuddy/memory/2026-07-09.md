# 2026-07-09 工作日志

## 生成项目文件结构说明文档
- 任务：扫描 `E:\game` 全部文件，生成结构化 Markdown 文档，逐文件说明路径/类型/用途，代码文件列出函数/方法（参数、返回值、用途），配置文件说明参数，文档文件给摘要。
- 方法：用 AST 提取 43 个 `tools/*.py`（225 函数、4 类）；用正则提取 146 个 `回到地面/assets/scripts/**.ts`（133 类、994 方法、33 函数）；提取 105 篇 Markdown 文档首部做摘要分类。生成脚本在 `/tmp/extract_py.py`、`/tmp/extract_ts.py`、`/tmp/extract_docs.py`、`/tmp/gen_doc.py`，中间产物在 `.workbuddy/tmp_*.json`。
- 范围决策：批量资源目录（textures/library/build/temp/art_source，数千文件，多为生成物/缓存）按类型归类说明，未逐文件展开，避免无意义长清单。
- 补充：用户要求确认「交给另一个 AI 能否快速了解项目」。原文档是文件清单（catalog）但缺概念性架构叙事。已在文档顶部新增「项目速览（给接手 AI 的快速上手）」一节：定位、双工程结构、运行时架构骨架、必守约定、构建/运行、当前状态/规模、推荐阅读顺序。现文档可独立用于 AI 接手。仍可选补：游戏配置表（数值/装备/技能）数据 schema 与字段说明。
- 产出：`E:\game\项目文件结构说明.md`（约 3437 行 / 186KB，UTF-8 无乱码）。
- 说明：Cocos TS 源码注释少，TS 方法「用途」多为按命名推断，已在文档中注明。

## 补强：配置表 Schema + TapTap 构建修正
- 用户追加要求：① 把「游戏配置表 schema」补进文档（原 §3.5 之后新增 §3.6）；② 微信小游戏构建已过时，现发布平台为 TapTap 安卓。
- §3.6 内容：数据表总览表（12 张 `assets/resources/config/*.json`：battle/economy/elements/equipment/items/monsters/player/skills/zones/animations/characters/platform）+ 各表字段 Schema 表 + `GameConfig.ts` 编译期常量节选 + `env/dev.json` 开发覆盖说明。加载入口点名 `ConfigService.loadAll()`（`config/ConfigService.ts`）、校验（`validateMetadata`/`validateZoneMonsterRefs`）、类型（`ConfigTypes.ts`）。
- TapTap 修正：概览/构建运行/平台适配/工程概述/`build/` 目录说明全部改为 TapTap 安卓；明确 `platform.json` 当前 `channel=taptap`/`platform=android`/`login.provider=taptap`/`compliance.*=true`/`ads.enabled=false`；原 `build/wechatgame/` 标注废弃；§5 中仍写微信小游戏的旧文档（`docs/项目记忆与提审规则.md` 等）加「内容已过时，需同步更新」提示。
- 同步在「推荐阅读顺序」加了 §3.6 必读项。
- 产出：`项目文件结构说明.md` 现约 3638 行 / UTF-8 无乱码。
- 提示：根 `E:\game\` 与 `回到地面\` 下仍各有一套 `tools/`/`docs/`，微信相关旧文档尚未全量改写，仅文档描述层标注了过时。

## Icon 资源批量生成与 Prompt 修复
- 任务：用户发现 `art_source/textures_review/master/icons/items/` 下大量生成图与文件名用途不匹配，要求查根因并修复。
- 根因：`normalize_prompts_positive_style.py` 的 `ICON_SAFE_MAP` 仅覆盖 16 个 icon，其余 51 个走 `_generate_unique_concept()` 哈希回退，只随机组合形状/材质/装饰，不描述具体物品，导致 52/67 个 icon 概念不匹配。
- 修复：扩充 `ICON_SAFE_MAP` 到 63 个条目，覆盖 buff/debuff/element/item/relic/set/skill 所有子类别；剔除 `heart-shaped` 等风险词，改用正向描述。运行脚本重写 `prompts.json`。
- 验证：全量扫描后 0 泛化概念，概念均与文件名匹配。
- 全量生成：重新生成 67 个 icon，物理文件 67/67 存在；进度文件状态 65 validated、2 failed（`icons/items/item_healthPotion.png` / `item_largeHealthPotion.png`），失败项无错误信息，生成图可见且正确，疑为状态跟踪问题。
- 后续发现：pipeline generate 会自动导入 runtime，65 个 validated 的 icon 已自动更新 Runtime（7月9日新版），仅 2 个 failed 的仍是旧版（6月29日）。所有运行时 icon 实际已换新。

## 生成完整资源状态报告
- 任务：用户要求生成一份包含所有 499 个资源详细状态（母版路径/状态/生成时间/人工确认/入库路径/状态/时间）的 MD 文件。

## Backgrounds 全量生成（俯视战斗背景）
- 任务：用户要求按俯视视角重新生成所有 17 个背景（combat/event/room），参考 bg_combat_abyss 风格：四周强 3D 边缘装饰、中央空旷干净。
- 背景规格：1920×1080 JPG，cover 模式，渲染在 5 层最底层 Background(0)，俯视 2D 地牢 6×6 网格居中。
- prompt 调整为 top-down 俯视描述，各区域特色 3D 边缘（树干/冰柱/熔岩/水晶/石柱等）、中央留空给玩家操作。
- 成果：17/17 新版 JPG 生成并入库（350~550KB/张，技术门禁全部通过），旧版 PNG 母版全部清理。
- 修复：art_pipeline.py 加了中断残留清理逻辑（_cleanup_stale_temps），下载+后处理段加了 try/finally；art-pipeline SKILL.md 明确了人工确认门禁。
- 数据库清理：对 master/backgrounds 中旧版 PNG 进行清理，仅保留与新版 JPG 一一对应的文件。
- 任务：用户要求生成一份包含所有 499 个资源详细状态（母版路径/状态/生成时间/人工确认/入库路径/状态/时间）的 MD 文件。
- 方法：扫描 `prompts.json`（499 个资源）、`art_pipeline_progress.json`（生成进度）、文件系统（master/runtime 存在性和修改时间），逐资源对比。
- 产出：`docs/progress/resource_status.md`（559 行，131KB），含总览表和 8 个类别详情表。
- 洞察：发现 pipeline 的 generate --self-check 会自动将 validated 资源 copy 到 runtime（非 import 专属功能）；只有 failed 的资源不会自动入库。

- 任务：用户反馈调用 art-pipeline 生成 `tiles/abyss` 与 `tiles/catacombs` 资源时始终出现骷髅、心脏、血液元素，要求从 prompt、资源内容、skill 配置、模型行为、pipeline 中间环节、资源差异六个维度排查。
- 核心发现：
  1. `prompts.json` 中 tile prompt 干净，无危险词。
  2. `art_pipeline.py:1517` 将 `ART_RESOURCE_RULES.md` 第 15 节的 `safety_block` 拼入最终 prompt，而该 `safety_block` 包含 `no blood / no skull / no organs / no wound` 等危险词，违反项目「危险词不发送给 AI」规则。
  3. Agnes image-2.1-flash 存在否定失明（negation blindness），prompt 中写「禁止骷髅」反而提高骷髅生成概率，这是根因。
  4. Skill 文档称 tile 是「程序化生成」，但 `procedural_categories: []` 导致 tile 实际走 AI，从而受到含危险词的 safety_block 影响。
  5. 后处理（抠图、调色板压缩、裁剪）不可能引入新语义；问题只出现在 2026-07-09 18:02 左右由 Agnes 重新生成的资源上。
- 已验证：`tile_abyss_floor.png`、`tile_abyss_wall.png`、`tile_catacombs_floor.png` 确实包含骷髅/心脏/血液状元素。
- 产出：`E:\game\docs\analysis\abyss_catacombs_skull_blood_root_cause_analysis.md`（含完整六维度分析、修复优先级、关键代码位置、问题资源清单）。
- 待决策：是否立即重写 `safety_block`、是否将 tile 改为程序化生成、是否对 2026-07-09 生成的问题 tile 批量 reject 并重做。
