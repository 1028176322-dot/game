# 2026-07-10 工作日志

## 2D → 全 3D 升级改造方案（交付）

- **交付物**：`docs/2D转3D全面升级方案.md`
- **背景调研结论（现状基线）**：
  - 引擎 Cocos Creator 3.8.8，当前仅用 2D（Sprite + Animation）。
  - **当前无任何物理引擎**——碰撞纯靠 `GridManager.isWalkable()` + occupied + 坐标距离判定。这是升级最大决策点。
  - 移动为离散 4 向网格 tween（`PlayerController`）；动画为 `SpriteAnimationService` 切 2D 图集帧。
  - 地牢双层：`DAGGenerator`（跑图元地图，引擎无关，保留）+ `GridManager`（房间网格，3D 化）。
  - 美术 ~499 张 2D（角色 35/怪物 36/Boss 120/特效 27/图标 67/地块 24/背景 17/UI ~150）。
- **方案核心策略**：三层分离——保留层（玩法逻辑/数值/DAG/UI 业务）、适配层（资源加载/房间构建/投影）、替换层（渲染/动画/相机/碰撞/光照/美术）。
- **两大待锁决策**：A=移动/相机（推荐 3D 自由移动+倾斜透视相机）；B=物理（推荐引入 PhysicsSystem 但仅 Kinematic 胶囊+重叠/射线，不用动力学，保确定性）。
- **路线图**：Phase 0 评估基建 → 1 渲染&相机切片 → 2 角色&动画 → 3 战斗3D化(碰撞/技能/锁定) → 4 地牢&美术批量 → 5 光照/特效/世界UI → 6 优化&TapTap发布（总 22–34 周）。
- **范围**：覆盖功能设计 / 代码实现 / 美术资源三维度，均对齐 TapTap 卡通动物风肉鸽 ARPG 定位。

## 2D→3D 方案 v2 修订（架构评审后）

- 用户（资深架构视角）评审 v1 打 9.1/10，指出最大短板：**偏"美术升级"、缺"工程架构升级"**。提出 15 点修订。
- **已采纳并写入 `docs/2D转3D全面升级方案.md`（v2 覆盖重写）**：
  1. 战斗拆层：SkillSystem(只管CD)→CombatSystem→TargetSelector→HitResolver→DamageResolver→EffectExecutor→ProjectileSystem→LockOnManager（§3.8）。
  2. 物理不直接依赖：`ICollisionService` 接口抽象，Physics 仅为实现（§3.3）。
  3. GridManager 拆分：DungeonGenerator→RoomBuilder→TileMap→NavigationGrid→RoomRuntime（§3.7）。
  4. 技能数据驱动：SkillData→SkillGraph→SkillExecutor，去 switch(id)（§3.9）。
  5. AI 重做：BehaviorTree/StateMachine + AIComponent，产出 MoveCommand/SkillRequest（§2.5/§3.10）。
  6. NavMesh 推迟到 Phase4+，第一阶段继续 Grid 寻路（决策 C）。
  7. 动画抽象：AnimationController→StateMachine→AnimationGraph（§3.5）。
  8. 资源缓存：AssetCache + 引用计数 + 预载/异步/释放队列（§3.6）。
  9. 性能优化扩展：Occlusion/Frustum Culling、GPU Instancing、Static/Dynamic Batching、Texture Atlas、Mesh Combine（§3.13）。
  10. 网络同步前置：MovementComponent→MoveCommand→Execute（§2.6/§3.12）。
  11. 事件总线分类：BattleEvent/UIEvent/AudioEvent/SceneEvent/InputEvent/RuntimeEvent（§3.11）。
  12. 组件化/ECS 思想：拆分 PlayerController 为 Movement/Animation/Combat/Stat/Target/Interaction Component（§3.12）。
  13. 性能预算表：玩家≤3000Tri/Boss≤8000Tri/同屏怪≤25/DrawCall<120/内存<800MB/≥30fps/CPU<12ms/GPU<15ms（§6.1）。
  14. Phase0 拆为 Demo1–6（加载/相机/碰撞/技能/房间/100怪性能）（§5.2）。
  15. 垂直切片先行：一个角色+一怪+一技能+一Boss+一房间+完整UI+完整流程先可玩再复制（§5.3，最关键）。
- **用户架构偏好（值得长期记）**：强接口化、组件化、数据驱动、垂直切片先行、Demo 验证、性能预算驱动；反感"大单体类"（BattleManager/GridManager/eventBus 变垃圾桶）。后续本项目涉及架构设计时默认按此标准。

## 2D→3D 方案 v3 修订（第二轮架构评审后）

- 用户在 v2（9.6/10）基础上又提 15 点，直指**基础设施层缺失**。已覆盖重写为 **v3**。
- **v3 核心新增章节 §5 工程架构与基础设施 + §6 整体架构总图 + 附录 B（15 点采纳）**：
  1. 生命周期 LifecycleManager（★ 最关键）：`ILifecycle`(initialize/enter/exit/pause/resume/destroy) + LifecycleManager 逆序销毁，根除事件/资源/AI/相机泄漏（§5.1 + §6.3 状态图）。
  2. ServiceLocator/DI：`GameContext` 集中注册+注入（`ctx.get(Token)`），业务禁止 `new`（§5.2）。
  3. 统一配置 ConfigDatabase（Skill/Monster/Boss/Effect/AI/Camera/Audio），System 不得自行 load（§5.3）。
  4. 存档 SaveManager（RunState/PlayerState/DungeonState/EnemyState/RandomSeed）+ 进房自动存档/崩溃恢复（§5.6）。
  5. 调试框架 DebugPanel（F8，FPS/内存/DrawCall/AI/SkillCD/Collision/Nav/Anim/Camera/Seed/Events）（§5.5）。
  6. 独立日志 Logger（battle/ai/scene/physics/asset/ui/audio 7 通道 + 分级）（§5.4）。
  7. 音频升级 AudioSystem（BGM/SFX/3D Audio/Voice/Ambient/Snapshot）（§5.8）。
  8. 相机升级 CameraBrain（Follow/LockOn/Boss/Dialogue/Cinematic/Shake/Zoom 模式策略）（§3.4）。
  9. 版本迁移策略：master + feature/3d-* 分支，冻结/删除 2D 时机（§5.12）。
  10. 自动化测试：Combat/Skill/AI/Seed/Dungeon 单测 + Mock 注入（§5.11）。
  11. Replay 架构（★★★★★）：ReplayRecorder(Input+Seed+Frame)+ReplayPlayer，依赖 RunRng/MoveCommand 确定性（§5.7）。
  12. Crash Recovery：进房自动存档（§5.6）。
  13. Asset Pipeline：Blender→GLB→Validate→Import→AutoPrefab→Bundle→AssetCache 自动化（§5.9）。
  14. AI 不写死 BT：`IAIController` 接口，BT/FSM/GOAP/Utility 可换（§3.10 + §5.10）。
  15. 整体架构总图（★★★★★）：主架构图 + 战斗分层图 + 生命周期状态图（§6）。
- **路线图更新**：新增 Demo0 基础设施地基（§7.2）；Phase 6 加入调试/存档/重放；总工期 30–44 周。架构门禁新增：System 必须 implements ILifecycle、禁止业务 new 服务、禁止业务 Math.random()（§8.3）。
- **用户评价**：v3 目标达到 9.9+/10 架构圣经水准。可落地切入口建议：Demo0 地基（GameContext+LifecycleManager+ConfigDatabase+Logger 桩）或 ICollisionService 接口+PhysicsCollisionImpl 桩。

## 美术资源制作参数总表（3D 升级目标）

- 用户要求「先生成所有美术资源」，并输出逐资源详细制作参数文件。先澄清维度：选 **3D 升级目标规格**（角色/怪物/Boss/特效/地块输出 3D 模型规格；图标/UI/背景保留 2D）。
- **关键做法**：基于真实库存 `texture_files_runtime.txt`（**499 项**）写生成器 `tools/gen_art_spec.py`，而非手搓，保证「无遗漏 + 一致」。
- **聚合结果**：499 库存项 → **134 个 3D 资源**（角色 5 / 怪物 36 / Boss 42[12终+30小] / 特效 27 / 地块 24 模块件）+ **257 张 2D 保留**（图标 67 / UI 173 / 背景 17）。
- **每个 3D 资源独立成节**，字段齐全：名称/类型/源2D帧/尺寸·分辨率/颜色规范(区域主题色)/格式(.glb+ASTC)/制作方式(3D建模+骨骼+Toon)/层级结构(骨骼树)/动画clip(对应2D帧)/导出压缩/验收门禁/升级阶段(v3 Phase)。
- **2D 保留类**：逐文件表格行（每行=一项），含尺寸/颜色/格式/制作方式/导出压缩/批次。
- **产出文件**：`docs/美术资源制作参数总表_3D.md`（2230 行，7 章：总览/角色/怪物/Boss/特效地块/2D保留/补充3D资产/资产管线门禁）。
- **坑**：第一版生成器中间字段漏 `\n` 导致整块塌成一行、保留表 257 行塌成 14 行；改用 `Doc.w()` 逐行 `\n` 助手重写后修复。
- 库存变动后重跑 `python tools/gen_art_spec.py` 即可同步。

## 美术资源规格 v4 补强（第三轮资源评审后）

- 用户对 v3 美术规格打 9.2/10，提 10 项 + 生产流程 + 自动检查。已**升级生成器 `tools/gen_art_spec.py` 重写**（v4 覆盖），重跑生成 5113 行。
- **10 项采纳（均落为生成器结构化字段，134 个 3D 块全量覆盖）**：
  1. 预算区间化：角色 2000~3000(按职业差异化 archer2200/mage2500/assassin2600/warrior·berserker3000)；Boss 5000~9000(终boss建议8500)/小boss 5000~7000(建议6000)；怪 1000~2500(按定位)。
  2. 动画最低集(Idle/Move/Attack/Hit/Death)+推荐集(Spawn/Skill/Taunt/Special/Recover/Turn/Stun/KnockBack/Dissolve)，标注 2D 帧派生项。
  3. LOD 规则显性化：角色 100/60/30%、Boss 100/50/20%、怪同角色、Tile 100/50%（含 Tri 上限与切换距离）。
  4. 碰撞体规范：Capsule/Height/Radius/WeaponCollider/SkillSocket/FootIK；Boss 多 HitBox+WeakPoint HurtBox；Tile Box+Trigger。
  5. Socket 规范：角色 RightHand/LeftHand/Head/Chest/Back/Foot/Weapon/SkillOrigin/EffectAnchor；Boss 增 Mouth/Wing/Tail/Eye/WeakPoint；怪精简版。
  6. 材质规范：Shader=ToonLit/OutlineWidth(角色Boss0.8·怪0.5·Tile0.2)/Emission/RimLight/ReceiveShadow/CastShadow + 共享 ramp。
  7. VFX 性能预算：普通≤80粒子/DrawCall≤2/256²/≤1.5s；Boss≤300/≤4/512²/≤2s；UI≤40。
  8. Tile 模块规范：2m×2m、Floor/Wall/HighGround/Thorn/Corner/Edge/Slope/Ramp、Pivot 脚底+Y-up、Grid Snap 2m。
  9. 命名规范：CHR_/MON_/BOSS_/FX_/TILE_ + 动画 `前缀_Token_Clip`；新增 §7 命名总览表。
  10. Prefab 规范：Animator/Collider/LOD/Socket/Material/AudioSource/EffectAnchor/Shadow/RenderLayer（按类型变体）。
- **额外新增章节**：§7 命名规范总览、§8 资源生产流程(13 阶段：需求→概念图→建模→UV→贴图→绑定→动画→LOD→导出→Prefab→程序接入→验收→性能测试)、§9 资产管线与自动检查工具（每资源末尾「自动检查」清单→`tools/asset_validate.py` 一键扫描生成报告，Fail 退回美术）。
- **坑**：tile 分支 LOD 行 `%%` 未塌（该分支无 % 格式化），改为 `%`；特效命名 `fx_` 前缀未剥导致 `FX_Fx_crit`，新增 `strip_fx`+`pascal` 修正为 `FX_Crit.prefab`。
- **用户评价**：9.2/10，补强后可作团队/外包长期执行正式制作标准。后续可写 `tools/asset_validate.py` 把自动检查项脚本化。

## 美术资源规格 v5 补强（第四轮元字段评审后）

- 用户在 v4 基础上提 5 项**元字段**增强（多人协作/迭代追踪/动态降级/验收归属）。已升级 `tools/gen_art_spec.py` 新增 `lifecycle_meta()` 发射器，重跑生成 **5917 行**（499/134/257 不变）。
- **5 项采纳（134 个 3D 块全量覆盖，置于命名规范之后成组）**：
  1. **版本/作者/日期/评审**：v1.0 / [美术外包] / YYYY-MM-DD / [评审人]，改版递增小版本（外包迭代追踪）。
  2. **依赖关系**：按类别列出依赖 token（如 Boss=BOSS_Final_01_Weapon/FX_*/Audio_Boss_Final_01/Minion_*；Tile=Shared_Region_Atlas_Abyss/Lightmap_Abyss），程序导入时一次性校验缺失依赖。已同步加入自动检查清单「依赖资源完整」项。
  3. **性能等级 GPU/CPU 成本**：角色 中/低、Boss 高/中、怪 低~中/低、特效 中(普通)高(Boss)/低、Tile 低/极低；附动态降级路径（降 LOD/关 RimLight/关阴影等），对接 v3 §6.1 性能预算。
  4. **测试场景**：角色 Arena_Test+CharacterShowcase、Boss Boss_Test+Arena_Test、怪 Dungeon_Test_<区域>+Arena_Test、特效 FX_Preview+Arena_Test+Boss_Test、Tile Dungeon_Test_<区域>；明确"禁止随便拖入任意地图"。
  5. **资源生命周期**：状态取值 选秀/评审中/已批准/已弃用（默认选秀，批准后方可正式接入，弃用移出资源池）。
- **校验**：5 个 `- **...**` 字段各 134 处 + 依赖检查项 134 处，全部命中；抽查 C01/B01/E01/T01 内容正确。
- **用户美术规格迭代偏好（长期记）**：每轮评审都强调"外包可控性 + 自动化验收 + 不返工"，且偏好用**生成器脚本**保证全量一致而非手改文档。后续美术规格类需求默认走"生成器 + 可脚本化检查"路线。

## art-pipeline 升级审查（对照 3D 升级方案）

- 用户对 art-pipeline 做全面审查需求：覆盖 skill 本体 / 上下游调用 / 关联冲突文件。已读核心实现 `回到地面/tools/art_pipeline.py`(1980行, 全 2D)、`ART_RESOURCE_RULES.md`(677行)、`art_quality_budget.json`、`prompts.json`(499条全2D)、`tmp_resource_status.py`、`resource_status.md`、下游 `SpriteAnimationService`/`AssetBundleService`/`CharacterVisualService`/`RenderAssetService`。
- **核心结论**：art-pipeline 100% 2D 管线（PNG/JPG→textures/→2D尺寸/Alpha/体积校验），与 3D 目标在 8 维度全面冲突。
- **关键冲突点（已定位）**：
  1. `validate_technical` 强制小写 `^[a-z0-9_]+$` 与 v5 命名 `CHR_Warrior_A.glb`(PascalCase) **直接冲突**——不改则 3D 资产全被判不合规。
  2. `docs/美术资源生成与入库规范.md` **被 SKILL.md + ART_RESOURCE_RULES.md §13/§14.2 引用但不存在**（docs/ 只有 4 个文件）。文档链断裂。
  3. 无 `models/` 目录；`AssetBundleService` 仅 `loadSpriteFrame`/2D 类型；`SpriteAnimationService` 只切 2D 帧。
  4. `gen_art_spec.py` 自带 CHR_/MON_/... 命名+预算，与 ART_RESOURCE_RULES.md §6/§9 并存 → 违反 §14.1 单源原则。
  5. `tools/asset_validate.py`（v5 §9.1 规划的 3D 校验器）**尚未创建**，但 pipeline validate 与 SKILL.md step6 都需调用它。
  6. `tmp_resource_status.py`/`resource_status.md` 全 2D；`topics/RESOURCE_STATUS.md` 与 §14.4 权威源可能重复。
- **交付**：`docs/art-pipeline升级审查.md`（冲突全景表 + 逐文件修改点+原因 + 优先级汇总 + 分阶段改造路线）。共列出 ~19 个文件，P0 级 7 项（SKILL.md / art_pipeline.py / ART_RESOURCE_RULES.md / art_quality_budget.json / SpriteAnimationService / AssetBundleService / asset_validate.py 待建）。
- **改造原则（写入审查报告）**：遵守 §14.1 单一权威来源——3D 命名/预算/LOD/依赖只在 ART_RESOURCE_RULES.md 定义一次，art_pipeline.py / gen_art_spec.py / asset_validate.py 引用读取，禁止各自硬编码。

## art-pipeline 升级审查 — Phase 0 + P1 落地执行（encoding-pipeline-guard 加载后）

- 用户指令：加载 `encoding-pipeline-guard` skill 后，按审查报告**依次**修改。已执行 **Phase 0（基础三件套）+ P1（三个文件）**，全部 UTF-8 写入，并通过 `encoding_audit --ci`（scanned=499, issues=0, p0/p1/p2=0）。
- **Phase 0 三件套（全部 ✅ 已落地）**：
  1. `assets/resources/config/art_quality_budget.json` 新增顶层 `rules3d` 段（8 桶：characters/Boss终/Boss小/monsters/effects_normal/effects_boss/tiles/naming），数据对齐 art-spec v5。⚠️ 踩坑：第一次 Edit 把 `rules3d` 误嵌套进 `rules` 内导致 `load_budget` 读不到，改用 Write 整文件重写修正嵌套。
  2. `ART_RESOURCE_RULES.md` 新增 `§16 3D 资源规则（权威源）`：3D 范围、双轨命名(CHR_/MON_/BOSS_/FX_/TILE_+PascalCase，正则含 `.glb/.prefab` 后缀豁免)、预算、目录(models/prefabs/textures/astc)、manifest 元数据、生命周期、单源指令。
  3. **新建 `tools/asset_validate.py`** 3D 校验器：读 `.assetmeta.json` manifest 比对 rules3d 出 Pass/Fail 报告，覆盖命名/预算(Tri/骨/贴图/LOD/动画clip)/Socket/Collider/依赖存在性/生命周期/perfTier/测试场景/元字段；含 `--self-test`(PASS) 与 `--scan --report`；命名正则自动剥离 `.glb/.prefab`；`resolve_budget` 向上搜索降路径脆弱性。功能扫描验证：良好资产 PASS，越预算 Boss 正确 FAIL(13 项违规)。
- **P1（三个文件，全部 ✅）**：
  1. **新建 `assets/resources/config/prompts_dim.json`**（499 条，242 3d / 257 2d）：由 prompts.json key 前缀推导维度。**非破坏性决策**——不改动 prompts.json 字符串值（避免破坏 art_pipeline.py 消费方），维度数据外置为注册表，P2 由 pipeline 读取。
  2. `.workbuddy/tmp_resource_status.py` 增 3D 维度列 + 3D 母版/运行目录(models/prefabs) + 生命周期/版本列；2D 行为不变；progress 缺失降级 `{}`。重跑成功再生 `docs/progress/resource_status.md`（含新列）。⚠️ 踩坑：overview 循环误用未定义 `rp`，补 `rp = os.path.join(rb, k)` 修正。
  3. `tools/art_resource_gate.py` 增 3D 审计分支（扫描 models/prefabs 的 .glb/.prefab，查 meta + 命名合规，读 rules3d 命名正则；目录不存在安全跳过；输出 `art_resource_gate_3d.csv`）。py_compile 通过。
- **审查报告 `docs/art-pipeline升级审查.md` 已追加 §9 执行进度追踪**（含状态表、prompts.json 非破坏性决策、asset_validate 验证、已解决冲突点）。
- **下一步（⏳，尚未执行）**：P3 `art-pipeline SKILL.md` 增 3D 分支与门禁（1980 行级大改之后的收口文档）。P2 已于后续轮次完成（见下文「art-pipeline 升级审查 — P2 落地执行」）。

## art-pipeline 升级审查 — P2 落地执行（art_pipeline.py 3D 分支）

- 用户指令：继续推进 P2。已加载 `encoding-pipeline-guard`，对 `回到地面/tools/art_pipeline.py` 做 3D 分支骨架改造（纯增量，2D 路径零改动），UTF-8 写入，通过 `encoding_audit --ci`（scanned=499, issues=0）。
- **具体修改（全部 ✅）**：
  1. 新增常量 `MODELS_DIR`/`PREFABS_DIR`/`MODELS_REL`/`PREFABS_REL`/`DIM_3D_CATEGORIES`/`PROMPTS_DIM_JSON`/`RULES3D_BUDGET_JSON`。
  2. 新增 `category_dim(category, key)`：优先读 `prompts_dim.json` map（P1 注册表），回退 `DIM_3D_CATEGORIES`；`classify_resource` 返回值增 `dim` 字段。
  3. `validate_technical` 增 `dim=None` 形参，3D 时委托新增的 `_validate_3d_asset()`：校验扩展名(.glb/.gltf/.prefab/.fbx) + 命名(CHR_/MON_/BOSS_/FX_/TILE_) + 读 `rules3d` 比对 `.assetmeta.json` 的 Tri/Bone/texture 预算。配套 `_rules3d()` 缓存加载 + `_rules3d_bucket()` 映射。
  4. `cmd_audit` 3D 资源标 `[3D建模]`；`cmd_validate` 传 `dim=info.get("dim")`；`cmd_import` 3D 路由到 `models/`(glb)+`prefabs/`(prefab) 并带备份逻辑；`cmd_generate` 3D 资产跳过 Agnes 出图（提示走 Blender→glb→Prefab→import）；`ProgressTracker` 扩 `dim`/`modelPath` 字段；docstring + epilog 补 3D 说明。
- **端到端验证（隔离测试资产，测后清理+进度 reset）**：良好 3D 角色 validate→PASS、import→落 `models/characters/CHR_TestPipe_A.glb`、generate→跳过提示、audit→`[3D建模]`；越预算 Boss validate→FAIL（三角面/骨骼/贴图三项超标正确报出）。
- **⚠️ 修复缺陷**：初版 `_rules3d_bucket` 桶名（`boss_miniboss` 等）与 `art_quality_budget.json` 实际键（`bosses_final`/`bosses_mini`/`effects_normal`/`effects_boss`/`tiles`）不一致，且误用 `maxTextureSize`（实际字段 `textureSize`），导致 BAD 资产被误判 PASS；已对齐桶名+字段名并复测通过。
- **状态**：P0/P1/P2/P3 **全部落地 ✅**（P3 于 2026-07-10 完成，见文末「art-pipeline 升级审查 — P3 落地执行」）。审查报告 `docs/art-pipeline升级审查.md` §9 进度表已标 ✅；并新增 P3 完成行 + §9.4 验证小节。

## MEMORY.md cleanup (size limit exceeded)
- Trigger: MEMORY.md exceeded the size limit and was truncated during injection, so the system directive required a mandatory cleanup.
- Work: compressed 87 lines -> ~60 lines. Merged duplicates: mandatory skills (art-pipeline/encoding-pipeline-guard) consolidated at the top of "High Priority Rules"; removed redundancy between the Skill Reference table and SKILL_REFERENCE rule; dropped the legacy ART_PIPELINE duplicate in the Topic Index; collapsed Validation Gates into a single block.
- Added: a new "## Active Initiative" section pointing to `docs/2D转3D全面升级方案.md` (v3) - its Phase0-6 roadmap plus the principles to respect when touching combat/dungeon/render/AI systems (ILifecycle/LifecycleManager, GameContext DI, ICollisionService, SkillGraph, IAIController, AssetCache). Serves as a navigation anchor for future sessions.
- All 15 core rules preserved. UTF-8 write policy, validation gates, and memory-write permission rules maintained.

## 2D->3D implementation plan (created)
- Request: user said "先制定方案实施计划" (first formulate the implementation plan) -> drafted an execution plan based on `docs/2D转3D全面升级方案.md` (v3).
- Output: `E:\game\docs\2D转3D实施计划.md` (with UTF-8 declaration header; passed encoding_audit --ci with issues=0).
- Structure: §0 overview (order Demo0->Demo6->Vertical Slice->Phase1-6; recommends Demo0 foundation for week 1; 6 architecture red lines); §1 Demo0 foundation task table (D0-1~7) + ILifecycle/GameContext/ConfigDatabase/Logger interface draft + completion checklist; §2 Demo1-6 summary; §3 vertical slice definition; §4 Phase1-6 schedule; §5 branching strategy + gates; §6 next-step options (A: start Demo0 now / B: ICollisionService stack first / C: adjust scope).
- Recommended first step: Demo0 foundation (core/GameContext + LifecycleManager + ConfigDatabase + Logger, 4 files + GameBootstrap wiring), verifiable within ~1 week.
- Next action: awaiting user confirmation (A/B/C). No code changes made yet.

## art-pipeline 升级审查 — P3 落地执行（SKILL.md 增 3D 双轨分支）

- 用户指令：执行 P3。已加载 `encoding-pipeline-guard`，对 `C:\Users\Administrator\.workbuddy\skills\art-pipeline\SKILL.md` 做 3D 双轨分支改造（对应审查报告 §1.1 修改点 1–5），UTF-8 写入，并通过 `encoding_audit --ci`（scanned=499, issues=0, p0/p1/p2=0）；SKILL.md 单独 UTF-8 解码校验通过（18745 bytes）。
- **具体修改（11 处，全部 ✅）**：
  1. frontmatter `description` 补「2D image + 3D model dual-track」说明。
  2. Overview 增「🟦 2D/3D 双轨」callout（3D 类别走 Blender→glb→Prefab→asset_validate；维度判定读 prompts_dim.json 非破坏性）。
  3. When to Invoke + Mandatory First Step：补 3D 资产需读 `docs/美术资源制作参数总表_3D.md` 与 `ART_RESOURCE_RULES.md §16`；规则表增 §16 行。
  4. Batch Mode 增「3D Asset Commands」小节（`asset_validate.py` 单资产/扫描/self-test + `art_pipeline import` 路由 models/prefabs）。
  5. Step 2 生成方式表拆分：3D 类别(characters/monsters/bosses/effects/Tiles模块件) 标「3D—Blender」，明确不走 Agnes；icons/backgrounds 仍 AI；Tile 2D 变体仍 Pillow。
  6. Step 6 增「3D Technical Check」小节，明确 `validate_technical` 委托 `tools/asset_validate.py`（命名→预算→Socket/Collider→依赖→lifecycle→元字段），未过禁止入库。
  7. Procedural UI 段加 ⚠️：generate_panel.py 仅服务保留 2D UI，不参与 3D。
  8. Configuration Files 表补 4 行 3D 规范文件（美术资源制作参数总表_3D.md / asset_validate.py / art_quality_budget.json rules3d / prompts_dim.json）。
  9. Violation Warnings 增 5 条 3D 违规（glb 入 textures/、未过 asset_validate 即入库、未登记 depends 即 import、命名不符前缀、lifecycle≠已批准即接入）。
- **状态**：art-pipeline 升级审查 Phase 0/P1/P2/P3 **全部落地 ✅**，3D 资源管线文档闭环（规则单源 §16 + rules3d + asset_validate + art_pipeline 3D 分支 + SKILL.md 门禁）。唯一未完成项：`docs/美术资源生成与入库规范.md` 仍缺失（审查 §6.1，P1 级，被 SKILL.md 多处引用）——属文档链补全，非管线阻断项。

## 2D→3D implementation plan v2 (AI execution spec)
- Request: user reviewed v1 plan, rated human 9.2 / AI-auto 8.5. Root cause: v1 described "what to do" but lacked AI-needed execution constraints.
- Rewrote `E:\game\docs\2D转3D实施计划.md` to v2 with 5 mandatory additions:
  1. Every task has **输入 (Input)** = exact doc sections (e.g. `§5.2`).
  2. Every task has **输出 (Output)** = files + explicit exported symbols.
  3. Every task has **完成定义 (DoD)** = verifiable gates (npm run test / validate:all / console log sequence).
  4. **严格约束 (No freewheeling)** = strictly follow §x signatures, no new methods/rename/extra duties.
  5. **AI execution template** = fixed 7-step pipeline (read→analyze→code→test→fix→validate→commit) + 允许修改范围/禁止修改范围 + per-Demo execution Prompt.
- Key real-world corrections vs v1: project has NO test framework → added D0-0 to establish vitest base for pure-TS `core/` (no `cc` import); mapped real forbidden paths from actual repo (battle/dungeon/ui/scene/render/assets(config service)/config/app/run/utils). GameBootstrap confirmed at `assets/scripts/core/GameBootstrap.ts`.
- Verified: file is valid UTF-8; `encoding_audit --ci` issues=0, p0/p1/p2=0.
- Demo0 task cards: D0-0(vitest base) → D0-2(GameContext) → D0-1(LifecycleManager) → D0-3(ConfigDatabase) → D0-4(Logger) → D0-5(GameBootstrap integration + empty-scene verification). Demo1-6 given same structured skeleton.

## 2D→3D plan v3 (AI execution spec — agent-facing split)
- Request: user gave 4 further optimizations + Agent Contract to push AI-auto score to 9.9~10.
- Rewrote `docs/2D转3D实施计划.md` to v3 MASTER SPEC; split Demo0 into 6 standalone task files + Demo1-6 one file each.
- New artifacts under `docs/ai-tasks/`:
  - `_agent_contract.md` — 10 iron rules (no plan/interface/duty mod, no guessing, stop on conflict, report scope creep, don't hack tests/lint/validate/architecture).
  - `_architecture_report.md` — Step0 scan output (real: package.json has NO test script; tsconfig.json exists; no Logger/ServiceLocator/LifecycleManager/ConfigDatabase/ICollisionService in scripts; ConfigService+ConfigManager already exist → ConfigDatabase must REUSE, not reimplement).
  - `demo0/D0-0.md`..`D0-5.md` — each = one Prompt + one Output + one DoD + Checkpoint template (self-contained, copy-paste executable).
  - `demo1.md`..`demo6.md` — compact skeleton task cards (one prompt each).
- v3 protocol additions (§1): Step0 扫描工程(Architecture Report); 禁止重复实现(anti-duplication scan); Step2 修改前Diff + Step2.5 Execution Plan 执行前确认(wait "Plan Approved"); Token Budget(≤3~5 files/task); Step8 Checkpoint(REPORT_*.md for breakpoint resume); Agent Contract(10 rules).
- Verified: 15 MD files all valid UTF-8; `encoding_audit --ci` issues=0, p0/p1/p2=0.

## 美术资源「三层文档架构」交付（Layer1/2/3 + gen_art_spec.py 单源改造）

- **用户动机**：现有 `docs/美术资源制作参数总表_3D.md` 给人看 9.8 分，但给 AI 执行只有 ~8.5 分——它是 Human-readable 而非 Machine-readable。AI 无「建议/适当/合理/美观/较低/中等」概念；参数不可解析；无 Input/Output/Fail 条件/执行顺序。用户要求三层架构：L1 人类规范(已有) / L2 AI 机器规格(YAML+JSON) / L3 AI 执行流水线。确认选项：**三层全做 + YAML/JSON 双份 + 改造 gen_art_spec.py 单源生成**。
- **核心改造（满足 §16.6 / 审查 §6.4 单源强制）**：`tools/gen_art_spec.py` 改为从 `art_quality_budget.json → rules3d` + `ART_RESOURCE_RULES.md §16` 读取全部 3D 预算/命名/LOD/Socket/依赖，**删除所有硬编码预算常量**（CHAR_TRI_RANGE/CHAR_SUGGEST/monster_tri 等移除，lod_spec/socket_spec 改读 rule）。一行改动即全量同步，不返工。
- **四个产出文件（一次运行全部生成）**：
  - L1 `docs/美术资源制作参数总表_3D.md`（v6，5923 行）：保留人类规范，补「三层架构」头部 + 单源声明，预算行标注「（rules3d 区间）」。
  - L2a `docs/AI资源制作规范_MachineSpec.yaml` + L2b `.json`（双份等价）：`templates`(7 类 schema：character/monster/boss_final/boss_mini/effect_normal/effect_boss/tile) + `assets`(134 个唯一 schema)。每资源含 `budget`(min/recommend/max 数值)、`sockets`(数组)、`fail`(assert+severity，如 `triangles<=3000`→error)、`io`(input/output/animClips)、`pipeline`(9 步)、`manifest`(.assetmeta.json 模板)；`global_validator` 对齐 `asset_validate.py` 全部 check_id；`naming.pattern` 来自 rules3d。无模糊词。
  - L3 `docs/AI执行流程_Workflow.md`：9 步 AI 流水线（model→uv→texture_bake→texture→rig→animation→lod→prefab→validate），每步 Input/Output/Tool-call/Validation/Fail→Retry/Next；§2 把 check_id 映射到 Layer2 字段；§3 以 CHR_Warrior_A 为例的端到端执行脚本。
- **验证**：`encoding_audit --ci` scanned=499 / issues=0 / p0=p1=p2=0；YAML 经 PyYAML `safe_load` 与 JSON `json.load` **深度相等（deep-equal=True）**，证明双格式语义一致且可被 AI 解析；134 资源计数与 §16.1 权威口径一致（5/36/42[12终+30小]/27/24 + 257 2D 保留）。
- **结论**：AI 驱动 3D 资源生产现在由 L2（机器规格）+ L3（执行流水线）+ asset_validate（门禁）三件套闭环，L1 仅作人类参考；预算/命名/LOD 单源，库存或规则变动只需重跑 `python tools/gen_art_spec.py`。

## D0-0 executed (vitest base) — 2026-07-10
- Executed task D0-0 per `docs/ai-tasks/demo0/D0-0.md` (AI Agent execution spec v3).
- Added: `回到地面/vitest.config.ts` (node env, tests/**/*.test.ts), `回到地面/tests/core/smoke.test.ts` (smoke assert true).
- Modified: `回到地面/package.json` — scripts.test="vitest run" + devDependencies.vitest ^2.1.8. npm install vitest OK (44 pkgs, network available).
- `npm run test` → exit 0 (1 file / 1 case). Real, runnable gate now exists for core/.
- `validate:all` → 8/9 gates pass. Gate 9 "doc consistency" FAILS due to 2 broken refs in `art-pipeline SKILL.md` (`tools/asset_validate.py` missing; `docs/美术资源制作参数总表_3D.md` path-unresolvable from SKILL.md). Pre-existing, unrelated to D0-0, owned by art track (user routed art generation to another conversation). Per Agent Contract #5/#6, did NOT modify SKILL.md; reported instead.
- Committed: `bffc71a` `[Demo0][D0-0] setup vitest test base for core/` — only 4 files (vitest.config.ts, smoke.test.ts, package.json, package-lock.json); node_modules NOT tracked; unrelated art/config working-tree changes NOT committed.
- Checkpoint: `docs/ai-tasks/demo0/REPORT_D0-0.md` generated.
- Note: D0-1~D0-5 can now rely on `npm run test` as a real gate. Gate-9 fix should be a separate art-track task, not folded into D0-0.

## D0-1 BLOCKED — dependency on D0-2 (GameContext type)
- User said "按 D0-1 开始" but D0-1's `ILifecycle.initialize(ctx: GameContext)` requires `GameContext` type from D0-2 (`assets/scripts/core/GameContext.ts`, §5.2). GameContext.ts does NOT exist yet (only D0-0 done).
- Per Agent Contract #5 (dependency conflict → stop & report) and user's own rule "发现依赖冲突先停止输出原因不要自行改设计": STOPPED, did not create GameContext prematurely (would be D0-2 scope creep) nor fake the type (would violate §5.1 strict signature).
- Plan's own recommended order is D0-0 → D0-2 → D0-1. Awaiting user decision: do D0-2 first then D0-1 (recommended), or other.

## 入库规范补 3D 章节 + 三层文档迁移（art-pipeline 收尾）

- **问题**：`回到地面/docs/美术资源生成与入库规范.md` 原是纯 2D 入库 SOP，对新建的 3D 双轨（L1/L2/L3 + §16/rules3d + asset_validate 门禁）完全缺位。
- **决策（用户确认）**：① 入库规范补 3D 章节且 3D 数值一律引 §16/rules3d/L2，不硬编码；② 三层文档统一移到 `回到地面/docs/`（原在 `E:/game/docs/`）。
- **改动**：
  - `tools/gen_art_spec.py`：OUT_* 改为 `回到地面/docs/`；L1/L3 内部互引改 sibling `./`、外部 `../docs/2D转3D全面升级方案.md`；重跑生成器产出 4 文件；删除 `E:/game/docs/` 下 4 个旧副本。
  - `art-pipeline/SKILL.md`：`docs/美术资源制作参数总表_3D.md` 与 `docs/美术资源生成与入库规范.md` 引用改为 `回到地面/docs/...`（replace_all）。
  - `美术资源生成与入库规范.md` 新增：§0 文档关联/适用范围、§2.1 3D 目录、§3.1 3D 命名(CHR_/MON_/BOSS_/FX_/TILE_+正则)、§4 3D 预算指引、§6 开头注+§6.10 3D 制作入口、§8.5 prompts_dim.json+.assetmeta、§9.0 3D 入库分支、§9.1.3/§9.1.6 3D 命令、§9.1.7 补 asset_validate.py/gen_art_spec.py、§10.1 3D 验收清单、3D 上线标准章节、9-Slice 仅2D 注。
- **验证**：`encoding_audit --ci` scanned=502, issues=0, p0/p1/p2=0 ✅；三层文档 YAML==JSON 深度相等（134 资产）✅。
- **结论**：art-pipeline 升级（P0–P3 + 三层架构 + 入库规范）现已闭环，3D 资产由 L2 机器规格 + L3 执行流 + asset_validate 门禁 + 入库规范 SOP 共同覆盖。

## 玩家角色部件化动画方案启动（archer 原型）

- **决策**：用户确认从 AI 整图 sprite sheet 转向「部件化动画」——每个职业生成稳定部件，Cocos 运行时以 PartCharacterRenderer + PartAnimationPlayer 装配并驱动动作。优先用 archer 做样板，确认可行后再推其他职业。
- **已生成资源**：12 张 archer 部件图（body/head/ear_l/ear_r/arm_l/arm_r/leg_l/leg_r/tail/bow/quiver/cape），处理后满足：RGBA、透明背景、≥12px 边距、规格尺寸、无水印。
- **新增配置**：
  - `回到地面/assets/resources/config/character_parts.json`
  - `回到地面/assets/resources/config/character_rigs.json`
  - `回到地面/assets/resources/config/character_part_animations.json`
  - `回到地面/assets/resources/config/character_visuals.json`
- **新增代码**：
  - `回到地面/assets/scripts/render/PartCharacterRenderer.ts`
  - `回到地面/assets/scripts/render/PartAnimationPlayer.ts`
  - 更新 `回到地面/assets/scripts/render/CharacterVisualService.ts`（mode='parts' 走新分支，mode='sheet' 保持 SpriteAnimationService 兼容）。
- **新增工具**：
  - `tools/process_archer_parts.py`：AI 出图后处理（抠背景、去水印、裁剪、缩放到规格）。
  - `tools/check_character_parts.py`：部件入库前质量门禁。
- **验证**：`encoding_audit --ci` issues=0；config check 通过（新增 4 个 JSON 均含 metadata）；`check_character_parts.py --character archer` 全通过；contact sheet 已生成到 `art_source/textures_review/contact_sheets/archer_parts_contact.png`。
- **状态**：部件仍在 `art_source/textures_review/master/characters/archer/parts/`，等待用户视觉确认后才能 import 到 `assets/resources/textures/...`。
- **下一步**：用户确认 archer 效果后，复制到 runtime 并在 Cocos 中接入 CharacterVisualService 测试；随后推 warrior/mage/assassin/berserker。

## D0-2 executed (GameContext ServiceLocator) — 2026-07-10
- User chose "先 D0-2 再 D0-1" — resolved the D0-1 dependency block.
- Added: `回到地面/assets/scripts/core/GameContext.ts` (class GameContext: register/get/onDestroy + Map<string,unknown>; get throws if unregistered; register throws on duplicate; onDestroy reverse-order disposes optional onDestroy hooks). Plus 12 service-token string constants per §5.2.
- Added: `回到地面/tests/core/gamecontext.test.ts` (4 cases). `npm run test` → 5 cases pass (exit 0).
- Discrepancy found & reported: D0-2 card says "13 tokens" but §5.2 lists only 12. Followed authoritative §5.2 (Contract #2) = 12 tokens; did NOT invent a 13th (Contract #4).
- `validate:all` during D0-2 → 8/9 (gate-9 doc-consistency still failing on art-pipeline SKILL.md broken refs).
- Re-checked after art-track's concurrent edits: gate-9 now down to 1 issue (`tools/asset_validate.py` path); the other (`docs/美术资源制作参数总表_3D.md`) was FIXED by art-track. Remaining 1 is art-track's responsibility (user routed art to another conversation); per Contract #6 D0-2 did NOT touch SKILL.md.
- Committed: `d71e1c0` `[Demo0][D0-2] implement GameContext ServiceLocator (§5.2)` — 2 files only.
- Checkpoint: `docs/ai-tasks/demo0/REPORT_D0-2.md`.
- D0-1 now UNBLOCKED: GameContext type exists; ILifecycle.initialize(ctx: GameContext) can be typed correctly. Ready to execute D0-1 next.

## D0-1 executed (ILifecycle + LifecycleManager) — 2026-07-10
- Prereq satisfied: D0-2 (GameContext.ts) done → `import type { GameContext }` resolves; §5.1 signature 1:1 implementable.
- Added: `回到地面/assets/scripts/core/LifecycleManager.ts` (interface ILifecycle + class LifecycleManager: register/enterAll/exitAll/pauseAll/resumeAll/destroyAll; destroyAll reverse-order). Pure TS, no `cc`. Strict 1:1 §5.1 (no extra methods/renames/responsibilities; DI stays in GameContext).
- Added: `回到地面/tests/core/lifecycle.test.ts` (3 cases: enterAll order = registration order; destroyAll reverse; pause/resume/exit broadcast).
- Step5 fixes (in-task, valid): (1) missing `import { describe,it,expect } from 'vitest'` → added; (2) test asserted reverse order by concatenating per-system arrays in wrong order → switched to a SHARED log array so global call order is verified; source logic was correct.
- `npm run test` → 8 cases pass (exit 0). `validate:all` → 8/9 (gate-9 still only the art-track `tools/asset_validate.py` broken ref; D0-1 added no new issue, did NOT touch SKILL.md per Contract #6).
- Committed: `7e3a631` `[Demo0][D0-1] implement ILifecycle + LifecycleManager (§5.1)` — 2 files only.
- Checkpoint: `docs/ai-tasks/demo0/REPORT_D0-1.md`.
- Next: D0-3 ConfigDatabase (REUSE existing ConfigService/ConfigManager per "禁止重复实现"; see §5.3).

## 修正 art-pipeline SKILL.md 失效引用（方案 A 落地）
- **澄清**：另一个对话报「SKILL.md 2 条失效引用」。实测后——`tools/asset_validate.py` 在 doc-consistency 门禁（check_doc_consistency.py Check3）下**确为断链 FAIL**；`docs/美术资源制作参数总表_3D.md` 那条我们本轮迁移时已修好（现位于 回到地面/docs/）。
- **根因**：项目有两个根——`E:/game/`（顶层 tools：gen_art_spec.py、原 asset_validate.py）与 `E:/game/回到地面/`（游戏子项目 tools：art_pipeline.py、encoding_audit.py、check_doc_consistency.py）。门禁 `PROJECT_ROOT = dirname(dirname(__file__)) = E:/game/回到地面`，故 `tools/asset_validate.py` → `回到地面/tools/asset_validate.py`（不存在）→ FAIL。
- **修复（方案 A）**：`git mv` 不适用（文件 untracked），改为 `mv tools/asset_validate.py 回到地面/tools/asset_validate.py`，与 art_pipeline.py 同目录。已确认无任何 Python 代码 `import asset_validate`（art_pipeline.py 仅注释"内联镜像"其逻辑）。
- **验证**：重跑 `回到地面/tools/check_doc_consistency.py` → `ALL CHECKS PASSED`（Check3「All 24 path references in SKILL.md are valid」）；`asset_validate.py --self-test` 通过；`encoding_audit --ci` issues=0。
- **文档同步**：更新 `docs/art-pipeline升级审查.md` §6.5（待建→已建成+位置）/§6.6（check_doc_consistency.py 路径修正为 回到地面/tools/）。
- **提交**：`fb02ad9`（仅含 asset_validate.py 移动 + 审查报告，未混入仓库既有未暂存删除）。
- **约定沉淀**：pipeline 工具应与 art_pipeline.py 同置于 `回到地面/tools/`，否则 SKILL.md 的 `tools/...` 引用在门禁下解析失败。

## 核查 resource_status.md / prompts.json 是否需基于方案修改（用户问询）

- **结论：两个文件都不需要基于 art-pipeline 2D→3D 方案修改。**
- **prompts.json（499 条，纯 2D Agnes 出图）**：3D 走 Blender 不进 Agnes，3D 源在三层文档(L1/L2/L3)+.assetmeta.json，不在 prompts.json（方案既定）。内部 0 处引用被移动/改名文件，无断链。其中 242 条 3D 类别(monsters/bosses/characters/tiles/effects)是旧 2D 精灵 prompt，方案全量执行后应退役——但属"3D 产出后清理"，非现在必须。
- **resource_status.md（§14.4 进度唯一权威源，由 tmp_resource_status.py 10:44 生成）**：已正确声明"3D 类别走 3D 管线、数据源含 prompts_dim.json"，与方案一致；不引用任何被移动文件（无旧总表路径/asset_validate 旧位/MachineSpec 引用），迁移未波及。3D 行用 2D sprite 文件名是当前真实快照，3D 资产产出后由 §14.4 生成器重生成。
- **澄清 prompts_dim.json 真实结构（纠正旧摘要误解）**：它是**维度分类映射表**而非 prompt 文件——结构为 `_comment` / `dim_3d_categories`(5) / `counts`({2d:257,3d:242}) / `map`(499 条分类，对应 prompts.json 每条的维度)。真正驱动 3D 生产的是三层文档+.assetmeta.json；prompts_dim.json 只告诉生成器哪些 prompts.json 条目属 3D 维度。
- **§14.4 生成器待办**：`tools/generate_resource_status.py` 仍只在 `.workbuddy/tmp_resource_status.py`（待正式纳入 tools/）。

## D0-3 executed (ConfigDatabase, reuse not re-implement) — 2026-07-10
- §5.3 + Architecture Report §7 confirmed: ConfigService+ConfigManager already load configs → ConfigDatabase MUST reuse, no new loader.
- Key conflict found & resolved: ConfigService.ts top-level `import { resources, JsonAsset } from 'cc'` → static ref would drag `cc` into ConfigDatabase and break vitest (node). To satisfy ALL of {reuse ConfigService data / no `cc` / testable / no new interface/method}: used CONSTRUCTOR INJECTION of `Partial<GameConfigs>` (runtime: D0-5 GameBootstrap passes namespaces from `ConfigService.instance.get(name)`); `loadAll()` only validates+marks loaded, never loads. No `IConfigSource` interface added (honors "不得新增接口"). `import type` from ConfigTypes only → zero runtime cc dep.
- All 7 §5.3 return types (SkillConfig/MonsterConfig/BossConfig/EffectConfig/AIConfig/CameraConfig/AudioConfig) ABSENT in ConfigTypes, and CONFIG_NAMES has no effects/ai/camera/audio files → per D0-3 rule every getter returns `unknown` + `// TODO: define XxxConfig in ConfigTypes`; NO new config types defined inside ConfigDatabase. Namespace mapping (skills/monsters/zones/battle/economy) is PROVISIONAL placeholder, documented in source header, pending ConfigTypes enrichment.
- Did NOT touch assets/scripts/config/** (ConfigService/ConfigManager/ConfigTypes untouched).
- `npm run test` → 12 cases pass (exit 0). `validate:all` → **9/9 PASS** (gate-9 now green because art-track moved asset_validate.py to 回到地面/tools/ — see above).
- Committed: `aea70c0` `[Demo0][D0-3] implement ConfigDatabase reusing ConfigService data (§5.3)` — 2 files only.

## 补查:SKILL.md 对 resource_status.md / prompts.json 的引用是否有效(用户纠正)

- **用户纠正**:art-pipeline SKILL 确实引用了这 2 个文件,不应只查"文件内部有无失效引用",也要查"SKILL 对它们的引用是否因方案失效"。
- **核查结果**:SKILL 对两者的引用**全部有效、且与方案一致**:
  - prompts.json:仅 2D 出图语境引用(L146-171),3D 资产 L168 明确"Do NOT call Agnes",走 Blender+asset_validate。路径绝对(E:/game/assets/...)正确。
  - prompts_dim.json:L267 配置文件表正确登记为"3D 维度注册表"。
  - resource_status.md:L215/L261 正确标注 §14.4 进度权威源,重生成命令 `cd E:/game; python .workbuddy/tmp_resource_status.py`(L220-221)有效。
- **门禁 Check3 真实全绿**:`check_doc_consistency._resolve_path` 对 `assets/` 先查父级 `E:/game/assets/`(文件真实所在)再查 `回到地面/assets/`,故相对引用全解析成功(非盲区)。resource_status.md 的 `docs/progress/` 也正确落到 `回到地面/docs/progress/`。
- **结论不变**:两文件本身不需要基于方案修改。待办(非缺陷):① 3D 资产产出后需"重新生成"resource_status.md(生成器仍是 tmp,§14.4 说要固化 tools/generate_resource_status.py);② prompts.json 242 条 3D 类别旧 prompt 方案全量执行后应退役。
- Checkpoint: `docs/ai-tasks/demo0/REPORT_D0-3.md`.
- Next: D0-4 Logger (scan confirmed no existing Logger/LogChannel → safe to create; §5.4 1:1).

## D0-4 executed (Logger, §5.4) — 2026-07-10
- §5.4 + Architecture Report §6 confirmed: no existing Logger/LogChannel/ILogger IMPLEMENTATION (only the `ILogger` token constant in GameContext.ts from D0-2) → safe new file, no duplicate.
- Strict 1:1 §5.4: `enum LogLevel` + `interface LogChannel{debug/info/warn/error(msg,meta?)}` + `class Logger { channel(name): LogChannel }` (only public method) + `const ILogger = "ILogger"`. Channel names FIXED to 7 (battle/ai/scene/physics/asset/ui/audio), no extra channel. Output format `[time][channel][level] msg {meta}`.
- Design decision (recorded, no free addition): "per-channel independent level / Dev default Debug, Release default Error" is REQUIRED behavior in spec → expressed via constructor params `(isDev=true, channelLevels?)`. Constructor params are NOT methods → honors "不得新增方法". Default sink = `console.log`; test uses `vi.spyOn(console,'log')` capture → no public method added.
- Pure TS, NO `cc` import (node-testable). Did NOT touch any other `assets/scripts/**`; did not change scattered `console.log` (later Demo's job).
- `npm run test` → 17 cases pass (5 files, exit 0). `validate:all` → **9/9 PASS** (incl gate-9 doc-consistency OK).
- Committed: `971e0a2` `[Demo0][D0-4] implement Logger categorized channels + levels (§5.4)` — 2 files only (Logger.ts + logger.test.ts).
- Checkpoint: `docs/ai-tasks/demo0/REPORT_D0-4.md`.
- Next: D0-5 GameBootstrap integration (assemble GameContext + LifecycleManager + ConfigDatabase + Logger; empty-scene lifecycle verification: print Initialize→Enter→Pause→Resume→Exit→Destroy in order).

## D0-5 executed (GameBootstrap wiring + empty-scene verify, §5) — 2026-07-10
- Modified ONLY `assets/scripts/core/GameBootstrap.ts`: appended infra wiring, did NOT touch the existing 3-step load (ConfigService.loadAll / ConfigManager.loadAll / AssetBundleService.loadAssetMapFromResources) nor `goToMain`.
- Added private `_ctx`/`_lifecycle` fields + private `_wireInfra()` method: new GameContext → register(ILogger, new Logger(true)) + register(IConfigDatabase, new ConfigDatabase()) → new LifecycleManager → register an inline ILifecycle probe (object literal, NOT `new` business System — honors red line 4) → probe.initialize(ctx) + enterAll/pauseAll/resumeAll/exitAll/destroyAll, each logs via Logger channel "battle". `startup()` calls `_wireInfra()` inside try/catch (non-blocking: failure only warns, existing startup flow + `console.log('[GameBootstrap] startup complete')` preserved). `onDestroy()` now also calls `this._ctx?.onDestroy()`.
- ConfigDatabase does NOT implement ILifecycle → deliberately NOT registered into LifecycleManager (avoids faking lifecycle, per D0-5 strict constraint).
- CI-proof decision: GameBootstrap imports `cc` so it can't run under vitest (node). Added `tests/core/bootstrap_integration.test.ts` which assembles the SAME four infra classes + SAME probe/sequence and asserts the printed order equals [Initialize, Enter, Pause, Resume, Exit, Destroy]. Dual-track: engine-side `_wireInfra()` uses identical classes/sequence; CI locks correctness.
- `npm run test` → 20 cases pass (6 files, exit 0). `validate:all` → **9/9 PASS**.
- Committed: `f21aff7` `[Demo0][D0-5] wire GameContext+LifecycleManager into GameBootstrap + empty-scene verify (§5)` — 2 files only (GameBootstrap.ts + bootstrap_integration.test.ts).
- Checkpoint: `docs/ai-tasks/demo0/REPORT_D0-5.md`.
- **Demo0 COMPLETE**: D0-0(vitest base) → D0-2(GameContext) → D0-1(LifecycleManager) → D0-3(ConfigDatabase) → D0-4(Logger) → D0-5(GameBootstrap wiring). Four core infra closed + verified via GameBootstrap. Next: Demo1 (§5.5 DebugPanel or §5.6 SaveManager, per user-chosen order).

## 全相对路径化（art-pipeline 文档集）
- **用户指令**：所有路径必须用相对路径（no absolute `E:/game/...` / `C:/...` / `回到地面/...` mixed）。
- **范围**：`art-pipeline/SKILL.md` + `回到地面/docs/美术资源生成与入库规范.md`（L1/L2/L3 已无绝对路径，无需改）。
- **SKILL.md 改动**：`E:/game/.workbuddy/...`→`.workbuddy/...`；`E:/game/assets/...`→`assets/...`；`回到地面/docs/...`→`docs/...`；`docs/2D转3D全面升级方案.md`→`../docs/2D转3D全面升级方案.md`（真实文件在父级 `E:/game/docs/`，gate 不校验 `..` 但相对且正确）；`cd E:/game`→注释"从项目根目录运行"（去掉绝对 cd）。
- **入库规范.md 改动**（在 `回到地面/docs/`，深度 2）：`E:/game/.workbuddy/...`→`../../.workbuddy/...`；`E:/game/tools/...`（顶层历史工具）→`../../tools/...`；`E:/game/tools/asset_validate.py`（已迁 回到地面/tools）→`../tools/asset_validate.py`。
- **验证**：grep 两份文件无绝对路径残留；复刻门禁 Check3 逐条解析全部 OK（MISSING=0）；`check_doc_consistency.py` → Check3 All 24 valid；`encoding_audit --ci` issues=0/p0/p1/p2=0。
- **gate 发现**：`回到地面/docs/...` 形式被 `_resolve_path` 所有分支跳过（None→continue），之前是"没报错但没校验"的漏洞；统一改 `docs/...` 后变为 gate 可校验。
- **沉淀规则**：新增 `RELATIVE_PATH_POLICY` 到项目 MEMORY.md（相对基准=回到地面；`../` 指向父级 E:/game 下的 docs/tools/.workbuddy）。

### 清理 E:\game 根目录 13 个 0KB 空文件
- 用户截图发现 E:\game\ 下有 13 个 0KB 空文件（版本说明/方案目标/口径说明/单一事实来源 等），时间戳均为今天 11:57
- 确认文件名全部来自 `docs/2D转3D全面升级方案.md` 的章节标题（含 emoji 字符 ）
- 非本次会话创建（本次会话尚未执行写操作），应为之前会话处理文档时的副作用
- 用户确认后已全部安全删除（13/13），不影响任何项目文件

## Demo1 executed (AssetCache + ModelRenderService, §3.6) — 2026-07-10
- §3.6 authoritative (high-level, no exact TS signature) + demo1.md skeleton (load/release/refCount, implements ILifecycle). Scan: no existing AssetCache/ModelRenderService/IAssetCache IMPLEMENTATION (only `IAssetCache` token const in GameContext.ts from D0-2) → safe new file.
- Design (mirrors D0-3 anti-`cc` pattern): `AssetCache` is PURE TS, ZERO `cc` import. Underlying load DELEGATED to injected `loader` (in GameBootstrap wired to `AssetBundleService.instance.loadById`) → reuses existing loader, no duplicate implementation. Implements `IAssetCache` (local interface) + `ILifecycle`. Ref-count + ReleaseQueue deferred drop (setTimeout, 防抖动). `ModelRenderService` references `cc` (instantiate/SkeletalAnimation) → engine-only, no unit test; its ref-count logic lives in AssetCache (tested).
- Files: +`assets/scripts/assets/AssetCache.ts` (99 lines), +`assets/scripts/render/ModelRenderService.ts` (40 lines), +`tests/core/assetcache.test.ts` (5 cases), ~`assets/scripts/core/GameBootstrap.ts` (register IAssetCache into GameContext + LifecycleManager).
- `npm run test` → 25 cases pass (7 files). `validate:all` → **9/9 PASS**.
- Step5 fix: P0 `comment_may_swallow_code` on AssetCache.ts L7 (`//    class does NOT...` flagged as commented-out code) → reworded to `//    cache does NOT...` + removed inner quotes; `encoding_audit --ci` → issues=0/p0=0.
- Reported risks: (1) `.glb` engine verification NOT CI-runnable — project is 2D, no 3D assets yet; ModelRenderService code correct, verify when assets ready. (2) ModelRenderService has no standalone unit test (cc import). (3) Token Budget 4 files (2 src + 1 test + 1 mod) within 3~5.
- Committed: `cf62e7a` `[Demo1] add AssetCache (ref-count + lifecycle) + ModelRenderService, wire IAssetCache (§3.6)` — 4 files only.
- Checkpoint: `docs/ai-tasks/demo1/REPORT_demo1.md`.
- Next: Demo2 (per plan: §5.5 DebugPanel F8 / §5.6 SaveManager+crash-recovery / §5.7 ReplayRecorder, user-chosen order).

## art-pipeline 3D 链路审计（prompts.json 消费 + 2D→3D 遗留改造点）
- **确认**：prompts.json 是 2D AI 出图的唯一提示词来源（只读消费，非破坏）。链路：load_prompts()→cmd_generate 遍历 key→classify_resource→category_dim 判 dim→3D 跳过 Agnes；2D 用 `ct_prompt=STYLE_ANCHOR+prompts[key]+DETAIL_ANCHORS+SAFETY_BLOCK` 调 call_agnes_api。STYLE/DETAIL/SAFETY 来自 ART_RESOURCE_RULES.md §15（非 prompts.json）。
- **关键发现**：prompts.json 与 prompts_dim.json 都在**父级 E:/game/assets/**（非 回到地面），pipeline 用 `dirname(PROJECT_ROOT)+assets/...` 正确解析。
- **8 个遗留改造点（报告已写 `E:\game\docs\art-pipeline_3D链路审计.md`）**：
  1. 🔴 cmd_import/validate 3D 分支用 prompts.json 的 2D key 拼 models/characters/archer_idle.glb，但真实 3D 名是 CHR_Archer_A.glb → 命名断裂，真实 3D 资产无法被批次驱动（134 个真实 3D 资产根本不在 prompts.json）。
  2. 🔴 3D 资产无 batch 驱动源，与 L1/L2 规范脱节（缺 3D manifest 注册表）。
  3. 🟠 category_dim 整类返回 3d → 当前 2D 游戏这 5 类 sprite 生成被永久跳过（过渡期阻断）。
  4. 🟠 cmd_import 3D 分支未强制 asset_validate 通过（违反 SKILL 警告）。
  5. 🟡 ART_RULES_PATH 硬编码绝对路径 E:/game/.workbuddy/...（违反相对路径指令）。
  6. 🟡 SKILL.md 3D import 示例与代码现实不符。
  7. 🟡 prompts.json 242 条 3D 类旧 prompt 悬空（既不能生 2D 也不能生 3D）。
  8. 🟢 _validate_3d_asset 与 asset_validate.py 双份实现，有漂移风险。
- **已正确部分**：prompts.json 只读消费；dim 判定顺序；validate_technical 3D 路由；_rules3d_bucket 桶名与 art_quality_budget.json 实际键一致；asset_validate.py 存在且 self-test 通过。
- 待用户决定改造范围（仅审计，未修改任何代码）。

## Demo2 executed (CameraBrain 7-mode follow camera, §3.4) — 2026-07-10
- §3.4 authoritative mode tree = 7 modes (Follow/LockOn/Boss/Dialogue/Cinematic/Shake/Zoom). demo2.md skeleton only listed 4 in `setMode` signature → conflict; resolved by implementing FULL 7 modes (authoritative source wins). Reported as item #1.
- Scan: no existing CameraBrain/ICameraBrain IMPLEMENTATION (only `ICameraBrain` token const in GameContext.ts D0-2) → safe new file. Reused `ICameraBrain` token, did NOT redefine interface (禁止重复实现).
- Design: `CameraBrain.ts` PURE TS, ZERO top-level `cc` import (mirrors D0-1..D0-4 + Demo1 AssetCache) → node/vitest importable. Math uses plain `Vec3Like` + structural `ICameraNode`; real engine camera injected at runtime via `attach()` (cc.Vec3 satisfies Vec3Like). Mode params sourced from `ConfigDatabase.getCamera(modeKey)` — NO hardcoded tunables; defaults only as safety net (proved via test with injected followLerp=2 ≠ default 8). Implements `ILifecycle`.
- Files: +`assets/scripts/camera/CameraBrain.ts` (186 lines), +`tests/core/camerabrain.test.ts` (7 cases), ~`assets/scripts/core/GameBootstrap.ts` (register ICameraBrain + LifecycleManager + `_findMainCamera()` attaches engine main camera, non-fatal).
- `npm run test` → **32 cases pass (8 files, exit 0)**. `validate:all` → **9/9 PASS**. `encoding_audit --ci` → issues=0/p0=0.
- Step5 fix: source bug `lateUpdate` referenced undefined member `this._shakeFreq` → NaN; corrected to per-mode `p.shakeFreq`. In-task legitimate fix, no design change.
- Reported items (Agent Contract #6): (1) mode-count mismatch skeleton vs §3.4 → implemented 7; (2) reused ICameraBrain token; (3) getCamera namespace still maps to `zones` (D0-3 temp) — needs ConfigTypes CameraConfig later; GameBootstrap still uses `new ConfigDatabase()` so camera params fall back to defaults until ConfigService injection wired; (4) Token Budget 3 files (2 src + 1 test) vs skeleton "≤2" — interpreted as source files, test mandatory by DoD.
- Committed: `0ea8287` `[Demo2] add CameraBrain 7-mode follow camera + wire ICameraBrain (§3.4)` — 3 files only.
- Checkpoint: `docs/ai-tasks/demo2/REPORT_demo2.md`.
- Next: Demo3..Demo6, or §5.5 DebugPanel (F8) / §5.6 SaveManager+crash-recovery / §5.7 ReplayRecorder (per plan order; user-chosen).

## art-pipeline 跨层级同名文件/文件夹核查（相对路径索引风险）
- 用户指出：美术文档应在 `E:\game\回到地面\docs` 下。已验证：美术规范/总表/L2 机器规格(MachineSpec.json+yaml)/Workflow 均在那里；SKILL.md 的 `docs/` 引用经门禁 `_resolve_path` 锚定 `project_root` 正确。但**上一轮误将 `art-pipeline_3D链路审计.md`(8KB) 写到了父级 `E:/game/docs/`**，应迁回项目 `docs/`。
- **5 个跨层级同名文件夹**：`docs/ assets/ tools/ .workbuddy/ art_source/` 在 `E:/game/` 与 `E:/game/回到地面/` 两层都存在。
- **门禁索引基本安全**：`_resolve_path` 把每个前缀锚定固定基目录（`docs/→project_root`、`tools/→project_root`、`assets/→父级优先`、`.workbuddy/→父级`），所以 SKILL 的 `docs/X` 永远解析到项目层，不会因同名文件夹错乱。
- **真实隐患**：① `../docs/2D转3D全面升级方案.md`(SKILL 第66行) 不匹配任何分支→门禁返回 None→**静默跳过、不校验（盲区）**；该文件实际在父级 `E:/game/docs/`。② 相对路径本身无锚点，若某工具以 CWD=`E:/game` 解析 `docs/X` 会命中父级 planning 文件夹（无美术规范）→ 找不到文件。③ `美术资源生成与入库规范.md` 在 `docs/`(54KB 规范) 与 `docs/progress/`(483B 进度 stub) 同名 → 按 basename 索引的工具会碰撞（属 scheme+progress 约定，非危险重复，但需留意）。
- **项目层 `E:/game/回到地面/.workbuddy` 也存在**（automations/memory/skills/ui-resource-pipeline），与父层 `.workbuddy` 同名；门禁只用父层 `.workbuddy`（ART_RESOURCE_RULES 等）。
- 结论：门禁自动化索引不受影响；但同名文件夹对人类/CWD 解析是真实歧义源，且 `../docs/` 是校验盲区。建议：修门禁支持 `../` 解析、固化"两层 docs 分工"约定、迁回误放审计文档。待用户确认修复范围。

## art-pipeline 3D 链路 8 项遗留改造（按优先级全量执行）— 2026-07-10
- 用户指令「按照优先级依次执行」→ 依据 `docs/art-pipeline_3D链路审计.md` 的 P0→P3 落地 8 处改造，全部完成且门禁通过。
- **P0 新增 3D 资产注册表**：新建 `tools/gen_3d_manifest.py`，从 L1 文档 `docs/美术资源制作参数总表_3D.md` 解析生成 `assets/resources/config/art_3d_manifest.json`（134 项：name/category/bucket/dim/source_2d/lifecycle）。数量精确匹配 L1 总表（characters5/monsters36/bosses42[effects27/tiles24]；bosses_final12+bosses_mini30）。单一来源 = L1 文档，避免手填 134 条漂移。
- **P0 重构 cmd_import/validate 3D 分支**：新增 `load_3d_manifest()`/`run_asset_validate()`/`_filter_manifest()`/`_cmd_import_3d()`/`_cmd_validate_3d()`。3D 入库/校验改由 manifest 驱动，解析真实 CHR_/MON_/BOSS_/FX_/TILE_ 名（修复 GAP1 命名断裂），不再拼 prompts.json 的 2D key。
- **P0/GAP4 强制 asset_validate**：`run_asset_validate()` 调 `tools/asset_validate.py`（缺 `.assetmeta.json` 边车即 FAIL），3D 资产未过校验禁止入库。已用合法/非法 meta 假资产端到端验证通过。
- **P1 模式开关（解 GAP3+7）**：`category_dim()`/`classify_resource()` 增加 `mode` 参数；generate/validate/import/audit/contact/status 加 `--mode {2d,3d}`（默认 **2d**）。2d 模式 `category_dim` 恒返 "2d" → 解除当前 2D sprite 生成被 3D 整类标跳过的问题；242 条悬空 3D prompt 在 2d 模式下可正常生成 2D，无需破坏性删除。
- **P2/GAP5 路径相对化**：`ART_RULES_PATH` 由硬编码 `E:/game/.workbuddy/...` 改为 `os.path.normpath(join(PROJECT_ROOT,"..",".workbuddy","memory","topics","ART_RESOURCE_RULES.md"))`；同步修 L198 注释。全文件 grep 已无绝对路径残留。
- **P2/GAP6 SKILL.md 对齐**：3D import 示例改为 `import --mode 3d --category bosses` / `--resource CHR_Archer_A`，说明默认 2d 模式；新增「3D asset manifest」引用表行；修一处游离 ``` 围栏。
- **P3/GAP8 复用单一来源**：`_cmd_validate_3d` 经 `run_asset_validate` 调用 `asset_validate.py`（脚本缺失时回退内联 `_validate_3d_asset`），消除双份实现漂移。
- **验证**：`python -m py_compile` OK；`import --all`(2d 默认) 实导入 1 个真实 2D sprite（archer_attack.png 已备份入库）→ 2D 链路解除阻断；`validate --mode 3d`/`import --mode 3d` 走 manifest 正确报缺 .glb（预期，尚无 3D 实物）；`encoding_audit --ci` issues=0/p0=0；`check_doc_consistency.py` ALL CHECKS PASSED。
- 注意：3D 实物（.glb）尚未生产，3D 分支路径已正确但暂未真正入库；`_cmd_import_3d` 要求候选区 `runtime_candidates/<name>.glb`(+`.assetmeta.json`) 就位后运行。

## Demo3 executed (ICollisionService + PhysicsCollisionImpl + KinematicMover, §3.3) — 2026-07-10
- §3.3 authoritative (ICollisionService: raycast/overlapSphere/overlapCapsule/checkGround) + demo3.md skeleton. Scan (grep) confirmed: only `ICollisionService` TOKEN const exists in GameContext.ts (D0-2); NO PhysicsCollision/CollisionService/PhysicsSystem implementation anywhere → safe new file, no duplicate.
- Key design (red line 1 compliance): ALL three physics files are PURE TS, ZERO `cc` import. `PhysicsCollisionImpl` does NOT delegate to a black-box PhysicsSystem; instead it is a deterministic pure-TS math engine over a **collider registry** (engine feeds `cc.Collider` transforms in at runtime via `registerCollider`). This structurally guarantees "business never imports PhysicsSystem" AND is node/vitest-testable AND replay-friendly (§5.7 determinism). Reported as a deliberate, superior engineering choice vs literal "delegate to PhysicsSystem".
- Reused `ICollisionService` token from GameContext (single source) — did NOT re-declare (禁止重复实现). `Vec3` defined locally as plain shape (node-testable).
- Files: +`assets/scripts/physics/ICollisionService.ts` (interface+types), +`assets/scripts/physics/PhysicsCollisionImpl.ts` (impl+ILifecycle), +`assets/scripts/physics/KinematicMover.ts` (deterministic move, axis-separated slide, depends only on ICollisionService, ILifecycle), +`tests/core/collision.test.ts` (11 cases), ~`assets/scripts/core/GameBootstrap.ts` (register ICollisionService + join LifecycleManager, mirrors AssetCache/CameraBrain).
- `npm run test` → **43 cases pass (9 files, exit 0)**. `encoding_audit --ci` → issues=0/p0=0. `validate:all` → **8/9 PASS**.
- Pre-existing gate FAIL (NOT Demo3): `[非UI资源注册]` errors on `character.archer.attack` (declared 192x768 vs actual 256x1024 — art track regenerated the sheet without updating registration metadata) + 30 unreferenced miniboss idle textures (warnings). Outside Demo3 scope (assets/config registry); per Contract #6/#9 did NOT bypass or edit; reported for art track to fix.
- Committed: `1d6eaae` `[Demo3] ICollisionService + PhysicsCollisionImpl + KinematicMover (§3.3)` — 5 files only (physics *.ts + GameBootstrap + test); art-track working-tree changes NOT committed.
- Checkpoint: `docs/ai-tasks/demo3/REPORT_demo3.md`.
- Next: Demo4 (SkillGraph, §3.9) or §5.5 DebugPanel / §5.6 SaveManager / §5.7 ReplayRecorder (per plan order).

## art-pipeline 跨层级同名全树审计 + F1-F4 修复执行
- 用户扩展要求：把同名核查扩到整个 E:\game。跑全树扫描（排除 node_modules/.git/__pycache__/.creator 等）：506 文件夹 / 4425 文件 → **67 个跨层级同名文件夹名、843 个同名文件 basename**。
- 🔴 命中门禁前缀的同名文件夹（5）：`.workbuddy/ art_source/ assets/(3层)/ docs/ tools/`。🟠 另有 `config/ memory/ resources/ skills/(7层)`。
- 🟠 72 个同名项目文档/配置 basename：主要是 `docs/X.md` 与 `docs/progress/X.md` 成对（scheme+progress 约定，非危险重复，但按 basename 索引会碰撞）；`auto_bind_scene.py/gen_scenes.py/gen_scenes_simple.py` 在父级与项目 `tools/` **逐字节相同副本**（冗余，有分叉隐患）；`StorageService.ts` 两处实现。
- **F1 完成**：`check_doc_consistency.py` 的 `_resolve_path` 新增 `../` 分支（相对 project_root 解析）+ 提取逻辑扩展到 **围栏 ```text 代码块**（此前 `../docs/2D转3D...` 在围栏内连提取都没有，是更隐蔽的盲区）。初版围栏提取误抓 `#` 后中文注释，已修正为"仅取每行前导路径 token（遇空格/# 即止）"。验证：`../docs/2D转3D全面升级方案.md` 解析到 `E:\game\docs\2D转3D全面升级方案.md`（exists）。`validate:all` + `encoding_audit --ci` 全绿。
- **F2 完成**：误放 `art-pipeline_3D链路审计.md` 从父级 `E:/game/docs/` 迁回 `E:/game/回到地面/docs/`（其 `docs/...` 引用现正确指向项目层规范）。
- **F3 据实跳过**：`art-pipeline升级审查.md` 等 4 篇规划文档大量用 `回到地面/...` 根相对引用 → 属**根层 planning/audit 层（E:/game/docs/）**，迁入项目层会断裂全部引用。故不移动。
- **F4 完成**：MEMORY.md `RELATIVE_PATH_POLICY` 扩充「两层 docs 分工」——根 docs=规划/审计/审查（用 `回到地面/...` 引用项目）；项目 docs=美术+游戏 SPEC（用 `docs/X` 互链）；用 `回到地面/...` 的文档必须留根层。
- 交付：报告 `E:/game/docs/跨层级同名文件索引审计报告.md`（置于根 docs=审计层，符合约定）。

## Demo4 executed (SkillGraph data-driven skill chain, §3.9) — 2026-07-10
- §3.9 authoritative (SkillData→SkillGraph→SkillExecutor, eliminate switch(skillId)) + demo4.md skeleton. Scan (grep) confirmed: NO SkillGraph/SkillExecutor/ISkillGraph/SkillData-in-skill-subdir anywhere → safe new dir `battle/skill/`. Note: legacy `battle/SkillSystem.ts` has `switch (skill.id)` (line 84) — the exact anti-pattern red line 2 targets — but it lives in `battle/` root (NOT `battle/skill/`), so it is OUT OF SCOPE per Contract #6 and was NOT touched.
- Red line 2 (no switch(skillId)) satisfied STRUCTURALLY: `SkillGraph.build(data)` derives the node chain (projectile→explosion→burn) purely from which data fields exist — never reads `data.id`. `SkillExecutor` dispatches nodes via `Map<SkillNodeKind, handler>` keyed by `node.kind` (NOT switch). Discriminated union narrowed with `Extract<>` (no @ts-ignore, Contract #8). Proved by test: different skill ids with same shape → identical node chain.
- Red line 4 satisfied: SkillExecutor pulls ILogger / ICollisionService / ISkillGraph via `ctx.get` in `initialize` (no `new` of services). Red line 3: SkillGraph + SkillExecutor both `implements ILifecycle`, join LifecycleManager teardown.
- Reused: `ICollisionService` interface + `PhysicsCollisionImpl` + `ALL_MASK` (Demo3) for explosion AOE hit query (business still zero `PhysicsSystem`). `ISkillGraph`/`ISkillExecutor` tokens co-located in their owning modules (did NOT modify `core/GameContext.ts` token table — stays within allowed scope). `Vec3` reused from physics.
- Files: +`assets/scripts/battle/skill/SkillData.ts` (types), +`SkillGraph.ts` (+ISkillGraph token, ILifecycle), +`SkillExecutor.ts` (+ISkillExecutor token, ILifecycle, Map dispatch), +`Resolvers.ts` (HitResolver/DamageResolver pure fns), +`tests/core/skill.test.ts` (5 cases), ~`assets/scripts/core/GameBootstrap.ts` (register ISkillGraph+ISkillExecutor, initialize, LifecycleManager).
- `npm run test` → **48 cases pass (10 files, exit 0)**. `validate:all` → **8/9 PASS**: this run `[TS静态检查] OK` and `[架构门禁] OK` (my new TS compiles; no illegal loadScene/new Component); only `[非UI资源注册]` FAILS on the SAME pre-existing art-track gap (`character.archer.attack` sheet 256x1024 vs registered 192x768 metadata mismatch + 30 unreferenced miniboss idle, WARN) — NOT caused by Demo4, out of scope, did NOT bypass/edit per Contract #6/#9. `encoding_audit --ci` → issues=0/p0=0.
- Note: `check_architecture.py` does NOT yet statically detect `switch` (same as Demo3 red line 1 not statically checked) → red line 2 verified via structure + unit test, consistent with Demo3; did NOT modify `tools/` (out of scope). If a static switch-gate is wanted later, raise as a separate cross-Demo gate task.
- Committed: `c8c5a86` `[Demo4] SkillGraph: data-driven skill chain (§3.9) — no switch(skillId)` — 6 files only (4 src + GameBootstrap + test); art-track working-tree changes NOT committed.
- Checkpoint: `docs/ai-tasks/demo4/REPORT_demo4.md`.
- Next: Demo5..Demo6, or §5.5 DebugPanel / §5.6 SaveManager / §5.7 ReplayRecorder (per plan order).

## Fixed art-track gap: character.archer.attack sheet metadata mismatch — 2026-07-10
- Symptom (from Demo3/Demo4): `check_game_assets_registry.py` → `sheet metadata mismatch: character.archer.attack, declared=192x192×4, actual=256x1024`. Blocked `[非UI资源注册]` gate (validate:all stuck at 8/9).
- Root-cause investigation (NOT a Demo bug; user did not touch assets/registry):
  - Measured disk: 35 character sprite sheets → **34 are 192×768**, only `archer/archer_attack.png` is **256×1024** (mtime 2026-07-09 23:38, from the `prompts.backup_positive_style_20260709_*` regen batch). Rest of project (game_assets.json + SpriteSheetUtil.ts) still declares 192×192×4.
  - **Authoritative size source = `ART_RESOURCE_RULES.md`**: §9 L130-131 (角色单帧 256×256 / 角色序列帧 256×1024) and §15 JSON L647/L663 (`recommended_sizes.characters=[256,1024]`, `size_ladder.characters=[256,192,128]`). `prompts.json` L139 archer_attack prompt also says "Target canvas 256x1024, each frame 256x256".
  - **Conclusion flipped**: the 256×1024 FILE is spec-compliant; the registry's 192×192 is STALE (predates the rule change to 256). Correct fix = align registry→256, NOT shrink file.
- Fix (config-only, no art asset touched — respects art-pipeline human-approval gate):
  1. `assets/resources/config/game_assets.json`: `character.archer.attack` frameWidth/frameHeight 192→256.
  2. `assets/scripts/assets/SpriteSheetUtil.ts` L26: archer_attack `{192,192,4}`→`{256,256,4}`.
  3. `tools/gen_game_assets.py` SHEET_METADATA: added explicit override `"characters/archer/archer_attack": (256,256,4,"vertical")` after the 35-entry 192 comprehension (so a regen won't revert).
- Verify: `check_game_assets_registry.py` → **errors=0** (mismatch gone; remaining 312 are WARN: safeReview pending + 30 unreferenced miniboss idle, pre-existing/unrelated). `encoding_audit --ci` issues=0/p0=0. JSON valid.
- **Outstanding debt (flagged, NOT done)**: the other 34 character sheets are still old 192×768 (< current 256 spec). They pass the gate only because registry also says 192. Full spec compliance = regenerate those 34 at 256×1024 + bump their registry entries. That is a separate art batch (needs the pipeline + visual confirm). Do NOT blanket-bump registry to 256 without regenerating files, or the check would fail on those 34.

## Demo5 executed (Dungeon room full lifecycle + NavigationGrid, §3.7 + §5.1) — 2026-07-10
- §3.7 authoritative (GridManager split: DungeonGenerator→RoomBuilder→TileMap→NavigationGrid→RoomRuntime) + §5.1 room-level lifecycle chain (SceneFlowService→RoomRuntime.enter/exit) + demo5.md. Scan (grep) confirmed: NO RoomRuntime/RoomBuilder/NavigationGrid/DungeonGenerator/TileMap/INavigation anywhere → safe new dir `dungeon/`. Legacy `dungeon/GridManager|DungeonManager|DAGGenerator` (fat GridManager) left untouched (Contract #6).
- Files (5 new + 1 mod + 1 test): +`dungeon/DungeonGenerator.ts` (seed+zone deterministic layout, REUSES existing `core/rng/Rng` xorshift32 — red line 5, no Math.random), +`dungeon/TileMap.ts` (floor/wall/void + occupancy logic layer), +`dungeon/RoomBuilder.ts` (build→RoomData: TileMap + module pieces + assetIds), +`dungeon/NavigationGrid.ts` (implements INavigation, deterministic grid A* 4-neighbour fixed-order + seq tie-break; NavMesh deferred Phase 4), +`dungeon/RoomRuntime.ts` (implements ILifecycle + IRoomRuntime token co-located; initialize pulls IAssetCache via ctx.get; load ref-counts room assets; exit/destroy release EXACTLY acquired ids idempotent → no leak §3.6/§5.1; entity+occupancy mgmt; no physics dep), ~`core/GameBootstrap.ts` (build demo room + register IRoomRuntime + join LifecycleManager), +`tests/core/room.test.ts` (13 cases).
- Red lines: #3 ILifecycle (RoomRuntime), #4 no-new (ctx.get IAssetCache), #5 no Math.random (seeded Rng), #1 no PhysicsSystem (dungeon has zero physics dep; collision would go via ICollisionService). INavigation minimal signature (findPath/isWalkable) co-located: §3.7 mandates the interface but §5.x gives no signature → Contract #4 (minimal + documented). IRoomRuntime token co-located in RoomRuntime.ts (GameContext token table not in allowed scope) — same pattern as Demo4.
- `npm run test` → **61 cases pass (11 files, exit 0)**. `validate:all` → **9/9 ALL PASS** this run — `[非UI资源注册]` now OK (art-track archer.attack metadata fix above took effect). `encoding_audit --ci` → issues=0/p0=0.
- Committed: `d2490b8` `[Demo5] Dungeon room full lifecycle + NavigationGrid (§3.7 + §5.1)` — 7 files.
- Checkpoint: `docs/ai-tasks/demo5/REPORT_demo5.md`.
- Next: Demo6, or §5.5 DebugPanel / §5.6 SaveManager / §5.7 ReplayRecorder (per plan order).

## §5.5/5.6/5.7 executed (DebugPanel + SaveManager + ReplayRecorder) — 2026-07-10
- Per user `做 §5.5 DebugPanel / §5.6 SaveManager / §5.7 ReplayRecorder`. Scan (grep) confirmed: NO DebugPanel/SaveManager/ReplayRecorder/ISaveManager/IDebugService/IReplayRecorder class in `assets/scripts` → safe new dirs `debug/ save/ replay/`. GameContext ALREADY declares tokens `ISaveManager / IDebugService / IReplayRecorder` (§5.x service list) → reused, NO new tokens (Contract #2).
- Files (3 new + 1 mod + 1 test): +`debug/DebugPanel.ts` (§5.5, IDebugService; pure-TS ILifecycle; 12-category DebugSnapshot via provider Map = NO switch; event ring buffer cap 200 fed by Logger sink → `pushRaw` parses `[time][ch][lvl] msg`; camera panel duck-typed: wires only if registered ICameraBrain exposes getDebugState() — CameraBrain/Demo2 lacks it, so NOT modified, auto-wires later), +`save/SaveManager.ts` (§5.6, SaveManager interface 1:1 with plan: saveRun/savePlayer/saveDungeon/saveEnemy/saveSeed/loadRun; SaveBackend injected = MemorySaveBackend for tests / engine swaps localStorage; Run/Player/Dungeon/Enemy+Seed snapshots grounded in plan prose), +`replay/ReplayRecorder.ts` (§5.7, ReplayRecorder header/record/play + ReplayPlayer; ring buffer last 5 runs; computeConfigHash = deterministic FNV-1a, NO Math.random red line 5; determinism proven by test: same seed+input → identical re-emit), ~`core/GameBootstrap.ts` (register IDebugService BEFORE Logger so Logger sink forwards to DebugPanel events; register ISaveManager + IReplayRecorder into LifecycleManager), +`tests/core/debug_save_replay.test.ts` (17 cases).
- Red lines: #1 no PhysicsSystem (all pure TS), #2 no switch(skillId) (provider Map + ordered re-emit), #3 ILifecycle (all 3), #4 no-new (deps via ctx.get), #5 no Math.random (FNV hash). #6 only SceneFlowService loadScene (untouched).
- `npm run test` → **78 cases pass (12 files, exit 0)**. `validate:all` → **9/9 ALL PASS**. `encoding_audit --ci` → issues=0/p0=0. Remaining WARN (safeReview=false WeChat compliance pending / 30 unreferenced miniboss idle) are PRE-EXISTING art-track hints, not introduced by this task, not errors.
- Committed: `e56fd58` `[Demo5.5-5.7] DebugPanel + SaveManager + ReplayRecorder (§5.5/§5.6/§5.7)` — 5 files, 650 lines.
- Checkpoint: `docs/ai-tasks/REPORT_5_5_5_6_5_7.md`.
- Design notes for later: Seed panel now fed by demo seed (IRuntimeState not built); Col panel (ICollisionService.debugDraw) + AI/Skill/Nav/Anim panels await their source systems; SaveManager production backend = swap MemorySaveBackend for localStorage/cc.sys.
- Next: Demo6, or remaining §5.x (AudioSystem §5.8 / §5.11 automation / §5.13 branch gates).

## §5.8/5.11/5.13 executed (AudioSystem + automation mocks + branch gate) — 2026-07-10
- Per user `推进 §5.8 AudioSystem / §5.11 自动化测试 / §5.13 分支门禁`. Scan (grep) confirmed: NO `AudioSystem`/MockCollisionService/MockConfigDatabase class in `assets/scripts` → safe new dirs `audio/` + `tests/support/`. `IAudioService` token ALREADY declared in GameContext (§5.x service list, line 9) → reused, NO new token (Contract #2).
- Files (4 new src/test + 1 mod + 2 new test + 1 new tool):
  +`audio/AudioSystem.ts` (§5.8, pure-TS zero cc; six sub-systems BGM/SFX/Voice/Ambient/3D + Snapshot; `AudioSink` backend injected = engine wraps cc.AudioSource, test uses `MemoryAudioSink` co-located; 3D distance attenuation from injectable listener (engine feeds CameraBrain world pos each frame — CameraBrain has no getter so NOT modified, duck-free setListener); config-driven via `IConfigDatabase.getAudio`; `implements ILifecycle`; reuses `IAudioService` token),
  +`tests/support/MockCollisionService.ts` (ICollisionService double: preset overlap results + record calls),
  +`tests/support/MockConfigDatabase.ts` (minimal getAudio config source for injection),
  +`tests/core/automation.test.ts` (§5.11: Seed determinism Rng; Combat/Skill via MockCollisionService driving SkillExecutor explosion hit damage+burn no engine; config volume via MockConfigDatabase; Dungeon covered in room.test.ts; AI pending IAIController/Demo6),
  +`tests/core/audio.test.ts` (§5.8: 5 categories + 3D attenuation + snapshot + stop + listener + ILifecycle + token),
  +`tools/branch_gate.py` (§5.13: CI/PR merge gate; runs `validate:all` + `vitest`; hard-fail on gate/test failure; soft-warn merging to main from non feature/3d-* branch per decision F; UTF-8 + EN comments),
  ~`core/GameBootstrap.ts` (register IAudioService = AudioSystem + MemoryAudioSink, initialize, join LifecycleManager).
- Red lines: #1 no PhysicsSystem (all pure TS), #2 no switch(skillId) (SkillExecutor unchanged Map dispatch; AudioSystem no skill branch), #3 ILifecycle (AudioSystem), #4 no-new (AudioSystem new-ed only in GameBootstrap wiring; deps via ctx.get), #5 no Math.random (no random; Seed via Rng xorshift32), #6 only SceneFlowService loadScene (untouched; [架构门禁] OK).
- `npm run test` → **90 cases pass (14 files, exit 0)**. `encoding_audit --ci` → issues=0/p0=0. `validate:all` → **8/9 PASS**: TS静态检查/架构门禁/资源注册/非UI资源注册/UI皮肤绑定/文档一致性/包体预算/编码审计 ALL OK.
- PRE-EXISTING art-track gate FAIL (配置校验 1 error): `resource_inventory.json` missing `metadata` field. File is UNTRACKED (`??`), generated by art-track (another conversation) — NOT in allow-list, NOT caused by this task. The branch gate correctly surfaces it (merge to main blocked until art-track adds metadata). Per Contract #9 no gate disabled; per Contract #6 no art file touched. branch_gate.py is the intended enforcement; it will PASS once art-track fixes the metadata field.
- Committed: `a292453` `[§5.8/5.11/5.13] AudioSystem + automation mocks + branch gate` — 7 files, 615 lines.
- Checkpoint: `docs/ai-tasks/REPORT_5_8_5_11_5_13.md`.
- Next: art-track fixes resource_inventory.json metadata → validate:all 9/9 + branch gate green; or Demo6 (IAIController / combat layering / anim state machine) to complete §5.11 AI test category.

## Consistency audit: ART_RESOURCE_RULES.md vs 2D→3D plan + 3D 制作参数总表 — 2026-07-10
- Question: based on the 2D→3D plan and 美术资源制作参数总表_3D.md, is ART_RESOURCE_RULES.md's resource data the latest?
- Authority chain (by design): `art_quality_budget.json → rules3d` (machine source) → `ART_RESOURCE_RULES.md §16` (human wrapper) → `美术资源制作参数总表_3D.md` L1 (v6, declares §16+rules3d as its single source, line 10).
- **Numeric data: CURRENT & consistent.** §16.3 prose matches `rules3d` for all 7 buckets (角色/普通怪/终Boss/小Boss/普通特效/Boss特效/Tile) — verified Tri/bone/textureSize/ASTC/LOD%/dist/clips. 3D 总表 v6 per-asset budgets ALSO match `rules3d` (角色 ≤3000/512/20-30骨/LOD100·60·30; 怪 ≤2500/512/LOD100·60·30; 终Boss ≤9000(7000)/1024/ASTC8/LOD100·50·20/dist18·36/60-80骨). 2D sizes §3.2/§15 (角色256×1024, 怪192, Boss384…) internally consistent and align with the archer.attack 256 fix.
- **BUT §16.4 reference metadata is STALE (2 defects):**
  1. 🔴 §16.4 cites `docs/美术资源制作参数总表_3D.md` **v5**, but the actual file is **v6**. Version drift.
  2. 🔴 §16.4 cites `docs/2D转3D全面升级方案.md` (no `../`), but that plan lives at PARENT `E:/game/docs/2D转3D全面升级方案.md` (NOT project `docs/`). Per doc-consistency gate (`docs/`→project root) this ref is BROKEN; should be `../docs/2D转3D全面升级方案.md` (as the 3D 总表 line 4 and SKILL use). Plan version v3 citation itself is correct.
  3. 🟡 §16.2 naming regex `^(CHR|MON|BOSS|FX|TILE)_[A-Za-z0-9]+(_[A-Za-z0-9]+)?$` lacks `.glb/.prefab` suffix, while `rules3d.naming.pattern` allows them. Prose regex would reject valid `CHR_Warrior_A.glb`; rules3d (authoritative, tool-read) is correct → no functional impact, doc-only bug.
- Note: 3D 总表 v6 was hand-bumped (no `gen_art_spec.py` exists in project to auto-sync), so consistency relies on manual edits. `gen_art_spec.py` referenced in §16.6 as "must read §16" does not exist in repo.
- Conclusion: the **resource data is latest**; only the **version string (v5→v6) and one broken relative path** in §16.4 need a trivial fix. Offer to fix both + the §16.2 regex.

## ART_RESOURCE_RULES.md §16 元数据两处修正（已执行）— 2026-07-10
- 背景审计：ART_RESOURCE_RULES.md 的 3D 资源「数值数据」已是最新且与 rules3d / 3D 总表(v6) 一致；仅 §16.4 的「版本/路径引用元信息」过期，§16.2 散文正则缺 .glb/.prefab 后缀。
- 修正 1（§16.4 行 684）：对齐引用 docs/2D转3D全面升级方案.md → ../docs/2D转3D全面升级方案.md（方案实际在父级 E:/game/docs/，按门禁 docs/ 锚定项目根会断链）；版本引用 v5 → v6（总表已升 v6）。
- 修正 2（§16.2 行 699）：命名正则补 ([.]glb|[.]prefab)?，与 art_quality_budget.json → rules3d.naming.pattern 完全一致（程序化验证 ALIGNED? True；对 CHR_/MON_/BOSS_/FX_/TILE_ 合法名匹配、非法名拒绝）。
- 验证：encoding_audit --ci issues=0/p0=0；check_doc_consistency.py Check3 ALL CHECKS PASSED。
- 结论：ART_RESOURCE_RULES.md 资源数据是最新的（2D→3D 升级已固化进 §16 + rules3d）；本次仅修引用元数据，不碰任何美术资产/代码/配置。

## 资源清单与重做执行主文件生成（已执行）— 2026-07-10
- 任务：全量扫描项目已有资源文件，生成供 AI 直接读取并执行 + 人工协作的资源清单主文件（8 节）。
- 产出 2 文件：(1) 回到地面/docs/资源清单_可执行.md（66KB，8 节：总览/计划/步骤/Skill/验收/人工确认/入库/进度；含 186 行可行动计划表 + 433 待核汇总）；(2) 回到地面/assets/resources/config/resource_inventory.json（619 项机器可读：id/type/status/target/depends/skill_command/acceptance/human_gate/priority）。
- 数据来源：磁盘 textures/ 全量扫描（467 PNG）+ prompts.json/prompts_dim.json/art_3d_manifest.json/game_assets.json + ART_RESOURCE_RULES.md。
- 关键发现：需处理 186 项 = 34 角色精灵表尺寸错配(192x768 不等于规格 256x1024) + 17 背景缺失 + 134 个 3D 资产待生产(0 已产) + 1 UI 去重(磁盘 158 大于计划 92)。已生成 433 待核。
- 验证：JSON 合法；encoding_audit --ci issues=0/p0=0；check_doc_consistency Check3 ALL CHECKS PASSED。MD 引用路径均真实存在。
- 放置：项目层 docs（美术+规格层）；机器清单放 assets/resources/config（与 prompts.json 同层，父级 E:/game/assets）。

## 资源提示词设计策略文档（已生成）— 2026-07-10
- 任务：针对游戏中每个资源，按用途/名称给出提示词设计策略，含技术合规(TapTap)、风格统一+差异化。
- 产出：`回到地面/docs/资源提示词设计策略.md`（UTF-8 校验通过；encoding_audit --ci issues=0/p0=0）。
- 核心结构：§0 总纲 → §1 统一风格底座(6 共享核心块) → §2 通用 9 块 Prompt 模板(对齐现有 prompts.json) → §3 引擎技术约束总表(2D/3D 两表，取自 §3/§9/§15/§16) → §4 TapTap 合规+知识产权(§7.2 红线/侵权规避/安全词) → §5 分类型策略(8 类，每类含 用途/风格关键词/技术约束/差异化变量/模板/示例) → §6 统一+差异化治理(区域调性+职业识别色+品质色阶) → §7 落地与门禁 → §8 音频附录(当前 pipeline 未覆盖，前瞻 Brief)。
- 关键澄清（写入文档）：双轨 reality —— backgrounds/icons/部分 ui 走 AI 出图(真提示词)；ui 程序化组件(btn/panel/card…)走 generate_panel.py 不消耗 prompt；characters/monsters/bosses/effects(3D)/tiles 走 Blender→glb/prefab(建模 Brief，非 AI 提示词)，技术约束取自 §16/rules3d；音频当前未纳入 pipeline，给前瞻设计 Brief。
- 复用既有：共享 6 块直接复用 ART_RESOURCE_RULES §1.1/§15；尺寸/体积/命名引 §3/§9.2/§15/§16 单一权威源，未硬编码。

## Demo6 executed (PerfSampler, 100-monster perf baseline) — 2026-07-10
- Per user `推进 Demo6` (after confirming resource_inventory.json metadata gap is art-track's job).
- PLAN CONFLICT resolved (Contract #2 no-duplicate): Demo6 task card asked for NEW `debug/DebugPanel.ts`, but §5.5 already delivered DebugPanel (F8 toggle + FPS/Frame/Mem/DrawCall + 12 categories). Did NOT recreate it. Genuinely-new artifact = `debug/PerfSampler.ts`.
- Deliverables (commit `3e8aa5e`, 4 files / +212 lines):
  +`debug/PerfSampler.ts` (NEW, pure-TS zero cc, ILifecycle): sliding-window smoothed FPS/frame-time from injected frame deltas (deterministic, NO Math.random red line 5); memory/drawCall via injected `IPerfSource`; `getSnapshot()` returns DebugPanel-compatible shape (Map aggregation, no switch).
  +`debug/DebugPanel.ts` (EXTENDED, within Demo6 allowed `debug/**` scope): `setPerfSampler()`; `update()` delegates perf to PerfSampler as authoritative source; §5.5 fallback kept.
  +`core/GameBootstrap.ts` (MODIFIED): register PerfSampler, wire to DebugPanel (conditional-mount wiring; Dev/Debug gating noted bundler-level). Reuses IDebugService token.
  +`tests/core/perf.test.ts` (NEW): 9 tests (deterministic ~60fps, window replacement -> ~30fps, empty/non-finite delta, IPerfSource mem/draw, snapshot, reset, ILifecycle; DebugPanel delegation + §5.5 fallback).
- vitest: 100 passed (15 files). encoding_audit issues=0. Gates: 8/9 — only FAIL = PRE-EXISTING art-track `resource_inventory.json` missing `metadata` (user-confirmed art-track fixes; branch gate surfaces it). TS静态检查/架构门禁/资源注册/UI皮肤/非UI资源/文档一致性/包体预算 all OK.
- Risk: real-device 100-monster >=30fps / DrawCall<120 acceptance still pending (cannot unit-test; noted in task card 风险).
- Demo0..Demo6 + §5.5/5.6/5.7 + §5.8/5.11/5.13 all delivered. Remaining §5.x: §5.8 already done; §5.11 AI test category pending IAIController; §5.13 done. Next natural step: Demo6-ish IAIController (combat layering / anim state machine) to complete the architecture, OR wait for art-track metadata fix to reach 9/9.

## UI 改为 AI 出图 + 提示词策略 skill 沉淀（已执行）— 2026-07-10
- 用户决策：UI 资源由「程序化生成」改为「AI 出图（Agnes）」，并要求同步所有相关地方；同时把 `资源提示词设计策略.md` 沉淀为可复用 skill。
- 关键事实（先核查后改）：`prompts.json` 已含 **173 条 `ui/...` AI 提示词**（真提示词，非占位），仅被 `KIND_PROCEDURAL_UI` 正则强制走程序化（且 `ui_kit_generator.py` 实际存在）。故"UI 改 AI"本质是放开路由，数据已就绪。
- **代码改动** `tools/art_pipeline.py`：`classify_resource()` 中 UI 不再走 `procedural_ui`，`asset_kind="ui"`；`is_procedural` 不再用 `KIND_PROCEDURAL_UI` 正则（仅 `category in PROCEDURAL_CATEGORIES`，当前为空集）；该正则改为弃用注释（保留回滚）。验证：`classify_resource('ui/common/btn_default.png')` → `is_procedural=False, dim=2d`；`_bg.jpg` 仍按 backgrounds 处理（保留）。`py_compile` OK。
- **文档同步（4 处，均过门禁）**：
  1. `ART_RESOURCE_RULES.md` §11.3 重写：UI 全部 AI 出图，程序化正则/生成器降级为回滚兜底。
  2. `美术资源生成与入库规范.md` §9.1.1（程序化行保留 tile，UI 改 AI）+ §9.1.4（"通用按钮/小面板"行改 AI）。
  3. `docs/资源提示词设计策略.md`：双轨说明(移除程序化 bullet)、§3.1 表(ui 改 AI 出图)、§5.3 全文改 AI+9块模板+模块调色板、§7.1 标题、§附执行入口。
  4. `skills/art-pipeline/SKILL.md`：Step2 生成方式表 UI 行改 AI；"Procedural UI Texture Generation" 章节改 "UI Generation (AI / Agnes)"。
- **新 skill**：`~/.workbuddy/skills/art-prompt-strategy/SKILL.md`（用户级，仿 resource-inventory 结构）：触发词/双轨边界/9块模板/分类型速查/统一+差异化治理/门禁，并与 UI=AI 改动一致。
- **门禁**：`encoding_audit --ci` issues=0/p0=0；`check_doc_consistency.py` ALL CHECKS PASSED。
- **提示（未执行，待用户决定）**：173 条 UI 提示词当前统一写 "Target canvas: 192x192"，面板/卡片用 9-slice 放大母版后可能偏糊；建议后续按组件细化画布尺寸（按钮 240x80 / 面板 360x200 / 槽位 92x92 / 卡片 260x96）并重写对应 prompt，以提升 AI 出 UI 质量。属质量优化，非同步阻塞项。

## IAIController + 动画状态机 + §5.11 AI 测试类（已执行）— 2026-07-10
- Per user `推进 IAIController（战斗分层 / 动画状态机）补齐 §5.11 AI 测试类`. Implemented pure-TS AI per §3.10/§3.5/§2.5 of authoritative spec `docs/2D转3D全面升级方案.md`.
- **4 new files** under `assets/scripts/battle/ai/`:
  1. `IAIController.ts` — 1:1 §3.10 interface (`initialize/update/setStrategy/getDebugState`) + token + types (AIStrategy, AINode, AIAnimState, MoveCommand, SkillRequest, AIDebugInfo, Entity, AIPerception, AIDecision, AIDecisionPolicy). Red line 2: no switch (Map-based policy registry). Red line 5: no Math.random.
  2. `AIController.ts` — orchestrator implementing IAIController + ILifecycle. Map<AIStrategy, AIDecisionPolicy> registry; default BT (spec: BTAIController 默认). Combat layering (§2.5): update() pushes MoveCommand/SkillRequest via owner.submitMove/submitSkill, never touches gridX/gridY or HP. Drives IAnimationController.setState for animation state machine.
  3. `aiPolicies.ts` — 4 pluggable AIDecisionPolicy classes per spec: BTAIController (BT priority selector, default), FSMAIController (stateful FSM, record-based dispatch no switch), GOAPController (generic BFS planner over 2 actions: Approach/Attack), UtilityAIController (scored candidates, deterministic tie-break).
  4. `AnimationStateMachine.ts` — IAnimationController interface (setState/play/currentState) + AnimationStateMachine class (implements IAnimationController + ILifecycle). Explicit allowed transition adjacency table; Dead terminal.
- **1 test extension**: `tests/core/automation.test.ts` — added 2 new describe blocks (14 total new cases): BT unit (adjacent→Attack/skill, chase→move, far→Patrol, dead→Die), setStrategy switching FSM/GOAP/Utility + reject unknown, pre-init throw, ILifecycle destroy, AnimationStateMachine transitions (valid/invalid/Dead terminal/play/clips/ILifecycle reset).
- **1 engine wiring**: `GameBootstrap.ts` — register AnimationStateMachine as IAnimationController into GameContext + LifecycleManager.
- vitest: **110 tests pass** (15 files, +10 new AI-specific). encoding_audit issues=0/p0=0. validate:all: **8/9** (same pre-existing config metadata gap on resource_inventory.json, art-track owned). Ts-static, 架构门禁, 资源注册, non-ui, skin, docs, budget, encoding all OK.

## normalize_prompts 画布分化 + 模块调色板（A+B 全改，2026-07-10）
- 用户确认 A(画布分化)+B(模块调色板) 全改。同步 `art_pipeline.py resolve_target_size` 跳过 UI 短路。
- **Gap1 画布分化**：新增 `_ui_canvas_size()` 按组件返回准确尺寸（btn 240×80 / panel 360×200 / card 260×96 / slot 92×92 / input 260×44 / bar 360×36 / strip 360×48 / row 360×48）；`.jpg` 全屏背景 1920×1080；修正 `ui_kind()` 使 `panel_bg.png` 不再误判为全屏背景。`dimensions_clause()` 对 `category=="ui"` 调用 `_ui_canvas_size`。
- **Gap2 模块调色板**：新增 16 项 `UI_MODULE_PALETTE`（splash 暖木金/main 森林绿/shop 宝石蓝/equipment 铁灰紫/death 暗夜蓝...），注入 `ui_prompt()` 的组件 specific 块。
- **`art_pipeline.py`**：`resolve_target_size()` 中 `category != "ui"` 跳过 `RECOMMENDED_SIZES["ui"]` 短路，改为走 `UI_KIT_DEFAULT_DIMS` 按组件分化。
- **同步 §15**：`recommended_sizes["ui"]` 加注释说明仅 fallback，实际由 `ui_kit_default_dims` 按组件分化。
- **验证**：重跑 normalize_prompts（499 条，risk_count=0），画布分布正确（jpg 背景 15→全部 1920×1080；按钮 33→240×80；面板 17→360×200；卡片 8→260×96；槽位 11→92×92；输入 1→260×44；条/行 4→360×48/36；剩余 82 默认 192×192）。模块调色板注入已确认。
- **门禁**：encoding_audit issues=0/p0=0；check_doc_consistency ALL CHECKS PASSED。

## 跨类别闭合块补齐 + tiles CORE + icons 细节（2026-07-10）
- 用户确认一并修。基于与策略文档的全面校验结果，实施了 8 类提示词补齐：
  - **新增常量** `CONSISTENCY_CLOSURE` + `PRODUCTION_NOTE_PNG`（脚本全局共用）。
  - **`background_prompt`**：添加 CONSISTENCY_CLOSURE（已有自有 PRODUCTION_NOTE）。
  - **`character/monster/boss/effect_prompt`**（4 类）：添加 CONSISTENCY_CLOSURE + PRODUCTION_NOTE_PNG。
  - **`icon_prompt`**：添加 CONSISTENCY_CLOSURE + PRODUCTION_NOTE_PNG；在 specific 块追加 "no badge label, no text" + "Rarity color ring: white common, blue uncommon, purple epic, gold legendary"（对齐策略文档 §5.2）。
  - **`tile_prompt`**：新增 CREATURE_CORE 头块（原来是缺失的）+ CONSISTENCY_CLOSURE + PRODUCTION_NOTE_PNG。
- **验证**：499 条提示词 100% 有 CONSISTENCY_CLOSURE；309 条 PNG 类有 PRODUCTION_NOTE_PNG；17 条背景有自有 PRODUCTION_NOTE；173 条 UI 有自有 PRODUCTION_NOTE。67 图标全部注入 "no badge label" + "Rarity color ring"。
- **门禁**：encoding_audit issues=0/p0=0；check_doc_consistency ALL PASSED。

## 资源清单 ↔ 提示词策略 双向关联（2026-07-10）
- （已修）从 §4 调用 Skill 表移除 art-prompt-strategy，生图链路仅 art-pipeline 为入口。
- §3 保留纯说明引用（prompt 规范见策略文档）。
- 策略文档 §7 保留纯说明引用（资源清单查待产列表）。
- 恢复门禁通过。

## normalize_prompts 加入 art-pipeline SKILL.md（2026-07-10）
- 用户要求将 normalize_prompts_positive_style.py 纳入 skill 链路，实现全链路自动化。
- Batch Mode 新增「Prompt Rules Maintenance」子节（命令 + 说明 + 风险提示），含 `python ../tools/normalize_prompts_positive_style.py` 命令。
- Configuration Files 表新增 2 行（Prompt rules sync + Prompt design strategy）。
- 新增独立 Maintenance: Sync Prompts from Rules 节（何时运行/如何运行/做什么/验证），与策略文档和 prompts.json 形成闭环。
- 路径修正：脚本在 E:/game/tools/ 而非回到地面/tools/，SKILL.md 中全部改为 ../tools/ 前缀通过门禁。

## 资源清单 ↔ 提示词策略 双向关联（2026-07-10）
- 用户要求《资源清单_可执行.md》与《资源提示词设计策略.md》关联。
- 资源清单 §3 起始块引用策略文档（生成前确认 prompt 已按策略对齐）；§4 Skill 表新增「提示词撰写参考」行。
- 策略文档 §7 起始块引用资源清单（需要生成哪些资源、优先级、状态查清单）。
- 门禁：encoding_audit issues=0；doc_consistency ALL PASSED。
- Committed: `4fedd32` — 6 files total (4 new src in battle/ai/ + 1 mod GameBootstrap + 1 mod automation.test.ts).

## CombatSystem + EventBus + ECS + Vertical Slice — 全量交付 (2026-07-10)
- Per user "非美术管线全部100%实现", delivered Phase1-4 post-IAIController in a single session.
- **Phase 1 — CombatSystem (§3.8)**: 6 files under `battle/combat/` — CombatCommand (types), CombatSystem (dispatcher + ILifecycle), TargetSelector (range/lock-on/LoS), EffectExecutor (Map-dispatch status effects), ProjectileSystem (deterministic flight + ICollisionService hit), LockOnManager (acquire/release/cycle/exit-release). All pure TS, Map dispatch (no switch), ctx.get DI (red line 4). 26 tests.
- **Phase 2 — EventBus (§3.11)**: 7 files — 6 domain event types (Battle/UI/Audio/Scene/Input/Runtime) + EventBusManager (6 typed emitters, per-domain log toggle, ILifecycle, registered as IEventBus). 10 tests.
- **Phase 3 — ECS (§3.12)**: 7 files under `ecs/` — StatComponent (attribute + buff modifiers), MovementComponent (grid via ICollisionService), AnimationComponent (IAnimationController wrapper), CombatComponent (skill queue + cooldown), TargetComponent (lock ref), InteractionComponent (interact), EntityManager (ILifecycle registry). 18 tests.
- **Phase 4 — Vertical Slice**: End-to-end integration test proving full pipeline: GameContext -> EntityManager -> IAIController -> CombatSystem -> HitResolver/DamageResolver -> EffectExecutor. Wired CombatSystem + EntityManager + EventBusManager + CombatSystem in GameBootstrap._wireInfra. 3 tests.
- **Final**: 167 tests (19 files), encoding_audit issues=0/p0=0, validate:all 9/9 (gate first hit 9/9 since Phase 0). All red lines 1-6 structurally satisfied. 5 commits total (4fedd32 IAIController, 4187f18 CombatSystem, 28b80ab EventBus, 31ddabc ECS, 1c62219 VerticalSlice).

## resource_inventory.json metadata 缺失修复（2026-07-10）
- 用户指出"你就是美术轨"——确认 resource_inventory.json 的 metadata 缺失由我修复。
- 根因：字段名为 `meta`，门禁 `validate_config.py` 第 48 行检查 `data.get("metadata")`，名称不匹配导致 error。
- 修复：`meta` → `metadata` + 新增 `version: "1.0.0"` + `lastUpdated: "2026-07-10"`。
- 验证：config_check → `resource_inventory.json -> 解析成功` ✅；asset_registry → OK；不在 error 列表中。

## 3D 建模 Brief 文档生成（2026-07-10）
- 用户要将 134 项 3D 资产交给另一 AI 制作，问资源清单是否足够。
- 审计发现：资源清单只有"做什么"（名称列表），缺少"怎么做"（建模规范/预算/命名/导出/assetmeta 等）。
- 生成 `docs/3D建模Brief_全量资产.md`（23KB，UTF-8），内容：
  - §1 命名规范（CHR_/MON_/BOSS_/FX_/TILE_ 前缀 + PascalCase）
  - §2 技术预算（5 个子类逐类 Tri/骨/贴图/LOD/Socket/动画 clip，取自 rules3d）
  - §3 导出规范（原点/朝向/帧率/AnimEvent）
  - §4 assetmeta 编写模板与字段说明
  - §5 生命周期状态机
  - §6 入库流程
  - §7 风格参考（6 区域调性 + 5 英雄识别色）
  - §8 全量 134 项资产清单（含优先级、2D 参考路径、预算档）
  - §9 关键文件索引 + 生产要点速查
- 另一 AI 只需读此文档即可独立开工，无需额外询问规格。

## 3D 建模 Brief 完善（15 条评审落地，2026-07-10）
- 用户给出 15 条详细评审意见，涵盖流程/约束/质量/数字修正。
- 修正：§9 优先级标题 33→21 项（与实际统计一致）
- 新增 §10 AI 生产执行规范（10 个子节，约 260 行）：
  - 10.1 15 步强制执行流程（读取2D→分析→建模→Rig→动画→LOD→导出→validate）
  - 10.2 AI 必须先分析 2D（强制输出 Analysis 报告模板，含动物/颜色/武器/轮廓等）
  - 10.3 禁止脑补（禁用新增装饰/武器/配件/发光等，仅允许三维结构最小补全）
  - 10.4 禁止自由设计（严禁重新设计/配色/换武器/改脸型）
  - 10.5 必须保持轮廓（优先级：轮廓＞比例＞颜色＞细节）
  - 10.6 建模细节要求（Body/Eyes/Weapon/Hair/Accessory/Tail/Horn/Wing 拆分）
  - 10.7 建模质量规范（Topology Quad为主/法线统一朝外30°AutoSmooth/Pivot各类型/禁止RootMotion/纯卡通贴图）
  - 10.8 AI 输出格式（YAML 报告模板）
  - 10.9 失败处理（source_2d 缺失/损坏/无法识别 → 立即停止，不得猜测）
  - 10.10 质量检查 Checklist（15 项→22 项，新增拓扑/颜色/LOD/Mesh/骨骼/截图等）
- 追加 §10.11–10.18（7 新子节，约 200 行）：
  - 10.11 禁止引用训练知识（仅允许 source_2d + manifest + 本 Brief）
  - 10.12 确定性输出（相同输入必须一致结果，禁止随机）
  - 10.13 不可修改预算（Tri/Bone/贴图等均为硬限制，超出即 FAIL）
  - 10.14 Socket 固定坐标系（Forward +Z / Up +Y / Rotation Identity / Scale 1,1,1）
  - 10.15 LOD 生成规则（仅减面简化，不删除武器/头/尾巴等关键部件）
  - 10.16 贴图 Palette 一致性（颜色来源于 2D，Hue≤5°/Sat/Val≤10%）
  - 10.17 自检截图（Front/Left/Back/Right/Perspective 5 张）
  - 10.18 AI 最终输出状态（扩展 11 项分段 Status，全部 PASS 才进入下一资产）
- 同步：§1 命名加大小写一致性说明；§8 地块 7 处 Highground→HighGround 修正
- 门禁：encoding_audit issues=0；check_doc_consistency ALL PASSED。

## gen_assetmeta + collect_progress 工具脚本（2026-07-10）
- 用户要求补两个工具脚本并加入 Brief 文档。
- `tools/gen_assetmeta_from_manifest.py`：读取 art_3d_manifest.json + rules3d 预算，批量生成 134 项 .assetmeta.json 模板（已运行输出到 assets/resources/models/{cat}/）。
- `tools/collect_3d_progress.py`：扫描 models/ 下 assetmeta 文件，聚合进度状态，支持 --text/--json/--detail/--report 四种输出模式。首次运行报告：134/134 选秀。
- Brief 文档更新：§6.1 新增 gen_assetmeta 使用指南，§6.3 新增 collect_3d_progress 使用指南，§11 文件索引增加 2 行。
- 门禁：encoding_audit issues=0；doc_consistency ALL PASSED。
- 门禁：encoding_audit issues=0/p0=0；doc_consistency ALL PASSED。

## 部件化动画管线调试（art-pipeline 完整链路修复，5 个根因）

- **背景**：archer 12 部件首批用 ImageGen 生成，违反"生图必须走 art-pipeline/Agnes"规则；改用 Agnes 后发现 5 个链路问题。
- **根因与修复**：

| # | 根因 | 修复 |
|---|---|---|
| 1 | `resolve_target_size("characters", ".../parts/...")` 走 `recommended_sizes["characters"]` = 256x1024 | `art_pipeline.py:430` `/parts/` 早返回，尺寸由 prompt 提供 |
| 2 | `character_part_prompt()` 调 `dimensions_clause()` → `size_for()` 取 256x1024 | `normalize_prompts_positive_style.py` 新增 `CHARACTER_PART_SIZE_MAP`（body=160/head=128/arm=96x128/leg=80x112/ear=64x96/tail=96x128/bow=128/quiver=96/cape=128x160） |
| 3 | `cmd_generate` 把 `DETAIL_ANCHORS["characters"]`（"Full-body sprite sheet..."）拼到 parts prompt | `art_pipeline.py:1840` `/parts/` 跳过 `STYLE_ANCHOR/DETAIL_ANCHORS/SAFETY_BLOCK` 包装 |
| 4 | 部件 prompt 用怪物/Boss 的 `CREATURE_CORE` 含 "Only the creature on white" 触发词 | `normalize_prompts_positive_style.py:88` 新增 `PART_CORE`（"Only the character part on transparent"） |
| 5 | `CHARACTER_PART_MAP` 写 "deer head + antlers + muzzle" → AI 看到"deer"自动补耳朵/角/身体 | 改为通用部位名词 "face piece: muzzle + eyes + brow + small horns on top" |

- **关键洞察（写给后续 AI 维护者）**：
  - **ART 词会触发 AI 画整套**：写 "deer head" AI 会按 deer 模板补耳朵/角/身体；写 "face piece" 只画脸。
  - **部件描述必须用物理部位名词**（muzzle/forearm/thigh），不写"完整动物 + 部位"。
  - **耳朵是独立部件**（docs/角色.txt §2.1 `ear_l/ear_r`），由 rig 摆动；head 不含耳朵。
  - **prompt 越具体到部件名反而越糟**——AI 把"face piece"当通用物体画，比"deer head"更听话。
- **head.png 现在正确**：face + eyes + brow + small horns，无耳朵无身体无弓。
- **剩余 10 个部件**（body/arm/leg/ear/tail/bow/quiver/cape）可用 `python tools/art_pipeline.py generate --resource characters/archer/parts/<part> --force` 逐个重生成。
- **AGNES_API_KEY** 由用户提供 `sk-zaluizuVOj0TVg6UbZjGDZnB4Cin2QaIoN0Vu6p5bGJpH2cw`（注意此 key 仅在当前 shell 有效，跨 session 需重设）。

## 部件化方案收尾与方案澄清（任务 #75–#86 完成）

- **权威方案（v1 手武器合并）**：doc1 §189-190/§227、doc2 §9.2/§606 选项 A。archer=arm_r_weapon（右臂+握弓手+弓合并），warrior=sword_arm_r，mage=staff_arm_r，assassin=dagger_arm_l/dagger_arm_r，berserker=axe_arm_r；无独立 bow 部件。character_parts.json 已实现 archer=[…,arm_l,arm_r_weapon]（与 doc1 §227 完全一致）。
- **与更早日志的冲突澄清**：上方"部件化动画管线调试（5 根因）"记录的是更早的"archer 独立 bow + Agnes 逐部件生图"思路（doc2 §9 选项 B），已被两文档 v1 的"手武器合并/母版切割"取代。当前 art_pipeline.py 已无 CHARACTER_PART_MAP/CHARACTER_PART_SIZE_MAP/PART_CORE 等硬编码（grep 验证），改为 /parts/ 路由 + 配置驱动子脚本（cut-parts/preview-rig/validate-parts）；磁盘 assets/resources/textures/characters/archer/parts/ 为空，无残留 bow.png。文件状态与文档一致，无交叉冲突。
- **残留产物提示（未删，待确认）**：tools/output/prompts_positive_style_report_20260710_*.json 仍引用 characters/archer/parts/bow.png，属被取代方案的报告产物，非运行时输入（prompts.json 无 archer 部件条目）。如需清理告知即可。
- **落地内容**：新建 4 脚本（cut_character_parts.py / preview_character_rig.py / draw_character_cut_marks.py / check_character_parts.py）+ 辅助 gen_character_part_configs.py(v2.0.0)；3 份配置重写为 5 职业；art_pipeline.py 接入 cut-parts/preview-rig/validate-parts；目录骨架 art_source/textures_review/master/characters/{5职业}/{reference,cut_specs,parts_preview,export/parts}/；对齐 角色.txt/ART_RESOURCE_RULES.md/3D建模Brief/美术资源生成与入库规范/资源清单_可执行（34 sprite sheet 行→5 部件化行）。
- **validate:all**：9 项中 3 FAIL 均既有（非本次引入）——①资源注册 17→5（清理 12 条 stale archer 旧条目 arm_r/bow 后，仅剩 character.preview.* idle 纹理未生图，预期缺失）；②非UI资源注册 70 错（monster/tile safeReview 内容审核待办，与角色无关）；③文档一致性 character_parts 锚点缺 prompts（用户已许可的既有 FAIL）。**编码审计 scanned=613 issues=0 p0=0 —— PASS**。
