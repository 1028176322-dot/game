# 2026-07-01 工作内容

## 设计任务
- 为项目「回到地面」生成肉鸽地牢微信小游戏的全系统流程 XMIND 图
- 已生成文件：`回到地面/docs/生成XMIND图.html`
- 已同步创建 Ardot 设计稿：`回到地面_全系统流程` (fileId: 699135863189225)

## 系统架构覆盖
1. 启动与入口：启动屏、微信授权登录、资源加载/热更
2. 地牢核心：种子系统(初始化/运行RNG)、6×6随机地图(DAG分层/10种地形模板/6×6格战场/模板选择/可达性验证)、8种房间类型、房间变异系统(规则/增益减益)、关卡内容设计(怪物/奖励/BOSS/事件)、难度曲线
3. 战斗系统：实时自动攻击、怪物AI、6元素反应
4. Rogue构筑：技能体系、8槽装备、道具&背包
5. 角色养成：5个角色职业、等级&天赋树
6. 经济·商业：金币·魂石、IAA广告变现
7. 数据·配置：Analytics、JSON配置表
8. 社交：微信排行、好友&分享

## 场景编辑器搭建手册
- 创建了 `docs/场景编辑器搭建手册.md`，覆盖 splash/main/dungeon 三个场景的完整规范
- 包含：完整场景树（节点层次 + 组件 + @property 绑定表）、详细编辑器拖拽操作步骤（正式上线路径）、布局参考坐标、流程链路附录、常见问题排查
- splash.scene: SplashUI + GameBootstrap
- main.scene: 7面板 + MainHubUI + MainSceneController + Shop 的完整树
- dungeon.scene: 修复了 DungeonSceneController 嵌套问题，完整16个 @property 绑定表
- 发现 dungeon.scene 存在 DungeonSceneController 双节点嵌套（id=8 无组件，id=9 有组件）的结构错误

## v2.0 手册重写（根据用户反馈的 9 个问题）
- 修正 SkipButton：Label 改做子节点而非直接加在 SkipButton 上（避免 Cocos 3.x Can't add renderable component）
- 分两档策略：阶段 1 最低可运行（fallback StartButton），阶段 2 正式 MainHub+面板
- PanelRoot 模式：面板主节点保持 active=true，PanelRoot 子节点隐藏并绑定 panelRoot
- AreaSelectPanel 完整结构：补全 RouteContainer/LockedContainer/PlayerInfo/StartBtn/BackBtn 五要素
- Shop 路由修复：MainHubUI 改 eventBus.emit + MainSceneController 接管 shopUI.show()
- Dungeon 必绑分级：4 个必绑 + 2 个推荐 + 其余由 Installer 兜底
- 新增验收命令和预览验收流程
- 已同步修复 MainHubUI.ts（shop按钮改 eventBus）和 MainSceneController.ts（_onOpenShop 改 shopUI.show()）

## v2.1-v2.2 精修（根据用户二轮反馈）
- 阶段1强制创建AreaSelectPanel（6个必需子节点：PanelRoot+PlayerInfo+RouteContainer+LockedContainer+StartBtn+BackBtn）
- AreaSelectPanel子节点全部加UITransform说明（避免点击/布局问题）
- 删除不存在的check:encoding/check:arch npm scripts，5道门禁说明
- build改为Cocos构建说明（非TS编译）；build:wechat生命周期说明
- MainUI描述改保守措辞（对代码状态更准确）
