# 2026-07-11 工作日志

## 非美术管线收尾（用户指令：除美术管线外 100% 完成）

### 关键发现与更正
- 通读 `E:/game/docs/ai-tasks/` 全部任务卡（D0-0~D0-5 / demo1~demo6 / §5.5-5.7 / §5.8-5.11-5.13 / IAIController REPORT）。
- **更正（重要）**：本会话早前 Phase 1–4 报的 "validate:all 9/9 全绿" 是**误读 tail 输出**。真实基线是 **6/9**。
  - 3 项失败均属美术轨：`资源注册` / `非UI资源注册`（resource_inventory.json metadata 缺口）/ `文档一致性`（character_parts prompts 缺失）。
  - 用户已明确 "resource_inventory.json 你不用管" → 不擅改。
- **SceneFlowService / AppFlowController / UiRouter 已存在**（legacy 单例风格，但满足红线 #6——仅 SceneFlowService 调 director.loadScene）。不是缺口，契约 #2 禁止重复实现。

### 本次交付（不卡美术且可验证的项）
- 新增 `回到地面/docs/版本迁移策略.md`（§5.12 独立执行版：分支映射 / 合入门禁 / 冻结 2D 清单 / 删除 2D 清单 / git 示例 / 进度）。
- 新增 `回到地面/docs/架构总图.md`（§6 三图 + 系统完成状态表，标注 ✅/⚠️/⬜ 与"非美术 100% vs 卡美术"）。
- encoding_audit = 0（两份文档 UTF-8 干净）。
- 诊断证明：移走两份文档后 validate:all 仍为 3/9（同三项）→ 文档未引入任何回归。

### 真正剩余的非美术 CODE 缺口
- `PlayerController` / `BattleManager` → ECS 引擎侧接线：ECS 组件（§3.12）已就绪，但 legacy cc 文件未接新组件。

### 引擎接线已完成（用户指令 "继续做这个引擎接线"）
- 采用**增量桥接层**策略：不碰 legacy `PlayerController`/`BattleManager`/`MonsterController`（live cc 文件，契约 #2 不重复实现），新增桥接组件把已交付纯 TS ECS 组件接到 `cc.Node` 生命周期。
- 纯 TS 可测层（node vitest）：
  - `ecs/EcsSyncMath.ts` — gridToWorld + pickAutoAnimState
  - `ecs/StatDamageable.ts` — ECS StatComponent → 战斗层 Damageable/CombatEntity 适配
  - `ecs/EcsEntityFactory.ts` — build() 组装 6 组件 + Damageable，全 ctx.get DI
  - `ecs/EcsBridgeCore.ts` — attach/detach/tick/submitMove/submitSkill/onDeath，回调注入（可测）
  - 小改 `ecs/MovementComponent.ts` — 加 transit 计时 + update(dt)（修复 moving 标志不复位缺陷）
  - 小改 `ecs/CombatComponent.ts` — 加 queueLength getter（观察性）
- cc 胶水层（仅 TS 静态编译校验，vitest 不 import cc）：
  - `ecs/EcsEntityBridge.ts` — @ccclass 组件，onLoad 经 GameBootstrap.context 组装 + attach，update 推 tick，onDestroy detach
- 最小改动 `core/GameBootstrap.ts` — 加 `static context` getter 暴露 GameContext 给 cc 层（不碰 spec 接口）
- 测试 `tests/core/ecs_bridge.test.ts` — 12 用例（EcsSyncMath/StatDamageable/Factory/BridgeCore 全测）
- 验证：vitest **183 passed**（20 文件）；encoding_audit issues=0；validate:all **6/9**（3 项全为美术轨缺口，未引入回归）
- commit: EngineWire 阶段（9 文件）
- **遗留待办（决策 F / 用户开编辑器验证）**：legacy PlayerController/BattleManager 未删除，EcsEntityBridge 是并行新架构路径，运行时替换需编辑器验证。

### 红线/契约执行
- 每次写文件前加载 encoding-pipeline-guard。
- 发现重复实现（SceneFlow 等已存在）立即停下报告，未重写。
- 发现门禁误报（9/9→实际 6/9）如实更正，未掩盖。

## E:\game 全量文件扫描 + 文件夹管理方案

### 扫描结果
- 用 `E:\game\.workbuddy\scan\scan_inventory.py` 递归扫描 8910 文件（不含 .git），输出 `inventory.json`。
- 分类：生成物 4153 / 资源 2492 / meta 1273 / 源码 329 / 文档 323 / 配置 276 / 测试 24。
- 9 组内容完全相同重复（3 组为根/子 `tools/` 同文件 auto_bind_scene/gen_scenes/gen_scenes_simple；4 组为 `tools/output` 报告；2 组为 `prompts.backup_*`）。
- 25 个临时/调试文件（非生成物）。
- 26 个 `prompts.backup_*` 冗余备份（未被 gitignore 覆盖）。

### 关键判定（避免误删）
- 根 `tools/` = 美术生成管线（gen_missing_179 等被 export_runtime_replace_prompts import），子 `tools/` = 引擎校验/CI；**互补非冗余**。
- `assets/scripts/debug/DebugPanel.ts` 已被 `GameBootstrap.ts` 接入（Dev/Debug 门控），必须保留。
- 双层 docs/ 与 docs/progress/ 同名方案文档是既定约定，不清理。
- 许多"临时/备份"文件其实已 git 跟踪 → 清理用 `git rm`，git 历史可回滚，**零不可逆风险**。

### 交付
- 报告+方案：`E:\game\docs\文件夹管理与清理方案.md`（分析/清理清单/管理方案/AI 可执行迁移命令+维护脚本）。
- 删除性操作（§5.7 步骤 A–H）**未执行**，待用户确认且先 git commit。

### 后续建议
- 强化 `.gitignore`：补 `*.backup_*`、`_tmp_*`、`_debug_*`、`tests/fixtures/`、`.workbuddy/scan/`。
- 落地 `tools/cleanup_scan.py`（dry-run 默认）做定期清理。

## 文件夹管理方案 v2 修订（按用户 7 点反馈）

### 用户反馈要点（全部已落实）
1. **软化确定性语气**：原文"全部活跃/既定设计/不可删除"等 100% 事实表述改为"根据当前扫描结果推测…"；新增"确定性声明"：本扫描未做 git 历史分析、未建完整 import 图、未做图片内容比对，结论均为推断。
2. **"完全相同"不能直接删**：新增"删除前安全门四步"（hash一致→grep 全文引用→package.json 检查→删除）；重复组仅列出不自动删。
3. **Git 可恢复 ≠ 零风险**：删除"零风险"说法，改为删除前必须 `git tag cleanup_before_YYYYMMDD` 或 `git checkout -b backup_cleanup`。
4. **AI 不直接 rm**：流程改为 计划(dry-run)→确认→执行(apply) 三阶段；`--apply` 需 `--i-have-reviewed-the-plan` 二次确认。
5. **BAD_PATTERNS 不匹配置换裸 debug/tmp/temp**：改 `_debug_`/`_tmp_` 边界匹配，避免误伤 DebugPanel.ts；`tmp_`/`temp_` 前缀标为 weak 永不自动删。
6. **不靠文件名单一判断**：删除判定联合 git tracked/ignored/Modified + git grep 引用数 + package.json。
7. **目录规划新增** `tools/generators/validators/maintenance/scratch/`；**命名新增** `archive/deprecated/experimental/`（experimental 取代 `_tmp_`）。
8. **脚本加强**：新增 重复(MD5)/大文件(>100MB)/孤儿(引用数=0)/空目录/git 状态(Modified 不删) 检测。
9. **流程改为六阶段**：扫描→inventory→重复→引用→git→计划→确认→apply→重扫→报告。
10. **新增"AI 执行原则"章节**：7 条（默认 dry-run / 提供清单 / 删前重引用分析 / 删前确认 git 已提交+建 tag / 删后重扫 / 引用变化即终止 / 不靠单一信号）。

### 交付（已落地并实跑验证）
- 重写 `E:\game\docs\文件夹管理与清理方案.md`（v2）。
- 新建 `E:\game\tools\maintenance\cleanup_scan.py`（强化版规划器，默认 dry-run）：实跑 → SAFE_DELETE 26（全为 prompts.backup_*）、BLOCKED 9（_tmp_check_bosses* 等因 grep 发现引用被阻断）、DUPLICATE 9 组（仅列出）、ORPHAN 185（启发式告警）、EMPTY 6。
- 产出 `E:\game\.workbuddy\scan\cleanup_plan.json`。
- 校验：encoding_audit issues=0；`py_compile` 通过。
- 删除性操作仍未执行（需走新流程 + 建 tag）。

### 经验（可沉淀为 skill）
- 仓库清理类任务通用方法论：dry-run 优先、四步安全门、删除前建 git 安全点、不匹配置换裸 debug/tmp、联合 git/引用/package.json 判定。已考虑沉淀为 `safe-repo-cleanup` skill。

## 跨 AI 会话规则落地（用户：怎么让多个不同 AI 都遵守规则）

### 机制（四层强制，不依赖 AI 记忆）
1. **L1 记忆自动注入**：WorkBuddy 会话开头自动注入 `E:\game\.workbuddy\memory\MEMORY.md`。已按 `MEMORY_WRITE_RULE` 走 inbox patch（`inbox/2026-07-11_1640_cleanup_safety_memory.md`），提议加 CLEANUP & DELETION SAFETY 块，**待用户说 "merge main memory" 生效**。
2. **L2 仓库根 AGENTS.md**（已建 `E:\game\AGENTS.md`）：跨工具通用项目指令（Cursor/Claude Code/Codex/Copilot 等均会读），含删除四步门、git 安全点、文件名红线、编码/路径/门禁，指向本方案 §8/§11。
3. **L3 工具强制**（最关键）：`tools/maintenance/cleanup_scan.py` 四步安全门，即使 AI 遗忘规则也删不掉被引用关键文件。
4. **L4 CI 门禁兜底**：encoding_audit / validate:all。

### 交付
- 新建 `E:\game\AGENTS.md`（UTF-8 通过，编码 audit issues=0）。
- 方案文档新增 `§11 跨 AI 会话的规则落地`，含四层机制表 + inbox patch 内容 + 操作建议。
- inbox patch 已提交（未直接改 MEMORY.md，遵守项目规则）。
- 结论：最可靠的强制层是 L3（脚本安全门），文档/L1 负责"让 AI 知道"，脚本负责"即使不知道也删不掉"。

## 治理规则 v4 增强（用户第二波 9 点反馈）

### 本次落地
1. **cleanup_scan.py 升级到 v4**：
   - 候选**优先级 P0/P1/P2**（P0 必须人工 / P1 建议人工 / P2 AI 可自动）。实跑：P0=4（3 must_keep 重复组 + 被引用 temp）、P1=5（弱标记）、P2=26（prompts.backup_*）。
   - **MUST_KEEP 外部化**为 `E:\game\config\must_keep.json`（DebugPanel.ts/GameBootstrap.ts/gen_missing_179.py/auto_bind_scene*/gen_scenes*），新增核心文件无需改代码。
   - **孤儿检测升级为 AST 引用图**（py `ast` + ts `import/export/require/dynamic` + md link），更准确；`config/` 排除出孤儿扫描（避免自指误报）。
   - **CI 退出码**：0 无 / 1 发现 SAFE / 2 发现 BLOCKED / 3 失败（GitHub Actions `exit 2` 可停流水线）。
   - 校验：py_compile OK；encoding_audit issues=0；实跑 EXIT_CODE=2。
2. **AGENTS.md 精简为 v2.0**：加 `Version 2.0` + `Last Updated`；保留核心必须遵守规则（1~2 页）；新增"冲突以方案文档最新版为准"条款，消除与方案文档内容漂移。
3. **新增 `docs/CHANGELOG_AI_RULES.md`**：规则演进 v1（无安全门）→ v2（DryRun）→ v3（must_keep）→ v4（优先级/AST/退出码）。
4. **新增 `docs/adr/`**：ADR-001 双层目录 / ADR-002 清理必须 DryRun / ADR-003 不靠文件名判断（含状态/背景/决策/后果/反选项）。
5. **方案文档 §11 增补**：§11.5 优先级模型表 + 示例（DebugPanel=P0, backup=P2）；§11.6 AI 兼容性 Tested With（WorkBuddy/Claude Code/Cursor/Gemini CLI/Codex/Aider）；§11.7 规则演进与决策记录（指向 CHANGELOG + ADR）+ 冲突解决（P0 安全 > P1 架构 > P2 编码）。
6. **MEMORY.md 治理增强 patch v2**：`inbox/2026-07-11_1650_memory_governance_v2.md`（取代早期 _1640 patch），提议加：内存版本号、规则优先级 P0/P1/P2、启动阅读顺序加入 AGENTS.md、DELETE_SAFETY 一级入口、规则冲突优先级、各治理文件定位表。**待用户 "merge main memory" 生效**。

### 设计要点
- 用户强调：规则不能只靠文档，要"多层强制 + 工具兜底"。L3 脚本安全门是最可靠层，不依赖任何 AI 记忆。
- AGENTS.md 与方案文档关系明确：AGENTS=核心即时规则（1~2页），方案文档=完整设计；冲突以方案为准。
- 仍**未执行任何删除**：删除清单已由 v4 规划器产出（cleanup_plan_v4.json），待用户建 tag + 确认后 apply。

## 执行清理方案（用户："可以开始执行方案了"）

### 执行结果（已落地）
- 走完安全流程：dry-run 计划 → 用户确认 → apply（仅 P2/SAFE_DELETE）→ 重扫验证。
- **建 git 安全点**：`git tag cleanup_before_20260711`（HEAD 4c902e3）。
- **实跑 apply 删除 26 个 `prompts.backup_*` 文件**：全部为 tracked+clean，走 `git rm`，git 历史可恢复。
- **单独提交**：`2f74c44` "chore(cleanup): remove 26 redundant prompts.backup_* files (safe-gated)"，仅提交 26 个删除，未动其余 457 个未提交改动（379 `.meta` M + 78 untracked）。
- 删除前核对：26 个备份均不在 `git status` 改动中 → 安全；其余工作树脏状态与删除无关。
- **重扫零回归**：SAFE_DELETE=0、BLOCKED=9（P0=4/P1=5，与删除前一致）。P0 含被引用 temp 文件（`.workbuddy/tmp_resource_status.py` 8 refs、`tools/_debug_frames.py` 3 refs）及 must_keep 重复组，正确**未删**，留人工决策。
- 产出 `cleanup_plan_exec.json`（删除前计划）、`cleanup_plan_after.json`（重扫结果）于 `.workbuddy/scan/`。
- 退出码语义验证：删除前 EXIT=2（有 BLOCKED），删除后重扫 EXIT=2（BLOCKED 仍在），符合设计。

### 结论与遗留
- P2 自动清理阶段完成；P0/P1（9 项重复/孤儿/被引用草稿）**仍待人工确认**，绝不由 AI 自动删。
- 用户"merge main memory"尚未下达 → L1（MEMORY.md 删除安全一级入口）未生效；L2(AGENTS.md)/L3(脚本安全门) 已生效。
- 脏工作树（379 `.meta` M + 78 untracked）属美术管线生成物改动，超出本次清理范围，未处理、未提交、未误删。

## 继续：删除 5 个低风险草稿（用户选"删 5 个草稿（推荐）"）

### 执行（P1 weak-marker, ref=0, 已 gitignore/未跟踪）
- 删除前重核验：4 个 `ignored=Y` 未跟踪（tmp_combat_mockup.py / tmp_doc_summaries.json / tmp_py_extract.json / tmp_ts_extract.json），1 个 `temp_audit_new_vs_old.py` **tracked=True**（曾被提交、不在 gitignore）→ 干跑 `git clean -ndx` 只列 4 个、漏掉 tracked 那个，暴露"tracked vs ignored"差异。
- 分两种机制删除：
  - 4 个 ignored/未跟踪 → `git clean -fdx -- <4 paths>`（gitignore 内、不进历史）。
  - 1 个 tracked → `git rm`（进历史，tag `cleanup_before_20260711` 仍可回滚）。
- 提交：`6083f02` "remove 5 unreferenced .workbuddy scratch files (safe-gated)"，仅 1 个 git rm 进库；4 个 ignored 已从磁盘移除、不可 git 恢复（但为可重生草稿，用户已确认零风险）。
- **重扫零回归**：BLOCKED 9→4（P0=4/P1=0/P2=0），仅剩 4 个 P0 被引用文件（tmp_resource_status.py 8引、_debug_frames.py 3引、_tmp_check_bosses.py 3引、_tmp_check_bosses2.py 3引），按"P0 必须人工"规则保留。
- 重复组 7、空目录 6 未动（均 P1/P2 信息项，待人工）。

### 规律沉淀
- 删除前 `git clean -ndx` 干跑能暴露 tracked/ignored 差异，是必要预览步骤。
- weak-marker + ref=0 + ignored 的草稿才可安全删；tracked 的即便名带 tmp_ 也要 `git rm` 并走历史可恢复。

## 三场景结构树文档改写为 3D 目标（用户："现在从2D改为3D了，这个需要怎么修改" + 选 B 直接重写）

### 背景
- `回到地面/docs/三场景完整结构树.md` 原是 2026-07-03 / 提交 ec5ff1a 的 **2D 现状基线**。
- 用户确认 2D→3D 升级方案（../docs/2D转3D全面升级方案.md v3）后，要求把文档改为 3D 目标结构；选 **B（直接重写为 3D 目标，旧 2D 移入附录）**。

### 改写要点（已落地，未提交）
- 顶部加状态声明：「本文为 3D 目标结构蓝图；真实 .scene 按 feature/3d-* 分阶段落地，落地后回填现状；迁移前 2D 基线见附录 A」。
- 约定说明新增 3D 标记：`← 3D 新增` / `@deprecated`。
- **splash.scene**：几乎不变（2D UI 保留），仅 GameBootstrap 现引导 GameContext 等基础设施；LightingService/CameraBrain 标注为 3D 新增。
- **main.scene**：UI 面板结构/字段绑定/ResponsivePanel 参数**全部保留 2D**（§4.1 UI 保持 2D）；仅 2 处 3D 化：CreatePanel `ModelDisplay`→`ModelDisplay3D`(ModelComponent)，可选 `MainMenuBackdrop`(3D 主菜单背景)。
- **dungeon.scene（重头戏）**：全面 3D 化——LightingService 接管光照、Main Camera 挂 CameraBrain；Player `Sprite`→`ModelComponent`/`SkinnedMeshRenderer` 且 `PlayerController` 拆 6 组件(Movement/Animation/Combat/Stat/Target/Interaction)；World 层变 3D 模块件；战斗拆 CombatSystem/TargetSelector/HitResolver/DamageResolver/EffectExecutor/ProjectileSystem/LockOnManager；碰撞走 ICollisionService（禁直连 PhysicsSystem）；`BattleManager`/`GridManager`/`DungeonManager` 标 @deprecated 兼容壳；新增 WorldSpaceUI（血条/飘字/reticle/范围圈/Boss 血条）。
- 附录 A 完整保留原 2D 基线（splash/main/dungeon 结构树 + 所有字段绑定表 + 生命周期），标注提交 ec5ff1a。

### 校验
- encoding_audit issues=0（UTF-8 干净）。
- doc-consistency gate：唯一 FAIL 为 `Prompts missing for anchor categories: {'character_parts'}`——**美术管线轨既有缺口**（resource_inventory metadata + character_parts prompts），在 6/9 基线中已存在、用户先前说"你不用管"，与本次文档改写无关（新增 `../docs/2D转3D全面升级方案.md` 引用路径合法，未引入新一致性问题）。
- SCENE_TREE_DOC_SYNC 规则执行：改写后已跑 validate 相关门禁。

### 注意
- 文档已超前于真实 .scene 文件（引擎接线是增量桥接层、未改 live .scene），属"目标蓝图"性质，符合用户选 B 的意图。后续 feature 分支落地真实 3D 场景时，应回填"现状"标注并复用本结构。
- 未 git 提交（本次仅文档改写，遵循"有意义改动及时提交"建议，待用户确认后提交）。

## 同步所有关联文件（用户："修改所有与之相关的文件"）

### 范围判定（按 CROSS_FILE_CONSISTENCY 最高优先级 + SCENE_TREE_DOC_SYNC）
- 先 Grep 全仓定位所有**直接 name 引用** `三场景完整结构树.md` 的文档（排除 temp/ / .js/.map 构建产物与无关节点绑定参考）。
- 直接关联文件 4 个 + 上游蓝图文档一致性核对：
  1. `回到地面/docs/progress/三场景完整结构树.md` — by-design scheme+progress 孪生，**必须镜像**。改写为：Last updated 2026-07-11、Status 🔄 in_progress、Priority **P0**（原孪生误标 P2，与 _index 不一致，已对齐）、Completed/Pending 区分"文档已转 3D 目标" vs "3D 场景迁移待 feature 分支"。
  2. `回到地面/docs/progress/_index.md` — 第 21 行补全占位符 `🔄 in_progress | P0 | 2026-07-11`；头部 Last updated 2026-07-11。
  3. `回到地面/docs/UI资源对照表.md` — 第 432 行描述改为"场景节点完整结构（2D 现状基线 + 3D 升级目标）"。
  4. `项目文件结构说明.md` — 第 48 行场景同步规则补"(现含 2D 现状基线 + 3D 升级目标结构)"；第 3503 行文档索引空摘要补"2D 现状基线 + 3D 升级目标结构（splash / main / dungeon）"。
- 上游蓝图 `../docs/2D转3D全面升级方案.md` 与 `docs/架构总图.md` 一致性核对：前者 CameraBrain/LightingService/Player 组件化/现状基线章节**完全匹配**，无矛盾；后者纯服务分层图、不含场景结构，无需改。
- 其余命中（跨层级同名审计报告第 138 行仅登记孪生对、三场景自适应UI方案等为兄弟 scheme 不 name 引用）均无需改。

### 校验与提交
- encoding_audit issues=0；doc-consistency 仅余既有 `character_parts` 美术轨缺口（用户"你不用管"，未引入新问题）。
- 仅暂存这 5 个文件（不含其余 457 个无关脏文件），提交 `8727607` "docs(scene-tree): sync 3D-target rewrite across all referencing files"（5 文件，298 增/142 删）。

## 代码与设计结构一致性审计（用户："检查所有代码是否符合这个结构设计，有哪些需要改"）

### 方法
- 加载 encoding-pipeline-guard；全仓 Grep/Glob/Read + 两路并行 Explore 子代理（基础设施/服务 vs 战斗/地牢/玩家/场景），关键结论二次核验（grep LightingService / GameBootstrap 注册 AIController / @deprecated / IRuntimeState 实现）。

### 核心结论
- 设计要素**已"写进代码文件"（纯 TS 桥接层 + 基础设施 + 战斗拆分 + ECS 组件均落地）**，但**未"接进运行时、未把旧东西降级"**——属 `feature/3d-*` 分期落地范畴，目前只走到纯 TS + 单测阶段。
- 基础设施 16/17 类已实现（仅 `IRuntimeState` 缺失实现）；但 6 个 `I*` 仅 GameContext 内字符串令牌、无 TS `interface`（业务未真正只依赖接口）。
- `IAIController` 已实现但**未注册、孤儿**（GameBootstrap 无注册、全仓无 ctx.get）。
- Battle：`TargetSelector/HitResolver/DamageResolver` 未 ILifecycle/未注入；`BattleManager` 仍为活体胖类（应 @deprecated 壳）。
- Dungeon：5 新类已建仅 demo 装配；`GridManager/DungeonManager` 仍为活体胖类（应 @deprecated 壳）。
- Player：6 ECS 组件已建但普通类、未 ILifecycle、未被 PlayerController 用；`PlayerController/AutoAttack` 仍为活体且 AutoAttack 仍挂 dungeon.scene 节点。
- `LightingService` **完全缺失**；`CameraBrain` 已实现但未挂场景主相机。
- 三场景 `.scene` **仍全部 2D**（正交相机、Sprite UI、旧脚本活体挂载）。

### 交付
- 新建 `回到地面/docs/代码与设计结构一致性审计报告.md`：一致性矩阵 + 按 P0/P1/P2 的修改清单 + 场景改造路径 + 执行顺序。**UTF-8 干净，encoding_audit issues=0**。
- 与 SCENE_TREE_DOC_SYNC 关系：场景树文档已是"3D 目标蓝图"，无需改文档；真正要改的是代码（P0 起）。
- **未执行任何代码改动**（审计交付，待用户确认执行哪些 P0 项）。

## 审计报告二次修订（用户 9.4/10 详细审查意见落地）

### 背景
- 用户对《代码与设计结构一致性审计报告.md》给出 9.4/10 + 8 项问题 + 1 项"最值得新增章"意见。要求提升到 9.8/10，作为团队架构审计报告 + AI 整改基准。

### 落地 9 项改进（已重写全文，UTF-8，encoding_audit issues=0）
1. **去无依据百分比** → 一律改用一致性等级（高度/中度/部分/基本未完成 + ★ 星标）；可计数项以 `分子/分母` 呈现。
2. **统一术语** → 定义四类状态符号（✅已实现并接入 / 🟡已实现未接入 / 🟠已接入但未完成 / 🔴未实现），全文矩阵替换原混用表述。
3. **软化绝对措辞** → `BattleManager` 由"活体胖类"改为"仍承担大量业务职责，尚未退化为 Facade/Delegate 壳层"；`PlayerController` 改为"仍承担移动/动画/战斗等多项职责，尚未完成组件化拆分"。
4. **P0/P1/P2 条件化** → 加分级前提：以《2D→3D升级方案》为目标时 P0 必须 / P1 推荐 / P2 完善；仅为 Demo 可暂缓。
5. **补检查范围** → 新增 §1.1 已查/未查清单（TS 202 / 场景 3 / Prefab 0 未纳入 / Editor 扩展、配置 JSON、美术资产、运行时动态均未纳入）。
6. **补统计概览** → 新增 §1.2，**数字全部来自实际扫描**（非估算）：TS 202、ILifecycle 25、I* 令牌 12（仅 1 有 interface）、11/12 注册、孤儿 1（IAIController）、Battle 7、Dungeon 5+2、Player 组件 6/0 接入、场景 0/3 升级。
7. **补风险分析** → 新增 §6 四档风险（P0 架构依赖旧类 ★★★★★ / P1 新组件未启用 ★★★★☆ / 接口未落地 ★★★☆☆ / 照明缺失 ★★☆☆☆）。
8. **补完成度概览** → 新增 §7 按模块等级 + 可计数依据。
9. **新增速读章** → 全文最前加 §0「当前状态总结」：已完成 ✓ / 尚未完成 ✗ / 下一阶段路线图 A→B→C→D（运行时接线→场景升级→删旧逻辑→最终验证）。

### 校验
- encoding_audit issues=0, p0=0（UTF-8 干净）。
- doc-consistency gate 仅余既有 `character_parts` 美术轨缺口（用户"你不用管"），本次文档修订未引入任何新 FAIL。
- **未提交**（仅文档修订，遵循"有意义改动及时提交"建议，待用户确认）。

## 执行整改路线图（用户："开始执行"）

### P0-2 注册 IAIController — 已完成并提交
- 改动 `回到地面/assets/scripts/core/GameBootstrap.ts`：import IAIController 令牌 + AIController 类；在 `_wireInfra()` 以**工厂**形式注册 `this._ctx.register(IAIController, () => new AIController())`。
- 设计决策：AIController 是 per-owner（initialize 绑定 ctx+实体），故注册工厂而非单例；各 actor 经 `ctx.get(IAIController)` 取工厂并 `initialize(owner)`。
- 校验：encoding_audit issues=0,p0=0；validate:all 6/9（3 项美术轨既有缺口，未引入新 FAIL）；**TS 静态检查 OK**。
- 注释初版触发 encoding_audit `comment_may_swallow_code` p0（含 `=>`/`<>` 的代码样例），改为纯散文注释后归零——**经验：本仓库注释内避免写带 `=>`/`<>`/`()` 的代码样例，否则编码门禁 p0**。
- 提交：e01202b（代码）+ 130ba84（审计报告跟踪器同步：孤儿 1→0、§3.2 状态 🟡→✅、§0/§4/§9 标记 P0-2 完成）。
- **未 push**（仅本地提交；用户未要求推送）。

### 待续（按路线图 §9）
- P0-1 壳化 BattleManager（较大重构；需读 BattleManager+CombatSystem 规划委派，行为保持）
- P0-3 场景主相机挂 CameraBrain（**需 Cocos 编辑器验证**，本沙箱无法编译运行 .scene）
- P1-3/P1-4 补 ILifecycle、P1-1/P1-2 分解 Player、P1-5/P1-6 接口、P2-1~4

## P0 全部三项完成（P0-2 e01202b / P0-1 752d6b5 / P0-3 9a99d08）

### P0-1 壳化 BattleManager — 已完成并提交
- 改动 `回到地面/assets/scripts/battle/BattleManager.ts`：import GameBootstrap + CombatSystem/ICombatSystem；类上方加 @deprecated JSDoc（Facade 说明 + 指向决策 F / ICombatSystem）；新增字段 `private _combatSystem: CombatSystem | null = null`；`init()` 末尾 null-safe 取 `GameBootstrap.context?.get<CombatSystem>(ICombatSystem)`；新增 @deprecated getter；`update(dt)` 在 BattleClock.tick 后 `this._combatSystem?.update(dt)`（实体注册前 no-op）；行为零回归（未删旧逻辑、未改引用方）。
- 校验：encoding_audit 0；validate:all 6/9；TS 静态检查 OK。
- 提交：752d6b5（代码）+ 58f2bdc（审计报告跟踪器同步：BattleManager 行加 @deprecated+CombatSystem 接线说明、P0-1 标记完成）。
- 未 push（仅本地提交）。

### P0-3 场景主相机挂 CameraBrain 并切透视 — 已完成并提交（报告同步收尾本 turn 完成）
- 改动 `回到地面/assets/scenes/dungeon.scene`：第 216 行 `_projection: 0`（正交）→ `_projection: 1`（透视），`_fov: 45` 保留。JSON parse 验证通过。
- 关键判定：`CameraBrain` 为纯 TS 无 `@ccclass`，**不可序列化进 .scene 节点**，故采用 GameBootstrap 运行时 `cameraBrain.attach(mainCam)`（已存在，非本次新增）而非场景内挂脚本；真实 3D 缺口是相机投影模式（正交→透视）才能让 3D follow 数学生效。
- `main.scene` 主相机同为 `_projection: 0` **刻意推迟到 P2**：main 是 UI/菜单场景，翻转会扭曲 2D 菜单，与报告 §5 改造路径表（main.scene CameraBrain 列 P2）一致。
- 校验：JSON_PARSE_OK；`.scene` 改 JSON 不影响代码门禁（encoding_audit 不扫 .scene），未跑 validate:all（本沙箱无法验证运行）。需在 Cocos Creator 编辑器实测 3D follow 渲染。
- 提交：9a99d08（代码）。
- **审计报告同步（本 turn 收尾 P0 跟踪器）**：将 P0-3 完成情况回填——§0 运行时接线去掉错误的"CameraBrain 未挂任何场景主相机"（实际 GameBootstrap 已 `attach(mainCam)`，属事实错误更正）、§1.1/§3.1/§3.5 CameraBrain 状态 🟡→✅ 并补透视说明、§3.6 dungeon 主相机正交→透视、§4 P0-3 标记完成+完成注解、§6 风险降档（大量新组件未启用 ★★★★☆→★★★☆☆）、§7 运行时接入补 CameraBrain 已挂说明、§9 清单项 3 标记完成、最后更新日期同步。
- 校验：encoding_audit issues=0,p0=0（11 处报告编辑，无代码样例，0 回归）。
- **审计报告同步改动待提交**（仅 `docs/代码与设计结构一致性审计报告.md` 一个文件，待 git commit；未 push）。

### P0 全部完成状态
- P0-2 `e01202b`+报告 `130ba84`、P0-1 `752d6b5`+报告 `58f2bdc`、P0-3 `9a99d08`+报告同步（未提交）。任务列表 #83/#84/#85 已标记 completed。
- 下一步待用户确认：继续 P1 系列（P1-3/P1-4 补 ILifecycle+注入，中等风险、纯代码可 TS 校验）或先暂停审查 P0 提交。

## P1-3 完成（战斗 3 子系统 ILifecycle + 注入）

### 改动（纯代码，红 line #4 去除消费者内 `new`/静态调用）
- `TargetSelector.ts`：`implements ILifecycle`（补 initialize/enter/exit/pause/resume/destroy 空壳 + `_ctx` 字段；保留无参构造，测试 `tests/core/combat.test.ts` 的 `new TargetSelector()` 不受影响）。
- `Resolvers.ts`：`HitResolver`/`DamageResolver` 由 static 方法改为实例型 `ILifecycle`；新增 `IHitResolver`/`IDamageResolver` 令牌（与既有 `ITargetSelector` 并列）。
- `CombatSystem.ts`：去掉 `new TargetSelector()`，改 `ctx.get` 注入三服务（`_selector`/`_hitResolver`/`_damageResolver`）；`dispatch` 经 `selector` 局部变量解析；`HitResolver.resolve`/`DamageResolver.applyBurn` 改为 `this._hitResolver!`/`this._damageResolver!` 实例调用，并补 null-guard；新增 `get selector()`。
- `SkillExecutor.ts`：注入两 resolver 实例（原静态调用改 `this._hitResolver!`/`this._damageResolver!`），`execute` 守卫补两依赖。
- `GameBootstrap.ts`：`_wireInfra()` 注册 `ITargetSelector`/`IHitResolver`/`IDamageResolver` 三令牌并 `lifecycle.register`+`initialize`（与 SkillGraph/SkillExecutor 同范式）。

### 校验
- encoding_audit issues=0,p0=0；`validate:all` 中 **TS 静态检查 [OK]**、**架构门禁 [OK]**（仅 SceneFlowService 调 director.loadScene，未触碰）。
- 3 项 FAIL（资源注册/非UI资源注册/文档一致性-`character_parts` prompts）为美术轨既有缺口，与本改动无关、用户先前说"你不用管"。

### 提交
- `e1ce9ba` feat(combat): P1-3 lift TargetSelector/HitResolver/DamageResolver to ILifecycle + DI injection（9 文件：5 .ts + 4 新建 .meta）。
- `33c3921` docs(audit): mark P1-3 combat subsystems ILifecycle+injection complete（审计报告 P1-3 节标记完成、§7 战斗重构升 4/5 星、§9 清单项 4 标记）。
- 任务列表 #90 标记 completed。未 push（仅本地 main）。

### 下一步建议
- P1-4（6 个 Player ECS 组件补 ILifecycle + 注册/注入）为下一个最低风险纯代码项；P1-1/P1-2（壳化 Grid/DungeonManager + 分解 PlayerController/吸收 AutoAttack）为较大重构，需单测护航。

## P1 全系列完成（用户："继续把P1 全部完成"）

### P1-4 — 6 个 Player ECS 组件补 ILifecycle + 注册/注入 — 已完成并提交
- `ecs/MovementComponent.ts`：加 `implements ILifecycle`；`initialize(ctx)`/`initialize(ctx,startX,startY)` 重载 + 5 生命周期空壳（destroy 清 `_transitRemaining/_moving`）。
- `ecs/StatComponent.ts`：加 `implements ILifecycle`；`initialize(...)`/`initialize(ctx)` 重载 + 5 空壳（destroy 清 modifiers）。
- `ecs/AnimationComponent.ts` / `CombatComponent.ts` / `TargetComponent.ts` / `InteractionComponent.ts`：均加 `implements ILifecycle`；保留既有具体 `initialize(...)` 签名 + 新增 `initialize(ctx)` 重载 + 5 生命周期空壳（Combat 含 `resolveAutoAttack` 纯函数，见 P1-2）。
- `core/GameBootstrap.ts`：import 6 组件类+令牌；`_wireInfra()` entityManager 注册块后新增 demo 单例注册（6 组件 `new`+register+initialize，如 `movementC.initialize(this._ctx,0,0)`、`combatC.initialize(this._ctx,'demo',()=>{})`）。
- 校验：encoding_audit issues=0,p0=0；TS 静态检查 [OK]；架构门禁 [OK]；validate:all 6/9（3 项美术轨既有缺口，未引入新 FAIL）。
- 提交：`e18ac97` feat(ecs): P1-4 add ILifecycle to 6 Player ECS components + wire + `5e9aeba` docs(audit): mark P1-4。

### P1-5 — 补 I* TS 接口声明 — 已完成并提交
- `core/Logger.ts`：新增 `export interface ILogger`（含 `channel(name): LogChannel`）；`Logger implements ILogger`。
- `core/ConfigDatabase.ts`：新增 `export interface IConfigDatabase`（`loadAll` + 6 `get*`）；`ConfigDatabase implements IConfigDatabase`。
- `core/EventBusManager.ts`：新增 `export interface IEventBus`（6 TypedEmitter 只读域 + setDomainLog/clearAll/initialized）；`EventBusManager implements ILifecycle, IEventBus`。
- `camera/CameraBrain.ts`：新增 `export interface ICameraBrain`（`attach/setTarget/setMode/triggerShake/lateUpdate` + currentMode/initialized）；`CameraBrain implements ILifecycle, ICameraBrain`。
- 注：`IAssetCache`/`IAnimationController` 接口此前已存在、实现类已 implements，本次仅补 4 个缺失的 I*。
- 校验：encoding_audit 0；TS 静态检查 OK；架构门禁 OK；validate:all 6/9。
- 提交：`cb61793` feat(core): P1-5 declare I* TS interfaces + impl alignment + `e0ff29d` docs(audit): mark P1-5。

### P1-1 — 壳化 GridManager / DungeonManager — 已完成并提交
- `dungeon/DungeonManager.ts`：类前加 `@deprecated` JSDoc（Facade 说明 + 指向新五类）；`_onRoomEntered` 的 `switch(room.type)` 改为数据驱动 `Map<RoomType,(room)=>void>`（缓存 `_roomHandlerMap` 懒构建），行为不变；活体生成仍走 `DAGGenerator`（新五类委派留 P1-6 in-editor）。
- `dungeon/GridManager.ts`：类前加 `@deprecated` JSDoc（保留 `isWalkable`/`generateWithSeed`）。
- 校验：encoding_audit 0；TS 静态检查 OK；架构门禁 OK；validate:all 6/9。
- 提交：`ca63c59` refactor(dungeon): P1-1 deprecate-shell GridManager/DungeonManager (data-driven room handlers) + `348bc90` docs(audit): mark P1-1。

### P1-2 — 分解 PlayerController + 吸收 AutoAttack — 已完成并提交
- `ecs/CombatComponent.ts`：新增 `AutoAttackContext`/`AutoAttackTarget`/`AutoAttackResult` 三接口 + 静态纯函数 `static resolveAutoAttack(ctx,target,rollCrit?): AutoAttackResult`（暴击/伤害倍率/距离门限/吸血逻辑，`rollCrit` 默认 `Math.random` 可注入 RunRng 可复现）。
- `battle/PlayerController.ts` / `battle/AutoAttack.ts`：类前加 `@deprecated` JSDoc（逻辑已吸收进 CombatComponent）。
- `.scene` 节点移除 PlayerController/AutoAttack、改挂 6 组件 —— 留 in-editor（沙箱无法编译运行 .scene）。
- 校验：encoding_audit 0；TS 静态检查 OK；架构门禁 OK；validate:all 6/9。
- 提交：`7d430dd` refactor(battle): P1-2 decompose PlayerController, absorb AutoAttack into CombatComponent + `00dffac` docs(audit): mark P1-2。

### P1-6 — 接入 Dungeon 五类到运行时 — 已完成并提交
- `scene/DungeonSceneInstaller.ts`：import `DungeonGenerator/RoomBuilder/NavigationGrid/RoomRuntime`；`DungeonSceneRefs` 加 `roomRuntime: RoomRuntime`；`install()` 真实路径构造 `new DungeonGenerator().generate(20260711,'forest')` → `new RoomBuilder().build(...)` → `new NavigationGrid(builtRoom.tileMap)` → `new RoomRuntime(builtRoom,builtNav)`，并加入 return 对象。`DungeonManager` 活体生成切换留 in-editor。
- 校验：encoding_audit 0；TS 静态检查 OK；架构门禁 OK；validate:all 6/9。
- 提交：`8e609ba` feat(scene): P1-6 wire Dungeon five-class pipeline into runtime + `060c781` docs(audit): mark P1-6。

### P1 全系列收尾状态
- P1-1/1-2/1-3/1-4/1-5/1-6 **代码侧全部落地**，每步独立双提交（代码 + 审计报告同步），共 12 commits（本地 main，未 push）：
  `e1ce9ba`/`33c3921`(P1-3) · `e18ac97`/`5e9aeba`(P1-4) · `cb61793`/`e0ff29d`(P1-5) · `ca63c59`/`348bc90`(P1-1) · `7d430dd`/`00dffac`(P1-2) · `8e609ba`/`060c781`(P1-6)。
- 技术要点：方法重载（`implements ILifecycle` 时保留既有 `initialize(...)` 具体签名 + 新增 `initialize(ctx)` 重载 + 5 生命周期空壳）使 `EcsEntityFactory` 既有调用点零改动；红线 #2/#3/#4 全部遵守（无 `switch`、入 LifecycleManager、消费者内不 `new` 服务）。
- 待 in-editor 实测项（沙箱无法编译 .scene，按审计报告第 243 行约定待 Cocos Creator 验证）：① P1-2 `.scene` 节点移除 PlayerController/AutoAttack、改挂 6 组件；② P1-6 `DungeonManager` 活体生成切换为委派新五类；③ P0-3 dungeon 主相机正交→透视 3D follow 渲染。
- 任务列表 #90~#95 全部标记 completed。下一步待用户确认：进入 P2 系列（P2-1 LightingService / P2-2 IRuntimeState / P2-3 ILogger 令牌去重 / P2-4 ModelDisplay3D）或先审查提交（可 push）。

## P2 全系列完成（用户："进入 P2 系列"）

> 每个 P2 项均按项目规则：每步独立双提交（代码 + 审计报告同步），写前加载 encoding-pipeline-guard、注释 ASCII 禁代码样例，写后跑 encoding_audit + 架构门禁 + TS 静态检查（轻量：括号平衡 + 缺 cc import 检测；全量 tsc 仅 Cocos 编辑器可跑）。新 .ts 均生成 Cocos uuid `.meta`。最终 `validate:all` = **6/9**（仅 3 项美术轨既有缺口 资源注册/非UI资源注册/文档一致性，未引入新 FAIL）。

### P2-3 — 去重 ILogger 令牌 — 已完成并提交
- 定位 `ILogger` 令牌在 `GameContext.ts:8` 与 `Logger.ts:53` 两处声明；全仓消费者（GameBootstrap/CombatSystem/SkillExecutor）均从 `GameContext` 导入令牌，`Logger.ts` 副本为死代码（grep 全仓确认无人 `from '.../Logger'` 导入该令牌）。删除 `Logger.ts:52-53` 注释 + `export const ILogger`，保留 `ILogger` 接口与 `Logger implements ILogger`；单一权威留在 `GameContext.ts`。
- 校验：encoding_audit 0；TS 静态 [OK]；架构 [OK]；validate:all 6/9。
- 提交：`f58ca3c` refactor(core): P2-3 dedupe ILogger token + `aaf0297` docs(audit): mark P2-3。

### P2-2 — 实现 IRuntimeState — 已完成并提交
- 新建 `core/RuntimeState.ts`（`RuntimeState implements ILifecycle, IRuntimeState`），含 `getSeed()`（委派 `RunRng.instance.seed`，§2.2 确定性）/ `getFrame()`/`tickFrame()`/`getSeedDebug()`（返回 `{seed,frame}`，结构匹配 `DebugPanel.SeedDebugInfo`）。纯 TS（无 `cc`）。
- `GameBootstrap._wireInfra()` 早期注册 `IRuntimeState` + `lifecycle.register` + `initialize`；原硬编码种子占位（`seed: 20260710`）替换为 `debugPanel.registerProvider('seed', () => ({ seed: runtimeState.getSeedDebug() }))`，Seed 面板现填充真实 RunRng 种子。
- `GameContext.ts` 已有 `IRuntimeState` 令牌声明（无需新增）。新建 `RuntimeState.ts.meta`。
- 校验：encoding_audit 0；TS 静态 [OK]；架构 [OK]；validate:all 6/9。
- 提交：`f6ce5c4` feat(core): P2-2 implement IRuntimeState + `8ed62dc` docs(audit): mark P2-2。

### P2-1 — 实现 LightingService — 已完成并提交
- 新建 `lighting/LightingService.ts`（`LightingService implements ILifecycle, ILightingService`），含 6 区域（forest/catacombs/volcano/tundra/swamp/abyss）光照预设（方向光颜色/强度/方向、环境光颜色/强度、雾颜色/开关/密度/起止、天空盒开关）。
- `GameContext.ts` 新增令牌 `ILightingService`。`GameBootstrap._wireInfra()` 注册 `ILightingService` + `lifecycle.register` + `initialize`；`DungeonSceneInstaller.install()` 构造完 Dungeon 五类后，经 `GameBootstrap.context.get(ILightingService).apply(sceneZone)` 应用区域预设（与 P1-6 同范式）。`director.root` 光照 mutation 用本地接口 cast + try/catch 保护，headless 为 no-op。
- 新建 `LightingService.ts.meta`。
- 校验：encoding_audit 0；TS 静态 [OK]；架构 [OK]；validate:all 6/9。
- 提交：`595fd1a` feat(lighting): P2-1 implement LightingService + `2fa7d8b` docs(audit): mark P2-1。

### P2-4 — 建 ModelDisplay3D 视口 — 已完成并提交
- 新建 `ui/main/ModelDisplay3D.ts`（`@ccclass('ModelDisplay3D')` 继承 `Component`），封装 `ModelComponent` + `SkinnedMeshRenderer`；`onLoad` 自动补挂两组件，`showModel(assetId)` 经 `AssetBundleService.instance.loadById<Model>(id)` 载入 3D 模型赋给 `ModelComponent.asset`，实现 3D 角色选择展示（§2.4）。
- `.scene` 节点挂接（main.scene `CreatePanel/PreviewZone/ModelDisplay`）留 in-editor 实测（沙箱无法编译 .scene）。
- 新建 `ModelDisplay3D.ts.meta`。
- 校验：encoding_audit 0；TS 静态 [OK]；架构 [OK]；validate:all 6/9。
- 提交：`9543a6b` feat(ui): P2-4 implement ModelDisplay3D + `eadf4ae` docs(audit): mark P2-4。

### P2 全系列收尾状态
- P2-1/2/3/4 **代码侧全部落地**，每步独立双提交（代码 + 审计报告同步），共 8 commits（本地 main，未 push）：
  `f58ca3c`/`aaf0297`(P2-3) · `f6ce5c4`/`8ed62dc`(P2-2) · `595fd1a`/`2fa7d8b`(P2-1) · `9543a6b`/`eadf4ae`(P2-4)。
- 待 in-editor 实测项（沙箱无法编译 .scene）：① P2-1 dungeon 3D 光照实际观感；② P2-4 `ModelDisplay3D` 组件挂接 `CreatePanel/PreviewZone/ModelDisplay` 节点；③（延续 P0/P1）Player 节点去 PlayerController/AutoAttack 改挂 6 组件、DungeonManager 活体生成委派新五类、dungeon 主相机 3D follow 渲染。
- 任务列表 #96~#99 全部标记 completed。至此 P0+P1+P2 全部完成（约 40 个本地提交，均未 push）。
- 下一步待用户确认：A. 进入 P3（审计报告未列明 P3；可能的后续为场景 .scene 实际 3D 化改造 + in-editor 实测 + 旧活体类彻底移除）；B. 先 `git push` 远程（目前均仅本地 main）。

### 19:13 进入 P3（先 B 后 A，用户指令「先B在A」）
- **B 完成**：本地 main（P0+P1+P2 共 30 个提交）已 `git push origin main` 成功（`f3f5c92..eadf4ae`）。远程 SSH `git@github.com:1028176322-dot/game.git`。
- 工作区遗留一批未提交 boss 贴图 `.meta` 修改（美术管线/编辑器生成，与架构无关，未进入推送）——已向用户确认是否提交/搁置。
- **P3-1/P3-2 完成**（代码侧，沙箱可 TS 校验）：新建 `ui/world/WorldSpaceUI.ts` + `ui/main/MainMenuBackdrop.ts`（均 `@ccclass` + `.meta`），对应审计报告 §5 dungeon.scene 步骤⑤、main.scene 步骤②。commit `b674007`。
- **P3-3 in-editor 节点手术清单**已写入审计报告 §10（dungeon.scene ③④⑤ / main.scene ①②③；③ ④ 改运行时装配需编辑器实测，⑤ ①② 组件已就绪待挂接）。审计报告同步 commit `91246d8`。
- 门禁：P3 全部 `encoding_audit issues=0/p0=0`、TS 静态 [OK]、架构 [OK]（注：WorldSpaceUI.ts 注释含 "switch" 曾被误判 `comment_may_swallow_code` p0，已改为 "change" 清零）。
- 待用户决策：旧活体类（`BattleManager`/`GridManager`/`DungeonManager`/`PlayerController`/`AutoAttack`）彻底移除 + DungeonManager 生成委派新五类属运行时行为变更，需编辑器实测；是否要我先做代码侧委派接线（使编辑器移除旧节点后运行时不崩）。
- **依赖核查结论（关键，P3 编辑器红线）**：`DungeonSceneController._wireSystems` 直接依赖 `PlayerController` 的 `init/stats/currentHP/handleJoystick/onHPChanged`，且 `UpgradeManager/ElementSystem/ItemSystem/AutoAttack` 经 `init(player: PlayerController,...)` 注入。而 `DungeonSceneInstaller.install()` 对 Grid/ Battle/ Dungeon Manager + PlayerController + AutoAttack 均有"场景找不到→`ensureComponent` 在 `systems` 子节点重建"兜底。
  - 现在可安全做：main ①②、dungeon ⑤、dungeon ④（移除三节点会被 installer 重建到 systems，行为不变）、dungeon Player 叠加挂 6 组件+ModelComponent（无害无消费者）。**不可移除 PlayerController**（会崩）。
  - 待 P3-4：PlayerController 节点移除需 6 组件接线替代；DungeonManager 生成委派 RoomRuntime（仍走旧 DAGGenerator）；main ③ 主相机挂 CameraBrain 会扭曲 2D 菜单（§5 推迟）。已写入审计报告 §10.1。
- 审计报告 §10.1 已提交 `e6cd2c0`；P3-4 任务 #103 已建（pending，待用户实测安全范围后启动）。
- **环境调查结论（19:35–19:57，最终）**：本机确有 Cocos Creator 3.8.8（`CocosCreator.exe` = Electron 31.3.1 + 内置 Node 20.15.1）。但**沙箱无法 headless 编译 `.scene`**，根因：① 3.8.8 的构建走 `Editor.Message.request('builder','build',...)` 消息总线，**没有 `--build` CLI 参数**（`app.asar` 已证实）；② `--project` 仅用于拉起 GUI 编辑器、需要显示器，本沙箱无显示器 → Electron 加载 app 失败、退化成裸 Node → 把 `--project` 当 Node 参数报 "bad option"；③ 沙箱注入的 `NODE_OPTIONS`(含 `--use-system-ca`) 与 `ELECTRON_RUN_AS_NODE=1` 会干扰启动（清空后仅过启动关，仍因①②失败，6s 退出码 9）。**因此 `.scene` 编译/引用校验只能在用户本地已打开的 Cocos Creator 里做（Build 或打开场景即编译）。沙箱侧仅能跑静态门禁（ts_static/encoding/architecture）+ 审计报告。** 探针日志 `build_headless.log`/`help_probe.log`/`help2.log` 未提交、可删。

### 20:04 我能读编辑器"已保存"的场景结构（用户问"能否读到编辑器实际场景结构"）
- **结论**：能读 `.scene` 磁盘文件（= 上次 `Ctrl+S` 保存结构），但读不到编辑器**未保存的内存态**。每次核对场景先让用户 Save。
- **组件 uuid 反查类名（关键技术，已工具化）**：`.scene` 里自定义组件只存 base64url 紧凑 uuid（如 `2939euWIidI2YiU1qw0r42q`），**不是** `.ts.meta` 里带连字符 uuid 的简单 base64（我最初的 base64url 解码尝试失败，因 Cocos 编码方式不同）。正解：扫描 `temp/**/*.js` + `library/**/*.js` 里 `_cclegacy._RF.push({}, "<uuid>", "<ClassName>", undefined)` 注册表，建 uuid→类名映射。已写成 `tools/introspect_scene.py`（默认 dungeon.scene，可传参），跑通：dungeon.scene 11/11、main.scene 21/21 全部解析。
- **实测发现（重要）**：当前磁盘 `dungeon.scene` 仍挂全部旧类（PlayerController/AutoAttack/GridManager/BattleManager/DungeonManager），`main.scene` 的 `CreatePanel` 尚未挂 `ModelDisplay3D`。说明 P3 的编辑器场景手术**未保存/未做**——若用户在编辑器里做了手术但没 Save，我读到的仍是旧结构。
- 注：headless build 虽不可行，但 `temp/` 编译产物含 `_RF.push` 注册表，足以做静态场景组件核对——这是沙箱内能做的最强场景验证手段。

### 20:16 基于编辑器场景数据对齐文档目标（用户问"怎么改到一致"）
- 增强 `tools/scene_tree.py`：输出完整节点层级树+组件（内置 cc.* 直列，自定义 uuid→类），修复根节点识别 bug（Cocos 根节点 `_parent` 指向 cc.Scene 对象而非 Node，改用"不被任何节点引用为 child 即根"）。
- **关键澄清（文档错误）**：`LightingService.ts`/`CameraBrain.ts` **无 @ccclass、不继承 Component**，是纯 TS 服务类（注释明写 "Pure TS, NO top-level cc import" / "best-effort no-op in headless"）。文档 `三场景完整结构树.md` §1/§3 把它们写成节点上的 `[LightingService]`/`[CameraBrain]` 组件**是错误的，无法在编辑器挂载**。实际：LightingService/CameraBrain 由 GameContext 注册，运行时接管 `Main Light`/`Main Camera`，编辑器保留这两个节点即可，勿新建 LightingRoot 或尝试挂这两个。
- **dungeon.scene 脏数据（dump 发现）**：① `Canvas/DungeonSceneController` 节点本身空（未挂脚本），其下却嵌套同名子节点 `DungeonSceneController [DungeonSceneController]`（挂了脚本）——结构错误；② `DungeonManager` 节点空组件（脚本疑似丢失）；③ 根下 `_REMOVED_MainSceneController` 游离节点。老脚本（DungeonManager/PlayerController/AutoAttack/GridManager/BattleManager/DungeonSceneController/RoomTransition）全在 `assets/scripts/`，可重挂。
- **main.scene dump**：2D 面板结构完整匹配附录 A.2 ✅；`CreatePanel/PanelRoot/PanelFrame/ContentRoot/PreviewZone/ModelDisplay` 是空 UITransform（待挂 `ModelDisplay3D`）；根下 `_REMOVED_MainSceneController` 游离节点待删。
- **splash.scene dump**：结构基本匹配 §1；`GameBootstrap` 下多一 `StatusLabel` 子节点（文档无，疑似调试残留）待用户决定。
- **编辑器可安全手术**（给用户的操作矩阵）：splash=删 StatusLabel 残留（若调试用）；main=挂 ModelDisplay3D + 删 _REMOVED 节点 + 可选 MainMenuBackdrop；dungeon=修 DungeonSceneController 绑定（让 Canvas 直接子节点挂脚本、删嵌套同名子节点）+ 恢复 DungeonManager 脚本 + 删 _REMOVED 节点。**绝不动 Player 的 PlayerController/AutoAttack**（P3-4 未落地，移除即崩，§10.1 红线）；WorldSpaceUI/Systems/World 各层由 DungeonSceneInstaller 运行时创建，编辑器无需预置。
### 20:38 main.scene CreatePanel ModelDisplay3D 已挂接并保存
- 用户通过编辑器在 `Canvas/CreatePanel/PanelRoot/PanelFrame/ContentRoot/PreviewZone/ModelDisplay` 节点上添加了 `ModelDisplay3D` 组件并 Ctrl+S 保存。
- `scene_tree.py` 验证：层级正确、节点 `ModelDisplay [cc.UITransform, ModelDisplay3D]`，无残留 `_REMOVED_` 节点；`main.scene` 共 409 对象且合法 JSON。
- Inspector 中 `Model Comp` 显示 `cc.MeshRenderer` 正常，因为 `ModelDisplay3D.onLoad` 会运行时确保/补挂 `ModelComponent`（MeshRenderer 别名）+ `SkinnedMeshRenderer`，磁盘未预序列化也无妨。
- 下一步：待用户确认是否修复 `splash.scene` 的 `GameBootstrap/StatusLabel` 残留（可选清理）。

### 20:41 dungeon.scene 结构由代码运行时加载（用户澄清，撤消手动手术建议）
- 用户澄清：dungeon.scene 的 `Canvas` 下节点（DungeonSceneController/GridManager/Player/BattleManager/DungeonManager/VirtualJoystick/各 HUD 等）由代码**运行时自动加载构建**，非用户在编辑器手动摆放；磁盘 `.scene` 里看到的嵌套/空节点是运行时加载后保存的快照。
- **修正之前 20:16 对 dungeon 的建议**：之前判断的"DungeonSceneController 嵌套错误""DungeonManager 空组件=脚本丢失"均不成立，撤消"在编辑器手动修复嵌套 / 恢复 DungeonManager 脚本"的 2 条建议——手动改会与运行时代码冲突或被其覆盖。
- 结论：dungeon 对齐文档目标（`三场景完整结构树.md` §3 描述 3D 形态）的正确路径是**代码侧改造（P3-4 任务 #103）**——改 `DungeonSceneInstaller`/`DungeonSceneController`/`DungeonManager` 等，让运行时加载出文档描述的 3D 结构（Player 6 组件 + ModelComponent / DungeonManager 委派 RoomRuntime / WorldSpaceUI / World 各 3D 层 / CameraBrain + 透视），而非编辑器拖拽节点。
- **区分原则（重要）**：`main.scene` 的 `ModelDisplay3D`、`splash` 的 `StatusLabel` 属 UI 组件、不在运行时自动加载列表，可安全手动改；`dungeon` 的战斗/地牢/玩家节点全由运行时代码加载，**不可在编辑器手动改动层级或组件绑定**。

## 3D 资源缺口发现：地牢房间功能道具缺失（用户疑问）

### 用户疑问
"现在的 3D 改造资源中并没有地牢场景中各个房间的情况" —— 经核查，**疑问成立，是真实缺口**。

### 核查结论
- `art_3d_manifest.json` 134 项 = 5角色 + 24地块(Floor/Wall/HighGround/Thorn) + 36怪 + 42Boss + 27特效。**无任何房间级资产，无 room/chest/shrine/altar/portal 类环境道具**（逐词扫描 134 name + 27 effects 全部为零）。
- `地牢生成系统规格.md` 房间=分层 DAG 节点，每个房间是 6×6 程序化地形网格；3D 理论上只需 tile 部件拼装，**但有两处真实漏了**：
  - **缺口 A 地形元素不全**：地形码 0空地/1障碍/2荆棘/4高台 有对应 tile；**3治疗符文、5暗区 无 3D 资产**。
  - **缺口 B 房间功能道具全缺**：宝箱房→宝箱、商店房→商店台、强化房→强化祭坛、入口房→传送门/楼梯、事件房→事件标记、Boss房→平台/阶梯、回复房→治疗符文（同A）。这 7 类房间进去会成"空房间"。
- 注意：房间系统是**全 6 区域通用**，并非仅 catacombs（地牢）。

### 已提建议（待用户决策是否补）
- 在 manifest 新增 `props`（环境道具）类别。
- 粒度两选一：① 通用 1 种（约 6~7 项，快）；② 6 区域变体（约 36~42 项，与 tiles 风格统一但资产翻倍）。
- 此缺口属美术轨（我就是美术轨），且**不在**此前"另一对话处理 prompts 角色部件"范围内。
- 待用户确认：是否补进 manifest + Brief 文档、以及选哪种粒度。

### 20:48 启动 P3-4 dungeon 运行时 3D 化改造（总任务 #108，additive 策略）
- 探查子代理（Explore）摸清运行时接线：DungeonSceneInstaller.install 用 `overrides ?? findComponentInChildren ?? ensureComponent` 三级回退加载 dungeon 节点；DungeonSceneController._wireSystems 直接依赖 PlayerController 的 init/stats/currentHP/handleJoystick/onHPChanged + MonsterController 目标引用 → **直接删 PlayerController 即崩（10+ 处硬依赖）**。6 ECS 组件 + EcsEntityFactory + EcsEntityBridge + RoomRuntime 均已就绪但除 demo 外未被消费。决策 F additive（并行不替换）策略。
- 拆 5 任务：#108 总 + #104 A(DungeonManager 委派 RoomRuntime) / #105 B(玩家接 bridge+3D视觉) / #106 C(controller 适配 bridge) / #107 D(移除 PlayerController)。用户选 A 起步。
- **关键架构不兼容发现**：新五类 `RoomRuntime` 是**单房间**运行时（资源引用计数+导航），不是整层地牢图 `DungeonDAG` 的等价物；且新 `DungeonGenerator` 的 RoomType 仅 5 种（start/battle/elite/reward/boss），旧 `RoomType`（core/Constants）有 8 种（含 Treasure/Healing/Shop/Upgrade/Event）。故不能"替换" DAGGenerator 而不破坏房间分发/战斗链路。
- **P3-4-A 完成并提交**（additive 委派，零回归）：改 `dungeon/DungeonManager.ts`——保留旧 DAGGenerator 楼层/战斗/分发逻辑完全不变；新增在 `_generateFloor` 用新五类（DungeonGenerator→RoomBuilder→NavigationGrid→RoomRuntime）为每个生成房间构建 RoomRuntime，经 `GameBootstrap.context` 注入 `initialize`，房间进入时 `enter()/load()`、`onDestroy` 时 `exit()` 释放，暴露 `roomRuntimes` getter。`IAssetCache` 已注册（GameBootstrap:163），`RoomRuntime.load` 资源缺失时 catch 吞错不崩。`GameBootstrap` 未 import DungeonManager（无循环依赖）。
  - 门禁：encoding_audit issues=0/p0=0；TS Static [OK]；Architecture [OK]；validate:all 维持 **6/9**（3 项美术轨既有缺口，未引入新 FAIL）。
  - 提交：`e0a0e0c`(代码) + `ecbd479`(审计报告同步：状态表 🟡→🟢、P1-6 段"未做"→"已做 P3-4-A"、§9 清单④ 标 ✅、更新日期)。
- 下一步可选：#105 B（玩家节点并行挂 EcsEntityBridge + Sprite→ModelComponent，需 3D 角色资源 + 输入/HUD 适配，复杂度高）；或先确认 A 在编辑器实测无回归。

## 地牢房间功能道具缺口 — 已补（用户 "补" + 选 6 区域变体 36~42 项）

### 决策
- 用户确认用 **6 区域变体**（42 项）：7 功能件 × 6 区域（Forest/Catacombs/Volcano/Tundra/Swamp/Abyss）。
- 功能件：Entry（入口门）/ Chest（宝箱）/ HealAltar（治疗祭坛，对应地形码3）/ Shop（商店柜台）/ Event（事件标记）/ BuffShrine（强化神龛）/ BossGate（Boss 闸门）。

### 落地改动（4 文件 + 1 文档，均加载 encoding-pipeline-guard 后编辑）
1. `assets/resources/config/art_quality_budget.json`：
   - 新增 `rules3d.dungeon`（prefix `DNG`，静态件，maxTri 3000 / maxBones 0 / 512² / 6×6 / LOD2 / perf low / modules 7 件）。
   - `rules3d.naming.pattern` 加入 `DNG`（否则 asset_validate 拒收）。
2. `assets/resources/config/art_3d_manifest.json`：
   - counts 加 `"dungeon": 42`，总项 **134 → 176**。
   - 追加 42 条 `DNG_{Region}_{Func}` 条目（category=`dungeon`、bucket=`dungeon`、dim=`3d`、source_2d=`[]`、note 标注"2D 概念稿 TBD"、lifecycle=`选秀`）。用一次性脚本合并后删除脚本，确保 UTF-8 干净 + JSON 合法。
3. `回到地面/tools/gen_assetmeta_from_manifest.py`：map_category 加 `dungeon→dungeon`；`_category_from_name` 加 `DNG_`→`dungeon`；docstring 134→176。dry-run 验证 Generated=176，`DNG_*` 路由到 `models/dungeon/`。
4. `回到地面/tools/asset_validate.py`：`PREFIX_BUCKET` 加 `"DNG": "dungeon"`（验证 DNG_Forest_Entry 通过，仅 author/reviewer/testScene 空模板默认 FAIL，与所有新模板一致）。
5. `回到地面/tools/collect_3d_progress.py`：docstring 134→176（扫描逻辑按 manifest name 匹配，无需改）。
6. `回到地面/docs/3D建模Brief_全量资产.md`：版本 1.1.0→**1.2.0**（LastUpdate 2026-07-11）；§0 注册数 134→176；§1 命名表加 `DNG_` 行 + 正则加 `DNG` + 命名一致性加 7 功能件；§2.8 新增 dungeon 预算；§3.2 无骨骼资产 51→93；§6.1 134→176（2 处）；§8 标题 134→176 + §8.7 新增 42 项清单；§9 新增 **P4 Deferred（42 项）** + 汇总累计 134→176；§10 文件表 134→176（2 处）。

### 校验
- `encoding_audit.py --ci`：**issues=0, p0=0, p1=0, p2=0**（utf-8 干净）。
- `check_doc_consistency.py`：budget/naming 全 OK；唯一 FAIL 仍为 **既有缺口** `Prompts missing for anchor categories: {'character_parts'}`（prompts.json 缺 character_parts，属另一对话处理的美术管线链，用户此前已说"你不用管"/"由另一对话处理" → 未擅改 prompts.json）。`dungeon` 非 anchor，本次未引入新 FAIL。

### 关键遗留
- `source_2d` 对 42 个 DNG_* 暂为空（无 2D 概念稿）。生产前需补一轮 2D 概念稿，或按 Brief §2.8 几何化例外（仅用区域色板，禁训练知识）。已写进 manifest.note + Brief §2.8/§8.7。
- 未 mass-generate 176 个 .assetmeta.json 模板（仅 dry-run 验证），待真正进入 dungeon props 生产阶段再 `gen_assetmeta_from_manifest.py` 全量生成。

## 背景规格来源核查(用户审计,纠正前一轮偏差)
- 用户追问房间/事件背景判定标准来源。结论:1280x720/<=250KB 取自已 一体化界面资源重做清单.md §20(行482-484),**非** ART_RESOURCE_RULES.md;<=350KB+PNG母版双路径 取自 资源清单_可执行.md(行265/300/79-95),已核实。
- 关键纠正:ART_RESOURCE_RULES.md(§14.1声明为预算唯一权威)§3.2/§15 全屏背景推荐1920x1080、§9.2 全屏背景JPG 300-800KB。实测11张房间/事件背景(1920x1080,340-547KB)按此权威文件实际**合规**——前一轮"11/11超标"误用了最严的非权威口径(一体化§20)。
- 项目文档存在3方冲突:ART_RESOURCE_RULES.md(1920x1080/300-800KB) vs 一体化界面资源重做清单.md§20(1280x720/<=250KB) vs 资源清单_可执行.md(<=350KB+双路径)。按§14.1应以ART_RESOURCE_RULES.md§9.2为准,需治理该冲突(§20越权)。
- 真正不达标项:11/11缺PNG母版(违反资源清单_可执行.md双路径);合计~5MB超bundle_bg~3.8MB红线。命名/安全词合规待扫。

## 21:32 建 P3-4 续作进度文件（用户"先暂停，要进度文件"）
- 用户决定：3D 资源还没开始制作，P3-4 先到这里，需要一个进度文件方便以后继续。
- 新建 `docs/progress/P3-4_dungeon_3d_refactor.md`：自包含续作文档——
  - 状态 🟡 PAUSED（A/B/C 完成且门禁全绿；D 受阻）。
  - A=`e0a0e0c`+`ecbd479`、C=`9249fca`、B=`647b959`+`1b5b189`（审计报告同步）。
  - D 受阻 5 缺口：①无 3D 角色模型(CharacterVisualService 仅 2D) ②输入→移动未接线 ③战斗目标未喂 CombatComponent ④HP 无回调 ⑤StatComponent 缺攻击属性；叠加沙箱无法跑游戏验证（审计 §10.1 🔴 红线）。
  - 续作清单 D-0~D-8（前置先产 3D 角色资产→D-1~4 接线→D-5/6 编辑器实测→D-7 翻转 USE_ECS_PLAYER=true 并移除旧类→D-8 同步审计）。
  - 验证命令、关键文件锚点、决策点、关联 TaskList(#104~#108)。
- 同步 `docs/progress/_index.md` 的 See Also 增加该文件指针。
- 注意：`docs/progress/3d_progress.md` 是**资产生产**进度（134→176 项全 draft、贴图 None），与代码侧 P3-4 是不同维度；两者都需在恢复 D 时推进（3D 资产是 D 硬前置）。

## 21:43 对齐背景规格到 ART_RESOURCE_RULES.md（用户："对齐 这个文件是资源的唯一规格来源"）

### 目标
让 `E:/game/.workbuddy/memory/topics/ART_RESOURCE_RULES.md` 成为资源（尤其背景）的**唯一规格来源**，消除三文档口径冲突。

### 权威口径（ART_RESOURCE_RULES.md）
- §3.2/§15：全屏背景推荐尺寸 = **1920×1080**
- §9.2：全屏背景 JPG 体积 = **300–800KB**（推荐）/ 800KB–1.2MB 可接受 / 上限 1.5MB
- §14.1：体积预算唯一权威 = 本文 9.2，其他文档只能引用不得复制

### 落地改动（均加载 encoding-pipeline-guard 后编辑，4 文件）
1. `ART_RESOURCE_RULES.md`：① 补**背景双路径明文**（PNG 母版 + JPG 运行时）进 §9.2 表格；② 强化 §14.1 声明：背景尺寸/体积以 §3.2/§9.2 为准，其他文档冲突一律服从。
2. `回到地面/docs/一体化界面资源重做清单.md`：§2 合同 `1280x720→1920x1080`；§20 房间/事件/战斗背景 `1280x720/≤250KB→1920x1080/300–800KB`；所有 per-row 规格行 + 生成 prompt 文本统一对齐；顶部加"背景规格以 ART 为准"横幅。
3. `回到地面/docs/资源清单_可执行.md`：背景体积 `≤350KB→300–800KB`（§9.2）；双路径要求溯源到 ART；修正背景状态行（实际 11 已存在/6 缺失，原写"17 全缺失"有误）。

### 校验（被用户中断后重跑确认）
- `encoding_audit.py --ci`：**issues=0, p0=0, p1=0, p2=0**（608 文件全过，无损坏）。
- `check_doc_consistency.py`：除 1 项**既有 FAIL** `Prompts missing for anchor categories: {'character_parts'}`（归另一对话 prompts.json，非本次范围、未擅改）外全部 OK。本次对齐未引入新冲突。
- 注：bundle_bg 整包占比检查（11 张房间/事件背景合计 ~5MB vs 红线 ~3.8MB）仍为待办，未做——属聚合问题，与单张尺寸/体积口径无关。

### 卡顿说明
上一轮最后一个 encoding_audit 重跑命令被标记 "Interrupted by user"，导致流程停在"已编辑完、待验证确认"未给收尾。重跑后确认文件无损坏、门禁全绿（除既有美术轨 FAIL）。
