/**
 * GameConfig - 全局数值配置表
 * 
 * 【规则】
 * 1. 所有数值型配置集中在此，代码中禁止出现任何魔法数字
 * 2. 每个配置项均标注：用途、默认值、合法取值范围
 * 3. 修改数值只需改此文件，无需搜代码
 * 4. 添加新配置时同步添加注释
 */

export const GameConfig = {
    // ============================================================
    // 1. 战斗系统
    // ============================================================

    /** 自动攻击间隔（秒） | 范围: 0.2~5.0 | 默认: 1.0 */
    AUTO_ATTACK_INTERVAL: 1.0,

    /** 攻击距离衰减系数（每超出 1 格降低命中率百分比）| 范围: 0~0.5 | 默认: 0.2 */
    RANGE_DECAY_RATE: 0.2,

    /** 翻滚无敌帧时长（秒）| 范围: 0.1~1.0 | 默认: 0.3 */
    DODGE_DURATION: 0.3,

    /** 翻滚冷却时间（秒）| 范围: 1.0~10.0 | 默认: 3.0 */
    DODGE_COOLDOWN: 3.0,

    /** D6 骰子面数 | 范围: 2~20 | 默认: 6 */
    D6_DICE_SIDES: 6,

    /** 暴击触发骰子值（骰子 >= 此值触发暴击）| 范围: 1~20 | 默认: 6 */
    CRIT_TRIGGER_VALUE: 6,

    /** 暴击倍率 | 范围: 1.0~5.0 | 默认: 1.5 */
    CRIT_MULTIPLIER: 1.5,

    /** 基础暴击率（仅用于无骰子系统的快速判定）| 范围: 0~1 | 默认: 0.05 */
    CRIT_BASE_CHANCE: 0.05,

    /** 防御减伤系数 `floor(DEF×系数)` | 范围: 0~1 | 默认: 0.5 */
    DEF_REDUCTION_FACTOR: 0.5,

    /** 伤害公式防御系数（同 DEF_REDUCTION_FACTOR，兼容旧引用）| 范围: 0~1 | 默认: 0.5 */
    DAMAGE_FORMULA_DEF_FACTOR: 0.5,

    /** 最小伤害保底值 | 范围: 1~10 | 默认: 1 */
    MIN_DAMAGE: 1,

    /** 技能前摇动画时间（秒）| 范围: 0.1~1.0 | 默认: 0.2 */
    SKILL_CAST_TIME: 0.2,

    /** 道具使用前摇动画时间（秒）| 范围: 0.1~1.0 | 默认: 0.3 */
    ITEM_USE_TIME: 0.3,

    /** 切换自动攻击目标延迟（秒）| 范围: 0.05~0.5 | 默认: 0.2 */
    SWITCH_TARGET_DELAY: 0.2,

    // ============================================================
    // 2. 玩家基础属性
    // ============================================================

    /** 玩家初始攻击力 | 范围: 0~100 | 默认: 10 */
    PLAYER_BASE_ATK: 10,

    /** 玩家初始防御力 | 范围: 0~100 | 默认: 3 */
    PLAYER_BASE_DEF: 3,

    /** 玩家初始最大生命值 | 范围: 1~1000 | 默认: 100 */
    PLAYER_BASE_MAX_HP: 100,

    /** 玩家移动速度（像素/秒）| 范围: 50~500 | 默认: 320 (TapTap 96px格×3.3格/秒) */
    PLAYER_MOVE_SPEED: 320,

    /** 玩家基础攻击范围（格）| 范围: 0.5~10 | 默认: 2 */
    PLAYER_BASE_ATTACK_RANGE: 2,

    // ============================================================
    // 3. 属性边界限制
    // ============================================================

    /** 攻击力最小值下限 | >=0 | 默认: 0 */
    STAT_ATK_MIN: 0,

    /** 防御力最小值下限 | >=0 | 默认: 0 */
    STAT_DEF_MIN: 0,

    /** 最大 HP 最小值 | >=1 | 默认: 1 */
    STAT_MAX_HP_MIN: 1,

    /** 移动速度最小值（像素/秒）| >=10 | 默认: 50 */
    STAT_MOVE_SPEED_MIN: 50,

    /** 攻击间隔最小值（秒，即最快攻速）| >=0.1 | 默认: 0.2 */
    STAT_ATK_SPEED_MIN: 0.2,

    /** 攻击间隔最大值（秒，即最慢攻速）| >=0.5 | 默认: 5.0 */
    STAT_ATK_SPEED_MAX: 5.0,

    /** 攻击范围最小值（格）| >=0.1 | 默认: 0.5 */
    STAT_ATTACK_RANGE_MIN: 0.5,

    /** 暴击率上限 | 0~1 | 默认: 1 */
    STAT_CRIT_CHANCE_MAX: 1.0,

    /** 暴击倍率最小值 | >=1 | 默认: 1 */
    STAT_CRIT_MULTIPLIER_MIN: 1.0,

    /** 生命偷取上限 | 0~1 | 默认: 1 */
    STAT_LIFE_STEAL_MAX: 1.0,

    /** 伤害倍率最小值 | >=0.01 | 默认: 0.1 */
    STAT_DAMAGE_MULTIPLIER_MIN: 0.1,

    /** 减伤上限 | 0~1 | 默认: 0.9 */
    STAT_DAMAGE_REDUCTION_MAX: 0.9,

    // ============================================================
    // 4. 怪物属性
    // ============================================================

    /** 冲锋型怪物攻击间隔（秒）| 范围: 0.5~5.0 | 默认: 1.5 */
    MONSTER_ATK_INTERVAL_CHARGER: 1.5,

    /** 远程型怪物攻击间隔（秒）| 范围: 0.5~5.0 | 默认: 2.0 */
    MONSTER_ATK_INTERVAL_RANGED: 2.0,

    /** 防御型怪物攻击间隔（秒）| 范围: 0.5~5.0 | 默认: 2.5 */
    MONSTER_ATK_INTERVAL_DEFENDER: 2.5,

    /** 怪物最小攻击伤害 | 范围: 1~10 | 默认: 1 */
    MONSTER_MIN_DAMAGE: 1,

    // ============================================================
    // 5. 网格系统
    // ============================================================

    /** 网格尺寸（N×N）| 范围: 2~10 | 默认: 6 */
    GRID_SIZE: 6,

    /** 每格像素大小 | 范围: 32~128 | 默认: 96 (TapTap) */
    TILE_SIZE: 96,

    // ============================================================
    // 6. 房间与关卡
    // ============================================================

    /** 房间切换过渡动画时长（秒）| 范围: 0.1~2.0 | 默认: 0.5 */
    ROOM_TRANSITION_DURATION: 0.5,

    /** 每层最小房间数 | 范围: 2~20 | 默认: 4 */
    FLOOR_MIN_ROOMS: 4,

    /** 每层最大房间数 | 范围: 2~20 | 默认: 7 */
    FLOOR_MAX_ROOMS: 7,

    /** DAG 生成重试上限 | 范围: 1~10 | 默认: 3 */
    DAG_RETRY_LIMIT: 3,

    /** 种子码长度（字符）| 范围: 4~12 | 默认: 6 */
    SEED_CODE_LENGTH: 6,

    // ============================================================
    // 7. UI 与过渡
    // ============================================================

    /** 启动屏最少显示时长（秒）| 范围: 1.0~5.0 | 默认: 2.0 */
    SPLASH_MIN_DURATION: 2.0,

    /** 事件房自动选择等待超时（秒）| 范围: 5~30 | 默认: 15 */
    EVENT_AUTO_SELECT_TIMEOUT: 15,

    /** 觉悟战弹出延迟（秒）| 范围: 0~2.0 | 默认: 0.2 */
    DEATH_UI_DELAY: 0.2,

    /** 结算后返回主界面延迟（秒）| 范围: 0~3.0 | 默认: 1.0 */
    SETTLEMENT_RETURN_DELAY: 1.0,

    // ============================================================
    // 8. 掉落系统
    // ============================================================

    /** 普通怪掉落概率 | 范围: 0~1 | 默认: 0.12 */
    DROP_NORMAL_CHANCE: 0.12,

    /** 精英怪掉落概率 | 范围: 0~1 | 默认: 0.40 */
    DROP_ELITE_CHANCE: 0.40,

    /** Boss 掉落概率 | 范围: 0~1 | 默认: 1.0 */
    DROP_BOSS_CHANCE: 1.0,

    /** 普通怪金币掉落最小值 | 范围: 0~100 | 默认: 1 */
    DROP_GOLD_MIN_NORMAL: 1,

    /** 普通怪金币掉落最大值 | 范围: 0~100 | 默认: 5 */
    DROP_GOLD_MAX_NORMAL: 5,

    /** 精英怪金币掉落最小值 | 范围: 0~200 | 默认: 5 */
    DROP_GOLD_MIN_ELITE: 5,

    /** 精英怪金币掉落最大值 | 范围: 0~200 | 默认: 15 */
    DROP_GOLD_MAX_ELITE: 15,

    /** Boss 金币掉落最小值 | 范围: 0~500 | 默认: 20 */
    DROP_GOLD_MIN_BOSS: 20,

    /** Boss 金币掉落最大值 | 范围: 0~500 | 默认: 50 */
    DROP_GOLD_MAX_BOSS: 50,

    // ============================================================
    // 9. 经济系统
    // ============================================================

    /** 金币上限 | 范围: 10~999 | 默认: 99 */
    GOLD_CAP: 99,

    /** 钥匙上限 | 范围: 1~10 | 默认: 3 */
    KEY_CAP: 3,

    /** 道具背包容量（格）| 范围: 1~20 | 默认: 5 */
    ITEM_BAG_SLOTS: 5,

    /** 装备背包容量（格）| 范围: 1~30 | 默认: 12 */
    EQUIP_BAG_SLOTS: 12,

    /** 同类道具最大叠加数 | 范围: 1~99 | 默认: 5 */
    ITEM_STACK_MAX: 5,

    /** 钥匙同类最大叠加数 | 范围: 1~10 | 默认: 3 */
    KEY_STACK_MAX: 3,

    /** 魂石层数基础系数 | 范围: 1~100 | 默认: 8 */
    SOULSTONE_BASE_RATE: 8,

    /** Boss 击杀魂石奖励 | 范围: 0~100 | 默认: 10 */
    SOULSTONE_BOSS_BONUS: 10,

    // ============================================================
    // 10. 遗物系统
    // ============================================================

    /** 持有遗物上限 | 范围: 1~6 | 默认: 3 */
    RELIC_CAP: 3,

    /** 圣盾遗物护盾吸收量 | 范围: 1~50 | 默认: 8 */
    RELIC_SHIELD_AMOUNT: 8,

    /** 圣盾遗物触发间隔（秒）| 范围: 1~30 | 默认: 10 */
    RELIC_SHIELD_INTERVAL: 10,

    /** 圣光套装自动回复量 | 范围: 1~20 | 默认: 2 */
    SET_RADIANCE_AUTO_HEAL: 2,

    /** 圣光套装自动回复间隔（秒）| 范围: 1~10 | 默认: 3 */
    SET_RADIANCE_HEAL_INTERVAL: 3,

    /** 圣光套装假死 CD（秒）| 范围: 10~300 | 默认: 60 */
    SET_RADIANCE_FAKE_DEATH_CD: 60,

    /** 铁壁套装常保护盾吸收量 | 范围: 1~50 | 默认: 10 */
    SET_IRONWALL_SHIELD_AMOUNT: 10,

    /** 铁壁套装护盾触发间隔（秒）| 范围: 1~20 | 默认: 5 */
    SET_IRONWALL_SHIELD_INTERVAL: 5,

    // ============================================================
    // 11. 装备系统
    // ============================================================

    /** 橙色装备掉落概率 | 范围: 0~1 | 默认: 0.01 */
    LEGENDARY_DROP_CHANCE: 0.01,

    /** 武器类型数量 | 范围: 1~10 | 默认: 6 */
    WEAPON_TYPE_COUNT: 6,

    /** 前缀数量 | 范围: 1~30 | 默认: 12 */
    PREFIX_COUNT: 12,

    /** 后缀数量 | 范围: 1~30 | 默认: 12 */
    SUFFIX_COUNT: 12,

    /** 稀有度掉落权重: 普通 | 范围: 0~100 | 默认: 60 */
    RARITY_WEIGHT_COMMON: 60,

    /** 稀有度掉落权重: 魔法 | 范围: 0~100 | 默认: 30 */
    RARITY_WEIGHT_MAGIC: 30,

    /** 稀有度掉落权重: 稀有 | 范围: 0~100 | 默认: 9 */
    RARITY_WEIGHT_RARE: 9,

    /** 稀有度掉落权重: 传奇 | 范围: 0~100 | 默认: 1 */
    RARITY_WEIGHT_LEGENDARY: 1,

    // ============================================================
    // 12. 商店
    // ============================================================

    /** 钥匙价格（金币）| 范围: 1~50 | 默认: 10 */
    SHOP_KEY_PRICE: 10,

    /** 高级钥匙价格（金币）| 范围: 1~100 | 默认: 15 */
    SHOP_ADVANCED_KEY_PRICE: 15,

    /** 洗点券价格（金币）| 范围: 1~50 | 默认: 12 */
    SHOP_REROLL_PRICE: 12,

    /** 元素卷轴价格（金币）| 范围: 1~50 | 默认: 10 */
    SHOP_SCROLL_PRICE: 10,

    /** 商店随机装备最低价 | 范围: 1~50 | 默认: 8 */
    SHOP_EQUIP_MIN_PRICE: 8,

    /** 商店随机装备最高价 | 范围: 1~100 | 默认: 15 */
    SHOP_EQUIP_MAX_PRICE: 15,

    // ============================================================
    // 13. 广告系统
    // ============================================================

    /** 激励广告时长（秒）| 范围: 5~60 | 默认: 30 */
    AD_REWARD_DURATION: 30,

    /** 插屏广告最小间隔（局数）| 范围: 1~10 | 默认: 3 */
    AD_INTERSTITIAL_INTERVAL: 3,

    /** 每日激励广告最大观看次数 | 范围: 1~50 | 默认: 20 */
    AD_DAILY_LIMIT: 20,

    /** 同一广告位重播最小间隔（秒）| 范围: 10~300 | 默认: 60 */
    AD_SAME_POSITION_COOLDOWN: 60,

    /** 广告加载失败最大重试次数 | 范围: 0~10 | 默认: 3 */
    AD_RETRY_LIMIT: 3,

    /** 广告重试最小间隔（秒）| 范围: 1~30 | 默认: 3 */
    AD_RETRY_INTERVAL: 3,

    /** 觉悟战复活 HP 百分比 | 范围: 0.1~1.0 | 默认: 0.5 */
    AD_REVIVE_HP_RATIO: 0.5,
} as const;

/** 配置键名类型（用于类型安全）*/
export type GameConfigKey = keyof typeof GameConfig;
