# 地牢重做设计 · 节点路线图肉鸽模式 (v0.4.2)

> **本文件已被取代。**

当前有效版本为 **v0.4.3**:`docs/地牢重做_节点路线图肉鸽_设计v0.4.3.md`

v0.4.3 相对 v0.4.2 的 4 处执行清单小矛盾修正(作 Demo7 正式任务依据):
1. `RouteSaveAdapter` 归属统一 `assets/scripts/core/save/RouteSaveAdapter.ts`(从 `dungeon/route/**` 移除);
2. Demo7 模块清单修正为新增 **8** 个 `dungeon/route` 模块,且**不再新建** `NodeRouteTypes.ts`(已被 `RouteSaveTypes.ts` 取代);
3. `RouteSaveAdapter.saveRoute()` 无 active `RunSave` 时返回 `false`,删除 `_defaultRun() { return {} as any }`;
4. `RoomFlowController` route payload 改由 `setRouteEncounterContext(ctx)` 注入,禁临时加不存在字段。
(可接受优化) `EncounterRoomLayoutType` 改从 `DungeonGenerator` 导入别名。
