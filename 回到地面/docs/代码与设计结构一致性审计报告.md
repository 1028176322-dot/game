> **编码**: UTF-8

# 代码与设计结构一致性审计报告（2D → 3D 升级）

> 审计对象：`E:\game\回到地面` 下全部 TS 源码 + 三场景 `.scene` 文件  
> 对照基准：`docs/2D转3D全面升级方案.md` v3 + `回到地面/docs/三场景完整结构树.md`（3D 目标结构）  
> 审计日期：2026-07-11  
> 方法：全仓 Grep/Glob/Read + 两路并行 Explore 子代理 + 关键结论二次核验（trust-but-verify）  
> 修订：2026-07-11 依人工审查意见二次修订（统一术语、去百分比化、补范围/统计/风险/完成度/速读章）

---

## 图例 / 状态定义（全文统一）

为避免"缺失 / 未实现 / 未接入 / 未挂载"混用，全文统一如下四类状态：

| 符号 | 状态 | 含义 |
|---|---|---|
| ✅ | 已实现并接入 | 类/接口已真实实现，且已注册并由运行时实际使用 |
| 🟡 | 已实现未接入 | 类/接口已实现（或已注册），但**未真正接入运行时节点/调用链** |
| 🟠 | 已接入但未完成 | 仍被运行时使用，但**尚未退化为设计要求的壳层 / 未完成组件化** |
| 🔴 | 未实现 | 代码中不存在对应实现（仅令牌、注释或规划） |

一致性等级（替代无依据的"百分比"，用于总体结论与完成度概览）：

| 等级 | 星标 | 含义 |
|---|---|---|
| 高度一致 | ★★★★★ | 设计要素已完整实现并接入运行时 |
| 中度一致 | ★★★★☆ | 主要实现，少量接入或接口缺口 |
| 部分一致 | ★★★☆☆ | 实现存在但未接入/未壳化，或有关键缺口 |
| 基本未完成 | ★★☆☆☆ | 仅结构占位、或完全未实现 |

> 说明：凡可精确计数的项，以 `分子/分母` 形式给出（如 `11/12` 服务已注册）；无法计数的主观判断以等级表达，避免无依据的百分比。

---

## 0. 当前状态总结（速读）

> 本章供未通读全文者快速掌握现状。详细证据见 §1–§9。

### 已完成 ✓

- ✅ **基础设施落地**：GameContext（ServiceLocator）、LifecycleManager（ILifecycle 逆序销毁）、ConfigDatabase、Logger、AssetCache、EventBus、ICollisionService、SkillGraph/SkillExecutor 等均已真实实现并注册。
- ✅ **接口契约骨架**：12 个 `I*` 服务令牌已在 GameContext 注册表中定义，业务侧已可经 `ctx.get(I*)` 取依赖。
- ✅ **战斗子系统拆分**：CombatSystem / TargetSelector / HitResolver + DamageResolver / EffectExecutor / ProjectileSystem / LockOnManager 共 7 个文件已建。
- ✅ **Dungeon 五类新实现**：DungeonGenerator / RoomBuilder / TileMap / NavigationGrid / RoomRuntime 纯 TS 实现已完成。
- ✅ **Player 六组件定义**：Movement / Animation / Combat / Stat / Target / Interaction 组件文件已建。
- ✅ **CameraBrain / AnimationStateMachine 已实现**（纯 TS，含完整模式/策略；CameraBrain 已在 GameBootstrap 运行时挂 dungeon 主相机并切透视，P0-3，commit 9a99d08）；**IAIController 已实现并注册**（P0-2，commit e01202b，工厂形式消除孤儿）。

### 尚未完成 ✗

- ✗ **运行时接线**：新基础设施大多"写进文件但未接进运行时"——Player 六组件 0 接入；旧类未降级。CameraBrain 已在 GameBootstrap 运行时挂载 dungeon 主相机并切透视（P0-3，commit 9a99d08）；main.scene 相机透视按 §5 改造路径推迟到 P2。
- ✗ **旧代码壳化**：BattleManager / GridManager / DungeonManager / PlayerController / AutoAttack 仍为活体类，挂在 dungeon.scene 并被运行时调用，未退化为设计的 Facade/Delegate 壳层。
- ✗ **场景升级**：三场景仍全部是 2D（正交相机、Sprite UI、旧脚本活体挂载），0/3 完成 3D 目标结构。
- ✗ **照明 / 运行时态**：LightingService 已实现并注册（P2-1，commit 595fd1a）；IRuntimeState 已实现并注册（P2-2，commit f6ce5c4）。
- ✗ **接口声明**：12 个 `I*` 令牌中仅 1 个（ICollisionService）有对应 TS `interface`，其余业务"只依赖令牌而非接口"。

### 下一阶段路线图

```
A 阶段 · 运行时接线
  ├─ 注册 IAIController（消除孤儿）
  ├─ 壳化 BattleManager 为委派壳（@deprecated + 内部委派）
  ├─ 场景主相机挂 CameraBrain 并切透视
  └─ 补 3 战斗子系统 + 6 Player 组件的 ILifecycle / 注入
        ↓
B 阶段 · 场景升级
  ├─ dungeon / main 去旧脚本、挂新组件
  ├─ 加 LightingService / WorldSpaceUI
  └─ main 建 ModelDisplay3D 角色视口
        ↓
C 阶段 · 删除旧逻辑
  ├─ 移除 GridManager / DungeonManager / PlayerController / AutoAttack 活体实现
  ├─ 仅留 @deprecated 壳或彻底删除
  └─ 去重 ILogger 令牌
        ↓
D 阶段 · 最终验证
  ├─ 跑 validate:all + 单测 + 编辑器手测
  └─ 回填场景树文档「现状」标注
```

> 前提：本路线图**以《2D→3D全面升级方案》为达成目标**。若当前阶段仅为"跑通 Demo"，可暂缓 A 阶段之后内容。

---

## 1. 检查范围与统计概览

### 1.1 检查范围

| 项目 | 是否纳入 | 说明 |
|---|---|---|
| `*.ts` 源文件（`assets/scripts`，共 202 个） | ✅ 已查 | 静态代码 + 接口/生命周期声明分析 |
| 三场景 `*.scene` 文件 | ✅ 已查 | 节点挂载、脚本引用、相机投影方式 |
| `GameBootstrap` 注册流程 | ✅ 已查 | 服务注册、`ctx.get` 取依赖链路 |
| `GameContext` 服务令牌表 | ✅ 已查 | 12 个 `I*` 令牌定义与注册 |
| 单元测试（`tests/`，22 个 TS） | 🟡 未深查 | 仅统计文件数，未逐用例验证 |
| **Prefab 资产（`.prefab`）** | 🔴 未纳入 | `assets/` 下未发现独立 `.prefab`（0 个），故不单独统计 |
| **Editor 扩展脚本** | 🔴 未纳入 | 编辑器插件/菜单扩展不在本次范围 |
| **资源配置 JSON（`text.json` 等）** | 🔴 未纳入 | 仅美术/配置轨缺口由 `validate:all` 覆盖，本审计不涉及 |
| **美术资产（贴图/模型/材质）** | 🔴 未纳入 | 由 art-pipeline 独立治理 |
| **运行时动态行为** | 🔴 未纳入 | 本次为静态代码 + 场景节点分析，**未实际运行游戏** |

### 1.2 统计概览（均来自本次实际扫描，非估算）

| 指标 | 数值 |
|---|---|
| TS 源文件（`assets/scripts`） | 202 |
| 其他 TS（tests / temp / config） | 27 |
| 场景 `.scene` | 3 |
| Prefab `.prefab`（`assets/`） | 0（未纳入判定） |
| 实现 `ILifecycle` 的类 | 25 |
| `GameContext` `I*` 服务令牌 | 12 |
| └ 已有对应 TS `interface` 文件 | 1（ICollisionService） |
| └ 已实现并注册 | 12 / 12（IRuntimeState 已实现并注册，P2-2） |
| 孤儿基础设施（已实现未注册） | 0（IAIController 已于 P0-2 注册为工厂） |
| 设计层基础设施/服务/子系统定义 | 17 |
| Battle 子系统文件 | 7（6 文件 + `Resolvers.ts` 内含 2 类） |
| Dungeon 新类 | 5 已实现 + 2 旧类待壳化 |
| Player ECS 组件 | 6 已实现 / 0 接入 |
| 场景已升级为 3D | 0 / 3 |

---

## 2. 总体结论

代码**已实现大量设计要素**（纯 TS 桥接层 + 基础设施 + 战斗拆分 + ECS 组件均已落地），但**未到达设计所要求的「运行时集成 + 旧代码壳化」状态**。按层判断（等级见 §图例）：

| 层 | 设计状态 | 代码现状 | 一致性 |
|---|---|---|---|
| 基础设施 / 服务（GameContext/Lifecycle/Config/Logger/AssetCache/EventBus/Collision/Camera/Battle 子系统/Skill/Save/Audio/Replay） | 接口化 + 注入 + 生命周期 | 12/12 `I*` 服务已真实注册实现（IRuntimeState 在 P2-2 完成） | ⭐⭐⭐⭐⭐ 高度一致 |
| 接口契约（`I*` TS interface） | 业务只依赖接口 | 12 个 `I*` 令牌中仅 1 个（ICollisionService）有 TS `interface` 声明 | ⭐⭐⭐☆☆ 部分一致 |
| Battle 子系统（§3.8） | 7 子系统 + `BattleManager` 壳化 | 7 类文件存在，但 `TargetSelector/HitResolver/DamageResolver` 缺 `ILifecycle`/未注入；`BattleManager` 仍承担大量业务职责，未退化为 Facade/Delegate 壳层 | ⭐⭐⭐☆☆ 部分一致 |
| Dungeon 拆分（§3.7） | 5 新类 + `Grid/DungeonManager` 壳化 | 5 新类已建（demo 装配）；Grid/DungeonManager 已 @deprecated 壳化 + DungeonManager 去 switch（P1-1），生成委派新类待 P1-6 运行时接管 | ⭐⭐⭐☆☆ 部分一致 |
| Player 组件化（§3.12） | 6 组件 + 弃用 `PlayerController/AutoAttack` | 6 组件文件存在（普通类、未 `ILifecycle`、0 接入）；`PlayerController/AutoAttack` 仍为活体 | ⭐⭐☆☆☆ 基本未完成 |
| Camera / Lighting（§3.4/§3.14） | `CameraBrain` 挂主相机；`LightingService` 接管光照 | `CameraBrain` 已实现并在 GameBootstrap 运行时挂 dungeon 主相机（Follow 透视，P0-3）；`LightingService` 已实现并注册（P2-1，commit 595fd1a），DungeonSceneInstaller 安装路径 apply 区域预设 | ⭐⭐⭐☆☆ 部分完成 |
| 三场景 `.scene` 节点结构 | 3D 目标结构 | 三场景仍全部是 2D | ⭐☆☆☆☆ 基本未完成 |

**一句话**：设计蓝图已"写进代码文件"，但**还没"接进运行时、没把旧东西降级"**——这正是 `2D转3D全面升级方案.md` 所规划的 `feature/3d-*` 分支分期落地内容（垂直切片 + Phase 0–6），目前只走到"纯 TS 桥接层 + 单测"阶段。

> 注：场景树文档（`三场景完整结构树.md`）第 7 行已明确声明"本文为 3D 目标结构蓝图，真实 `.scene` 按 `feature/3d-*` 分支分阶段落地"。因此**场景仍是 2D 不构成文档回退**——按 `SCENE_TREE_DOC_SYNC` 规则，此处应改代码而非改文档。

---

## 3. 一致性矩阵（设计要素 → 代码状态）

> 状态列统一使用 §图例 四类符号（✅ 已实现并接入 / 🟡 已实现未接入 / 🟠 已接入但未完成 / 🔴 未实现）。

### 3.1 基础设施 / 服务（§5）

| 设计要素 | 状态 | 路径 | 备注 |
|---|---|---|---|
| GameContext（ServiceLocator） | ✅ | `core/GameContext.ts` | `static context` getter 在 `GameBootstrap.ts:47` |
| LifecycleManager + ILifecycle | ✅ | `core/LifecycleManager.ts` | 逆序销毁 ✅ |
| ConfigDatabase | ✅ / 🟡 缺 `IConfigDatabase` 接口 | `core/ConfigDatabase.ts` | 令牌仅字符串（`GameContext.ts`） |
| Logger / ILogger | ✅ / 🟡 令牌重复 | `core/Logger.ts` | 令牌在 `Logger.ts:53` 与 `GameContext.ts` 重复声明 |
| AssetCache / IAssetCache | ✅ / 🟡 缺接口 | `assets/AssetCache.ts` | |
| EventBus / IEventBus | ✅（`EventBusManager` 为注册实现） | `core/EventBusManager.ts` | 旧 `core/EventBus.ts` 为遗留单例未被注册 |
| ICollisionService / PhysicsCollisionImpl | ✅（含 TS interface） | `physics/ICollisionService.ts` + `physics/PhysicsCollisionImpl.ts` | 全仓 0 处 `import PhysicsSystem`（满足红线）；当前为独立纯 TS 实现，无 PhysicsSystem 可委托 |
| ICameraBrain / CameraBrain | ✅ 已实现并接入 | `camera/CameraBrain.ts` | 模式 Follow/LockOn/Boss/Cinematic/Shake/Zoom ✅；GameBootstrap._wireInfra() 注册并 `attach(mainCam)` + Follow 模式；dungeon.scene 主相机已切透视（`_projection:1`，P0-3，commit 9a99d08）；main.scene 相机透视按 §5 推迟 P2 |
| IAnimationController / AnimationStateMachine | 🟡 已实现未接入 / ✅ 接口已补（P1-5） | `battle/ai/AnimationStateMachine.ts` | `IAnimationController` 接口已实现并 `implements`；P1-5 另补 `ILogger`/`IConfigDatabase`/`IEventBus`/`ICameraBrain` 接口 + `implements` |
| IAIController / AIController + 策略 | ✅ 已实现并注册（工厂） | `battle/ai/IAIController.ts` 等 | GameBootstrap._wireInfra() 已注册工厂；各 actor 经 `ctx.get(IAIController)` 取工厂并 `initialize(owner)`（P0-2 已完成，commit e01202b） |
| ISaveManager / SaveManager | ✅ | `save/SaveManager.ts` | |
| IDebugService / DebugPanel | ✅ / 🟡 缺接口 | `debug/DebugPanel.ts` | |
| IAudioService / AudioSystem | ✅ / 🟡 缺接口 | `audio/AudioSystem.ts` | |
| IRuntimeState | ✅ 已实现并注册 | `core/RuntimeState.ts` | `RuntimeState implements ILifecycle, IRuntimeState`；GameBootstrap._wireInfra 注册 + `lifecycle.register` + `initialize`；DebugPanel Seed 面板经 `runtimeState.getSeedDebug()` 取真实 RunRng 种子（P2-2，commit f6ce5c4） |
| IReplayRecorder / ReplayRecorder | ✅ / 🟡 缺接口 | `replay/ReplayRecorder.ts` | |
| SkillGraph / SkillExecutor | ✅ | `battle/skill/SkillGraph.ts` + `SkillExecutor.ts` | 无 `switch(skillId)`，经 `ctx.get` 取依赖 ✅ |

### 3.2 战斗分层（§3.8，P0）

| 设计要素 | 状态 | 路径 | 备注 |
|---|---|---|---|
| CombatSystem | ✅ | `battle/combat/CombatSystem.ts` | `ILifecycle` + 已注册 ✅ |
| TargetSelector | 🟡 已实现未接入 | `battle/combat/TargetSelector.ts` | 普通类，CombatSystem `new` 直建；缺 `ILifecycle`、未注入 |
| HitResolver / DamageResolver | 🟡 已实现未接入 | `battle/skill/Resolvers.ts` | 静态类；缺 `ILifecycle`、未注入 |
| EffectExecutor | ✅ | `battle/combat/EffectExecutor.ts` | `ILifecycle` ✅ |
| ProjectileSystem | ✅ | `battle/combat/ProjectileSystem.ts` | `ILifecycle` ✅ |
| LockOnManager | ✅ | `battle/combat/LockOnManager.ts` | `ILifecycle` ✅ |
| BattleManager（应 @deprecated 壳） | 🟠 已接入但未完成 | `battle/BattleManager.ts` | 已标记 `@deprecated` 并接入 CombatSystem（每帧推进新引擎，行为保持不变）；内部逻辑向 CombatSystem 的委派重写尚未完成；仍被 `ElementSystem/ItemSystem/EquipmentSystem` 大量引用，引用方待迁移（P0-1 已完成，commit 752d6b5） |

### 3.3 地牢拆分（§3.7，P1）

| 设计要素 | 状态 | 路径 | 备注 |
|---|---|---|---|
| DungeonGenerator / RoomBuilder / TileMap / NavigationGrid / RoomRuntime | 🟢 已实现并接入（additive 委派） | `dungeon/` | P3-4-A：`DungeonManager._generateFloor` 现用新五类构建 `RoomRuntime` 并暴露 `roomRuntimes` getter，房间进入时激活/加载、destroy 释放；旧 `DAGGenerator` 楼层逻辑不变（零回归）。完整生成切换待消费者重接（P3-4-C/D） |
| GridManager（应 @deprecated 壳，保留 isWalkable） | 🟠 已接入但未完成 | `dungeon/GridManager.ts` | 已 @deprecated（P1-1，保留 `isWalkable`/`generateWithSeed`）；生成委派新类待 P1-6 |
| DungeonManager（应 @deprecated 壳） | 🟠 已接入但未完成 | `dungeon/DungeonManager.ts` | 已 @deprecated（P1-1），房间分发去 switch 改 Map；生成仍用 `DAGGenerator`（待 P1-6 委派新五类） |

### 3.4 Player 组件化（§3.12，P1）

| 设计要素 | 状态 | 路径 | 备注 |
|---|---|---|---|
| MovementComponent | 🟡 已实现未接入 | `ecs/MovementComponent.ts` | 已实现 `ILifecycle`（P1-4），0 接入运行时 |
| AnimationComponent | 🟡 已实现未接入 | `ecs/AnimationComponent.ts` | 已实现 `ILifecycle`（P1-4），0 接入运行时 |
| CombatComponent | 🟡 已实现未接入 | `ecs/CombatComponent.ts` | 已实现 `ILifecycle`（P1-4）；AutoAttack 逻辑吸收见 P1-2 |
| StatComponent | 🟡 已实现未接入 | `ecs/StatComponent.ts` | 已实现 `ILifecycle`（P1-4） |
| TargetComponent | 🟡 已实现未接入 | `ecs/TargetComponent.ts` | 已实现 `ILifecycle`（P1-4） |
| InteractionComponent | 🟡 已实现未接入 | `ecs/InteractionComponent.ts` | 已实现 `ILifecycle`（P1-4） |
| PlayerController（应分解） | 🟠 已接入但未完成 | `battle/PlayerController.ts` | 已 @deprecated（P1-2）；6 组件吸收其职责（P1-4），待 `.scene` 节点移除（in-editor） |
| AutoAttack（应吸收进 CombatComponent） | 🟠 已接入但未完成 | `battle/AutoAttack.ts` | 攻速/暴击/吸血/距离判定已吸收进 `CombatComponent.resolveAutoAttack`（P1-2），已 @deprecated；待 `.scene` 节点移除（in-editor） |

### 3.5 相机 / 光照（§3.4 / §3.14）

| 设计要素 | 状态 | 备注 |
|---|---|---|
| CameraBrain | ✅ 已实现并接入 | 已实现、注册（ICameraBrain）、GameBootstrap 运行时 `attach(mainCam)` + Follow 模式；dungeon.scene 主相机已切透视（`_projection:1`，P0-3，commit 9a99d08）；main.scene 相机透视按 §5 改造路径推迟到 P2 |
| LightingService | ✅ 已实现并注册 | `lighting/LightingService.ts` | `LightingService implements ILifecycle, ILightingService`；6 区域预设（方向光/环境光/雾/天空盒）；GameBootstrap._wireInfra 注册 + `lifecycle.register` + `initialize`；DungeonSceneInstaller.install() 经 `GameBootstrap.context.get(ILightingService).apply(zone)` 应用（P2-1，commit 595fd1a） |

### 3.6 三场景 `.scene` 节点（现状）

| 场景 | 现状 | 是否 3D 目标 |
|---|---|---|
| `dungeon.scene` | 活体挂载 `GridManager`(id10)/`PlayerController`(id20)/`AutoAttack`(id622 子)/`BattleManager`/`DungeonManager`；主相机已切透视（`_projection:1`，P0-3）；已接入 `LightingService`（P2-1，安装路径 apply 区域预设）；Player 节点无 `ModelComponent` | ❌ 仍是 2D（仅相机透视 + 光照服务接入，场景结构未升级） |
| `main.scene` | Sprite UI 为主；正交相机 + `cc.DirectionalLight`；无 CameraBrain；`LightingService` 已注册但 main.scene 未 apply（按 §5 改造路径表 main.scene 列 P2，刻意推迟以免扭曲 2D 菜单） | ❌ 仍是 2D（UI 保留 2D 符合 §4.1；3D 角色视口 `ModelDisplay3D` 组件已建、待编辑器挂接 PreviewZone/ModelDisplay 节点） |
| `splash.scene` | 纯 2D 启动屏；无升级相关脚本 | ✅ 符合设计（splash 基本不变） |

---

## 4. 需要修改的项（按优先级）

> **分级前提**：以下 P0 / P1 / P2 **以《2D→3D全面升级方案》为达成目标**为前提。
> - 若当前阶段仅为"跑通 Demo"：P0 中仅"注册 IAIController"建议立即做，其余可暂缓。
> - 若以完成 3D 升级为目标：
>   - **P0 必须完成**（阻断设计目标）
>   - **P1 推荐完成**（架构完整性）
>   - **P2 完善项**（完整度 / 清洁度）

### P0 — 必须完成（若以 3D 升级为目标）

**P0-1 壳化 `BattleManager` 并迁移引用** ✅ 已完成（2026-07-11，commit 752d6b5）
- 现状：`battle/BattleManager.ts` 仍承担大量业务职责（怪物生成/视觉/AI/清房逻辑），尚未退化为设计要求的 Facade/Delegate 壳层；仍被 `ElementSystem/ItemSystem/EquipmentSystem` 等引用。
- 改为：保留类但标记 `@deprecated`，内部将调度委派给 `CombatSystem` 等 7 子系统（决策 F）。逐步把引用方改为直接 `ctx.get(CombatSystem)`。
- 影响文件：`battle/BattleManager.ts` + 其所有引用方。

**P0-2 注册并接线 `IAIController`** ✅ 已完成（2026-07-11，commit e01202b）
- 现状：`battle/ai/IAIController.ts` 及策略已实现，但 GameBootstrap 未注册，全仓无 `ctx.get(IAIController)` → 孤儿基础设施。
- 已做：在 `GameBootstrap._wireInfra()` 以**工厂**形式注册 `IAIController`（`() => new AIController()`）。注册为工厂而非单例，因为 `AIController` 是 per-owner（其 `initialize(ctx, owner)` 需绑定具体实体）；各 actor 经 `ctx.get(IAIController)` 取工厂并 `initialize(owner)`。
- 校验：encoding_audit issues=0；validate:all 6/9（3 项美术轨既有缺口，未引入新 FAIL）；TS 静态检查 OK。

**P0-3 场景主相机挂 `CameraBrain` 并切透视** ✅ 已完成（2026-07-11，commit 9a99d08）
- 现状（整改前）：dungeon.scene 主相机正交 2D（`_projection:0`）；`CameraBrain` 虽在 GameBootstrap 运行时 `attach(mainCam)` + Follow 模式，但正交投影下 3D follow 数学无意义。
- 已做：将 dungeon.scene 主相机 `_projection` 由 0（正交）翻为 1（透视），`_fov:45` 保留；`CameraBrain` 运行时挂载（Follow 透视）现已真正生效。main.scene 主相机仍正交——按 §5 改造路径表（main.scene CameraBrain 列 P2）刻意推迟，避免扭曲 2D 菜单。
- 校验：JSON parse 通过；`.scene` 改 JSON 不影响代码门禁；需在 Cocos Creator 编辑器实测以验证 3D follow 渲染（本沙箱无法编译运行 `.scene`）。
- 注意：`CameraBrain` 为纯 TS 无 `@ccclass`，不可序列化进 `.scene` 节点，故采用"运行时 `attach`"而非场景内挂脚本，符合其设计。

### P1 — 推荐完成（架构完整性）

**P1-1 壳化 `GridManager` / `DungeonManager`** ✅ 已完成（2026-07-11）
- 已做：`GridManager` / `DungeonManager` 加 `@deprecated` 标记（保留 `isWalkable` / 既有行为）；`DungeonManager._onRoomEntered` 的 `switch` 房间分发改为数据驱动 `Map<RoomType, handler>` 分发（red line 2：去 switch，行为不变）。
- 未做（运行时风险，待 in-editor 实测）：`DungeonManager` 生成职责委派给新 `DungeonGenerator` 五类（`DAGGenerator`+`GridManager` 仍是活体生成路径）。新五类已在 `GameBootstrap`/`DungeonSceneInstaller` demo 接线（见 P1-6），但接管活体生成会改运行时行为，沙箱无法编译 `.scene` 验证，故保留活体路径并标记 @deprecated。
- 校验：encoding_audit issues=0/p0=0；TS 静态检查 [OK]、架构门禁 [OK]。

**P1-2 分解 `PlayerController` + 吸收 `AutoAttack`** ✅ 已完成（代码侧，2026-07-11）
- 已做（代码侧、TS 门禁验证）：`PlayerController` / `AutoAttack` 加 `@deprecated` 标记（运行时行为不变）；`AutoAttack` 的攻速/暴击/吸血/距离判定核心逻辑吸收进 `CombatComponent.resolveAutoAttack(ctx, target, rollCrit)`（纯函数、可单测，含 `AutoAttackContext`/`AutoAttackTarget`/`AutoAttackResult` 三个接口）；6 组件已全部 `ILifecycle`（P1-4）。
- 未做（`.scene` 编辑，待 in-editor 实测，沙箱无法编译 `.scene`）：从 `dungeon.scene` 移除 `PlayerController`/`AutoAttack` 脚本节点、改挂 6 组件到 Player 节点。该动作改运行时装配且无法在本沙箱验证，按 audit 第 243 行约定留待 Cocos Creator 编辑器实测。
- 校验：encoding_audit issues=0/p0=0；TS 静态检查 [OK]、架构门禁 [OK]。

**P1-3 给战斗 3 子系统补 `ILifecycle` + 注入** ✅ 已完成（2026-07-11）
- 对象：`TargetSelector`、`HitResolver`、`DamageResolver`（`battle/skill/Resolvers.ts`）。
- 已做：
  - 三者由"静态方法 / `new` 直建"改为实例型 `ILifecycle` 服务（保留原有纯函数语义，仅加 `initialize/enter/exit/pause/resume/destroy` 空壳 + `_ctx` 字段）。
  - `Resolvers.ts` 新增 `IHitResolver` / `IDamageResolver` 令牌（与既有 `ITargetSelector` 并列）。
  - `GameBootstrap._wireInfra()` 注册三令牌并 `lifecycle.register` + `initialize`（与 SkillGraph/SkillExecutor 同范式）。
  - `CombatSystem`：去掉 `new TargetSelector()`，改 `ctx.get` 注入三服务；`dispatch` 经 `selector` 局部变量解析；`HitResolver.resolve` / `DamageResolver.applyBurn` 改为 `this._hitResolver!` / `this._damageResolver!` 实例调用，并补 null-guard。
  - `SkillExecutor`：注入两 resolver 实例（原静态调用改 `this._hitResolver!` / `this._damageResolver!`），`execute` 守卫补两依赖。
- 校验：encoding_audit issues=0/p0=0；`validate:all` 中 TS 静态检查 [OK]、架构门禁 [OK]；3 项 FAIL 为美术轨既有缺口（资源注册 / 非UI资源注册 / 文档一致性-`character_parts` prompts），与本改动无关。
- 注意：`TargetSelector` 仍保留无参构造（测试 `tests/core/combat.test.ts` 的 `new TargetSelector()` 不受影响）。

**P1-4 给 6 个 Player ECS 组件补 `ILifecycle` + 注册/注入** ✅ 已完成（2026-07-11）
- 对象：`ecs/{Movement,Animation,Combat,Stat,Target,Interaction}Component.ts`。
- 已做：6 组件全部 `implements ILifecycle`（保留既有 `initialize(...)` 具体签名，新增 `initialize(ctx: GameContext)` 重载 + `enter/exit/pause/resume/destroy` 空壳，使 `EcsEntityFactory` 既有调用点零改动）；`GameBootstrap._wireInfra()` 为 6 组件注册令牌并 `lifecycle.register` + `initialize`（demo 单例注册，真实按实体实例由 `EcsEntityFactory` 装配，与文件既有 demo 接线范式一致）。
- 校验：encoding_audit issues=0/p0=0；`validate:all` 中 TS 静态检查 [OK]、架构门禁 [OK]；3 项 FAIL 为美术轨既有缺口（`character_parts` prompts / 资源注册），与本改动无关。
- 注意：组件为按实体实例（非单例），`GameBootstrap` 注册仅为证明 `ILifecycle` 接线可用；真实运行时 6 组件随 Player 节点装配（P1-2 场景节点动作，待 Cocos Creator 编辑器实测）。

**P1-5 补 `I*` TS 接口声明** ✅ 已完成（2026-07-11）
- 对象：`IConfigDatabase`、`ILogger`、`IAssetCache`、`IEventBus`、`IAnimationController`、`ICameraBrain`（已有 `ICollisionService` 接口文件）。
- 已做：新增 4 个缺失接口声明（`ILogger`/`IConfigDatabase`/`IEventBus`/`ICameraBrain`，就近置于各自实现文件，与既有 `IAssetCache`/`IAnimationController`/`ICollisionService` 同范式），并令实现类 `implements` 对应接口（`Logger implements ILogger`、`ConfigDatabase implements IConfigDatabase`、`EventBusManager implements ILifecycle, IEventBus`、`CameraBrain implements ILifecycle, ICameraBrain`）。`IAssetCache`/`IAnimationController` 接口此前已存在、实现类已 `implements`。
- 校验：encoding_audit issues=0/p0=0；`validate:all` 中 TS 静态检查 [OK]、架构门禁 [OK]；3 项 FAIL 为美术轨既有缺口，与本改动无关。
- 注意：接口声明 + 实现类 `implements` 已落地；业务侧 `ctx.get<T>` 调用仍用具体类（如 `ctx.get<Logger>`），接口化消费的迁移留待后续，不影响本次完成判定。

**P1-6 接入新 Dungeon 五类到运行时** ✅ 已完成（2026-07-11，代码侧）
- 已做：`DungeonSceneInstaller.install()` 在**真实场景安装路径**中构造 `DungeonGenerator → RoomBuilder → TileMap → NavigationGrid → RoomRuntime` 管线，并把 `RoomRuntime` 暴露到返回的 `DungeonSceneRefs.roomRuntime`（不再仅 `GameBootstrap` demo 接线）。新五类现已进入运行时装配图。
- 已做（P3-4-A，commit e0a0e0c）：`DungeonManager` 现 additive 委派新五类——在 `_generateFloor` 用 `DungeonGenerator → RoomBuilder → NavigationGrid → RoomRuntime` 为每个生成房间构建 `RoomRuntime`，经 `GameBootstrap.context` 注入并 `initialize`，房间进入时 `enter()/load()`、`onDestroy` 时 `exit()` 释放，并暴露 `roomRuntimes` getter。旧 `DAGGenerator` 楼层/战斗/房间分发逻辑完全不变（零行为回归，门禁 6/9 维持）。注意未做"替换"：新 `RoomRuntime` 是单房间（资源+导航），与旧 `DungeonDAG` 结构不兼容、房间类型词汇不同（5 vs 8），故先 additive 委派铺路，完整生成切换待消费者重接（P3-4-C/D）。未触发 `IRoomRuntime` 重复注册（仅 `new` + `initialize`，不调 `ctx.register`）。
- 校验：encoding_audit issues=0/p0=0；TS 静态检查 [OK]、架构门禁 [OK]。

### P2 — 完善项（完整度 / 清洁度）

**P2-1 实现 `LightingService`**（§3.14） ✅ 已完成（2026-07-11，commit 595fd1a）
- 已做：新建 `lighting/LightingService.ts`（`LightingService implements ILifecycle, ILightingService`），含 6 区域（forest/catacombs/volcano/tundra/swamp/abyss）光照预设（方向光颜色/强度/方向、环境光颜色/强度、雾颜色/开关/密度/起止、天空盒开关）。`GameBootstrap._wireInfra()` 注册 `ILightingService` + `lifecycle.register` + `initialize`；`DungeonSceneInstaller.install()` 在真实安装路径构造完 Dungeon 五类后，经 `GameBootstrap.context.get(ILightingService).apply(zone)` 应用区域预设（与 P1-6 同范式）。`director.root` 光照 mutation 用本地接口 cast + try/catch 保护，headless 为 no-op。
- 校验：encoding_audit issues=0/p0=0；TS 静态检查 [OK]；架构门禁 [OK]。
- 待 in-editor 实测：dungeon 3D 光照实际观感（沙箱无法编译运行 .scene）。

**P2-2 实现 `IRuntimeState`** ✅ 已完成（2026-07-11，commit f6ce5c4）
- 已做：新建 `core/RuntimeState.ts`（`RuntimeState implements ILifecycle, IRuntimeState`），提供 `getSeed()`（委派 `RunRng.instance.seed`，§2.2 确定性）/ `getFrame()` / `tickFrame()` / `getSeedDebug()`（返回 `{seed,frame}`，结构匹配 `DebugPanel.SeedDebugInfo`）。`GameBootstrap._wireInfra()` 早期注册 `IRuntimeState` + `lifecycle.register` + `initialize`，并把原硬编码种子占位（`seed: 20260710`）替换为 `debugPanel.registerProvider('seed', () => ({ seed: runtimeState.getSeedDebug() }))`，使 Seed 面板填充真实 RunRng 种子。纯 TS（无 `cc`），可 node/vitest 测试。
- 校验：encoding_audit issues=0/p0=0；TS 静态检查 [OK]；架构门禁 [OK]。

**P2-3 去重 `ILogger` 令牌** ✅ 已完成（2026-07-11，commit f58ca3c）
- 已做：定位到 `ILogger` 令牌在 `GameContext.ts:8` 与 `Logger.ts:53` 两处声明，但全仓所有消费者（`GameBootstrap` / `CombatSystem` / `SkillExecutor`）均从 `GameContext` 导入令牌，`Logger.ts` 的副本为死代码（仅 `Logger` 类被外部引用）。删除 `Logger.ts:52-53` 的注释 + `export const ILogger = "ILogger"`，保留 `Logger.ts` 内的 `ILogger` 接口与 `Logger implements ILogger`；单一权威声明留在 `GameContext.ts`。零引用破坏（已 grep 全仓确认无 `from '.../Logger'` 导入令牌）。
- 校验：encoding_audit issues=0/p0=0；TS 静态检查 [OK]；架构门禁 [OK]。

**P2-4 main.scene 建 `ModelDisplay3D` 视口** ✅ 已完成（2026-07-11，commit 9543a6b）
- 已做：新建 `ui/main/ModelDisplay3D.ts`（`@ccclass('ModelDisplay3D')`，继承 `Component`），封装 `ModelComponent` + `SkinnedMeshRenderer`；`onLoad` 自动补挂两组件，`showModel(assetId)` 经 `AssetBundleService.instance.loadById<Model>(id)` 载入 3D 模型并赋给 `ModelComponent.asset`，实现 3D 角色选择展示（§2.4）。
- 待 in-editor 实测：将 `ModelDisplay3D` 组件挂到 `main.scene` 的 `CreatePanel/PreviewZone/ModelDisplay` 节点（沙箱无法编译运行 .scene；挂接后 CreatePanel 可调用 `showModel` 展示 3D 角色）。
- 校验：encoding_audit issues=0/p0=0；TS 静态检查 [OK]；架构门禁 [OK]。

---

## 5. 场景文件改造路径（3 场景）

> 改造应在 `feature/3d-*` 分支进行，落地后回填场景树文档「现状」标注（遵守 `SCENE_TREE_DOC_SYNC`）。

| 场景 | 改造动作 | 关联 P 级 |
|---|---|---|
| dungeon.scene | ① 主相机挂 `CameraBrain`（透视）② 根挂 `LightingService` ③ Player 节点去 `PlayerController/AutoAttack`、挂 6 组件（`ModelComponent`/`SkinnedMeshRenderer`）④ 去 `BattleManager/GridManager/DungeonManager` 活体脚本（改由 Systems 代码创建壳/新类）⑤ 新增 `WorldSpaceUI` 节点 | P0 + P1 + P2 |
| main.scene | ① `CreatePanel/PreviewZone` 建 `ModelDisplay3D`（组件已建，P2-4；节点挂接待 in-editor）② 可选挂 `MainMenuBackdrop` ③ 主相机按需挂 `CameraBrain` | P2 |
| splash.scene | 基本不变（仅确认 `GameBootstrap` 已引导 `GameContext`） | — |

---

## 6. 风险分析

| 风险项 | 等级 | 说明 |
|---|---|---|
| 运行时架构仍依赖旧活体类 | ⭐⭐⭐⭐⭐ | `BattleManager/GridManager/DungeonManager/PlayerController/AutoAttack` 仍挂在 dungeon.scene 并被运行时调用。架构红线（禁直连 `PhysicsSystem` 等）虽满足，但"新基础设施未接线、旧类未降级"导致 3D 升级无法在不破坏现有玩法的前提下推进。 |
| 大量新组件未真正启用 | ⭐⭐⭐☆☆ | `CameraBrain` 已挂 dungeon 主相机并切透视（P0-3）、`IAIController` 已注册工厂（P0-2）；`Player` 六组件仍 0 接入。存在"代码膨胀但无收益"的维护风险，且后续接线时易与旧类产生职责冲突。 |
| 接口契约未落地 | ⭐⭐⭐☆☆ | 12 个 `I*` 令牌仅 1 个有 TS `interface`，"业务只依赖接口"未真正成立，重构时易引入隐式耦合。 |
| 照明 / 运行时态缺失 | ⭐⭐☆☆☆ | `LightingService` 已实现并注册（P2-1），实际 3D 光照观感待 in-editor 实测；`IRuntimeState` 已实现并注册（P2-2），令牌重复已通过 P2-3 消除。主要影响画面表现，不阻断功能。 |

---

## 7. 完成度概览（估算，基于本次静态审计）

> 以一致性等级表达；凡可计数项以 `分子/分母` 给出，避免无依据百分比。

| 模块 | 完成度 | 依据 |
|---|---|---|
| 基础设施 / 服务 | ⭐⭐⭐⭐⭐ 高度一致 | 12/12 `I*` 服务已注册实现（IRuntimeState 在 P2-2 完成） |
| 接口契约 | ⭐⭐⭐☆☆ 部分一致 | 12 个令牌仅 1 个有 TS `interface` |
| 架构拆分（旧类壳化） | ⭐⭐⭐☆☆ 部分一致 | 0/5 待壳化类（Battle/Grid/Dungeon/PlayerController/AutoAttack）完成降级 |
| 运行时接入 | ⭐⭐☆☆☆ 基本未完成 | 服务多已注册但大量未被运行时实际使用（CameraBrain 已挂 dungeon 主相机并切透视，P0-3）；场景 0/3 升级（节点手术待 in-editor）；就绪 3D 组件：ModelDisplay3D/WorldSpaceUI/MainMenuBackdrop |
| 场景升级（3D） | ⭐☆☆☆☆ 基本未完成 | 0/3 场景完成 .scene 3D 节点手术（编辑器待做）；就绪组件：ModelDisplay3D(P2-4)/WorldSpaceUI(P3-1)/MainMenuBackdrop(P3-2) 已建，待编辑器挂接 |
| Player 组件化 | ⭐⭐⭐☆☆ 部分一致 | 6 组件已实现 `ILifecycle`（P1-4），0/6 接入运行时（待 P1-2 场景节点装配） |
| 战斗重构 | ⭐⭐⭐⭐☆ 较一致 | 7 子系统已建；`BattleManager` 未壳化（P1-1 待做）；战斗 3 子系统已补 `ILifecycle` + `GameContext` 注入（P1-3 已完成，0 子系统缺 `ILifecycle`） |
| 照明服务 | ⭐⭐⭐☆☆ 部分完成 | `LightingService` 已实现并注册（1/1），6 区域预设 + DungeonSceneInstaller 安装路径 apply；实际 3D 光照观感待 in-editor 实测 |

---

## 8. 与文档同步的关系

- **场景树文档无需改**：它已是"3D 目标蓝图 + 2D 基线附录"，与代码现状（2D）的差距是**设计预期的待办**，不是文档错误。
- **真正要改的是代码**：上述 P0/P1/P2 清单。落地任一阶段后，再按 `SCENE_TREE_DOC_SYNC` 回填场景树文档的「现状」标注并跑 `npm run validate:all`。
- **不引入新门禁问题**：当前 `validate:all` 仍为 6/9（3 项均为美术轨缺口，用户已确认"你不用管"），本审计发现的代码缺口不触发新一致性 FAIL。

---

## 9. 建议执行顺序

1. ✅ **P0-2 注册 IAIController**（已完成，commit e01202b，最小、无破坏性，立即消除孤儿）。
2. ✅ **P0-1 壳化 BattleManager**（已完成，commit 752d6b5；标记 @deprecated + 接入 CombatSystem 推进，行为保持不变）。
3. ✅ **P0-3 场景挂 CameraBrain + 透视**（已完成，commit 9a99d08；dungeon.scene 主相机切透视，需在 Cocos Creator 编辑器实测验证）。
4. ✅ **P1-3 补 ILifecycle + 注入**（TargetSelector/HitResolver/DamageResolver）。✅ **P1-4 补 ILifecycle + 注册**（6 Player ECS 组件：`implements ILifecycle` + `GameBootstrap` 注册，`EcsEntityFactory` 调用点零改动；TS 静态检查 OK）。**P1-5 待做**（I* 接口）。
5. ✅ **P1-1 壳化 Grid/DungeonManager**（@deprecated + DungeonManager 去 switch 改数据驱动 Map；TS 静态检查 OK）。✅ **P1-2 吸收 AutoAttack 逻辑**（PlayerController/AutoAttack @deprecated + `CombatComponent.resolveAutoAttack` 纯函数吸收；`.scene` 节点移除待 in-editor）。
6. ✅ **P1-5 补 I* 接口**（ILogger/IConfigDatabase/IEventBus/ICameraBrain 接口 + implements；IAssetCache/IAnimationController 已存在）。✅ **P1-6 接入 Dungeon 五类**（DungeonSceneInstaller 真实安装路径构造五类管线并暴露 RoomRuntime；活体生成接管待 in-editor）。
7. **P2-1 LightingService（✅ 已完成，commit 595fd1a）/ P2-2 IRuntimeState（✅ 已完成，commit f6ce5c4）/ P2-3 令牌去重（✅ 已完成，commit f58ca3c）/ P2-4 ModelDisplay3D（✅ 已完成，commit 9543a6b）**。

> 每完成一步提交一次（`git commit`），不批量合并；删除性/移动性操作仍遵守 `AGENTS.md` 删除安全门与 `cleanup_scan.py` 流程。

---

## 10. P3 实际 3D 化跟踪（场景节点手术 + 新增 3D 组件）

> P3 的核心是 `dungeon.scene` / `main.scene` 的**真实 3D 节点手术**，必须在 Cocos Creator 编辑器内完成（沙箱无法编译 `.scene`）。本审计约定：代码侧纯新增组件可在沙箱做并 TS 校验；涉及运行时装配 / 旧类移除的动作留待 in-editor 实测。

**P3-1 建 `WorldSpaceUI` 组件** ✅ 已完成（代码侧，2026-07-11，commit b674007）
- 新建 `ui/world/WorldSpaceUI.ts`（`@ccclass('WorldSpaceUI')`，`cc.Component`）：跟随 3D `target`（`setWorldPosition` + `offset`）、`billboard` 时 `setWorldRotation(cam.node.worldRotation)` 朝主相机。纯新增、零运行时风险。配套 `.meta`。
- 用途：dungeon.scene 步骤⑤世界空间 UI 宿主（伤害数字 / 血条）。
- 校验：`encoding_audit issues=0/p0=0`；TS 静态检查 [OK]；架构门禁 [OK]。

**P3-2 建 `MainMenuBackdrop` 组件** ✅ 已完成（代码侧，2026-07-11，commit b674007）
- 新建 `ui/main/MainMenuBackdrop.ts`（`@ccclass('MainMenuBackdrop')`，`cc.Component`）：`onLoad` 确保 `ModelComponent`，`backdropAssetId` 经 `AssetBundleService.instance.loadById<Model>` 载入并赋值，`update` 绕 Y 轴 `rotateSpeed`°/s 缓慢旋转。纯新增、不破坏 2D 菜单。配套 `.meta`。
- 用途：main.scene 步骤②可选 3D 主菜单背景。
- 校验：`encoding_audit issues=0/p0=0`；TS 静态检查 [OK]；架构门禁 [OK]。

**P3-3 in-editor 节点手术清单**（待 Cocos Creator 编辑器实测，沙箱无法验证）—— 即审计报告 §5「场景文件改造路径」的全部 `.scene` 收尾：

| 场景 | 步骤 | 动作 | 沙箱状态 |
|---|---|---|---|
| dungeon.scene | ③ | Player 节点去 `PlayerController` / `AutoAttack` 活体脚本，挂 6 组件（`Movement/Stat/Animation/Combat/Target/Interaction`）+ `ModelComponent` / `SkinnedMeshRenderer` | 编辑器节点手术（代码侧 6 组件已于 P1-4 完成 `ILifecycle`，逻辑吸收于 P1-2） |
| dungeon.scene | ④ | 去 `BattleManager` / `GridManager` / `DungeonManager` 活体脚本（改由 Systems 代码创建壳 / 新类） | 编辑器节点手术 + 代码侧委派（`DungeonManager` 生成委派新五类为运行时行为变更，需实测；新五类已在 P1-6 接入 `DungeonSceneInstaller`） |
| dungeon.scene | ⑤ | 新增 `WorldSpaceUI` 节点（组件已建，P3-1） | 编辑器挂接（组件已就绪） |
| main.scene | ① | `CreatePanel/PreviewZone` 挂 `ModelDisplay3D`（组件已建，P2-4） | 编辑器挂接（组件已就绪） |
| main.scene | ② | 可选挂 `MainMenuBackdrop`（组件已建，P3-2） | 编辑器挂接（组件已就绪） |
| main.scene | ③ | 主相机按需挂 `CameraBrain`（已在 P0-3 注册 Follow 透视） | 编辑器挂接 |

- 说明：步骤 ③ ④ 改运行时装配且无法在沙箱验证，按审计约定留待编辑器实测；⑤ ①② 组件均已就绪，仅需编辑器挂节点。

### 10.1 当前可安全执行的编辑器操作（2026-07-11 依赖核查后）

代码侧核查结论：`DungeonSceneController._wireSystems` 直接依赖 `PlayerController` 的 `init` / `stats` / `currentHP` / `handleJoystick` / `onHPChanged` 等 API；`UpgradeManager` / `ElementSystem` / `ItemSystem` / `AutoAttack` 均经 `init(player: PlayerController, ...)` 注入。因此：

- **可安全执行（纯新增 / installer 兜底，不破坏运行时）**：
  - main.scene ① 挂 `ModelDisplay3D`（CreatePanel/PreviewZone/ModelDisplay）—— 纯新增。
  - main.scene ② 可选挂 `MainMenuBackdrop` —— 纯新增。
  - dungeon.scene ⑤ 新增 `WorldSpaceUI` 节点 —— 纯新增（需另建 world-space UI Canvas + 相机才实际显示，组件本身无害）。
  - dungeon.scene ④ 可移除 `GridManager` / `BattleManager` / `DungeonManager` 场景节点 —— `DungeonSceneInstaller.install()` 在场景找不到时会 `ensureComponent` 于 `systems` 子节点重建（行为不变，等同于"改由 Systems 创建壳"）。保留亦可。
  - dungeon.scene Player 节点**可叠加**挂 6 组件 + `ModelComponent` / `SkinnedMeshRenderer`（纯新增、尚无消费者，无害），但**不要移除 PlayerController**。
- **暂不可执行（会破坏运行时，待 P3-4 代码接线后）**：
  - dungeon.scene ③ 移除 `PlayerController` 节点 —— `DungeonSceneController` 及各战斗系统仍依赖其 API，6 组件尚未接线替代，移除即崩。
  - ✅ dungeon.scene ④ 的「DungeonManager 生成委派新五类」—— **P3-4-A 已完成**（commit e0a0e0c）：`DungeonManager` 现 additive 委派新五类构建 `RoomRuntime` 并暴露 `roomRuntimes` getter，房间进入时激活/加载、destroy 释放；旧 `DAGGenerator` 流程不变（零回归）。完整生成切换（替换 `DAGGenerator`）待消费者重接（P3-4-C/D），因类型词汇不兼容（5 vs 8 房间类型）。
  - ✅ **P3-4-C 已完成**（commit 9249fca）：新增 `ecs/battle/IPlayerAgent.ts` 统一玩家契约（init/handleJoystick/takeDamage/heal/stats/currentHP/gridX/Y/isAlive/state/isDodging/onHPChanged/node）；`PlayerController implements IPlayerAgent`；12 个消费者（DungeonManager/UpgradeManager/SkillSystem/EventSystem/EquipmentSystem/ElementSystem/MonsterController/ItemSystem/BattleManager/RoomFlowController/EventUI/MutationRuntimeService）内部 field/param 类型改 `IPlayerAgent`，`@property(PlayerController)` 绑定与 `ensureComponent<PlayerController>` 保留（编辑器需 cc 类）；运行时行为零变化。
  - ✅ **P3-4-B 已完成（additive 挂载，默认 inert）**（commit 647b959）：`EcsEntityBridge` 现 `implements IPlayerAgent`；新增静态开关 `USE_ECS_PLAYER=false`，`onLoad`/`update` 在 false 时 early-return，bridge **完全惰性、不接管活玩家**（避免与 PlayerController 的 tween 抢位置）；`DungeonSceneInstaller` additively `ensureComponent<EcsEntityBridge>` 于玩家节点并 `setSpawn(center,'player')`+`init(gridManager)`。翻转开关须与 D 步骤（移除 PlayerController/AutoAttack）一同进行，且仅限编辑器实测后。
  - ⏸️ **P3-4-D 暂缓（受阻）**：移除 PlayerController/AutoAttack 此刻会产出**无可用玩家**——ECS 玩家路径功能不完整（无输入→移动接线、无战斗目标喂喂、无 HP 变化回调、StatComponent 缺攻击属性），且 `assets/` 下**零 3D 角色模型**（CharacterVisualService 仅 2D Sprite/parts），"Sprite→ModelComponent 的 3D 视觉"当前资产下无法落地。此即审计报告 §10.1 标红 🔴 项，且沙箱无法跑游戏验证。故保留 PlayerController/AutoAttack，bridge 维持 inert，待补完 bridge 的战斗/移动/HP 接线并翻转 `USE_ECS_PLAYER` 后再移除旧类。
  - main.scene ③ 挂 `CameraBrain` 于主相机 —— 会将 2D 菜单相机翻为透视 Follow，扭曲 UI（审计报告 §5 刻意推迟）。

---

> 最后更新：2026-07-11（P0 全完成 + P1 全系列（P1-1~P1-6）+ P2 全系列（P2-1~P2-4）+ P3 启动：P3-1/P3-2 组件已建（b674007）；P3-3 in-editor 清单已列；§10.1 编辑器红线（PlayerController 移除/生成委派待 P3-4）；**P3-4-A 已完成**（e0a0e0c：DungeonManager additive 委派 RoomRuntime 五类，旧 DAG 行为不变）；**P3-4-C 已完成**（9249fca：IPlayerAgent 契约 + 12 消费者解耦）；**P3-4-B 已完成**（647b959：EcsEntityBridge implements IPlayerAgent + additive 惰性挂载，USE_ECS_PLAYER 默认 false）；**P3-4-D 受阻暂缓**（ECS 玩家路径功能不完整 + 零 3D 角色资产，须补完接线并编辑器实测后移除旧类）；依据 `docs/2D转3D全面升级方案.md` v3 + `回到地面/docs/三场景完整结构树.md`）
