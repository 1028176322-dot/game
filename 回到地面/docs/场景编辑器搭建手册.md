# 场景编辑器搭建手册

> 正式上线规范路径 · v2.1  
> 覆盖 splash.scene / main.scene / dungeon.scene 三个场景  
> **核心原则：先跑通，再搭完整。** 每个场景都按"最低可运行结构 → 正式上线结构"两档推进。

---

## 约定与术语

- **编辑器布局（默认）**：左侧 = 层级管理器（节点树），中间 = 场景/游戏视图，右侧 = 属性检查器（Inspector），底部 = 资源管理器（Assets）
- **"右键 Canvas → 创建空节点"** = 在层级管理中左键点击选中 `Canvas` → 鼠标右键点击它 → 弹出菜单 → 左键点击"创建空节点"
- **"改名为 Xxx"** = 在层级管理器中双击节点的名称文字（不要点前面的图标，点文字）→ 进入编辑状态 → 输入新名称 → 回车确认
- **"添加组件"** = 选中节点后 → 在属性检查器底部找到蓝色 **"添加组件"** 按钮 → 左键点击 → 弹出搜索框 → 输入脚本名 → 左键点击搜索到的脚本
- **"拖入"** = 在层级管理器中左键按住某个节点不放 → 拖动鼠标到属性检查器中的某个属性字段上 → 看到属性字段边框变蓝/高亮 → 松开鼠标左键
- **"初始隐藏"** = 在面板节点下创建 `PanelRoot` 子节点 → 给 `PanelRoot` 取消勾选（变灰）→ `panelRoot` 绑到 `PanelRoot`，而非面板自身
- **属性检查器排列**：Cocos Creator 中 @property 字段从上到下按代码声明顺序排列

---

## 推荐搭建策略

| 阶段 | 做什么 | 预期结果 |
|------|--------|---------|
| **阶段 1** (快速) | 结构修复 + AreaSelectPanel 最小结构 | splash → 点 StartButton → AreaSelectPanel → 进 dungeon |
| **阶段 2** (正式) | MainHubUI + 7 个面板 + Shop | 完整主城功能 |
| **阶段 3** (完善) | 面板子节点 + 视觉布局 | UI 完整可交互 |

> ⚠️ **阶段 1 也必须创建 AreaSelectPanel**（至少 PanelRoot + PlayerInfo + RouteContainer + LockedContainer + StartBtn + BackBtn）。因为 `MainSceneController._onFlowState(AREA_SELECT)` 会调 `router.open('area_select')`，没有面板注册就会报 `panel not registered` 而卡住。  
> 阶段 1 **不需要**创建 MainHubUI、CharacterBtn、TopBar 等——StartButton 的点击由 `MainSceneController` 的 fallback 绑定接管。

---

## 一、splash.scene — 启动加载场景

### 1.1 当前已有内容

打开 `splash.scene`，在层级管理中展开 `Canvas`：

```
Canvas
└── SplashUI                    ← 有 SplashUI 脚本
    ├── SkipButton              [UITransform]  → ⚠️ 只有空节点，没有 Label
    └── SplashImage             [UITransform, Sprite]
```

### 1.2 需要补充

1. 给 SkipButton **创建子节点 Label**（不是直接给 SkipButton 加 Label 组件）
2. 创建 **LoadingLabel** 节点
3. 创建 **GameBootstrap** 节点（可选，代码会兜底创建）

### 1.3 操作步骤

#### 操作 1.1：给 SkipButton 加子 Label（⚠️ 不能直接加在 SkipButton 上）

**错误做法：** 给 SkipButton 自身加 Label 组件 → Cocos 3.x 可能报 `Can't add renderable component`。

**正确做法（请严格按以下步骤）：**

1. 在**层级管理器**中找到 `Canvas → SplashUI → SkipButton`
2. **左键点击** `SkipButton` 选中它
3. **右键点击** `SkipButton` → `"创建空节点"`
4. 立即双击新节点名字 → 输入 **`Label`** → 回车
5. 选中 `SkipButton` 下的 `Label` 子节点
6. 在**属性检查器**底部点 `添加组件` → 输入 `Label` → 左键点击
7. 在 `Label` 组件的 `String` 输入框中输入 **"跳过"**
8. 按 `回车`

**绑定到 SplashUI.skipLabel：**

9. 在**层级管理器**中**左键点击** `SplashUI`
10. 在**属性检查器**中找到 `SplashUI` 脚本的 `skipLabel` 字段（显示 `None(Label)`）
11. **左键按住**层级管理器中的 `SkipButton/Label` 子节点
12. 拖到属性检查器 `skipLabel` 字段的框上 → **松开**
13. 框中显示 `Label(Label)` 字样

**最终 SkipButton 结构：**

```
SkipButton          ← 空节点，不含 Label 组件
└── Label           ← 在这里加 Label 组件，绑给 skipLabel
```

---

#### 操作 1.2：创建 LoadingLabel 节点并绑定

1. **右键 `SplashUI`** → `"创建空节点"` → 改名 **`LoadingLabel`**
2. 选中 `LoadingLabel` → `添加组件` → 搜索 `Label` → 点击
3. 在 Label 组件的 `String` 输入框中输入 **`"正在加载..."`**

**绑定：**

4. 选中 `SplashUI`
5. 在属性检查器找到 `loadingLabel` 字段
6. **左键按住**层级管理器中的 `LoadingLabel` → 拖到 `loadingLabel` 的框上 → **松开**

---

#### 操作 1.3：绑定 SplashImage

1. 选中 `SplashUI`
2. 找到 `splashImage` 字段
3. **左键按住**层级管理器中的 `SplashImage` → 拖到 `splashImage` 的框上 → **松开**

---

#### 操作 1.4：创建 GameBootstrap（可选）

> 代码中的 `GameBootstrap.ensure()` 会兜底自动创建，此步骤非必须。但提前创建好更可控。

1. **右键 `SplashUI`** → `"创建空节点"` → 改名 **`GameBootstrap`**
2. 选中 → `添加组件` → 搜索 `GameBootstrap` → 点击
3. **右键 `GameBootstrap`** → `"创建空节点"` → 改名 **`StatusLabel`**
4. 选中 `StatusLabel` → `添加组件` → `Label` → String 留空
5. 选中 `GameBootstrap` → 把 `StatusLabel` 拖入 `statusLabel` 字段

---

#### 操作 1.5：调整布局

| 节点 | Position X | Position Y |
|------|-----------|-----------|
| `SplashUI/SkipButton` | 540 | 310 |
| `SplashUI/SplashImage` | 0 | 40 |
| `SplashUI/LoadingLabel` | 0 | -60 |
| `SplashUI/GameBootstrap/StatusLabel` | 0 | -130 |

**Ctrl + S** 保存。

---

### 1.4 splash 验收

在 Cocos Creator 中按 **Ctrl+P** 预览，预期：

```
splash.scene 加载
  → SplashUI 显示加载进度条
  → 进度 100%
  → AppFlowController.start()
  → SceneFlowService.goTo('main')
  → main.scene 加载
```

---

## 二、main.scene — 主城场景（分两档）

### 2.0 当前已有内容

```
Canvas
├── MainUI                     ← 可能有旧 MainUI 脚本，也可能空
│   ├── StartButton            [UITransform, Button, Sprite]
│   │   └── Label
│   └── MainSceneController    ← 有 MainSceneController 脚本
└── (Title 节点，可删除)
```

**Title 节点删除：** 选中 `MainUI/Title` → 右键 → 删除。

---

### 2A. 最低可运行结构（阶段 1）

> **目标**：只创建 `AreaSelectPanel` + 最小必须子节点，其余面板（LoginPanel/CreatePanel 等）都不需要。  
> 点击 StartButton → AreaSelectPanel 打开 → 点"开始"进地牢。

#### 操作 A1：创建 AreaSelectPanel（按 2C 模板 + 2D 专项）

阶段 1 只需要 1 个面板：`AreaSelectPanel`。按下面的步骤操作：

1. **按 2C 模板**（跳到 2C 查看）创建 `AreaSelectPanel` + `PanelRoot`，挂 `AreaSelectPanel` 脚本，`panelRoot` 绑到 `PanelRoot`
2. **按 2D 专项**（跳到 2D 查看）补全 `RouteContainer`、`LockedContainer`、`PlayerInfo`、`StartBtn`、`BackBtn` 五个子节点并绑定
3. `AreaSelectPanel` 的主节点保持 active=true，`PanelRoot` 子节点隐藏

#### 操作 A2：验证阶段 1 链路

```
StartButton 点击
  → MainSceneController._onOpenAreaSelect()
  → AppFlowController.goTo(AREA_SELECT)
  → eventBus.emit('appflow:state_changed', 'AREA_SELECT')
  → MainSceneController._onFlowState('AREA_SELECT')
  → router.open('area_select')          ← 需要 AreaSelectPanel 已注册
  → AreaSelectPanel 显示
  → 点击 StartBtn
  → RunCoordinator.startRun()
  → SceneFlowService.goTo('dungeon')
  → dungeon.scene 加载
```

---

### 2B. 正式 MainHub 结构（阶段 2）

> 在阶段 1 跑通之后，再补充完整的 MainHubUI + 按钮 + TopBar。

#### 操作 B1：给 MainUI 加 UITransform（如果缺失）

> **重要说明：** `MainUI` 不能有 `Canvas` 组件。`Canvas` 组件只在顶层 `Canvas` 节点上。

1. 选中 `MainUI`
2. 看属性检查器：如果只有 `Node` + `添加组件`，没有 `UITransform`
3. 点 `添加组件` → 搜索 `UITransform` → 点击添加

#### 操作 B2：添加 MainHubUI 脚本

1. 选中 `MainUI`
2. 点 `添加组件` → 搜索 **`MainHubUI`** → 点击
3. 属性检查器中出现 `MainHubUI (Script)` ，有 10 个空白字段：

| 字段 | 类型 | 说明 |
|-----|------|------|
| `startBtn` | Button | 绑定 StartButton |
| `characterBtn` | Button | 后续创建 |
| `shopBtn` | Button | 后续创建 |
| `logBtn` | Button | 后续创建 |
| `settingsBtn` | Button | 后续创建 |
| `charNameLabel` ~ `versionLabel` | Label | 后续创建 |

#### 操作 B3：绑定 startBtn

1. 选中 `MainUI`
2. **左键按住**层级管理器中的 `MainUI/StartButton`
3. 拖到属性检查器 `MainHubUI.startBtn` 字段的框上 → **松开**

#### 操作 B4：创建 5 个功能按钮

在 `MainUI` 下逐个创建（每个步骤相同，以 `CharacterBtn` 为例）：

1. **右键 `MainUI`** → `"创建空节点"` → 改名 **`CharacterBtn`**
2. 选中 → 添加 `Button` + `Sprite`
3. **右键 `CharacterBtn`** → `"创建空节点"` → 改名 `Label`
4. 选中 `Label` → 添加 `Label` → String 设为对应文字

| 节点名 | Label 文字 |
|-------|-----------|
| `CharacterBtn` | "角色" |
| `ShopBtn` | "商店" |
| `LogBtn` | "日志" |
| `SettingsBtn` | "设置" |

**绑定到 MainHubUI：** 选中 `MainUI`，拖入对应字段：

| MainHubUI 字段 | 从层级管理器拖入 |
|--------------|---------------|
| `characterBtn` | `MainUI/CharacterBtn` |
| `shopBtn` | `MainUI/ShopBtn` |
| `logBtn` | `MainUI/LogBtn` |
| `settingsBtn` | `MainUI/SettingsBtn` |

#### 操作 B5：创建 TopBar（5 个 Label）

| 节点名 | Label String | Position X | Position Y |
|-------|-------------|-----------|-----------|
| `CharNameLabel` | `Adventurer` | -500 | 310 |
| `CharClassLabel` | `Bear Warrior` | -300 | 310 |
| `LevelLabel` | `Lv1` | -100 | 310 |
| `SoulStoneLabel` | `Soul Stones: 0` | 200 | 310 |
| `VersionLabel` | `v0.1.0` | 500 | 310 |

**绑定到 MainHubUI：** 选中 `MainUI`，把这些 Label 节点逐一拖入对应字段。

#### 操作 B6：调整按钮布局

| 节点 | X | Y |
|------|---|---|
| `StartButton` | -400 | -230 |
| `CharacterBtn` | -230 | -230 |
| `ShopBtn` | -60 | -230 |
| `LogBtn` | 110 | -230 |
| `SettingsBtn` | 280 | -230 |

---

### 2C. 7 个面板的通用创建模板

> **核心原则：**
> 1. 面板节点自身保持 **active = true**（不取消勾选），确保组件的 `onLoad()` 生命周期稳定
> 2. 在面板下创建 **`PanelRoot`** 子节点 → 对 PanelRoot **取消勾选（初始隐藏）**
> 3. 脚本的 `panelRoot` 字段绑定 PanelRoot，而非面板自身
> 4. `open()` 设置 `panelRoot.active = true`，`close()` 设置 `panelRoot.active = false`

#### 通用创建步骤（以 LoginPanel 为例）

```
Canvas
├── LoginPanel            ← active = true（保持勾选），有 LoginPanel 脚本
│   └── PanelRoot         ← active = false（取消勾选），绑给 panelRoot 字段
```

**具体操作：**

**步骤 1：创建面板节点**
- 在**层级管理器**中**左键点击** `Canvas` 选中它
- **右键 `Canvas`** → `"创建空节点"` → 改名 **`LoginPanel`**
- 保持选中 → `添加组件` → 搜索 `LoginPanel` → 点击
- ✅ **保持属性检查器顶部的 checkbox 为勾选状态**（节点 active = true）

**步骤 2：创建 PanelRoot 子节点**
- **右键 `LoginPanel`** → `"创建空节点"` → 改名 **`PanelRoot`**
- 选中 `PanelRoot` → **取消勾选属性检查器顶部的 checkbox**（节点变灰）

**步骤 3：绑定 panelRoot**
- 选中 `LoginPanel`
- 在属性检查器找到 `panelRoot` 字段
- **左键按住**层级管理器中的 `LoginPanel/PanelRoot`
- 拖到 `panelRoot` 的框上 → **松开**

**步骤 4：处理面板自己的 @property**（可选，按需补充）
如果需要面板显示更完整的内容，可以继续创建子节点并绑定：

| 面板 | 可选子节点 | 绑定到 |
|-----|-----------|-------|
| `CreatePanel` | `cardRoot` (Node) | 角色卡片容器 |
| | `nameInput` (EditBox) | 名字输入框 |
| `AreaSelectPanel` | 见 2D 专项说明 | |
| `SettlementPanel` | `titleLabel` 等 | 可选 |

#### 按此模板创建以下 7 个面板

| 面板节点 | 创建位置 | 脚本 | panelRoot 绑定 |
|---------|---------|------|--------------|
| `LoginPanel` | 右键 `Canvas` | `LoginPanel` | `LoginPanel/PanelRoot` |
| `CreatePanel` | 右键 `Canvas` | `CreatePanel` | `CreatePanel/PanelRoot` |
| `CharacterPanel` | 右键 `Canvas` | `CharacterPanel` | `CharacterPanel/PanelRoot` |
| `AreaSelectPanel` | 右键 `Canvas` | `AreaSelectPanel` | `AreaSelectPanel/PanelRoot` |
| `SettlementPanel` | 右键 `Canvas` | `SettlementPanel` | `SettlementPanel/PanelRoot` |
| `SettingsPanel` | 右键 `Canvas` | `SettingsPanel` | `SettingsPanel/PanelRoot` |
| `AdventureLogPanel` | 右键 `Canvas` | `AdventureLogPanel` | `AdventureLogPanel/PanelRoot` |

---

### 2D. AreaSelectPanel 专项结构

> 与普通面板不同，`AreaSelectPanel` 的代码依赖多个子节点来显示内容和处理点击。只绑 `panelRoot` 会导致面板打开后一片空白、无法点击。

#### 完整结构

```
Canvas/AreaSelectPanel         ← active=true, 有 AreaSelectPanel 脚本
├── PanelRoot                  ← active=false, 绑给 panelRoot
│   ├── RouteContainer         Node (内容容器)
│   ├── LockedContainer        Node (锁定路线容器)
│   ├── PlayerInfo             Label (显示角色信息)
│   ├── StartBtn               Button + Sprite (开始按钮)
│   │   └── Label              Label → "开始冒险"
│   └── BackBtn                Button + Sprite (返回按钮)
│       └── Label              Label → "返回"
```

#### 操作步骤

**步骤 1：创建 AreaSelectPanel 和 PanelRoot**

按 2C 模板完成：创建 `AreaSelectPanel` → 挂 `AreaSelectPanel` 脚本 → 创建 `PanelRoot` 子节点 → 隐藏 PanelRoot → 绑 `panelRoot` 到 PanelRoot。

**步骤 2：创建 RouteContainer（右键 PanelRoot → 创建空节点 → 改名 RouteContainer，选中 → 添加 UITransform）**
**步骤 3：创建 LockedContainer（右键 PanelRoot → 创建空节点 → 改名 LockedContainer，选中 → 添加 UITransform）**
**步骤 4：创建 PlayerInfo（右键 PanelRoot → 创建空节点 → 改名 PlayerInfo，选中 → 添加 UITransform + Label）**

**步骤 5：创建 StartBtn**

1. 右键 `PanelRoot` → "创建空节点" → 改名 `StartBtn`
2. 选中 `StartBtn` → 添加 `UITransform` + `Button` + `Sprite`
3. 右键 `StartBtn` → "创建空节点" → 改名 `Label`
4. 选中 `Label` → 添加 `Label` → String 设 `"开始冒险"`

**步骤 6：创建 BackBtn（同上，步骤 5 照做，Label 文字设 "返回"）**

**步骤 7：绑定到 AreaSelectPanel 脚本**

选中 `AreaSelectPanel`，在属性检查器中：

| 字段 | 从层级管理器拖入 |
|-----|---------------|
| `playerInfo` | `AreaSelectPanel/PanelRoot/PlayerInfo` |
| `routeContainer` | `AreaSelectPanel/PanelRoot/RouteContainer` |
| `lockedContainer` | `AreaSelectPanel/PanelRoot/LockedContainer` |
| `startBtn` | `AreaSelectPanel/PanelRoot/StartBtn` |
| `backBtn` | `AreaSelectPanel/PanelRoot/BackBtn` |

---

### 2E. Shop 节点（路由断点已修复）

#### Shop 路由说明

**`ShopUI` 没有实现 `UIPanel` 接口，因此不能用 `UiRouter.instance.open('shop')`。**  
本手册配套的代码已经修复：

- `MainHubUI.ts` 的 shop 按钮改为 `eventBus.emit('ui:open_shop')`
- `MainSceneController.ts` 监听此事件后调用 `shopUI.show()`

#### 操作

1. **右键 `Canvas`** → `"创建空节点"` → 改名 **`Shop`**
2. 选中 → `添加组件` → 搜索 `ShopUI` → 点击
3. 选中 `Canvas/MainSceneController`
4. 在属性检查器找到 `shopUI` 字段
5. 把 `Canvas/Shop` 节点拖入 `shopUI` → **松开**

---

### 2F. 验收

#### 最终结构检查

```
Canvas
├── MainUI                          [MainHubUI: 10 属性绑定]
│   ├── StartButton → Label
│   ├── CharacterBtn → Label
│   ├── ShopBtn → Label
│   ├── LogBtn → Label
│   ├── SettingsBtn → Label
│   ├── CharNameLabel
│   ├── CharClassLabel
│   ├── LevelLabel
│   ├── SoulStoneLabel
│   └── VersionLabel
├── MainSceneController             [shopUI → Shop]
├── LoginPanel                      [panelRoot → PanelRoot, PanelRoot 隐藏]
│   └── PanelRoot [隐藏]
├── CreatePanel
│   └── PanelRoot [隐藏]
├── CharacterPanel
│   └── PanelRoot [隐藏]
├── AreaSelectPanel                 [所有字段已绑定]
│   ├── PanelRoot [隐藏]
│   │   ├── PlayerInfo
│   │   ├── RouteContainer
│   │   ├── LockedContainer
│   │   ├── StartBtn → Label
│   │   └── BackBtn → Label
│   └── ...
├── SettlementPanel
│   └── PanelRoot [隐藏]
├── SettingsPanel
│   └── PanelRoot [隐藏]
├── AdventureLogPanel
│   └── PanelRoot [隐藏]
└── Shop                            [ShopUI]
```

**检查项：**
- 7 个面板的 **主节点都保持 active=true**（不隐藏） ✓
- 7 个面板的 **PanelRoot 子节点都隐藏** ✓
- 只有 1 个 `MainSceneController` 节点 ✓
- `Shop` 节点已创建且有 `ShopUI` 脚本 ✓

**运行命令：**
```bash
npm.cmd run validate:all
```

**预览验收流程：**
```
splash → main → 点击 StartButton
  → AreaSelectPanel 打开（有路线卡片 + 开始按钮）
  → 点击"开始" → dungeon.scene 加载
  → 地牢有背景和玩家角色
```

**Ctrl + S** 保存。

---

## 三、dungeon.scene — 地牢场景

### 3.1 ⚠️ 当前结构错误

打开 `dungeon.scene`，你会看到：

```
Canvas
└── DungeonSceneController (id=8)     ← 空节点，无组件
    └── DungeonSceneController (id=9) ← 有 DungeonSceneController 脚本
        ├── GridManager
        ├── Player → PlayerController, AutoAttack
        ├── VirtualJoystick
        ├── BattleManager
        ├── DungeonManager            ← 没有 DungeonManager 脚本
        ├── BattleHUD
        ├── SkillUI
        ├── DungeonMapUI
        ├── UpgradeUI
        └── DeathUI
```

**问题：** 两个同名的嵌套节点。修复目标：**唯一的 `DungeonSceneController` 直接挂在 `Canvas` 下，GridManager、Player 等作为它的子节点。**

### 3.2 修复嵌套结构

**推荐方法 — 新建 + 挂载（最简单）：**

1. **右键 `Canvas`** → `"创建空节点"` → 改名 **`DungeonSceneController`**
2. 选中 → `添加组件` → 搜索 `DungeonSceneController` → 点击
3. 这就是新的正确节点

**移动子节点：**

4. 在旧嵌套结构中，**左键按住** `GridManager` → 拖到新的 `DungeonSceneController` 上 → 看到蓝线 → **松开**
5. 逐个拖动其余子节点：

| 移动顺序 | 节点 |
|---------|------|
| 1 | `GridManager` |
| 2 | `Player`（含其子节点 PlayerController + AutoAttack） |
| 3 | `VirtualJoystick` |
| 4 | `BattleManager` |
| 5 | `DungeonManager` |
| 6 | `BattleHUD` |
| 7 | `SkillUI` |
| 8 | `DungeonMapUI` |
| 9 | `UpgradeUI` |
| 10 | `DeathUI` |

**删除旧节点：** 确认所有子节点已移走后 → 删除旧的空 `DungeonSceneController`（id=8）

**最终结构：**

```
Canvas
└── DungeonSceneController        ← 唯一的节点，有脚本
    ├── GridManager               ← 有 GridManager
    ├── Player
    │   ├── PlayerController
    │   └── AutoAttack
    ├── VirtualJoystick
    ├── BattleManager
    ├── DungeonManager            → ⚠️ 需要手动加 DungeonManager 组件
    ├── BattleHUD
    ├── SkillUI
    ├── DungeonMapUI
    ├── UpgradeUI
    └── DeathUI
```

### 3.3 修复 DungeonManager

`DungeonManager` 节点存在但**没有 DungeonManager 脚本**：

1. 选中 `DungeonSceneController/DungeonManager`
2. 点 `添加组件` → 搜索 **`DungeonManager`** → 点击

### 3.4 绑定 DungeonSceneController 属性

> **必绑**（Installer 依赖它们作为上下文根基）：
> - `gridManager` → `DungeonSceneController/GridManager`
> - `player` → `DungeonSceneController/Player`
> - `battleManager` → `DungeonSceneController/BattleManager`
> - `dungeonManager` → `DungeonSceneController/DungeonManager`

> **推荐绑**（在场景中预置了节点）：
> - `joystick` → `DungeonSceneController/VirtualJoystick`
> - `battleHUD` → `DungeonSceneController/BattleHUD`

> **其余字段**（`roomTransition`, `skillSystem`, `upgradeManager`, `elementSystem`, `equipmentSystem`, `equipmentUI`, `itemSystem`, `inventoryUI`, `dungeonMapUI`, `upgradeUI`, `deathUI`）
>
> `DungeonSceneInstaller` 会**动态创建并自动查找**，不需要在编辑器中绑定。除非你要手动控制节点的位置。

**操作方法（以 gridManager 为例）：**

1. 选中 `DungeonSceneController`
2. 在属性检查器找到 `gridManager` 字段 → 显示 `None(GridManager)`
3. **左键按住**层级管理器的 `DungeonSceneController/GridManager`
4. 拖到 `gridManager` 的框上 → **松开**
5. 重复绑定其余 3 个必绑字段

### 3.5 布局参考

| 节点路径 | X | Y |
|---------|---|---|
| `DungeonSceneController` | 0 | 0 |
| `DungeonSceneController/Player` | 0 | 0 |
| `DungeonSceneController/VirtualJoystick` | -470 | -250 |
| `DungeonSceneController/BattleHUD` | -560 | 315 |
| `DungeonSceneController/SkillUI` | 390 | -265 |
| `DungeonSceneController/DungeonMapUI` | 320 | 160 |
| 其余系统节点 | 0 | 0 |

### 3.6 验收

```bash
npm.cmd run validate:all
```

**在编辑器中按 Ctrl+P 预览：**

```
splash → main → 点击 StartButton
  → AreaSelectPanel 打开 → 点击"开始"→ dungeon.scene 加载
  → DungeonSceneInstaller 使用已绑定的组件
  → 动态创建 World/Systems/UIRoot 容器
  → 背景图、玩家角色渲染
  → 虚拟摇杆可操作
```

**预期看到的：**
- 地牢背景渲染（Zone 默认 forest）
- 玩家角色（bear warrior）显示
- 虚拟摇杆在左下角
- BattleHUD 在左上角

---

## 四、验收命令汇总

| 操作 | 命令 | 说明 |
|------|------|------|
| 全门禁检查 | `npm.cmd run validate:all` | 配置 + 包体积 + 编码 + 架构 + TS 静态检查 共 5 道门禁 |
| Cocos 构建 | `npm.cmd run build` | Cocos Creator 完整构建，非轻量 TS 编译 |
| 微信构建 | `npm.cmd run build:wechat` | npm 生命周期先执行 prebuild:wechat → validate:all，然后构建微信包 |

---

## 五、常见问题

**Q：SkipButton 加了 Label 之后报错？**
→ SkipButton 不能自己带 Label 组件。按操作 1.1 改：把 Label 做在 SkipButton 的子节点上。

**Q：`panelRoot` 绑了但面板打不开？**
→ 检查两点：
  1. `panelRoot` 绑的是面板节点自身还是 `PanelRoot` 子节点？推荐绑 `PanelRoot`。
  2. 面板主节点是否是 active=true 的？不能整体隐藏。

**Q：Shop 打不开，控制台报 `[UiRouter] panel not registered: shop`？**
→ ShopUI 不是 UIPanel。代码已修复：Shop 按钮走 `eventBus.emit('ui:open_shop')`，MainSceneController 接管。确认 `shopUI` 属性已绑定到 `Canvas/Shop`。

**Q：AreaSelectPanel 打开了但是一片空白？**
→ 代码需要 `routeContainer`、`startBtn` 等子节点。按 2D 专项说明补完整结构。

**Q：地牢加载后黑屏？**
→ 检查 `DungeonSceneController` 的 `gridManager`、`player`、`battleManager`、`dungeonManager` 四个必绑字段是否绑定正确。

**Q：控制台报 `[DungeonSceneInstaller] render layers not initialized`？**
→ `DungeonSceneController` 不在 `Canvas` 的直接子节点下。回 3.2 修复嵌套结构。

---

## 文档版本

| 版本 | 日期 | 变更 |
|-----|------|------|
| v1.0 | 2026-07-01 | 初稿 |
| v1.1 | 2026-07-01 | 像素级拖拽说明 |
| v2.0 | 2026-07-01 | 两档搭建策略 + SkipButton 子Label + PanelRoot模式 + AreaSelectPanel完整结构 + Shop路由修复 + Dungeon必绑分级 + 验收命令 |
| v2.2 | 2026-07-01 | validate:all 5 道门禁说明；阶段1最小结构写全6个节点；build:wechat 生命周期说明 |
| v2.1 | 2026-07-01 | 阶段1强制创建AreaSelectPanel；补AreaSelectPanel子节点UITransform；删除不存在的check:encoding/check:arch脚本；build改为Cocos构建说明；MainUI描述改保守措辞 |
