# 地牢重做设计 · 节点路线图肉鸽模式 (v0.4.1)

> **版本**: v0.4.1 | **日期**: 2026-07-13 | **设计者**: GameDesigner
> **状态**: ⚠️ 已被 `docs/地牢重做_节点路线图肉鸽_设计v0.4.2.md` 取代(定稿前最后小修:补 encounterViewType elite/boss、修正 adapter import 路径、消除 core/save→dungeon/route 倒挂、RouteSaveAdapter 用 .instance、事件加 payload、移除 rewardRoomType、GameBootstrap 标伪代码)
> **范围边界**: 仅重做「肉鸽地图生成 + 节点执行层 + 3D 场景呈现」。不包含群体随从 / 被追击追逐(参考游戏无此设计,已确认丢弃)。

> **v0.4.1 关键变更(相对 v0.4, 7 处类型/架构冲突修正)**:
> ① `SingleRoomEncounterBuilder` 类型冲突:新增局部 `EncounterRoomLayoutType`(= `DungeonGenerator.RoomType`),`build()` 返回 `RoomLayout` 不再混用 `Constants.RoomType`;
> ② `RouteNodeType→Constants.RoomType` adapter 的 `start` 不再映射到不存在的枚举,改为返回 `null`(无房间事件);
> ③ Demo7 允许修改清单**补 `GameBootstrap.ts`**(P0 去 destroyAll 属运行时基础设施修复,必须授权);
> ④ `RouteSavePort` 落盘形态明确:扩展 `RunSave` 加 `route?: RouteRunSnapshot`,`RouteSaveAdapter` 挂 `RunSave.route` 调 `saveRun`,不另开独立 key;
> ⑤ `FNV-1a` 返回值数字化:新增 `deriveSeed(input, salt): number`,种子链全部经它转 `number` 再喂 `Rng`;
> ⑥ Boss 可达规则放宽:`reachable(boss) = parents(boss).some(p => completed.includes(p))`(完成任一父节点即可进下一行),非全父完成;
> ⑦ 明确调用边界:`RunCoordinator.startRun()` 创建整局 → `RouteRunController.startFloor()` 创建层路线图 → 进入场景只经 `RunCoordinator`/`SceneFlowService`(唯一允许 `director.loadScene`);`encounterSceneId='dungeon'` 复用现有 `SceneId`,不加新场景。

---

## 1. 目标与范围

### 1.1 设计目标
把现有"物理连续穿越 6×6 房间"的地牢,重做为《酋长你别跑》式的**节点路线图肉鸽**:进层看见整张节点地图 → 选路 → 点节点进入独立 3D 俯视场景打完/交互 → 回地图选下一节点。战斗本身不动(手动 ARPG 手感 + 元素反应 + 闪白/震屏/弹道)。

### 1.2 设计支柱(不可妥协)
1. **战略选择感** — 看全图、做路线赌注。
2. **单层清晰** — 路线一眼可见,不迷路。
3. **战斗零改动** — 复用现有 ARPG 战斗层。
4. **移动端稳健** — 断线可恢复,性能不崩。

### 1.3 与 ai-tasks 基座的关系
`docs/ai-tasks/`(Demo0–6 + §5.5–5.13) 已落地:`GameContext`/`LifecycleManager`/`Logger`/`ConfigDatabase`、`CameraBrain`、`ICollisionService`/`KinematicMover`、`SaveManager`(`ISaveManager`)、`SaveService`(`core/save`)、`ReplayRecorder`(FNV-1a 种子确定性)、`SkillGraph`、`DungeonGenerator`(多房间连通图)、`GameBootstrap`(服务注册 + lifecycle)。本重做**复用**这些基座。

> ⚠️ `GameBootstrap` 当前注册完服务即 `destroyAll()`(§8.4),非正式容器,本版 §8.4/§14.1 已将其纳入 P0 修复授权。

### 1.4 3D 主线 / 2D 兜底
- **正式目标路径 = 3D encounter**:节点进入后由 `SingleRoomEncounterBuilder` 造单房 → 3D 俯视场景战斗。
- **2D 仅开发兜底**:3D 底座未就绪时用 2D fallback 调试节点流程,**不作为发布形态**。

---

## 2. 核心循环 (Core Loop)

### Moment-to-Moment (0–30 秒)
- **Action**: 在节点地图上点一个**可达**节点。
- **Feedback**: 节点高亮脉冲 + 镜头推近 + 进入加载。
- **Reward**: "选择被兑现"的掌控感。

### Session Loop (5–30 分钟,单层)
- **Goal**: 从 `start` 走到 `boss`,沿途吃 Elite/Event/Shop/Rest 节点。
- **Tension**: 分支赌注——高风险 Elite 还是稳妥 Rest。
- **Resolution**: 击杀 Boss → `floor_cleared` → 下一层或结算。

### Long-Term Loop (小时–周)
- **Progression**: 多层累积 build / 解锁。
- **Retention Hook**: 每日种子挑战(同种子同图,复用 `ReplayRecorder` 确定性)。

---

## 3. 节点路线图数据结构

> 分裂为三件,消除双状态源(v0.4 已确立,本版未改结构)。

### 3.1 静态定义(生成一次,不可变)
```ts
export interface NodeRouteNodeDef {
    id: string;
    row: number;
    col: number;
    type: RouteNodeType;
    parents: string[];
    children: string[];
}

export interface NodeRouteMapDefinition {
    floorIndex: number;
    rows: number;
    nodes: Record<string, NodeRouteNodeDef>;
    edges: Array<{ from: string; to: string }>;
    startNodeId: string;
    bossNodeId: string;
    seed: number;
}
```

### 3.2 运行时状态(单点真相)
```ts
export interface NodeRouteRuntimeState {
    currentNodeId: string;
    reachableNodeIds: string[];
    completedNodeIds: string[];
    phase: RouteRunPhase;
}
```

### 3.3 UI 视图状态(派生,不存储)
```ts
export type RouteNodeViewState = 'current' | 'completed' | 'reachable' | 'locked';

export function getNodeViewState(
    nodeId: string,
    state: NodeRouteRuntimeState,
): RouteNodeViewState {
    if (state.currentNodeId === nodeId) return 'current';
    if (state.completedNodeIds.includes(nodeId)) return 'completed';
    if (state.reachableNodeIds.includes(nodeId)) return 'reachable';
    return 'locked';
}
```

---

## 4. 生成算法与验证规则

### 4.1 算法
- `rows = 10 + floorIndex`(`[PLACEHOLDER]` 调)。
- 每行 2–4 节点,`start` 独占第 0 行,`boss` 独占末行。
- 边生成: 每个节点连 1–2 个下一行节点,保证**至少 2 条**从 start 到 boss 的不重复路径。
- 节点类型分布(按权重 `[PLACEHOLDER]`): combat / elite / event / treasure / shop / rest / upgrade / boss。
- 全部随机来自 `routeSeed`(§10),禁 `Math.random`。

### 4.2 验证规则 (`NodeRouteValidator`)
- **连通性**: 从 `start` BFS 必达 `boss`。
- **无环**: 边的 `to.row > from.row` 恒成立。
- **保底**: 每行至少一节点被前一行覆盖。
- **Boss 可达(v0.4.1 修正, 放宽)**:
  ```ts
  // Boss reachable if ANY parent is completed (standard roguelike: clear one parent -> enter next row)
  const bossReachable = def.nodes[def.bossNodeId].parents
      .some(p => state.completedNodeIds.includes(p));
  ```
  > 不要求"所有父节点都完成"——那等于强制清完整层,削弱路线选择感。若未来设计确需"清完整层",作为可选 `requireAllParents: boolean` 开关(默认 `false`)。

### 4.3 "Broken" 定义
- 全单节点路径(无选择) → broken。
- Boss 不可达(无任何父节点可完成路径) → broken。
- 任意节点无出边且非 Boss → broken。

---

## 5. RouteRunController 状态机

### 5.1 七态
```ts
export type RouteRunPhase =
    | 'map_select'
    | 'node_entering'
    | 'encounter_running'
    | 'reward_pending'
    | 'node_resolved'
    | 'floor_cleared'
    | 'run_settled';
```

### 5.2 推进规则(唯一入口)
- **所有**节点流程只通过 `RouteRunController` 推进,UI 点击只调 `requestEnter(nodeId)`。
- 防呆: `node_entering`/`encounter_running`/`reward_pending` 期间**忽略一切节点点击**。
- **Boss 解锁(v0.4.1 修正)**:
  ```ts
  // Boss enters only when at least one parent is completed
  reachable(boss) = parents(boss).some(p => completed.includes(p));
  ```
  > 禁止"跳层提前解锁",但也不要求全父完成(见 §4.2)。
- `RouteRunController` 不依赖地图 UI、不依赖 3D 场景,只消费/产出 `NodeRouteRuntimeState` 与 `RouteNodeEncounterConfig`(§8.3)。**不直接调用 `director.loadScene`**(§8.5)。

---

## 6. 节点类型与旧 RoomType 映射

### 6.1 路由层类型(新)
```ts
export type RouteNodeType =
    | 'start' | 'combat' | 'elite' | 'event'
    | 'treasure' | 'shop' | 'rest' | 'upgrade' | 'boss';
```

### 6.2 与旧系统映射(经 adapter, start 返回 null)
| `RouteNodeType` | `Constants.RoomType` | 备注 |
|---|---|---|
| start | **null** | `Constants.RoomType` 无 Start;start 节点无房间事件,adapter 返回 null |
| combat | Normal | |
| elite | Elite | |
| boss | Boss | |
| treasure | Treasure | |
| shop | Shop | |
| event | Event | |
| upgrade | Upgrade | |
| rest | Healing / Rest | 旧 `Healing` 与 `Rest` 并存,adapter 两者都映射 `rest` |

```ts
// v0.4.1 修正: start -> null, 不映射到不存在的枚举
import { RoomType as ConstantsRoomType } from '../core/Constants';

export function toLegacyRoomType(type: RouteNodeType): ConstantsRoomType | null {
    switch (type) {
        case 'start':   return null;            // no room event for start node
        case 'combat':  return ConstantsRoomType.Normal;
        case 'elite':   return ConstantsRoomType.Elite;
        case 'boss':    return ConstantsRoomType.Boss;
        case 'treasure':return ConstantsRoomType.Treasure;
        case 'shop':    return ConstantsRoomType.Shop;
        case 'event':   return ConstantsRoomType.Event;
        case 'upgrade': return ConstantsRoomType.Upgrade;
        case 'rest':    return ConstantsRoomType.Rest;  // Healing also maps here
        default:        return null;
    }
}
```

> `DungeonGenerator.RoomType`(`'start'|'battle'|'elite'|'reward'|'boss`) 是 3D 房间建造用的**局部类型**(见 §8.2 `EncounterRoomLayoutType`),与 `RouteNodeType` 经 `RouteNodeTypeAdapter` 桥接;非战斗节点**不调** `DungeonGenerator`。

---

## 7. 节点地图 UI 规范(移动端)

- 节点点击热区 **≥ 72×72 px**(横屏适配)。
- 连线不拦截触摸(点击落在节点上)。
- 当前可达节点必须有脉冲/呼吸动画。
- Boss 节点:金色首领徽记(王冠/兽爪/宝石门),**禁红色**,常驻可见或提供快速定位按钮。
- 地图层与 HUD 层**互斥**:选路时隐藏战斗 HUD。
- 支持拖拽/缩放。

---

## 8. 节点执行层:SingleRoomEncounterBuilder + 3D 主线

### 8.1 ❌ 禁止误用 DungeonGenerator roomCount:1
`DungeonGenerator.generate(seed, zone, { roomCount: 1 })` 不可用:其 `line 54` 将 `roomCount` clamp 到 `[3,12]`,传入 1 实际变 3,且它是"多房间连通图"生成器,职责不匹配单节点。

### 8.2 ✅ SingleRoomEncounterBuilder(类型冲突修正)
> **v0.4.1 修正**: 原 `SingleRoomEncounterOptions.type: Constants.RoomType` 与返回 `RoomLayout.type`(`DungeonGenerator.RoomType`) 是**两套不兼容类型**。现引入局部 `EncounterRoomLayoutType` 对齐 `DungeonGenerator.RoomType`,`build()` 直接产 `RoomLayout`,不再混用 `Constants.RoomType`。

```ts
// local layout type, matches DungeonGenerator.RoomLayout.type exactly
export type EncounterRoomLayoutType = 'start' | 'battle' | 'elite' | 'reward' | 'boss';

export interface SingleRoomEncounterOptions {
    roomId: string;
    layoutType: EncounterRoomLayoutType;   // drives RoomLayout.type (NOT Constants.RoomType)
    rewardRoomType?: ConstantsRoomType;    // optional: for non-combat nodes that need legacy mapping
    zone: string;
    seed: number;
    minSize?: number;                       // default 7
    maxSize?: number;                       // default 11
}

export class SingleRoomEncounterBuilder {
    build(opts: SingleRoomEncounterOptions): RoomLayout {
        const rng = new Rng(opts.seed);
        return {
            id: opts.roomId,
            type: opts.layoutType,          // EncounterRoomLayoutType -> RoomLayout.type OK
            gridX: 0,
            gridY: 0,
            width: oddSize(rng, opts.minSize ?? 7, opts.maxSize ?? 11),
            height: oddSize(rng, opts.minSize ?? 7, opts.maxSize ?? 11),
            connections: [],
        };
    }
}
```
- 文件位置:`assets/scripts/dungeon/SingleRoomEncounterBuilder.ts`(非 `route/`)。
- 不破坏 `DungeonGenerator` 多房间职责,符合 ai-tasks 契约边界。

### 8.3 RouteNodeEncounterConfig(解耦契约)
```ts
export interface RouteNodeEncounterConfig {
    runId: string;
    floorIndex: number;
    nodeId: string;
    nodeType: RouteNodeType;
    zoneId: string;
    difficulty: number;
    seed: number;
    encounterSceneId: 'dungeon';                    // v0.4.1: fixed, reuse existing SceneId
    encounterViewType: 'combat' | 'shop' | 'rest' | 'event' | 'treasure' | 'upgrade';
    rewardProfileId: string;
}
```
- 地图层(`RouteRunController`)只**产** config;3D 场景层只**消费** config。
- `encounterSceneId` 固定 `'dungeon'`(§8.5),节点差异由 `encounterViewType` 呈现。

### 8.4 正式运行时硬规则(GameContext / Lifecycle)
> 当前 `GameBootstrap.ts:346` 注册完服务即 `destroyAll()`,属 Demo probe 行为。**正式运行时禁止**。

```ts
async startup(): Promise<void> {
    await this._loadConfigAndAssets();
    this._wireInfra();
    this._lifecycle.enterAll();   // services live for the whole session
}

// demo probe ONLY (separated from formal startup)
runLifecycleSmokeTestOnly(): void {
    this._lifecycle.enterAll();
    this._lifecycle.pauseAll();
    this._lifecycle.resumeAll();
    this._lifecycle.exitAll();
    this._lifecycle.destroyAll();
}
```
- `ISaveManager`/`IRoomRuntime`/`IAssetCache` 注册后**状态必须存活**至整局结束。
- Demo probe 拆到独立测试/调试入口,不混入 `startup()`。

### 8.5 调用边界:RunCoordinator / RouteRunController / SceneFlowService (v0.4.1 新增)
> 明确三者职责,避免重复切场景 / 场景归属混乱。

- **`RunCoordinator.startRun(config: RunStartConfig)`** — 创建**整局**(初始化 `DungeonRunState`: config/floor/zone/kills/...)。单局唯一入口。
- **`RouteRunController.startFloor()`** — 在整局内,为**当前层**生成 `NodeRouteMapDefinition` 并初始化 `NodeRouteRuntimeState`。**不切场景**。
- **进入 encounter**:`RouteRunController` 在 `node_entering` 阶段,**只通过 `RunCoordinator` 或 `SceneFlowService` 请求进入** `dungeon` 场景(二者是唯一允许 `director.loadScene` 的入口),自身不调 `director.loadScene`。
  ```ts
  // RouteRunController internally (NOT direct loadScene):
  await SceneFlowService.instance.goToDungeon();   // or RunCoordinator.enterFloorScene()
  ```
- **`encounterSceneId = 'dungeon'`**:所有节点 encounter **复用现有 `SceneId`**,不在 `SceneFlowService.SceneId` 加新场景值(`SceneId = 'splash'|'main'|'dungeon'`,line 13)。节点差异走 `encounterViewType` + 运行时装配,而非独立 scene 文件。
- 回地图:`encounter` 结束 → `RouteRunController` 回到 `map_select`(仍在 `dungeon` 场景或切回 `main`,由 `RunCoordinator` 决定,不在此规定 UI 宿主)。

---

## 9. 奖励预览与结算(防双发)

### 9.1 v0.3 双发风险(已确认)
`RoomFlowController.onBattleVictory()`(`line 44`)直接调 `grantRoomClearRewards`;route 模式若也走此方法,再由 `NodeRewardResolver` 发一次 → 双发。

### 9.2 修正:事件化
```ts
onBattleVictory(mode: 'legacy' | 'route' = 'legacy'): void {
    if (mode === 'route') {
        eventBus.emit('route:encounter_complete');
        return;
    }
    this._rewardService.grantRoomClearRewards(roomType);
}
```
route 模式只抛战斗完成事件,奖励由 `RouteRunController` 在 `reward_pending` → `NodeRewardResolver` 结算。

### 9.3 NodeRewardResolver(节点统一结算)
- 输入:`nodeType + floor + difficulty + rewardSeed` → 输出 `rewardPreview`(地图显示倾向) + `resolvedReward`(完成后实际发)。
- 地图 UI 显示"奖励倾向",完成后发实际奖励,**只发一次**(状态机保证)。

---

## 10. 种子与存档恢复

### 10.1 种子 key 链(数字化修正)
> **v0.4.1 修正**: `computeConfigHash` 返回 **hex string**(ReplayRecorder line 35 `.toString(16)`),但 `Rng` 需要 `number`。新增 `deriveSeed` 转数字,种子链全部经它。

```ts
import { computeConfigHash } from '../replay/ReplayRecorder';
import { Rng } from '../core/rng/Rng';

// hex string -> uint32 number seed
export function deriveSeed(input: unknown, salt: string): number {
    const hex = computeConfigHash({ input, salt });
    return parseInt(hex, 16) >>> 0;
}

// seed chain (all numeric, fed into Rng):
const runSeed       = userSeed;                        // from input / daily / random
const floorSeed     = deriveSeed(runSeed, `f${floorIndex}`);
const routeSeed     = deriveSeed(floorSeed, 'route');  // NodeRouteMapDefinition.seed
const nodeSeed      = deriveSeed(floorSeed, nodeId);
const rewardSeed    = deriveSeed(nodeSeed, 'reward');
const encounterSeed = deriveSeed(nodeSeed, 'encounter'); // -> config.seed
```
- 禁 `Math.random`;复用 `ReplayRecorder.computeConfigHash`(FNV-1a)。

### 10.2 两套存档现状(已确认)
- `assets/scripts/save/SaveManager.ts`(`ISaveManager`)
- `assets/scripts/core/save/SaveService.ts` + `SaveTypes.ts`(`RunSave` line 64)

### 10.3 ✅ RouteSavePort + 落盘形态(v0.4.1 修正)
```ts
export interface RouteRunSnapshot {
    schemaVersion: number;
    runId: string;
    zoneId: string;
    floorIndex: number;
    routeMap: NodeRouteMapDefinition;
    runtime: NodeRouteRuntimeState;
    activeEncounter?: RouteNodeEncounterConfig;
    seedState: { runSeed: number; floorSeed: number; routeSeed: number };
}

export interface RouteSavePort {
    saveRoute(snapshot: RouteRunSnapshot): boolean;
    loadRoute(): RouteRunSnapshot | null;
    clearRoute(): void;
}
```

> **落盘形态明确(v0.4.1)**: 扩展 `RunSave`(`SaveTypes.ts:64`)加 `route?: RouteRunSnapshot` 字段,**不另开独立 key**:
```ts
// SaveTypes.ts — extend RunSave
export interface RunSave {
    // ...existing fields (schemaVersion/runId/seed/.../floor/roomId/player/inventory/rng)
    route?: RouteRunSnapshot;   // v0.4.1: route run state rides on RunSave
}
```
- `RouteSaveAdapter` 实现 `RouteSavePort`: `saveRoute` 把 snapshot 挂到 `RunSave.route` 后调 `SaveService.saveRun(run)`;`loadRoute` 从 `SaveService.loadRun()?.route` 取;`clearRoute` 置 `run.route = undefined` 再 `saveRun`。
- 对上层只暴露 `RouteSavePort`;route 业务禁止直接 `new SaveManager()` / `new SaveService()`。
- 存盘点:进层即存、节点结算即存、进入场景即存。
- 恢复:从 `phase` 重建 `RouteRunController`;若 `phase==='encounter_running'`,用 `activeEncounter` 同 `encounterSeed` 重进同场景(幂等)。

---

## 11. 配置表规范
| 配置 | 字段 | 说明 |
|---|---|---|
| NodeTypeWeights | type, weight, floorMin | 节点类型权重 `[PLACEHOLDER]` |
| RewardProfile | nodeType, floor, rewardTier | 节点奖励档位 |
| EncounterScene | nodeType, encounterViewType, difficultyCurve | 节点呈现(全部 `encounterSceneId='dungeon'`) |
| RouteTuning | rowsBase, rowsPerFloor, minPaths, requireAllParents | 生成参数(`requireAllParents` 默认 false) |

---

## 12. 分阶段实施计划

| 阶段 | 内容 | 验收 | 依赖 |
|---|---|---|---|
| **P0 · 断层消除** | ① `SingleRoomEncounterBuilder` ② `GameBootstrap` 正式 `startup()`(去 destroyAll)+ Demo probe 拆出 ③ `RouteSavePort` 扩展 `RunSave.route` + 适配 `SaveService` ④ `RoomFlowController` 事件化防双发 ⑤ `deriveSeed` 工具 | 单房可造、服务存活、存读通、无双发、种子数值化 | 现有基座 |
| **P1 · 节点逻辑** | `NodeRouteGenerator` + `NodeRouteState` + `NodeRouteValidator` + 单测;同种子复现;Boss reachable=some;≥2 路径 | 单测全绿;validator 拦 broken | P0 |
| **P2 · 地图 UI** | 2D 覆盖层选路;派生状态;点击进 `dungeon` encounter | 选路→进场景闭环 | P1 |
| **P3 · 3D 节点闭环** | combat 节点进 3D 场景 → 完成回地图 → `NodeRewardResolver` 结算 → cleared | 单节点闭环 | P2 + P0① |
| **P4 · 节点类型** | elite / treasure / shop / rest / event / upgrade / boss 全接入(`encounterViewType` 区分) | 全类型可走 | P3 |
| **P5 · 多层+存档+种子** | 多层推进 + `RouteSavePort` 断线恢复 + 每日/种子挑战 | 中途退出可恢复;同种子同图 | P4 |

---

## 13. 门禁与测试用例
- `NodeRouteGenerator` 单测:同 seed 同图、Boss reachable(some 父)、≥2 路径、无环。
- `NodeRouteValidator` 单测:注入 broken 形态(单路径/Boss 不可达/孤立行)必拦。
- `RouteRunController` 状态机单测:重复点击忽略、Boss 不提前解锁、幂等恢复。
- `SingleRoomEncounterBuilder` 单测:同 seed 同尺寸、`connections:[]`、返回 `RoomLayout` 类型匹配。
- `RouteSavePort` 单测:`saveRoute`→`loadRoute` 往返一致(经 `RunSave.route`)、schemaVersion 校验。
- `deriveSeed` 单测:同输入同 salt → 同 number;hex→uint32 正确。
- `toLegacyRoomType` 单测:`start`→null,其余映射正确。
- 美术合规:`encoding_audit` + 资源门禁(Boss 金色徽记,禁红色/血/骷髅)。

### 13.x 美术安全规范
- Boss 节点:**金色首领徽记**(王冠/兽爪/宝石门),**禁红色**、禁暗黑、禁血/死亡/骷髅。
- 沿用 `topics/ART_RESOURCE_RULES.md` §7/§16:明亮材质、卡通大体型。

---

## 14. ai-tasks 协调:Demo7 卡与契约边界

> 路线系统不在 Demo0–6 卡内。依 `_agent_contract.md` #6,需**新增 Demo7 卡**授权,或本会话实现。

### 14.1 Demo7 卡允许修改清单(v0.4.1 补 GameBootstrap.ts)
- `assets/scripts/dungeon/route/**`(新增 7 模块)
- `assets/scripts/dungeon/SingleRoomEncounterBuilder.ts`(新增)
- `assets/scripts/core/GameBootstrap.ts`(**v0.4.1 新增** — P0 去 destroyAll 属运行时基础设施修复,必须授权,否则与契约冲突)
- `assets/scripts/run/RoomFlowController.ts`(事件化,§9.2)
- `assets/scripts/save/SaveManager.ts`(扩展 RunState/DungeonState,如需要)
- `assets/scripts/core/save/SaveTypes.ts`(`RunSave.route` 扩展,§10.3)
- `assets/scripts/core/save/SaveService.ts`(承载 `RouteSaveAdapter`,不改 `saveRun` 签名)
- `tests/core/route*.test.ts`(单测)

> 若不想把 `GameBootstrap.ts` 混入 Demo7,可拆 **Demo7-pre / InfraFix 单独卡**专门修运行时生命周期;但 P0 断层消除要求二者在同一轮落地,**首选并入 Demo7**。

### 14.2 契约红线
- ai-tasks Agent **只执行 Demo7 卡内任务**,不得改 `DungeonGenerator` 多房间职责(§8.1)、不得改 `GameBootstrap` 正式 `startup()` 之外逻辑。
- 若本会话实现,commit message 标注越工作流,并同步更新本 GDD 一致性表。

### 14.3 协调决策(待用户拍板)
1. `SingleRoomEncounterBuilder` 与 `route/` 模块:本会话直接做 / 写入 Demo7 卡交 ai-tasks。
2. `NodeRouteMap` 与旧 `DAGGenerator`:并存(旧 DAG 兜底)/ 逐步替换。
3. `GameBootstrap.ts` 修复:并入 Demo7 / 拆 InfraFix 单独卡(建议并入)。

---

## 15. 版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v0.1 | 2026-07-13 | 节点路线图肉鸽重做初版 |
| v0.2 | 2026-07-13 | 评审重写:解耦/7 模块/状态机/adapter/NodeRewardResolver/种子链/存档/Boss 金色/路径修正/13 章 |
| v0.3 | 2026-07-13 | 对齐 ai-tasks 基座:DungeonGenerator/SaveManager/ReplayRecorder 复用、三套类型 adapter、边界 |
| v0.4 | 2026-07-13 | 消除"3D 底座 ↔ 正式运行时"断层:3D 主线/2D 兜底、SingleRoomEncounterBuilder、GameBootstrap 硬规则、RouteSavePort、防双发、状态源拆分、Demo7 清单、ASCII 规范、编号修正 |
| v0.4.1 | 2026-07-13 | 小修 7 处类型/架构冲突:① `EncounterRoomLayoutType` 解决 SingleRoomEncounterBuilder 类型冲突(`build()` 返回 `RoomLayout` 不再混 `Constants.RoomType`) ② `toLegacyRoomType` 的 `start` 返回 `null`(无此枚举) ③ Demo7 清单补 `GameBootstrap.ts` ④ `RunSave.route?: RouteRunSnapshot` 落盘形态(不另开 key) ⑤ `deriveSeed()` 将 FNV-1a hex 转 uint32 喂 `Rng` ⑥ Boss 可达改 `parents.some(completed)`(非全父) ⑦ 明确 `RunCoordinator.startRun`→`RouteRunController.startFloor`→`SceneFlowService.goToDungeon` 调用边界,`encounterSceneId='dungeon'` 复用现有 SceneId |

> **ASCII 注释规范**: 所有 `.ts` 代码块注释只用 ASCII;中文说明一律置本 Markdown 正文。
