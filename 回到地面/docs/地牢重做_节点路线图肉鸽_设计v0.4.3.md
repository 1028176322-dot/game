# 地牢重做设计 · 节点路线图肉鸽模式 (v0.4.3) — 已作废

> ⚠️ **本版已被 `docs/地牢重做_节点路线图肉鸽_设计v0.4.4.md` 取代。**
> v0.4.4 在 v0.4.3 基础上补了 2 处"实现会卡住"的必须项 + 1 处职责表述微调,并作为 **Demo7 正式主规格**。请直接打开 v0.4.4。

## v0.4.3 → v0.4.4 差异速览
| # | v0.4.3 缺口 | v0.4.4 补正 |
|---|---|---|
| ① | Demo7 授权清单漏 `RunCoordinator.ts`;但 v0.4.3 规定"RunSave 只能由 RunCoordinator 创建",若不在清单授权 + `startRun()` 不落 RunSave,则 `RouteSaveAdapter.saveRoute()` 永远 false | 清单补 `RunCoordinator.ts`;新增 §10.3.1 RunSave 生命周期契约,`startRun()` 必须落基础 `RunSave`(`route: undefined`) |
| ② | `RoomFlowController.ts` 要用 `RouteNodeType` 但文档未写 import 路径,另一 AI 易写成 `../../core/...` 或从 `dungeon/route` 引,重新制造倒挂 | 明确 `import type { RouteNodeType, RouteEncounterContext } from '../core/save/RouteSaveTypes'`;`RouteEncounterContext` 收归 `RouteSaveTypes.ts` 纯数据 |
| ③ | §14.1 写 `SaveService.ts(承载 RouteSaveAdapter 单例调用...)` 让 SaveService 承载 adapter 逻辑 | 修正:SaveService 只保持 `loadRun`/`saveRun` 签名不变,不承载 adapter;adapter 自取 `SaveService.instance` |
