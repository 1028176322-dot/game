> **编码**: UTF-8

# 整体架构总图（§6 独立版）

> 来源：`docs/2D转3D全面升级方案.md §6`
> 说明：三张图 + 完成状态表。✅ = 已交付（纯 TS，已单测）；⚠️ = 部分交付，余下卡美术；⬜ = 未启动（卡美术 / P2 延迟）。

---

## 6.1 主架构总图

```
                          GameBootstrap
                               │
                       ┌───────┴────────┐
                       │   GameContext   │  ← ServiceLocator + 生命周期宿主
                       │ (register/get)  │
                       └───────┬────────┘
         ┌─────────────────────┼──────────────────────┐
         │                     │                       │
   ConfigDatabase          AssetCache             EventBus(分类)
    Skill/Monster/          RefCount+              Battle/UI/Audio/
    Boss/Effect/AI/         Preload/Async/         Scene/Input/
    Camera/Audio            Release Queue          Runtime
                               │
                       RuntimeManager
         ┌─────────────────────┼──────────────────────┐
         │                     │                       │
    SceneFlowService      RoomRuntime            BattleRuntime
    (场景级生命周期)       (房间级生命周期)        (战斗级生命周期)
                               │                       │
                               │           ┌───────────┴───────────┐
                               │           │   LifecycleManager     │
                               │           │  (广播 enter/exit/      │
                               │           │   pause/resume/destroy)│
                               │           └───────────┬───────────┘
         ┌─────────┬───────────┼───────┬────────┬──────┼──────┬────────┐
         │         │           │       │        │      │      │        │
      Render    Combat       AI     Dungeon   Audio   Camera  Debug   Save/
    ModelRender CombatSystem AIController DungeonGen AudioSystem CameraBrain DebugPanel Replay
   +Toon      Projectile  IAIController RoomBuilder  3D-Sound  Follow/  FPS/     SaveManager
   Materials  HitResolver BT/FSM/     TileMap    Snapshot  Lock/Boss  AI/Skill  RunState
              DamageResolver GOAP/     NavigationGrid          /Cine/    /Col/Nav
              EffectExecutor Utility   RoomRuntime             Shake/Zoom /Anim/
              LockOnManager                                Seed/Events
```

- **依赖方向**：业务 System → 接口（`ICollisionService` 等）→ `GameContext` 注入实现。绝不反向、绝不直接 `new` 引擎底层。
- **生命周期方向**：`GameBootstrap → SceneFlow → RoomRuntime → BattleRuntime → LifecycleManager → 各 System`。

---

## 6.2 战斗分层图（§3.8 细化）

```
SkillSystem(CD) ──BattleCommand──▶ CombatSystem(dispatch)
                                        │
                                        ▼
                                  TargetSelector(锁定/范围/视线)
                                        │
                              ┌─────────┴─────────┐
                              ▼                   ▼
                        HitResolver(命中)    LockOnManager(取景/相机)
                              │
                              ▼
                        DamageResolver(伤害公式)
                              │
                    ┌─────────┴─────────┐
                    ▼                   ▼
              EffectExecutor         ProjectileSystem
              (特效/状态/抛射触发)     (飞行物生命周期)
                    │
                    ▼
              ICollisionService(raycast/overlap)  ← 不直接依赖 PhysicsSystem
```

- 全部经 `ICollisionService` + `ConfigDatabase` + `IAssetCache`，由 `GameContext` 注入；扩展 Boss 技能只加 `SkillGraph` 节点。

---

## 6.3 生命周期状态图（§5.1）

```
        initialize(ctx)
   [Created] ─────────────► [Initialized]
                               │ enter()
                               ▼
                           [Active] ◄─────┐
                          │        ▲      │ resume()
                  pause()│        │exit() │
                          ▼        │
                       [Paused]    │
                          │ enter()┘ (重进 = exit + enter)
                          │ destroy()
                          ▼
                       [Destroyed]
```

- `LifecycleManager` 统一驱动；`destroy` 逆序执行，确保依赖者先于被依赖者释放。

---

## 7. 系统完成状态表（非美术口径，2026-07-11）

| 系统 | 章节 | 状态 | 备注 |
|---|---|---|---|
| LifecycleManager | §5.1 | ✅ | ILifecycle + 逆序销毁 |
| GameContext | §5.2 | ✅ | 12+ 令牌，register/get |
| ConfigDatabase | §5.3 | ✅ | 类型化查询 |
| Logger | §5.4 | ✅ | 7 通道 + sink |
| DebugPanel | §5.5 | ✅ | 12 类快照 |
| SaveManager | §5.6 | ✅ | 分层存档 |
| ReplayRecorder | §5.7 | ✅ | 确定性回放 |
| AudioSystem | §5.8 | ✅ | 6 类音频 + 3D 衰减 |
| 自动化测试 | §5.11 | ✅ | 167 单测 + Mock 套件 + branch_gate |
| 分支门禁 | §5.13 | ✅ | tools/branch_gate.py |
| ICollisionService | §3.3 | ✅ | 纯 TS 运动学 |
| CameraBrain | §3.4 | ✅ | 7 模式 |
| AnimationStateMachine | §3.5 | ✅ | 状态机已交付；骨骼引擎集成 ⚠️ |
| AssetCache | §3.6 | ✅ | 引用计数 + 延迟释放 |
| Dungeon 拆分 | §3.7 | ✅ | 5 件套 |
| CombatSystem 分层 | §3.8 | ✅ | 7 件套 |
| SkillGraph 数据驱动 | §3.9 | ✅ | 零 switch |
| IAIController | §3.10 | ✅ | BT/FSM/GOAP/Utility |
| EventBus 分类 | §3.11 | ✅ | 6 域类型化 |
| ECS 组件集 | §3.12 | ✅ | 6 组件 + EntityManager |
| 垂直切片 | §3.17 | ✅ | 端到端测试 |
| SceneFlowService | §3.x | ✅ | 已存在（legacy 单例，满足红线 6） |
| AppFlowController | §3.x | ✅ | 已存在（legacy 单例） |
| UiRouter v2 | §3.x | ✅ | 已存在（legacy 单例） |
| 渲染管线 / Toon | §3.2 | ⚠️ | 卡美术（3D 资产） |
| 骨骼动画引擎集成 | §3.5 | ⚠️ | 卡美术（3D 模型） |
| 性能优化 LOD/Instancing | §3.13 | ⬜ | P2，卡美术 |
| 光照阴影 | §3.14 | ⬜ | 卡美术 |
| 特效粒子 | §3.15 | ⬜ | 卡美术 |
| 资产管线 | §5.9 | ⚠️ | 跨工程 / 美术轨 |
| PlayerController→ECS 接线 | §3.12 | ⚠️ | 引擎侧，需编辑器验证 |
| BattleManager→CombatSystem 接线 | §3.8 | ⚠️ | 引擎侧，需编辑器验证 |

**结论**：非美术管线的纯 TS 架构与基础设施 **100% 交付并验证**（167 测试 / 9-9 门禁）。剩余未完项全部属于「美术资产依赖」或「引擎运行时集成（需 Cocos 编辑器验证）」两类，均不阻塞逻辑层。
